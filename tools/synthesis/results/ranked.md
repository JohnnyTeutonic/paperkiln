# Synthesis search: ranked candidates

355 candidates scored; verdicts: ADJACENT 129, OPEN 117, TAKEN 72, UNSCOPED 37

Rank = novelty (OPEN 30 / ADJACENT 10 / TAKEN 0) + 2*splash + feasibility. Novelty is the scoper's verdict against arXiv; read the closest paper before believing any OPEN.

## Controls
- ctl-choquet expected ADJACENT got ADJACENT: **PASS**; closest 2607.26164; 2510.13079; 2606.09886
- ctl-kvdiff expected TAKEN got TAKEN: **PASS**; closest 2505.16950; 2601.02076; 2510.14973

## Ranked

| # | verdict | splash | feas | name | claim | closest | survives |
|---|---|---|---|---|---|---|---|
| 1 | OPEN | 4 | 4 | Hebbian fast-weight FFN overlay | outer-product Hebbian fast weights applied to the FFN so that recent token associations are stored and read back within the same forward pass, decaying over a short horizon | 2607.11614; 2605.11196 | None of the retrieved papers place a per-token outer-product Hebbian update directly onto the FFN weight matrix (W + A_t) with decay, read back within the same forward pass; the closest work does associative memory either as a separate recurrent module across segments or inside linear attention, so the specific FFN-weight fast-weight overlay for mid-sequence binding remains untested. |
| 2 | OPEN | 4 | 4 | atomic commit fast-weight edits | transactions-and-rollback applied to weights-at-inference so that a batch of proposed within-sequence weight edits is validated against a held-out probe before being committed, with automatic rollback to the frozen base weights if the probe check fails | 2503.11842; 2601.21012 | Nothing in the set stages a candidate ΔW in a buffer, compares held-out probe loss of frozen vs staged weights, and commits/rolls back the weight perturbation itself (as opposed to gating tokens, predictions, or reports); that specific transactional weight-edit mechanism with a provable non-degradation guarantee at the weight level remains undemonstrated. |
| 3 | OPEN | 4 | 3 | delta-rule fast-weight router | Delta-rule fast-weight memory applied to the moe-router so that the gate's expert-scoring key-value memory is overwritten in-context based on observed downstream error, closing the feedback loop within a single sequence. | 2609.14859; 2609.04895 | Nothing in the set closes the loop of a delta-rule fast-weight memory embedded specifically in the MoE gate, updated in-context token-by-token from a lagged realized-loss/regret signal attributable to the chosen expert, to change future expert scoring within the same sequence; HeRo does router memory but for depth-skip routing via linear-attention aggregation rather than delta-rule error-correective overwrite tied to observed expert-attributable loss, so a router-specific credit-assigned delta-rule fast-weight mechanism remains open. |
| 4 | OPEN | 4 | 3 | Barrier-constrained expert routing | Control-barrier-functions applied to the moe-router so that gate logits are projected at inference onto the nearest point of a safety set (e.g. minimum per-expert utilization over a sliding window, or maximum logit gap bound) whenever the raw top-k choice would leave that set, guaranteeing the constraint holds every token without retraining. | 2608.14205; 2604.09474 | Nothing in the set places a CBF/QP safety-set projection directly on MoE gate logits to give a per-window, per-token certifiable no-starvation/no-overload guarantee without retraining; the nearest work (FreeBalance) solves load balancing predictively/statistically rather than via a barrier-certified logit correction, so this specific mechanism-at-component-for-purpose combination remains open. |
| 5 | OPEN | 3 | 5 | PID-controlled expert capacity | Proportional-integral-derivative feedback control applied to the moe-router so that per-expert capacity thresholds are continuously adjusted to drive measured load imbalance to zero, replacing the static aux-loss weight. | 2605.15403; 2604.26340 | None of the candidates implement an explicit PID (proportional-integral-derivative) feedback controller with distinct P/I/D terms added as a non-gradient bias directly to router logits in place of an aux-loss; the nearest work (φ-Balancing) uses mirror-descent/EMA-based adjustment, which is a different control law without explicit derivative/integral gain structure, so a genuine PID formulation with Kp/Ki/Kd terms tested against matched-FLOPs aux-loss baselines would still be new. |
| 6 | OPEN | 4 | 3 | periodic bidirectional residual denoising | Masked-diffusion-denoising applied to the residual stream so that a bounded lookback window of past residual writes can be revised (partially erased/overwritten) by later context, instead of only ever receiving additive updates. | 2207.02495; 2601.16971 | Nothing in the set applies masked-diffusion-style bidirectional denoising specifically to residual-stream cache entries (as opposed to encoder states in ASR, token logits, or fast-weight memories) to allow non-causal in-place erasure/overwrite of past residual writes while preserving causal output emission; that specific placement, mechanism, and purpose combination remains open. |
| 7 | OPEN | 4 | 3 | TTT gate on residual | test-time-training applied to the residual stream so that a block can attenuate or overwrite directions written by earlier blocks rather than only add to them | 2608.07110; 2605.03953 | No paper combines test-time (inference-time gradient) updated fast weights with a gating/rewrite operation specifically placed on the residual stream to let later blocks suppress or invert earlier blocks' contributions; Modular TTT explicitly finds residual/gating additions unhelpful within the fast-weight network itself rather than testing a TTT-driven gate on the stream, so the specific combination (online TTT-updated gate performing in-place attenuation/inversion of the residual stream) remains untested. |
| 8 | OPEN | 4 | 3 | Delta-rule content-adaptive positions | Delta-rule associative overwriting applied to positional encoding so that a token's effective position code is a content-conditioned, overwritable memory entry rather than a fixed function of its integer index. | 2605.10414; 2511.19279 | The proposal's specific twist—an explicit per-sequence associative memory M keyed by content embeddings, updated via delta-rule erase-then-write, whose output is injected specifically as a positional-adjustment vector to let the model collapse/stretch its effective index at structural boundaries—is not demonstrated by any candidate; GAPE and MapFormer make positional encoding content-aware but through gating or Lie-algebra transforms rather than an explicit delta-rule memory-overwrite keyed by content and dedicated to re-indexing. |
| 9 | OPEN | 4 | 3 | Redundant-code cache repair | Error-correcting-code redundancy applied to attention-topology so that cache entries are stored with a learned parity/checksum projection, and every k tokens a syndrome computed from later hidden states is used to detect and repair drift in earlier cache entries before future reads. | 2608.28293; 2602.06161 | The specific mechanism of a learned parity/checksum projection with a syndrome-threshold trigger for selective, auditable cache repair (an explicit ECC-style detect-then-correct loop trained end-to-end) is not implemented by any candidate, which instead use probabilistic correction, drift-aware verification scores, or causal substitution audits rather than a parity-check code. |
| 10 | OPEN | 3 | 4 | Kalman-fused router logits | Kalman filtering applied to the moe-router so that expert selection fuses a prior gate score with a noisy downstream-error measurement from the previous token's routing outcome, in proportion to their estimated uncertainties. | 2606.00761; 2511.08277 | Nothing in the set maintains a per-expert online (mean, variance) state updated causally across tokens and blends the current router logit with a downstream-error measurement via an explicit Kalman gain before top-k selection; the nearest papers either use EKF for unrelated sensor fusion or adapt routing sharpness/budget from static per-token router confidence without any temporal filtering state. |
| 11 | OPEN | 3 | 4 | Transactional block commit | transactions-and-rollback applied to the residual stream so that a block's write is either fully committed or fully discarded based on a learned validity check, giving the stream an erase-like veto it lacks by default | 1710.04773; 2412.05768 | A learned per-block scalar gate trained with a straight-through estimator that can hard-round to zero at inference (full discard of a block's write, checked against a KL budget) is not implemented by any candidate; all found papers either study residual-stream refinement dynamics passively or use residual streams/connections for unrelated compression, steering, or distillation purposes. |
| 12 | OPEN | 4 | 2 | graph-propagated position state | graph-message-passing applied to positional-encoding so that each token's position representation is updated by passing messages along an explicit sequence graph (edges to recent tokens, sink tokens, and cache-retained tokens) instead of being a fixed function of its integer index | 2606.26631; 2605.05696 | None of the retrieved papers define positions via message passing over an explicit sequence-token DAG (predecessor/sink/retained edges) that lets edited/evicted tokens collapse to shared effective positions during autoregressive generation; the nearest work either rewrites/rebinds cached positional coordinates by direct reassignment (PRCR) or corrects RoPE angles in closed form (Irminsul/MiniPIC) rather than computing positions via learned graph propagation, so a GNN-based positional state trained jointly for streaming edit-buffers remains a distinct, unclaimed mechanism-purpose pairing. |
| 13 | OPEN | 3 | 4 | test-time position adapter | test-time-training applied to positional-encoding so that a small set of position-dependent parameters (e.g. RoPE frequency scaling factors or ALiBi slopes) are updated via a self-supervised next-token loss on the incoming long stream itself, adapting the effective position function to the actual lengths encountered at inference. | 2607.07678; 2504.18857 | Yes: none of these perform actual gradient-based test-time training (online backprop on a self-supervised next-token loss computed on the live stream) restricted to a handful of positional scalar parameters (RoPE frequencies/ALiBi slopes); they use static rescaling rules, heuristic dimension selection, or offline training, so an online TTT loop specifically on positional parameters remains novel. |
| 14 | OPEN | 4 | 2 | Kalman-fused positional estimate | Kalman filtering applied to positional-encoding so that the position signal fed to attention is a fused estimate combining the nominal index (a noisy 'prediction' that becomes unreliable past train length) with a measurement derived from content-based cues (e.g. induction-head-style repeat detection or attention-entropy signals), weighted inversely to each source's estimated uncertainty at that distance. | 2506.00920; 2405.14722 | The specific mechanism of an explicit Kalman-style fusion combining a nominal RoPE angle (with a learned, distance-dependent uncertainty schedule from validation loss) and a content-derived positional measurement (from induction-head/attention-entropy probes), fused per-token via inverse-variance weighting, is not implemented in any of these papers, which instead use histogram filters, static/learned biases, or entropy-based rescaling without explicit dual-source uncertainty fusion. |
| 15 | OPEN | 3 | 4 | mixture-density multi-horizon head | Mixture-density heads applied to the output-head so that the model predicts a small mixture of distributions over tokens at offsets {1,2,4} rather than a single point next-token distribution. | 2608.05454; 2412.10418 | Yes: no paper places a jointly-trained mixture-density (multi-component categorical) auxiliary head specifically at offsets {1,2,4} on top of the standard next-token LM head to produce calibrated multi-modal, multi-horizon token distributions in a single forward pass for speculative/early-exit use; the closest works either use single-mode multi-token prediction, non-mixture uncertainty representations (zonotopes), or separate draft models instead of auxiliary mixture heads on the same output layer. |
| 16 | OPEN | 3 | 4 | fast-weight sampling memory | Hebbian outer-product fast-weights applied to the sampler so that recently sampled tokens transiently bias per-position top-k/temperature decisions for the next few steps, giving the decoder a short adaptive memory instead of one static global knob. | 2503.08057; 2606.23601 | None of the candidates implement a literal Hebbian outer-product fast-weight matrix over token embeddings that is written after each sampled token and exponentially decayed to bias the next few steps' logits before top-k/temperature truncation; this specific mechanism-object pairing (fast-weight cache as the sampler's short-term memory, with built-in decay dynamics rather than learned policy or layer-contrast heuristics) remains undemonstrated. |
| 17 | OPEN | 3 | 4 | multi-lane consensus prefill | consensus-protocols applied to prefill so that several cheap, differently-approximated prefill lanes (e.g. sliding-window, sink, low-rank) vote on the token-1 distribution and the vote itself is used as the emission signal, with the exact full-attention prefill running as an arbiter only when lanes disagree | 2608.20397; 2503.03588 | The specific mechanism of running several differently-approximated prefill lanes in parallel and using cross-lane agreement/voting itself as the emission trigger (with exact prefill deferred as a post-hoc arbiter only on disagreement) is not described in any candidate; nearest works either guarantee exactness via splice-and-repair (Nexus) or substitute a single cheap approximator (KV Prediction) rather than consensus among multiple lanes. |
| 18 | OPEN | 4 | 2 | spectral prefill attention | A spectral-transform applied to the prefill sequence so that long-range dependencies in the prompt appear as sparse/local coefficients in frequency space, allowing prefill attention to be computed with a truncated spectral mixer instead of full O(n^2) pairwise attention. | 2502.00585; 2607.19368 | Yes: no candidate implements a causal, prefill-only FFT/learned-orthogonal-transform token mixer that truncates to top-r frequency coefficients as a direct O(n log n) replacement for a subset of self-attention layers while retaining local/sink attention for near-token detail, trained end-to-end at matched parameter count; the nearest works either do general spectral transformer replacement (not prefill-specific, not causal-truncated) or spectral approximation for token compression/hashing rather than direct spectral token-mixing attention replacement. |
| 19 | OPEN | 3 | 4 | hebbian fast-weight depth gate | Hebbian fast-weight outer-product updates applied to the layer-schedule controller so that recently-seen token/context patterns transiently lower or raise the depth a token is routed through within a sequence, without any slow-weight (backprop) update. | 2404.02258; 2605.05222 | None of the retrieved papers place a Hebbian outer-product fast-weight accumulator (updated only via decay/Hebbian rule, never touched by the optimizer) directly as the gating signal controlling per-token layer-skip depth within a streaming sequence; the closest works either use backprop-trained MLP gates for depth (MoD, TSA) or Hebbian fast-weights for a different purpose (memory contribution in few-shot learning), so a Hebbian-computed depth gate exhibiting within-sequence adaptation to repeated tokens without any slow-weight update remains unaddressed. |
| 20 | OPEN | 4 | 2 | Kalman-gated block re-entry | kalman-filtering applied to the layer-schedule so that a block's re-application is triggered and weighted by fusing the block's own output (measurement) with a running prediction of the token's hidden state (prior), re-applying the block only when the fused uncertainty stays high | 2605.05222; 2607.20519 | Yes: no candidate paper uses an explicit Kalman gain (fusing a learned observation-noise head with a running variance estimate) as the halting/reapplication signal for a transformer block, which is a mechanistically distinct data fusion rule from confidence-MLP gates or exit-distribution readouts used in the adaptive-depth literature found here. |
| 21 | OPEN | 3 | 4 | Delta-rule FFN memory edits | Delta-rule associative writes applied to the FFN so that the per-token key-value memory is erased-and-rewritten online instead of staying fixed after pretraining. | 2301.04213; 2405.16720 | None of the retrieved papers place a DeltaNet-style per-token online erase-and-rewrite fast-weight update inside the FFN's own up-projection to let the FFN act as a continuously overwritable associative memory without gradients; the nearest works either localize/edit facts offline via backprop (ROME/LAW) or use static/learned adapters for efficiency, so the specific mechanism-object-purpose triple (delta-rule fast weight replacing FFN weight matrix for gradient-free mid-stream fact overwrite) remains open. |
| 22 | OPEN | 4 | 2 | Graph-message-passing FFN memory | Graph message-passing applied to the FFN so that its key-value memory slots are nodes that exchange updates with each other (not just with the token stream) at each step, letting related memory slots co-adapt. | 2605.08143; 2007.10637 | Nothing in the set makes FFN hidden units into graph nodes that exchange value updates with each other after each token write so that one write propagates to semantic neighbors; nearest work (HoReN) does pattern-completion retrieval over codebook entries via Hopfield dynamics for editing, not per-token neighbor-to-neighbor message passing among key-value slots during normal forward computation, so the specific mechanism-object pairing remains open. |
| 23 | OPEN | 3 | 4 | spectral cross-sequence normalisation | computing normalisation statistics in a frequency basis over a sliding window of token activations applied to normalisation so that low-frequency (long-range/slowly-varying) components are whitened jointly instead of per-token | 2306.16999; 2109.08780 | Nothing in this set performs a causal DCT over a sliding token window to jointly whiten low-frequency (long-range/document-level drift) components before per-token LayerNorm in a sequence model; the nearest analog (spectral batch norm) normalizes feature-map spectra per-sample for training stability, not sequence-position drift suppression, so the specific placement/purpose remains unclaimed. |
| 24 | OPEN | 3 | 4 | Thompson-sampled learning-rate bandit | Thompson sampling over a discrete bank of candidate learning rates applied to the lr-schedule so that step-size choice is made by posterior sampling over observed loss-improvement reward rather than a fixed curve | 1912.06910; 1709.04047 | No paper here applies Thompson sampling (or an equivalent posterior-sampling bandit) directly over a discrete bank of LR multipliers during neural network training to adaptively reallocate step-size exploration based on observed loss-improvement reward; the nearest bandit-for-training-hyperparameter work targets RL exploration parameters, and nearest Thompson-sampling work targets control systems, not LR schedules, so the specific placement (LR-schedule arm-selection via TS posterior updates each window) remains open. |
| 25 | OPEN | 3 | 4 | Information-bottleneck auxiliary future head | An information-bottleneck penalty applied to the training objective so that hidden states are compressed toward only what predicts a token ten steps ahead, in addition to next-token loss. | 2310.17284; 2605.23960 | None of the candidates combine a per-layer VIB penalty with a fixed k-step-ahead auxiliary LM head trained jointly with next-token CE at matched FLOPs; the specific placement (future-token auxiliary head + IB bottleneck at hidden states of an autoregressive LM, ablated against a matched-FLOPs beta=0 baseline) is not addressed by any paper here, which either apply VIB to non-LM domains or to LM layers without a future-token prediction target. |
| 26 | OPEN | 3 | 4 | Multi-horizon mixture-density prediction heads | Mixture-density heads applied to the training objective so that the model predicts a mixture of distributions over tokens at multiple future offsets (t+1, t+5, t+10) instead of a single point-distribution next-token target. | 2511.23260; 2407.09722 | None of these place mixture-of-softmaxes heads at multiple discrete future offsets (t+1,t+5,t+10) trained via mixture NLL specifically to yield calibrated multimodal uncertainty estimates for downstream ambiguity-detection in LLM token prediction; the nearest work either forecasts generic time series scalars/distributions or uses multi-token joint objectives for decoding speed/reasoning rather than explicit calibrated multi-horizon multimodal uncertainty as a discardable auxiliary training signal, so the specific mechanism-purpose pairing remains open. |
| 27 | OPEN | 3 | 4 | Mixture-density next-token head | Mixture-density heads applied to the training objective so that next-token prediction outputs a learned mixture over K component distributions instead of a single softmax, exposing multimodal predictive uncertainty as a first-class target | 2310.07820; 2511.04869 | None of the retrieved papers place a K-component mixture-density head with an explicit mixing-coefficient layer directly on the next-token softmax output, trained end-to-end with mixture NLL to expose aleatoric multimodality as a first-class calibration target; the MoE papers gate over expert subnetworks for capacity/routing, not over output-distribution components for calibrated multimodal next-token prediction, so this specific placement and purpose remains open. |
| 28 | OPEN | 4 | 2 | submodular content-aware sparse mask | submodular-maximisation applied to attention-topology so that the attention mask for each query is chosen at inference time by greedy maximization of a coverage-diminishing-returns objective over key content, instead of being fixed by position before content is seen | 2303.08810; 2502.20766 | The specific mechanism of casting mask selection as a submodular coverage-maximization problem solved by certified greedy (with lazy evaluation, (1-1/e) guarantee) trained jointly at matched FLOPs is not present in any candidate; all found methods use learned gates, thresholds, or routing scores rather than a submodular objective with approximation certificate. |
| 29 | OPEN | 3 | 3 | Conformal abstention mass in attention | Conformal-prediction applied to attention-scores so that a query token can allocate a calibrated 'null mass' to an abstention channel instead of forcing softmax weights to sum to one over content keys. | 2410.02703; 2505.11739 | No paper combines split-conformal calibration with a per-head learned null KV pair inserted into attention logits to give a distribution-free false-abstention-rate guarantee at the attention-score level itself (as opposed to output-level answer abstention or heuristic/parameter-free sink reweighting), so the specific placement and guarantee remain unaddressed. |
| 30 | OPEN | 3 | 3 | Mixture-density attention abstention | A mixture-density-head applied to attention-scores so that each query predicts a small mixture of candidate attention distributions (including a degenerate 'attend-to-nothing' component) instead of one fixed softmax, letting the model represent abstention as one mixture mode with nonzero learned weight. | 2410.11842; 2602.01468 | The specific mechanism of a per-query router predicting mixture weights over K full independent softmax-over-keys components, one of which is pinned to pure-null (abstain), trained jointly at every layer as a multi-modal generalization of the attention distribution itself, is not instantiated in any of these papers; nearest works either mix over heads/experts (MoH, gated attention theory) or use a scalar gate/no-op (clipped softmax) rather than a genuine multi-component mixture-of-distributions with an explicit abstain mode. |
| 31 | OPEN | 3 | 3 | Predictive-coding expert residual routing | Predictive coding applied to the moe-router so that the gate scores not the raw hidden state but the residual between the hidden state and a causally-predicted hidden state from the previous layer's chosen expert, so routing responds to what was *not* already explained. | 2608.14205; 2609.06403 | Yes: none of these papers compute a causal, per-token predictive-coding residual (predicted next hidden state from the previous layer's chosen expert, subtracted before gating) as the actual input to the router; FreeBalance predicts workload for scheduling and Share-First subtracts shared-expert content architecturally rather than gating on an expert-attributed prediction error, so a router that scores h_t - pred(prev-expert-output) specifically to suppress redundant re-selection remains unaddressed. |
| 32 | OPEN | 3 | 3 | Thompson-sampled expert selection | Thompson sampling applied to the moe-router so that expert selection is drawn from a posterior over each expert's expected quality-adjusted value rather than chosen by deterministic top-k on a point-estimate score, and load balance emerges from posterior uncertainty rather than an aux loss. | 2601.03577; 2602.17798 | The specific mechanism of maintaining online per-expert Beta/Gaussian reward posteriors updated via bandit-style conjugate updates and sampling additive Thompson noise into gate logits (so load balance emerges purely from exploration rather than any auxiliary loss or variational/geometric regularizer) is not implemented by any candidate paper. |
| 33 | OPEN | 3 | 3 | dual-process router override | Dual-process gating applied to moe-router so that a fast unconditioned reflex route (trained only for balance) is overridden by a slow deliberate route (trained only for quality) exactly when the deliberate path's confidence exceeds a margin, decoupling the two objectives into separate parameters instead of one contested loss. | 2605.06415; 2607.26340 | The specific two-gate architecture—a parameter-free reflex router guaranteeing uniform load versus a quality-only deliberate router, switched by a single trained per-layer confidence-margin scalar tau, with no shared loss term at all—is not instantiated by any candidate; nearest work either fuses balance and quality into one loss/parameter (E parameter, entropy-aware router) or treats round-robin purely as a communication-scheduling baseline rather than a confidence-gated fallback path. |
| 34 | OPEN | 3 | 3 | Per-token TTT residual gate | Test-time-training applied to the residual-stream so that each token's effective write-width adapts on the fly via a self-supervised update rather than being fixed at train time. | 2607.28669; 2608.07110 | The key novel claim is that the adapter itself receives an online, causal, per-token gradient update from a self-supervised loss at inference (adapting write-width on the fly), which none of the closest papers do—LARA and TALAN train/select statically, and Modular TTT's online updates are not placed as a residual-write-gating adapter. |
| 35 | OPEN | 3 | 3 | Multiresolution residual channels | Wavelet-multiresolution decomposition applied to the residual stream so that each token's representation is carried at several explicit bandwidths simultaneously, letting cheap tokens use only coarse channels while hard tokens engage fine channels. | 2609.05309; 2608.30320 | Nothing in the set applies an invertible wavelet/dyadic-band split to the transformer residual-stream channel dimension itself (folded into embed/unembed) with causal per-token gating that lets easy tokens skip fine bands entirely to save effective width; the nearest works either analyze post-hoc multi-stream usage (mHC) or gate whole learned branches (Qwen Gated Residual) rather than an explicit frequency-ordered channel hierarchy, so the specific mechanism-object pairing (wavelet multiresolution on residual channels, gated per-token band engagement) remains open. |
| 36 | OPEN | 3 | 3 | Fast-weight residual capacity buffer | Hebbian outer-product fast weights applied to the residual stream so that a token can temporarily borrow extra effective width from a decaying associative buffer instead of being constrained to the block's fixed channel count. | N/A | The core twist — a causal, decaying Hebbian outer-product matrix added as a low-rank perturbation to the residual stream's linear transform to give tokens dynamically expandable effective width, trained from scratch at matched compute budget — is not addressed by any retrieved paper; nearest prior work (fast weight programmers/linear attention) is only the generator's own acknowledged prior art, not present in this set. |
| 37 | OPEN | 3 | 3 | test-time-trained content positions | test-time-training applied to positional-encoding so that position embeddings are updated online from the model's own predictions on the incoming stream rather than fixed as a function of integer index | 2405.14722; 2605.10414 | The specific mechanism of updating a per-layer RoPE offset/correction module via an online gradient step on the model's own next-token prediction loss every k tokens (TTT applied specifically to positional encoding) does not appear in any candidate; nearest works either adapt PE within a single forward pass (DAPE, GAPE) or adapt PE online for camera geometry not content/position drift (RayTun3R), so the core claim—test-time-trained positional correction for detecting content-signaled position/boundary shifts in a text stream—remains open. |
| 38 | OPEN | 3 | 3 | sparse dictionary position codes | sparse-coding applied to positional-encoding so that each position is represented as a sparse combination of a learned dictionary of position-atoms, chosen per-sequence by content, instead of one fixed function of index | 2507.07544 / 2507.23083 (CARoPE); 2405.14722 | The specific mechanism of a shared ISTA-unrolled sparse encoder producing a k-sparse combination of a small learned atom dictionary added to RoPE angles, explicitly enabling atom-reuse across positions/documents for length generalization, is not instantiated by any candidate (CARoPE/DAPE use dense content-conditioned transforms, not sparse coding over an atom dictionary). |
| 39 | OPEN | 3 | 3 | renormalized multiscale position code | a renormalization-group coarse-graining procedure applied to positional-encoding so that position is represented as a hierarchy of scales where only the frequency components that survive block-averaging at each scale are kept, letting extrapolation reuse low-frequency (coarse-scale) structure unseen-length-independent | 2510.04130; 2302.08647 | No paper implements an explicit dyadic block-averaging RG pyramid of positional codes with a trained RG-step map linking adjacent scales and length-dependent scale-weighting for fallback to coarse scales at inference; this specific mechanism-object pairing remains unaddressed. |
| 40 | OPEN | 3 | 3 | hebbian fast-weight position cache | a hebbian outer-product fast-weight buffer applied to positional-encoding so that recently seen (position, content) pairings are stored as a decaying associative memory that biases the position code for the next few tokens | 2603.12513; 2405.14722 | Yes: no candidate applies a Hebbian outer-product fast-weight buffer specifically to bias the positional-encoding (RoPE angle/embedding) channel itself, keyed on recent (position, content) pairings and reset per sequence; the nearest works either adapt position indexing for cache/memory bookkeeping (MemRoPE) or adapt positional encoding via a learned network conditioned on context (DAPE), not via an un-trained forward-only Hebbian associative memory writing into the position code. |
| 41 | OPEN | 3 | 3 | delta-rule position rewrite memory | a delta-rule associative memory applied to positional-encoding so that position codes beyond the training length are looked up from a small adaptive memory that overwrites (rather than accumulates) stale key-position bindings, instead of extrapolating a fixed untrained function | 2605.10414; 2608.27763 | The specific mechanism of an erase-then-write delta-rule associative memory keyed on relative distance, trained end-to-end and gated to trigger only beyond a threshold L for the position channel itself, is not instantiated by any candidate; nearest works either apply delta-rule updates to content/KV memory or fix RoPE extrapolation via gating/remapping rather than a learned overwrite-based positional lookup table. |
| 42 | OPEN | 3 | 3 | Mixture-density positional decoder | Mixture-density heads applied to positional-encoding so that instead of a single deterministic embedding per index, each position emits a mixture of Gaussian phase/frequency components whose weights are trained to generalize smoothly past the training length. | 2506.00920; 2608.03769 | The specific combination of an MDN head producing (mean, variance, weight) over RoPE angle per position, trained with NLL auxiliary loss, whose mixture variance is read out as a scalar to gate interpolation between position-aware and mean-pooled position-agnostic attention, is not demonstrated in any candidate; nearest works use non-mixture probabilistic positions (histogram filter) or Gaussian-weighted fusion for a different purpose (mask-awareness) without a calibrated confidence-gated fallback mechanism. |
| 43 | OPEN | 3 | 3 | Hebbian fast-weight position refresh | Hebbian fast-weight outer-product updates applied to positional-encoding so that a short-lived, rapidly-decaying associative memory binds recent absolute positions to content, letting the model 'relearn' local position-content correlations online at any length rather than relying solely on the frozen, out-of-range static encoding. | 2405.14722; 2608.27763 | The specific mechanism of a decaying Hebbian outer-product (content-value x relative-position tag) fast-weight matrix injected as a low-rank bias into the value projection, reset per sequence and trained only via eta/decay/tag at nano scale, is not instantiated in any candidate; nearest works either apply fast-weights to generic recurrent state (not position) or adapt position encoding via remapping/attention-conditioned rules rather than an associative Hebbian trace. |
| 44 | OPEN | 4 | 1 | RG coarse-graining tokeniser | A renormalisation-group procedure that iteratively coarse-grains adjacent tokens and asks what statistical dependencies survive rescaling, applied to the tokeniser, so that the effective granularity at each layer/position is set by what information survives coarse-graining rather than by a fixed static merge table. | 2510.15517; 2510.08632 | The specific RG mechanism—iteratively pooling adjacent token embeddings and admitting a coarser level only if held-out mutual information with the target survives a threshold (measured by a probe classifier), then routing fine vs. surviving coarse tokens to different transformer layers—is not implemented by any candidate; existing hierarchical tokenizers use fixed BPE-style rules, learned codebooks, or diffusion vocabulary mappings rather than an explicit RG survival test, so this remains open. |
| 45 | OPEN | 3 | 3 | PID-controlled dynamic retokenisation | Feedback control applied to the tokeniser's merge table so that vocabulary composition tracks live model prediction error instead of being frozen from pre-training frequency stats. | 2605.27390; 2506.18831 | The specific mechanism of a per-merge-candidate PID control loop keyed to online next-token cross-entropy error, actively promoting/demoting BPE merges into a fixed-size active window during pretraining, is not implemented by any candidate; nearest work either applies PID control to a different component (activation steering) or adapts vocabulary via non-control mechanisms (pruning/selection, data-driven segmentation) for different purposes (speculative decoding speed, cross-lingual transfer). |
| 46 | OPEN | 3 | 3 | Mixture-density boundary prediction | Mixture-density heads applied to the tokeniser's segmentation decision so that token boundaries are drawn from a learned multimodal distribution over segmentation points instead of committed once via greedy frequency merges. | 2211.09761; 1804.10959 | The specific mechanism—an MDN-style head outputting K-component Gaussian/categorical mixtures over next-boundary-distance, jointly trained with the LM, sampled per forward pass, with mixture entropy used online as a per-position tokenization-uncertainty signal—is not implemented by any candidate; nearest work either samples from a fixed external unigram/BPE distribution (subword regularization) or predicts a single boundary probability autoregressively (dynamic pooling), neither maintaining an explicit multimodal boundary-distance mixture. |
| 47 | OPEN | 3 | 3 | BMA ensemble logit calibration | Bayesian model averaging applied to the output-head so that final next-token probabilities are a posterior-weighted mixture over several preference-tuned checkpoints/heads instead of one point-estimate model, restoring calibration that a single post-trained model loses. | 2601.04277; 2605.24773 | Yes: no candidate combines multiple frozen preference-tuned (DPO-style) checkpoints/heads sharing a base trunk via BMA with weights set by held-out log-likelihood evidence specifically to fix post-training overconfidence in next-token distributions; the closest works either align a single PoLM to a pretrained reference or apply BMA/posterior sampling in unrelated domains (point clouds, annotator soft-labels). |
| 48 | OPEN | 3 | 3 | Mixture-head null-component abstention | Mixture-density heads applied to the output-head so that one mixture component is trained to represent an explicit 'no confident token' null class, giving the model a first-class abstention option instead of forcing a point prediction. | 2603.22161; 2510.25001 | No paper adds a dedicated (K+1)-th mixture component whose weight is trained via an auxiliary disagreement-based loss to directly encode abstention as a first-class mixture output at the LM head; this specific architectural mechanism remains untested. |
| 49 | OPEN | 3 | 3 | predictive-coding residual head | Predictive coding applied to the output-head so that upper layers pass down only the prediction error against the current top-down next-token guess, rather than the layers being trained purely bottom-up to a single softmax target. | 2010.08012; 1603.03925 | No paper implements a fixed-T inner iterative loop at the final block where a top-down logit prediction is compared against bottom-up hidden state via a dedicated error-unit MLP and fed back before final logits, trained from scratch at matched FLOPs; this specific mechanism-at-output-head instantiation remains unaddressed. |
| 50 | OPEN | 3 | 3 | safety-set barrier sampler | control-barrier-functions applied to the sampler so that the token distribution is projected at each step onto the largest subset of the simplex for which a learned barrier function's value is guaranteed non-decreasing, preventing the trajectory from ever entering an unsafe/incoherent region rather than needing to roll back after the fact | 2605.10901; 2602.06623 | Yes: none of the candidates implement a control-barrier-function-style forward-invariance constraint (h non-decreasing along the trajectory) enforced via a per-step QP projection on the token simplex using a learned scalar barrier head on hidden states; the closest works either certify static classifiers, suppress toxic subspaces, or mask via grammar reachability, none giving a constructive forward-invariance guarantee at the sampling step. |
| 51 | OPEN | 3 | 3 | speculative layer-skip prefill | branch-prediction applied to prefill so that early layers speculatively predict the residual-stream state that deep layers would produce, letting the first token be emitted before full-depth prefill completes | 2502.02789; 2606.31315 | None of the candidates predict final-layer KV/residual states from shallow-layer activations to skip depth (rather than sequence length) for the first token while a background full pass overwrites the cache before token 2 — this depth-wise speculative-prefill mechanism with retroactive KV correction appears genuinely unaddressed. |
| 52 | OPEN | 3 | 3 | chunked atomic prefill commit | transactions-and-rollback applied to prefill so that the prompt is processed in committed chunks, each of which speculatively unlocks token emission for a prefix boundary before the whole prompt is consumed, with rollback if a later chunk changes the meaning of already-committed KV entries | 2502.02789; 2604.16395 | The proposal's core novelty is treating prefill itself as an incremental, speculatively-committed transaction where token generation begins before the full prompt is seen and a learned gate can trigger rollback of already-emitted decode tokens due to a later prompt chunk changing the meaning of earlier committed KV — no candidate paper begins emitting generation before full-prompt encoding with this kind of retroactive-invalidation rollback; nearest work either prunes tokens (SpecPrefill), streams/invalidates incoming context but not decode output (Stream2LLM), or rolls back draft continuation tokens against a fixed already-encoded prefix (TreeWY), so a system that begins *generation* after a partial prefill and rolls back *emitted* text due to prefill-chunk meaning-shift remains unaddressed. |
| 53 | OPEN | 3 | 3 | Thompson-sampled per-token depth | Thompson sampling applied to the layer-schedule so that each token's number of executed layers is chosen by sampling from a posterior over the value (accuracy-per-FLOP) of stopping at each depth, rather than a fixed depth or deterministic threshold. | 2605.05222; 2603.21365 | Nothing in the set applies Thompson sampling with online Bayesian posterior updates specifically to the per-token exit-layer decision in a transformer's layer schedule; the nearest early-exit/depth-routing papers use deterministic convergence checks or differentiable learned gates rather than bandit exploration with conjugate posterior updates from a reward signal. |
| 54 | OPEN | 3 | 3 | Sparse dictionary FFN keys | Sparse-coding applied to the FFN key-value memory so that each token activates a small learned support of atoms instead of scoring the full key set. | 2305.13999; 2604.08565 | None of the candidates implement the FFN as a literal learned dictionary matrix with per-token codes produced by an ISTA/matching-pursuit-style sparse encoder (rather than a discrete top-k router over blocks/experts/slices); if the specific claim is that continuous sparse-coding optimization (not discrete routing) scales atom count at fixed FLOPs and yields qualitatively different scaling behavior, that mechanism-level distinction from MoE/tree/slice routing is not directly tested by any listed paper. |
| 55 | OPEN | 3 | 3 | retrieval-mixed normalisation statistics | computing per-token normalisation statistics as a mixture of the token's own stats and the stats of its k nearest neighbours retrieved from a cached activation store applied to normalisation so that normalisation is informed by cross-sequence and cross-batch structure instead of being purely local to one token | 2112.12777; 2010.05625 | The core proposal—retrieving k nearest cached activation statistics per token and convexly blending them into the LayerNorm mean/variance computation itself—is not addressed by any candidate; the nearest work either normalizes retrieval scores (QB-Norm) or recalibrates BN statistics globally/post-hoc, not per-token via causal kNN fusion. |
| 56 | OPEN | 3 | 3 | MDL-selected normalisation window | choosing, per layer and online, the normalisation aggregation window (single token vs. causal running window of length L) that minimises a two-part code length (bits to describe the chosen L plus bits of the residual after normalisation) applied to normalisation so that the model adaptively normalises over the sequence extent that actually compresses activations best, instead of always at the single-token granularity | 1902.10658; 2305.09312 | Nothing in the set performs an online, per-layer MDL-based selection between per-token and causal running-window normalization granularities using a two-part code length; the nearest MDL-normalization paper (Regularity Normalization) uses MDL to derive a single incremental code-length-based scaling factor per layer rather than choosing among discrete window lengths, so the core mechanism (window-length model selection for normalization statistics) remains open. |
| 57 | OPEN | 3 | 3 | Energy-refined example replay | Energy-based refinement applied to data-order so that examples are not merely resampled by a fixed schedule but are iteratively refined (via gradient or MCMC steps on an auxiliary energy score) into harder or more informative variants before being re-shown to the model | 2505.18741; 2210.14724 | None of the candidates perform an in-place energy-based (Langevin/MCMC) rewrite of the actual example content in embedding space to create a genuinely new higher-loss variant before replay; they all reweight, reorder, or resample existing examples rather than refining them, so the core mechanism—iterative energy-guided perturbation of cached examples using the model's own loss as energy—remains unaddressed. |
| 58 | OPEN | 3 | 3 | Generational age-partitioned replay | Generational-garbage-collection applied to data-order so that recently-seen examples are revisited frequently from a 'young generation' buffer while older examples are promoted to a rarely-swept 'old generation' buffer, rather than being seen exactly once uniformly. | 2601.06540; 1811.11682 | Yes: no paper applies age-based generational-GC-style promotion/eviction (young vs. old ring buffers with differential sweep probabilities) specifically as a data-order/replay-scheduling policy for LM pretraining at matched token budget; nearest RL/continual-learning dual-buffer works use clustering or FIFO/diversity criteria rather than GC-style age-tiered promotion, and none target pretraining token-budget-matched replay. |
| 59 | OPEN | 3 | 3 | Multiresolution error-band scheduler | Wavelet multiresolution decomposition applied to the per-example loss stream over training time so that data-order selection responds to error trends at multiple timescales simultaneously instead of only the instantaneous loss. | 2309.12927; 2609.06772 | No paper applies wavelet/multiresolution decomposition specifically to a per-example loss-over-training-steps stream to separate persistent-hard from transiently-noisy examples for replay-budget allocation in data ordering; this specific placement and purpose remains unaddressed. |
| 60 | OPEN | 3 | 3 | graph-propagated fast-weight patch | graph-message-passing applied to weights-at-inference so that a per-sequence parameter patch is computed by propagating token states over a small induced graph before being written into a low-rank slice of the FFN weights | 2606.16847; 2602.00268 | None of the retrieved papers construct a per-window token graph, run message-passing over it, and write the resulting embeddings as a low-rank outer-product patch into an FFN weight matrix during inference; this specific mechanism-object pairing (graph propagation onto weights-at-inference) is not covered. |
| 61 | OPEN | 3 | 3 | Speculative gradient rollback | Speculate-and-verify applied to the gradient so that a cheap proxy step is taken immediately and only retroactively verified against the expensive exact gradient, rolling back if it would have increased loss. | 2511.07035; 2603.20526 | None of the candidates implement the specific speculate-verify-rollback loop where a cheap proxy/stale gradient step is applied optimistically to weights and later reconciled/rolled back against the exact gradient to recover exact-gradient training guarantees; the nearest works either checkpoint gradients for I/O overlap, gate backward passes via a cheap forward signal, or use verification for numerical error correction, not for optimizer-step speculation with rollback. |
| 62 | OPEN | 3 | 3 | Kalman-fused gradient estimate | kalman-filtering applied to the gradient step so that the noisy minibatch gradient is fused with a running model-based prediction of the gradient in proportion to their estimated uncertainties, instead of used raw | 1703.00209; 1704.03125 | None of the papers implement a per-step Kalman gain fusing an EMA-based gradient prediction with a fresh minibatch measurement (variance estimated from a second microbatch) to produce the applied optimizer update with a trackable fused covariance; the closest works either equate Kalman filtering to natural gradient on parameters, use gradient descent to approximate Kalman covariance updates for state estimation, or fuse cross-modal gradients via Dempster-Shafer combination — the specific placement (fusing prediction+measurement gradients via Kalman gain as the optimizer step itself) remains unaddressed. |
| 63 | OPEN | 3 | 3 | Loss-barrier constrained update filter | control-barrier-functions applied to the gradient step so that the update is projected onto the set of directions that keep a safety margin on validation loss (and gradient-norm blowup) never violated | 2204.01930; 2503.10384 | Nothing in the set applies a discrete-time CBF/QP projection of the raw optimizer step to enforce a hard, per-step non-violation invariant on a held-out probe loss (or gradient-norm) during standard LLM training, as opposed to general constrained-optimization barrier methods or step-size safeguards. |
| 64 | OPEN | 3 | 2 | learned-index KV lookup | learned-index applied to kv-cache so that a small predictive model replaces explicit per-token key storage for entries the model can regenerate positionally, cutting cost for predictable entries while keeping exact storage for the rest | 2502.04077; 2606.07684 | None of the retrieved works place a trained per-position/n-gram predictive model directly in the KV write path with a threshold-gated residual-or-pointer storage decision analogous to learned-index structures; the closest works either predict attention/importance scores for eviction or reconstruct via retrieval/low-rank projection rather than a positional regenerative model with explicit exact-vs-approximate branching per entry. |
| 65 | OPEN | 2 | 4 | Barrier-constrained attention mass | A control-barrier-function constraint applied to attention-scores so that the total mass assigned to any single value vector is dynamically capped to keep a defined 'safety set' (e.g. bounded influence of any one token on the residual stream) from ever being exited, allowing the softmax to abstain by pushing the remaining, unconstrained mass onto an explicit null output. | 2606.08105; 2503.07376 | The specific mechanism of an analytic per-head CBF projection on the softmax simplex that yields a provable per-forward-pass bound on any single value's contribution, with excess mass routed to a dedicated null slot as a formal safety certificate, is not implemented or claimed by any candidate, which either use CBFs for physical robot safety or describe null/no-op attention as an emergent/diagnostic pattern rather than a constructed guarantee. |
| 66 | OPEN | 3 | 2 | Consensus-voted attention abstention | A consensus-protocol applied to attention-scores so that multiple independent 'voter' sub-heads must agree a key is relevant before it receives nonzero mass, and if no quorum is reached across voters the query's output defaults to a null/abstain vector rather than a forced mixture. | 2602.06283; 2601.16397 | No paper implements multiple independent voter sub-heads that must reach quorum agreement per key, with a genuine null/abstain output vector when no key clears quorum, at the attention-score component; this exact mechanism-purpose pairing (certified abstention via redundant independent scorers, not just sparsification or post-hoc attribution) remains open. |
| 67 | OPEN | 2 | 4 | learned-index optimal-transport keys | A learned-index model that predicts each key's rank position under an optimal-transport-style sort is applied to attention-scores so that query-key similarity carries an explicit transport cost rather than raw bilinear dot-products. | 2511.07499; 2509.07963 | The specific mechanism of a jointly-trained small MLP 'learned-index' predicting a content-sorted rank coordinate per query/key, then subtracting a learned-lambda-scaled absolute difference of these predicted ranks from the dot-product at every layer, is not instantiated in any candidate; nearest works either use OT/Sinkhorn for adversarial guidance or distance-decay corrections without a rank-predicting index model trained jointly with the base transformer. |
| 68 | OPEN | 2 | 4 | Kalman-fused router confidence | Kalman filtering applied to the moe-router so that each token's router score is fused with a running per-expert reliability estimate (a Kalman state tracking expert accuracy over recent tokens) weighted by their respective uncertainties, instead of used raw | 2606.20544; 2603.09453 | No paper maintains a per-expert Kalman filter state (mean+variance of accuracy) updated from observed loss/correctness and fuses it via inverse-variance weighting with the raw router logit at every token; this specific online recursive Bayesian fusion mechanism for expert trustworthiness tracking is not covered by calibration, variational-routing, or confidence-based dynamic-k approaches. |
| 69 | OPEN | 3 | 2 | atomic expert-group commit | Transactions-and-rollback applied to the moe-router so that a token's routing decision across a block of consecutive MoE layers is grouped and only committed to the residual stream if a post-hoc consistency check across the whole group passes, otherwise the group falls back to a dense (all-expert-averaged) path. | 2608.22191; 2606.22798 | Yes: no paper implements a speculative-then-verify transactional mechanism at the moe-router that computes a multi-layer consistency score and rolls back the whole block to a dense/averaged FFN when routing across consecutive layers is incoherent; existing work only uses routing agreement for candidate/trajectory selection or decouples dispatch from aggregation weighting, not block-level commit/rollback of the routing decision itself. |
| 70 | OPEN | 2 | 4 | PID-controlled expert gate | PID feedback control applied to the moe-router so that the gate logit for each expert is corrected online by proportional, integral, and derivative terms computed from a causal running error signal (e.g. expert load deviation or realized loss residual), not just the raw linear score. | 2510.14853; 2506.14038 | The proposal's distinguishing twist is a genuinely causal, per-token PID control loop (proportional+integral+derivative terms from a running error signal) added directly to each expert's router logit before top-k, correcting drift within a single sequence rather than via training-time losses, post-hoc calibration, or migration-based balancing; no paper in the set implements this specific control-theoretic online logit correction mechanism. |
| 71 | OPEN | 2 | 4 | self-paced expert admission curriculum | Self-paced curriculum ordering applied to moe-router training so that load balance emerges from delaying hard-to-route tokens rather than from an auxiliary loss term. | 2506.14038; 2512.03915 | The specific mechanism of masking low-routing-confidence tokens from expert weight updates under an annealing threshold (a self-paced curriculum that never touches the loss function) is not what any candidate does; ALF-LB and grouped-expert methods still use explicit balancing objectives (bias terms or aux losses) rather than a token-inclusion schedule. |
| 72 | OPEN | 3 | 2 | ARC-policy residual-channel eviction | Adaptive-replacement-cache policy applied to residual-stream channels so that each token allocates its fixed write-budget only to the subset of channels currently balancing recency and frequency of use, instead of writing uniformly to all channels. | 2606.18246; 2509.09754 | Nothing found implements an ARC-style two-list (recency vs frequency) admit/evict policy operating on residual-stream channel slots per token with causal counters and a learned mixing coefficient; the closest works either statically taper width by depth, evict KV-cache entries, or gate whole-layer/whole-branch writes rather than sub-selecting which channels a token may write to under a fixed budget — that specific mechanism-object pairing (ARC cache-replacement logic applied to residual channels) is not covered. |
| 73 | OPEN | 2 | 4 | Contrastive position embedding structuring | A contrastive objective applied to positional encoding so that position embeddings are structured by learned content-position pairing rather than by a fixed a priori function of the index alone. | 2211.12773; 2606.00124 | No paper in the set trains position embeddings via an explicit contrastive loss pairing content representations with position embeddings (positive: co-occurring content-position pairs, negative: mismatched positions across the batch) to replace fixed sinusoidal/RoPE tables; the nearest work either regularizes learned position embeddings with a smoothness prior (molecular domain) or merely analyzes existing PE geometries, so the specific mechanism (contrastive content-position pairing loss shaping the PE table) remains untried. |
| 74 | OPEN | 2 | 4 | Posterior-averaged RoPE bases | Bayesian model averaging applied to positional-encoding so that extrapolation to unseen lengths is handled by mixing several rotary base frequencies weighted by their calibrated held-out likelihood instead of committing to one fixed base. | 2505.12423; 2405.13985 | No paper in the set forms an online, likelihood-weighted Bayesian mixture over multiple parallel RoPE base hypotheses at inference time; the closest works either calibrate/search a single frequency set (PSC) or replace RoPE with fixed directional attention (LookHere), so the proposed test-time BMA over K rotary bases with running validation-perplexity weighting remains open. |
| 75 | OPEN | 2 | 4 | PID-controlled merge rate | Feedback-control applied to the tokeniser's merge-rate parameter so that a PID loop drives the measured per-window compression ratio (or loss) to a target setpoint, continuously retuning granularity as local text difficulty changes. | 2506.01084; 2501.10064 | No paper closes a PID (or other explicit feedback-control) loop specifically on a tokenizer's merge-rate/threshold parameter using rolling per-byte loss as the error signal to hold compression ratio at a fixed setpoint; the nearest works either adapt granularity via learned/heuristic online compression (zip2zip) or apply PID control to a different target variable (activation steering), so applying PID explicitly to merge-threshold as the control variable for tokenization remains open. |
| 76 | OPEN | 3 | 2 | MDL-selected merge depth | A minimum-description-length criterion that scores candidate segmentations by total code length (model bits + data bits) applied to the tokeniser so that merge depth is chosen per-span to minimize total code length rather than by fixed frequency rank. | 2509.26157; 1807.09639 | None of the retrieved papers perform per-span, causal, online MDL selection among multiple precomputed tokenizer granularities (byte/BPE-small/BPE-large) under a shared multi-granularity LM at both train and inference time; the nearest work either fixes one vocabulary via MDL-like criteria or dynamically segments within a single granularity using entropy, so the proposal's core mechanism (discrete argmin search over precomputed granularity candidates via total code length, jointly trained with a granularity-tag token) remains unaddressed. |
| 77 | OPEN | 3 | 2 | Contrastive merge-embedding co-training | A contrastive objective applied to candidate subword merges during vocabulary construction so that merge selection is driven by which spans the model can predict well jointly with their contexts, rather than by raw co-occurrence frequency. | 2109.04556; 2010.10813 | Nothing in the set trains an auxiliary contrastive scorer to iteratively re-rank BPE merge candidates by predictive separability during vocabulary construction itself, before any main model exists; the nearest work either aligns embeddings post-hoc across languages/OOV settings or modifies merge graphs for compatibility, not selection-by-contrastive-loss during merge learning. |
| 78 | OPEN | 3 | 2 | Spectral block decoding | A frequency-basis transform (e.g. short-time DCT/FFT over token-embedding trajectories) applied to the sampler so that a block of future token representations can be generated by extrapolating low-frequency spectral coefficients in parallel, instead of one autoregressive step per token | 2405.18628; 2512.09535 | Nothing in the set applies a short-time DCT/FFT over token-embedding trajectories specifically to extrapolate low-frequency spectral coefficients as speculative drafts for a block of future LM tokens verified by a causal pass; the nearest works either draft via learned prompt tokens (no spectral transform) or use spectral/Fourier blocks for representation learning in non-LM-decoding contexts, so the specific spectral-extrapolation-as-speculative-draft-generator mechanism remains open. |
| 79 | OPEN | 2 | 4 | arithmetic-coded reversible sampler | arithmetic-coding applied to the sampler so that committed tokens can be losslessly re-expressed as a shorter code and re-expanded under a revised model without changing the emitted token distribution | 2601.05724; 2603.10444 | None of the papers place an arithmetic-coding bijection at the sampler specifically to let committed tokens be losslessly re-expressed and re-decoded under a different top-p/temperature for exact counterfactual replay without rerunning the forward pass; this specific retroactive re-ranking/replay capability via a stored code-stream receipt is not addressed by any candidate, which instead target compression ratio, verification-based lossless speculative decoding, or unrelated domains. |
| 80 | OPEN | 2 | 4 | Delta-rule per-token temperature memory | A delta-rule associative memory keyed on hidden state applied to the sampler so that temperature/top-k become a learned per-position readout instead of a fixed global knob. | 2603.18428; 2601.06022 | The specific mechanism—a delta-rule (erase-then-write) associative memory bank keyed on hidden state that is continuously overwritten with realized entropy/surprisal to output a per-position dispersion value for autoregressive sampling—is not implemented by any candidate; nearest works use RL policies or heuristic confidence signals instead of an associative fast-weight memory, so the core delta-rule-memory-as-sampler-controller mechanism remains open. |
| 81 | OPEN | 2 | 4 | Prioritized replay for sampling controller | Prioritized experience replay over a buffer of (hidden-state, realized-surprisal) pairs applied to the sampler's temperature/top-k controller so that per-position decisions are trained on the sequence positions where past predictions were most miscalibrated. | 2510.15674; 2410.03818 | No paper combines a ring buffer of (hidden-state, realized-surprisal) pairs with TD-error-style prioritized replay to online-train a temperature/top-k controller at decode time; this specific mechanism-object-purpose combination appears novel. |
| 82 | OPEN | 2 | 4 | importance-corrected per-token sampling | Importance-sampling reweighting applied to the sampler so that per-position temperature/top-k choices can be made cheaply from a proposal distribution while an exact correction restores the true model distribution on demand. | 1601.01125; 2309.02772 | The specific combination of per-position adaptive truncation (varying k/temperature by token position) as an explicit proposal q_t, logged importance weights w_t=p_t/q_t as a 'receipt', and periodic self-normalized resampling to recover exact model-distribution samples during autoregressive LLM decoding is not shown in any candidate; nearest work either does per-step SMC for state-space models or adaptive temperature without exact-recovery weighting. |
| 83 | OPEN | 3 | 2 | SMC-staged incremental prefill | Sequential Monte Carlo applied to prefill so that a population of partial-cache hypotheses at increasing prompt-prefixes is weighted and resampled, letting the first token be emitted from the highest-weight partial-prefill particle before the full prompt is exactly encoded. | 2604.16395; 2502.02789 | The core SMC mechanism—maintaining N weighted particles of partial-prefill KV caches, resampling at chunk boundaries via a proxy scorer, and collapsing to the exact cache before continued generation—is not implemented by any candidate; nearest work either overlaps/streams prefill without a particle population or drops tokens via importance scoring without resampling-corrected hypothesis populations, so the specific SMC-over-cache-population design for anytime TTFT remains open. |
| 84 | OPEN | 2 | 4 | PID-controlled early-exit depth | A PID controller applied to the layer-schedule so that each token's exit layer is set by driving a measured per-token confidence-error to zero rather than by a fixed or single-threshold rule. | 2604.14612; 2510.14211 | None of the candidates implement a literal PID (proportional-integral-derivative) controller with running error accumulation and derivative terms driving the per-token exit threshold; the closest works use static/heuristic thresholds or iterative search rather than closed-loop control theory, so a genuine PID-based correction of oscillation/overshoot in early-exit would still be new. |
| 85 | OPEN | 2 | 4 | EMA-trajectory layer folding | ema-averaging applied to the layer-schedule so that a block's output is replaced by an exponential moving average of its own repeated-application trajectory, giving a cheap proxy for depth-adaptive refinement without an explicit halting decision | 2512.19941; 2604.17286 | The specific mechanism—EMA over T repeated forward applications of the same block's activation output (not weights, not training iterates) substituted for the final iterate as a cheap variance-reducing proxy for adaptive depth at a fixed middle layer—is not directly instantiated in any candidate; nearest work either recurs blocks without EMA-averaging activations (Block-Recurrent Dynamics) or blends depth-adaptive token states for a different token-routing purpose (DepthVAR), leaving the exact placement/mechanism/purpose combination open. |
| 86 | OPEN | 3 | 2 | Sparse dictionary optimiser moments | Sparse-coding applied to per-parameter optimiser moments so that each module's second-moment tensor is represented as a sparse combination of a small shared per-module-type dictionary of atoms, giving the optimiser state module-level structure instead of pure per-parameter independence. | 2411.09816; 2606.07404 | The specific mechanism of maintaining a per-module-type online sparse dictionary that reconstructs Adam's second-moment EMA (with residual injection for exactness) to let new/rare parameters inherit moment estimates and compress moment storage is not addressed by any candidate; nearest works apply sparse/low-rank factorization to weights or gradients, not to the optimizer's per-parameter second-moment state itself. |
| 87 | OPEN | 3 | 2 | Spectral optimiser-moment sharing | Spectral-transform applied to per-parameter optimiser moments so that second-moment statistics for weight matrices are tracked in a frequency basis (2D DCT/FFT over the weight matrix's row/column index) where long-range correlations between distant parameters in the same module become local and can be captured by a small number of low-frequency coefficients. | 2501.07237; 2509.04713 | The specific mechanism of maintaining elementwise Adam second moments alongside a small set of low-frequency 2D-DCT coefficients of that same moment tensor, blended via inverse-DCT reconstruction into the denominator to capture cross-row/column curvature correlations (not simply compress or reduce memory), does not appear in any candidate paper. |
| 88 | OPEN | 2 | 4 | Graph-propagated optimiser moments | Graph-message-passing applied to optimiser-state so that per-parameter moment estimates are updated using averaged/gated signals from structurally adjacent parameters (same layer, same head, residual-stream neighbors) instead of being estimated in isolation. | 2410.23023; 2103.01089 | No candidate defines a static parameter-adjacency graph and performs message-passing/averaging over Adam's m/v statistics across same-layer or same-head neighbors; the nearest works either apply message passing to unrelated domains (AMP signal processing, GNN training) or modify optimizer state via projection/sharding rather than cross-parameter smoothing, so the proposed mechanism at this component for this purpose remains unaddressed. |
| 89 | OPEN | 2 | 4 | quantised curvature codebook | Product-quantisation applied to optimiser-state so that the final per-parameter second-moment vectors are compressed into a small codebook that can be shipped and reloaded to warm-start new training runs, instead of being thrown away at checkpoint time. | 2110.02861; 2501.07237 | All found papers quantize/compress optimizer state to shrink memory during a single training run; none construct a compact, reusable PQ codebook that is decoded to warm-start Adam's second moments at the start of a new (fine-tuning or restarted) run, so that specific cross-run transfer-via-codebook mechanism remains open. |
| 90 | OPEN | 3 | 2 | Hopfield-recalled curvature memory | Hopfield content-addressed retrieval applied to optimiser-state so that a bank of stored final second-moment patterns from prior training runs can be recalled by energy descent, keyed on the current model's early-training moment pattern, to warm-start optimiser state on a new but related run. | 2502.05164; 2603.07126 | Nothing in the set applies Hopfield-style content-addressed retrieval specifically to recall and blend a full per-layer Adam second-moment vector from a bank of prior completed training runs to warm-start a new run's optimizer state; the closest work either does Hopfield retrieval on tokens/attention or does non-Hopfield learned warm-starting for unrelated optimization domains, so the specific combination (Hopfield retrieval + optimizer curvature-state transfer across training runs) remains open. |
| 91 | OPEN | 2 | 4 | Kalman-fused learning rate | Kalman filtering applied to the lr-schedule so that the learning rate is set by fusing a predicted trajectory (from the schedule's prior curve) with a noisy measurement (the observed loss/gradient-noise) in proportion to their estimated uncertainties, rather than following either alone. | 2012.08867; 1910.08597 | No paper fuses a Kalman filter's predicted state (the prior LR schedule curve, e.g. cosine/warmup) with a noisy measurement (windowed loss-decrease/gradient-noise) via an uncertainty-weighted gain to directly set the applied per-step LR in an open-loop neural network training run; the closest work either filters model parameters (natural-gradient Kalman equivalence) or filters step-size in an unrelated adaptive-filter (AEC) context, so a Kalman-fused prior-schedule/measurement LR controller for standard NN pretraining/fine-tuning remains unaddressed. |
| 92 | OPEN | 2 | 4 | safety-set-bounded LR control | control-barrier-function constraints applied to the lr-schedule so that the learning rate is adjusted online to guarantee the loss (or its gradient-noise proxy) never leaves a pre-declared safety envelope, rather than following a fixed curve. | 2602.06797; 2602.20102 | No paper applies a control-barrier-function forward-invariance guarantee directly to the online lr-schedule as a per-step safety filter on a loss/gradient-noise barrier during training; the CBF work here is either classical robotics/control theory or applied to LLM latent activations for content safety, so the specific placement (CBF-constrained lr controller guaranteeing non-divergence via barrier decrease conditions on training-loss telemetry) remains unaddressed. |
| 93 | OPEN | 2 | 4 | ARC-policy data admission | Adaptive-replacement-cache (ARC) balancing of recency and frequency applied to data-order selection so that the training mixture is chosen online from a bounded working set instead of fixed in advance. | 2608.12103; 2403.18192 | The specific mechanism of maintaining two ARC-style LRU lists with a self-tuning partition parameter p, operating on individual example IDs as a bounded cache with hit/miss adaptation (rather than domain-level reweighting, static frequency tables, or hard-sample batch selection), appears novel; no paper applies genuine ARC cache-replacement logic to pretraining data order selection. |
| 94 | OPEN | 2 | 4 | Hebbian error-curriculum fast weights | Hebbian outer-product fast weights applied to data-order so that the curriculum reacts to the model's own recent loss instead of being fixed in advance | 2411.14318; 2412.00415 | None of the candidates implement the specific mechanism of an outer-product Hebbian fast-weight matrix over feature-embedding/loss pairs, decayed and used every k steps to score and reorder the sampler; the nearest works either reweight coarse domain mixtures via scaling-law velocity or reweight augmentation/selection by raw loss scores without any associative memory structure, so the Hebbian-fast-weight-as-curriculum-controller mechanism remains open. |
| 95 | OPEN | 2 | 4 | Conformal gradient-step gate | Conformal-prediction applied to the gradient so that each proposed optimizer step carries a distribution-free guarantee on the probability it increases held-out loss, before it is committed. | 2402.07407; 2510.18784 | The specific mechanism of calibrating a per-step non-conformity score against held-out loss-increase to reject/shrink the entire optimizer step (a distribution-free guarantee on the rate of loss-increasing updates) is not shown in any candidate; nearest work either applies CP to general chance-constrained optimization or reweights gradients/instances rather than gating full SGD/Adam commits. |
| 96 | OPEN | 2 | 4 | energy-verified gradient acceptance | energy-based-refinement applied to the optimizer step so that each backward-pass update is scored against a cheap scalar energy before being committed, rejecting or damping steps predicted to raise loss | 1907.01794; 2603.28248 | None of the candidates place an energy/quadratic check specifically as an accept/reject/damp gate on the optimizer's parameter update using a cheap stochastic held-out microbatch evaluation before committing; this precise placement (post-gradient, pre-commit, bounded backtracking via energy rather than loss) remains unaddressed. |
| 97 | OPEN | 2 | 4 | Atomic micro-batch commit groups | transactions-and-rollback applied to the gradient step so that a group of micro-batch gradients only updates weights if the group's aggregate loss-delta is verified non-increasing, otherwise the whole group is discarded | 2607.14952; 2608.08961 | None of the papers implement an explicit atomic commit/rollback protocol keyed to a held-out probe-loss check per micro-batch group, reverting to a prior checkpoint and retrying with reduced learning rate on failure — this transactional-training-loop mechanism at the optimizer-step granularity appears absent from the set. |
| 98 | OPEN | 2 | 3 | Byzantine-agreement head consensus | Consensus-protocol voting rounds applied to attention-scores so that per-token mixing weights reflect cross-head agreement rather than independent softmaxes. | 2506.00743; 2605.24024 | None of the papers implement a pairwise cross-head voting/consensus protocol (pairwise overlap comparison, agreement-fraction scaling) applied to per-head attention distributions before value-mixing; the closest works either aggregate heads by confidence/causal-effect scores or use cross-head variance for token pruning, not for a Byzantine-style multi-round agreement gate on each head's output. |
| 99 | OPEN | 2 | 3 | Predictive-coding residual sampler | A hierarchical predictive-coding loop that propagates only prediction errors between a fast shallow drafting stack and a slow deep verifying stack, applied to the sampler so that most decoding steps only need a cheap error-update rather than a full deep forward pass | 2603.04514; 2510.08994 | The specific mechanism—splitting a single transformer's layers into a shallow prefix and deep suffix, training a small auxiliary head to predict shallow-vs-full logit divergence, and skipping the deep suffix entirely (reusing its cache) when predicted error is below threshold—is not what any candidate does; the nearest works either gate diffusion denoising steps or accept/refine whole tokens/trajectories rather than gating depth of a single forward pass via a divergence-regression head, so this exact placement/mechanism combination appears open. |
| 100 | OPEN | 2 | 3 | Streaming teacher-student sampling controller | Online self-distillation between a slow (EMA) copy and a fast copy of a small controller head applied to the sampler so that per-position temperature/top-k adapt to the local statistics of the stream the model is currently generating. | 2403.14541; 2603.18428 | The specific mechanism of a fast/slow (EMA) teacher-student pair trained online via per-token KL self-distillation to control per-position temperature/top-k, mid-generation and without any reward or entropy heuristic, is not shown in EDT (formula-based, no learned teacher) or the RL policy sampler (uses reward signal and policy-gradient RL rather than mean-teacher self-distillation), so this exact controller design plausibly survives as new. |
| 101 | OPEN | 2 | 3 | multiresolution logit sculpting | Wavelet-multiresolution decomposition applied to the per-step logit vector so that top-k/temperature truncation acts differently on coarse (broad semantic) versus fine (lexical-choice) components of the distribution, yielding a genuinely position- and shape-dependent sampling rule instead of one scalar knob. | 2603.28114; 2505.11897 | No paper applies a wavelet/multiresolution decomposition to the per-step sampling logit vector at decode time to separately shrink high-frequency rank noise while retaining coarse multimodal shape as a sampler control (distinct from temperature/top-k); the nearest work (FiGKD, AFM) applies similar DWT/Fourier decomposition of logits or attention scores but for distillation or diffusion cross-attention, not autoregressive token sampling, so the specific placement and purpose remain open. |
| 102 | OPEN | 2 | 3 | Mixture-density draft-length head | A mixture-density output applied to the speculative-drafting confidence signal so that draft length is chosen from a predicted mixture over accept-run-length rather than a point-estimate confidence threshold, capturing multimodal reliability (e.g. bimodal 'easy vs. hard token' regimes). | 2512.13194; 2604.13634 | Yes: none of the retrieved papers replace the scalar confidence-to-k mapping with a trained mixture-density network over accept-run-length that explicitly models multimodal/bimodal uncertainty and samples k as a quantile of that mixture; existing work uses scalar uncertainty adjustments, heuristics, or frequency-based calibration rather than a distributional head over run-length. |
| 103 | OPEN | 2 | 3 | traced hot-path layer skipping | JIT hot-path tracing applied to the layer-schedule so that recurring token/context patterns get a cached, specialised (shorter) block sequence instead of running the full fixed depth. | 2601.02569; 2506.03700 | None of the candidates implement a hash-keyed trace table that memoizes an entire recorded layer-subset execution for a recognized recurring local context (n-gram/boilerplate/copy span) with periodic KL-verified validity, as opposed to per-token confidence/halting-based adaptive depth or temporal reuse between consecutive tokens; that JIT-tracing/memoization-of-repeated-context mechanism at the layer-schedule appears genuinely absent. |
| 104 | OPEN | 2 | 3 | submodular greedy FFN key selection | Greedy submodular-maximization over the FFN's key set applied to the FFN memory read so that per-token key activation is sparsified with a provable approximation ratio to the dense score instead of an ad-hoc top-k. | 2305.13999; 2508.14302 | None of the retrieved papers formulate FFN key selection as submodular maximization over a coverage/facility-location surrogate with a lazy-greedy (1-1/e) approximation certificate as a per-token, per-layer replacement for top-k; that specific mechanism-object pairing (submodular greedy with provable ratio vs ad-hoc top-k/rank-aggregation heuristics) is not covered. |
| 105 | OPEN | 2 | 3 | LSH-bucketed FFN keys | Hashing-LSH applied to the FFN memory so that keys are grouped into locality-sensitive hash buckets and only the token's own bucket is scored instead of all keys. | 2503.15798; 2511.09323 | None of the candidates implement LSH-style hyperplane hashing that buckets FFN keys/queries so that only same-bucket keys are scored, enabling sublinear/near-constant cost as key count grows arbitrarily; the nearest works use top-k gating or ID-based lookup tables rather than similarity-preserving hash buckets, so the specific mechanism-placement-purpose combination remains open. |
| 106 | OPEN | 2 | 3 | Contrastive cross-sequence normalisation stats | a contrastive objective applied to normalisation so that per-token scale/shift statistics are pulled toward a batch-level cluster of 'similar-context' tokens and pushed away from dissimilar ones, injecting cross-sequence structure into an otherwise purely local statistic | 2305.02582; 2103.03166 | No candidate applies an InfoNCE-style contrastive loss on the pre-normalization activation to pull same-context tokens' LayerNorm statistics together across different training sequences while leaving inference-time computation as ordinary per-token LayerNorm; this specific training-time auxiliary-objective-on-normalization-parameters mechanism for cross-sequence consistency remains unaddressed. |
| 107 | OPEN | 2 | 3 | Wavelet cross-token normalisation | Wavelet multiresolution decomposition applied to per-token activation normalisation so that normalisation statistics are computed jointly across nearby tokens at multiple temporal resolutions rather than per single token. | 2512.06929; 2101.11306 | Yes: no candidate replaces per-token LayerNorm's mean/variance computation itself with a causal, streaming, trainable-tap wavelet multiresolution decomposition over a sliding token window inside transformer layers; the nearest works either decompose CNN feature maps or time-series inputs as a separate preprocessing/detrending block rather than reworking the normalization statistic computation at each transformer layer. |
| 108 | OPEN | 2 | 3 | renormalised optimiser moments | Renormalisation-group coarse-graining applied to optimiser-state so that per-layer moment statistics are pooled across a scale hierarchy of parameter blocks instead of kept fully per-parameter. | 2608.19491; 2604.04281 | None of the papers pool per-parameter second-moment estimates into block-averaged coarse-grained statistics that are then blended back down to initialize or correct individual parameter moments upon activation; this specific RG-style hierarchical moment-inheritance mechanism for cold-start/curvature propagation remains unaddressed. |
| 109 | OPEN | 2 | 3 | curvature-retrieval second-order warmstart | kNN-retrieval over a saved bank of per-parameter Adam moment vectors (indexed by parameter identity plus a compressed loss-curvature signature) applied to optimiser-state so that a freshly initialised training run can warm-start its second-moment estimates from the nearest historical run instead of the standard zero/bias-corrected init. | 2410.17413; 2605.21539 | Nothing in the set retrieves nearest historical runs' Adam moment/curvature signatures via kNN to warm-start a fresh run's second-moment estimates; the nearest paper (fact tracing) uses optimizer-state correction for attribution, not for initializing new runs, so the cross-run kNN warm-start of (m,v) remains untried. |
| 110 | OPEN | 2 | 3 | ARC-style recency/frequency LR adaptation | an adaptive-replacement-cache-style recency/frequency balance applied to the lr-schedule so that the effective step size is set by an online balance between a 'recency list' (recent loss volatility) and a 'frequency list' (sustained long-run improvement rate), with the balance parameter itself adapted online | 2606.21238; 2506.08419 | Nothing applies the ARC cache-replacement self-balancing-p mechanism (two timescale trackers plus a hit-rate-driven adaptive mixing parameter) to the LR schedule itself; the KV-cache paper uses this mechanism for cache eviction, not step-size control, so the specific placement/mechanism combination remains open. |
| 111 | OPEN | 2 | 3 | reflex/override dual-rate controller | dual-process gating applied to the lr-schedule so that a fast per-step reflex adjustment (cheap, reactive to instantaneous grad-noise) is overridden by a slow deliberate controller (reactive to a windowed loss trend) whenever the two disagree, producing closed-loop lr control with two timescales instead of one fixed curve. | 2403.13728; 1910.08525 | The specific dual-rate architecture where a fast per-step grad-noise reflex multiplier is explicitly overridden by a slower windowed loss-slope controller whenever their signs disagree (an arbitration/override rule between two feedback loops at different timescales) is not implemented by any paper here; nearest works use a single feedback signal or a hierarchical constraint dispatch rather than a two-timescale conflict-resolution gate. |
| 112 | OPEN | 2 | 3 | Prioritized replay for horizon errors | Experience replay applied to the training objective so that training re-visits token positions with high historical multi-horizon prediction error on a prioritized schedule, is applied so that the model allocates more gradient updates to positions whose far-future predictability was previously poor. | 2402.04858; 2407.09702 | The proposal's specific mechanism—buffering by (sequence_id, position) keyed on an EMA of an auxiliary k-token-ahead prediction head's loss, mixed with fresh data via importance-weighted proportional sampling during a single from-scratch LM pretraining run—targets long-range predictability directly and is not instantiated by any candidate, which apply PER to RL trajectories, TD-error control tasks, or generic loss-based sample replay rather than multi-horizon LM token-position prioritization. |
| 113 | OPEN | 2 | 3 | Kalman-fused logit calibration | Kalman filtering applied to the training objective so that per-position logits are treated as noisy measurements fused with a running state estimate of the true token-frequency distribution, producing a calibrated posterior rather than a raw softmax point estimate. | 2511.09101; 2604.09624 | Yes: none of these papers explicitly formalize per-position logits as noisy measurements fused via a Kalman filter (predict/update with process and measurement covariance) against a latent true-frequency state per vocabulary bucket, which is the specific mechanism and object (logit vector as KF observation with trackable covariance) claimed novel here. |
| 114 | OPEN | 2 | 3 | PID-controlled replay rate | Feedback-control applied to data-order so that the rate of revisiting past examples is driven by a PID loop tracking a measured training-error signal instead of a fixed schedule. | 2603.09892; 2508.14926 | No paper closes a feedback loop where a PID controller (proportional-integral-derivative on a measured loss-slope error) directly sets the replay-buffer sampling fraction each interval; MSSR uses memory-strength heuristics and the driving paper uses PID for vehicle control, not data-order feedback, so the specific mechanism-object pairing (PID output -> replay rate) remains open. |
| 115 | OPEN | 2 | 3 | Kalman-fused mixture weights | Kalman filtering applied to data-order so that the per-domain sampling probability is a running fused estimate of predicted vs measured loss-improvement, weighted inversely by each domain's estimate uncertainty rather than by a fixed EMA rate | 2604.00388; 2608.20169 | No paper in the set applies a Kalman filter (predict/update with process+measurement noise and decaying per-domain variance) to fuse predicted and measured loss-improvement for LM data-mixture/curriculum sampling weights; the closest works use gradient-similarity scores or plain variance-weighted sampling rather than an explicit KF state-uncertainty fusion, so the specific mechanism-at-component pairing remains unaddressed. |
| 116 | OPEN | 2 | 2 | Learned-index FFN lookup | Learned-index applied to the FFN memory so that a small predictor model maps a token's query directly to the position(s) of relevant keys instead of scoring every key. | 2503.15798; 2501.00823 | None of the papers train an auxiliary predictor that maps queries directly to bucket/position indices into a sorted FFN key array to skip dense scoring at inference (the core learned-index-structure mechanism); the nearest works either use discrete-id lookup tables (MoLE) or similarity-based key-value retrieval (HoReN), so a genuine learned-index-over-sorted-keys FFN lookup trained jointly with an auxiliary bucket-prediction loss remains unaddressed. |
| 117 | OPEN | 1 | 3 | LSH-bucketed curvature memory bank | hashing-LSH over per-parameter (m,v) Adam state vectors, bucketing by locality-sensitive hash of a compressed curvature fingerprint, applied to optimiser-state so that curvature information from many past training runs can be pooled and cheaply retrieved without an exact nearest-neighbour scan. | 2410.17413; 2405.12207 | Nothing in the set hashes per-parameter Adam (m,v)/curvature fingerprints into LSH buckets to retrieve an approximate curvature-informed init in O(1) lookups across many past runs; the nearest papers do approximate retrieval indexing generically or influence/attribution via optimizer-state correction, not curvature-prior warm-starting of optimizer state via LSH. |
| 118 | ADJACENT | 4 | 4 | Choquet-capacity expert router | A 2-additive Choquet integral over a fuzzy measure with learned pairwise interaction terms is applied to the moe-router so that top-k expert selection can explicitly favor complementary experts and penalize redundant ones, instead of ranking experts by independent linear scores. | 2607.26164; 2607.28308 | The specific 2-additive Choquet-integral formalism with a learned low-rank pairwise interaction capacity, combined subset-scoring via greedy/beam search and a pairwise co-selection balancing loss, is not shown in any of these papers, so a rigorous instantiation and ablation of this exact mechanism at the router would still be new relative to the graph-interaction and Choquet-as-aggregator precedents. |
| 119 | ADJACENT | 4 | 3 | Predictive-error KV cache | Predictive-coding applied to the kv-cache so that each layer stores a prediction-error residual against a top-down prediction from later/higher layers instead of the raw key/value, letting later context implicitly rewrite what the entry 'means' at read time. | 2604.15356; 2605.06472 | The proposal's distinctive twist—training a per-layer top-down predictor conditioned on the current (later) decoding state so that the SAME stored residual yields a DIFFERENT effective K/V as later context grows, i.e. genuine reinterpretation without mutation—is not demonstrated in the nearest paper (2604.15356), which uses predictive residual coding purely for compression with a fixed (not read-time-recomputed-per-later-context) prediction; if the mechanism is shown to implement content-dependent revision under append-only storage rather than just entropy reduction, it would remain new. |
| 120 | ADJACENT | 4 | 3 | RG coarse-graining KV cache | Renormalisation-group coarse-graining applied to the kv-cache so that groups of old, low-salience entries are merged into fewer effective entries whose attention-relevant statistics are preserved. | 2602.16284; 2402.06082 | The specific RG-flavored iterative coarse-graining across scales (repeated block-merging with a size-preserving auxiliary projector plus local KL/L2 refit at each step, applied recursively so cache shrinks with a formal renormalization schedule as context grows) is a more principled/iterative version than the single-shot attention-matching or clustering methods, but the core mechanism (fit synthetic KV pairs to match block attention output, training-free) is already demonstrated by Attention Matching/Cartridges-style work. |
| 121 | ADJACENT | 4 | 3 | Slow-fast self-distilled cache consolidation | Online self-distillation applied to the kv-cache so that a slowly-updated teacher summary distills evicted entries into a compact consolidated representation before they are dropped by the fast cache. | 2606.07878; 2608.01247 | The specific two-tier fast/slow cache architecture with a continuously EMA-stabilized teacher and incremental every-k-token online gradient consolidation (rather than one-shot amortized synthesis or per-context optimization) is a narrower architectural variant that these papers do not explicitly instantiate, so a proposal centered on that exact online slow-teacher/fast-student consolidation loop could still be marginally new, though the core mechanism is already well covered. |
| 122 | ADJACENT | 3 | 5 | EMA-consolidated KV summary | EMA-averaging applied to the kv-cache so that evicted entries are folded into an exponentially-decayed running summary slot rather than discarded outright. | 2606.01563; 2605.29873 | The proposal's specific twist—an explicit extra 'summary slot' per head that receives exactly the evicted entry via alpha*summary+(1-alpha)*evicted_kv and then participates in attention like a normal KV pair (rather than smoothing all live entries or using moment statistics for score correction)—is a narrower, more literal instantiation than what MomentKV or KVSmooth do, so a tightly scoped version could survive, but it is very close to MomentKV's mechanism and purpose. |
| 123 | ADJACENT | 4 | 3 | RG-coarsened KV summaries | a renormalisation-group style coarse-graining applied to the kv-cache so that old KV blocks are iteratively merged into coarser-scale summaries whose surviving degrees of freedom are exactly the ones later attention still uses | 2406.02069; 2407.08454 | The specific RG framing—recursive, scale-boundary-triggered merging into a log-depth pyramid where the merge operator is explicitly trained to preserve exactly the attention outputs later queries would use (an information-theoretic renormalization criterion), applied purely at cache-write time as blocks age past scale boundaries—is a more principled and recursive construction than the single-level or layer-wise merging in KVMerger/PyramidKV/LightVLM, so a version that demonstrates genuine multi-scale (log-depth) recursive coarsening with RG-consistent training could still be new. |
| 124 | ADJACENT | 4 | 3 | RG coarse-grained attention cascade | Renormalisation-group coarse-graining is applied to attention-scores so that the model computes attention at multiple explicit scales, keeping only what survives block-averaging, instead of a single bilinear similarity at token resolution. | 2607.15449; 2512.04025 | The specific enforced fixed-point/consistency loss (requiring level L+1 attention weights equal the block-sum of level-L weights, i.e. a literal RG coarse-graining constraint rather than just multi-level pooled KV or post-hoc RG analysis) is not present in PSA or the RG-analysis paper, so a proposal built specifically around that trained consistency constraint as the mechanism generating the hierarchy would still be novel. |
| 125 | ADJACENT | 4 | 3 | wavelet multiresolution key-value bank | A wavelet multiresolution decomposition is applied to attention-scores so that keys and values are represented simultaneously at multiple frequency bands, letting a single attention operation mix content at both fine detail and coarse trend scales instead of one bilinear score per pair. | 2303.12398; 2506.11454 | A genuinely causal, streaming-safe wavelet transform applied specifically to the KV-cache of an autoregressive LM (enabling per-band inspection/ablation of cache entries during generation) is not demonstrated in these vision/PDE/segmentation-focused non-causal wavelet-attention works, so the causal KV-cache-native framing and interpretability angle could still be new if not merely an application-domain relabeling. |
| 126 | ADJACENT | 4 | 3 | test-time-trained router | Test-time-training applied to the moe-router so that a small subset of router parameters is updated via a self-supervised loss on the incoming token stream at inference time, closing the downstream-error feedback loop that a frozen open-loop gate lacks. | 2510.14853; 2502.20395 | The proposal's twist of updating only a small subset of router parameters via a hindsight-teacher-forced next-token loss (rather than additive router-logit vectors or output-side re-weighting) is a narrow implementation difference, not a new capability, so it would need a demonstrably different self-supervised objective or theoretical guarantee to survive as new. |
| 127 | ADJACENT | 4 | 3 | Per-token delta-rule width | delta-rule-memory applied to the residual stream so that each token can allocate an effective write capacity (erase-then-write at a token-specific key) proportional to its own need, instead of every token being forced through the same fixed-width additive channel | 2601.00417; 2609.07816 | The specific framing of adding a small-dimension (d/4) delta-rule memory bank in parallel with the residual stream at every block, explicitly evaluated as a token-adaptive capacity control (vs. sequence-mixing) at matched FLOPs/params against a fixed-width baseline, is not quite what DDL or the delta-rule linear-attention papers test, but DDL's gated rank-1 residual overwrite with explicit capacity/width separation is functionally the same mechanism-at-component-for-purpose, so only a very narrowly scoped empirical distinction (e.g. isolating capacity-allocation effects independent of sequence-mixing benefits) would be new. |
| 128 | ADJACENT | 4 | 3 | windowed masked-diffusion decoding lane | Masked-diffusion denoising applied to the sampler so that a block of upcoming positions is filled in parallel over a few bidirectional refinement passes instead of one autoregressive token at a time, with the result verified against the exact causal model before being committed. | 2510.02358; 2601.16971 | The specific combination of a trained small denoising adapter sharing the base backbone at matched FLOPs, plus a hard per-token exact-causal-probability threshold triggering rollback to strict autoregressive re-decoding (rather than soft lattice search or another model's revision), is a narrower verification mechanism not explicitly instantiated in DiffuSpec or Speculative Correction, so a paper demonstrating exact-distribution-preserving rollback under this specific adapter setup could still be new. |
| 129 | ADJACENT | 3 | 5 | conformal per-token abstention bands | conformal-prediction applied to the sampler so that each committed token carries a distribution-free guaranteed-coverage confidence set, letting downstream consumers or a rollback controller detect exactly which committed tokens are statistically unreliable | 2512.22682; 2509.00461 | The specific coupling of a calibrated conformal per-token set (not just entropy/confidence) to a rule-based rollback controller that triggers windowed re-decoding only on non-conformal spans, logging set-size events for downstream consumers, is not demonstrated in any single cited work. |
| 130 | ADJACENT | 4 | 3 | Information-bottleneck prefill cache pruning | An explicit information-bottleneck objective applied to prefill so that the KV cache built during prompt processing is compressed to the minimal sufficient statistic for predicting the continuation, discarding redundant prompt structure rather than caching every token's K/V. | 2509.19228; 2607.01520 | The specific instantiation as an explicit rate-distortion IB objective (cross-entropy relevance term + KL-to-prior rate term) with a trainable cross-attention query bottleneck compressing the entire prefill KV cache, trained from scratch/grafted and ablated against a matched-budget nano model, is not directly shown in any single paper, so a rigorous from-scratch IB-vs-fixed-scheme ablation could still be a genuine (if narrow) contribution. |
| 131 | ADJACENT | 4 | 3 | Predictive-coding depth iteration | Predictive-coding message passing applied to the layer-schedule so that each layer receives only the prediction-error residual from the layer below and iteratively refines its estimate over several internal steps before the token advances, producing a genuine loop where a block's own error signal determines re-application. | 2112.03378; 2006.04182 | The proposal's specific instantiation as a drop-in replacement for the transformer residual stream at nano-scale with matched-FLOPs comparison and adaptive-T convergence-gated re-application (rather than as a biologically-motivated alternative to backprop or a generic feedforward-to-PC translation) is a narrower systems/architecture claim not directly tested in the cited PC papers, so a demonstration that this gating yields a genuinely new capability (early-exit-like convergence detection at inference within a causal transformer) could still be novel. |
| 132 | ADJACENT | 4 | 3 | slow-teacher horizon distillation | online self-distillation applied to the training objective so that a slowly-updated (EMA) copy of the model provides an auxiliary target on its own future hidden state, adding a term that rewards representations for what they will be useful for several tokens ahead, not just the immediate next token. | 2509.24317; 2210.02077 | The specific twist of using the EMA teacher's hidden state at a fixed future token offset (t+k) as a longer-horizon auxiliary target for autoregressive LM training, via a delayed activation buffer, rather than same-position/same-frame consistency or masked-region prediction, is not explicitly instantiated in these papers and would need to be shown as the distinguishing contribution. |
| 133 | ADJACENT | 4 | 3 | content-built dynamic attention graph | graph-message-passing applied to attention-topology so that the connectivity pattern is constructed from a cheap first pass over content rather than fixed in advance | 2003.05997; 2505.00315 | A genuinely new angle would be the causal, layer-local KNN-graph-construction-plus-one-message-passing-step specifically as a pre-attention rewiring module trained jointly with a causal LM at matched FLOPs against a sliding-window+sink baseline, since the closest works either use k-means/expert routing instead of explicit graph message passing (Routing Transformer, MoSA) or apply dynamic graph message passing non-causally in vision rather than as a causal attention-topology precursor in autoregressive LLMs. |
| 134 | ADJACENT | 4 | 3 | Transactional buffered rewrite window | Transactions-and-rollback applied to attention-topology so that a bounded window of past representations can be atomically rewritten once later context arrives, without breaking causal decoding. | 2608.08086; 2511.01266 | The proposal's twist—an explicit all-or-nothing learned commit gate that atomically accepts/discards a bidirectional rewrite pass over a bounded trailing buffer in a text-token autoregressive transformer, trained from scratch at matched FLOPs—is not demonstrated by Archer (training-free KV cache reuse for already-bidirectional DLMs) or the video-streaming window/knot papers (smoothing/sink mechanisms without atomic commit gating or rollback semantics); a genuinely novel contribution would need to show this gated atomic transaction mechanism itself, not just windowed revision or cache reuse. |
| 135 | ADJACENT | 3 | 4 | Choquet-capacity expert selection | the choquet-integral applied to the MoE router so that top-k expert selection accounts for pairwise redundancy/complementarity instead of ranking experts by independent scores. | 2607.26164; 2510.13079 | A genuinely novel contribution would be the specific formalism of a learned 2-additive Choquet capacity used at inference time per-token to select the joint-utility-maximizing k-subset (not aggregation, not post-hoc pruning, not a heuristic diversity penalty), trained jointly with the router from scratch and shown to beat both redundancy-heuristics (GatePro) and independent top-k scoring at matched FLOPs. |
| 136 | ADJACENT | 3 | 4 | delta-rule KV overwrite | delta-rule-memory applied to the KV cache so that writing a new value at a previously-used key position first erases/interpolates the old value instead of purely appending. | 2607.02303; 2605.22884 | The proposal's specific twist—training the delta-memory strictly as a parallel, non-evicted auxiliary slot bank at matched param/FLOP budget purely to let later tokens overwrite stale content at matched-key positions while the full append-only KV remains untouched (rather than being fed by evictions, replacing state, or serving as the primary compressive path)—is a narrower combination not explicitly tested in HOLA, Tensor Cache, or FDM, but it is very close to all three and would likely read as a direct follow-up/ablation of this cluster. |
| 137 | ADJACENT | 4 | 2 | energy-descent KV refinement | energy-based-refinement applied to the KV cache so that cached key/value vectors are iteratively updated by gradient descent on a learned energy function conditioned on the growing context, instead of being fixed at write-time. | 2603.13875; 2505.15781 | The specific mechanism of a learned scalar energy function scored against a running context summary, iteratively refined every k tokens via gradient descent (not a single steering step or forward-pass caching schedule, and not tied to a diffusion denoising process or prefix-token compression), trained end-to-end as an inner-loop optimization is not demonstrated by any of these; this repeated energy-descent correction of frozen-key/updated-value pairs throughout ordinary autoregressive decoding remains a distinct proposal. |
| 138 | ADJACENT | 3 | 4 | Generational GC cache tiers | Generational-garbage-collection applied to the kv-cache so that entries are age-partitioned into a young (sliding-window, frequently re-evaluated) tier and an old (sink, rarely-touched) tier, and promotion between tiers triggers a consolidation rewrite instead of blind eviction. | 2505.16950; 2608.11701 | The specific generational-GC framing—two explicit age-based lanes (sliding-window young vs. sink old) with promotion triggering a trained consolidation rewrite specifically at the young→old boundary, matched-FLOPs against removed sink capacity—is a narrower instantiation than Bottlenecked Transformer's periodic whole-cache rewrite or Consolidator's STM→LTM routed transform, so a twist tying rewrite cost/trigger strictly to sliding-window aging dynamics could still be new, but the core mechanism (non-causal in-place KV rewrite at a tier transition) is already claimed. |
| 139 | ADJACENT | 4 | 2 | RG coarse-grained KV hierarchy | Renormalisation-group coarse-graining applied to the kv-cache so that entries are recursively merged into progressively coarser scales, keeping only the degrees of freedom that survive repeated blocking | 2605.06946; 2306.01070 | The specific mechanism of a trained recursive dyadic blocking/pooling transform applied jointly at pretraining to merge adjacent KV entries into coarser time-scale summaries (an explicit RG-like real-space decimation of cache entries over time, not layers or fixed frequency bands) is not clearly instantiated in any cited work, so a from-scratch model with this exact mergent operator and matched-FLOPs comparison could still be novel. |
| 140 | ADJACENT | 3 | 4 | Hebbian fast-weight KV overlay | Hebbian fast-weights applied to the kv-cache so that a short-lived outer-product overlay lets recent tokens' keys be nudged by later associations without touching the permanent stored K/V | 2605.22884; 2606.25342 | The specific framing of a bounded-norm, fast-decaying (lambda<1) additive correction term explicitly designed to let recent associations be transiently revised and then auto-forgotten without ever touching stored K/V—rather than as a compression/capacity mechanism (Tensor Cache) or a persistent state-tracking substrate (TTT/parametric attention)—is a narrower purpose not directly targeted by the closest papers, but it is a thin twist given how mechanically identical the update/read rule is. |
| 141 | ADJACENT | 3 | 4 | homeostatic KV precision allocator | homeostatic-plasticity applied to kv-cache so that each entry's bit-width adapts to keep its long-run attention-mass contribution near a fixed target instead of every entry costing the same | 2603.20616; 2502.13176 | The specific homeostatic control law that continuously re-quantizes each entry's bit-width (from a fixed set) to hold population-mean attention-mass-per-bit constant, without any profiling pass, budget target, or learned selector, is not shown in these papers; if that exact self-regulating EMA-to-quantization-level mapping is the twist, it could still be novel. |
| 142 | ADJACENT | 4 | 2 | Wavelet multiresolution KV cache | Wavelet multiresolution decomposition applied to the kv-cache so that each token's key/value is stored at multiple resolutions simultaneously, letting attention read coarse or fine detail depending on distance. | 2606.00724; 2606.10671 | The proposal's twist of a fixed invertible wavelet transform that keeps approximation coefficients permanently and reconstructs approximate keys/values via a learned inverse-projection head—rather than scoring tokens for eviction or merging adjacent entries—could still be new if it demonstrably differs from WaveFilter's use of wavelets purely as a token-importance/filtering signal. |
| 143 | ADJACENT | 3 | 4 | generational KV eviction tiers | generational garbage collection applied to the kv-cache so that eviction follows an age-partitioned survival policy instead of a single heuristic threshold | 2607.10582; 2608.05326 | The specific generational-GC mechanics (distinct sweep cadences per age tier, promotion between tiers based on attention-mass survival thresholds tuned to hit a cache-size budget, explicitly modeled on age-since-last-hit rather than semantic region or precision) is not directly instantiated by any single paper, so a system built precisely around age-partitioned differential sweep frequency with promotion thresholds could still be novel relative to MemDecay's region-based decay and QEvict's precision-tier promotion. |
| 144 | ADJACENT | 4 | 2 | Hopfield-addressed shared KV bank | Hopfield content-addressed retrieval applied to the kv-cache so that a shared cross-request bank of prefix KV patterns is recalled by energy descent from partial/noisy queries instead of requiring exact prefix matches | 2510.12872; 2606.09862 | The specific mechanism of energy-descent (modern-Hopfield) retrieval over a trained pooling/query projection against a bank of pooled prefix representations, with an explicit iterative attractor-dynamics formulation and partial-recompute verification, is more specific than KVCOMM's anchor-deviation estimation, so a paper demonstrating genuine Hopfield energy-descent convergence properties (not just nearest-anchor lookup) for KV approximation could still be new. |
| 145 | ADJACENT | 4 | 2 | Sparse-coded attention with null atom | Sparse-coding applied to attention-scores so that a query is represented as a sparse combination over a learned dictionary of value atoms including an explicit zero-atom, allowing exact abstention when no atom fits. | 2511.20194; 2605.21649 | The explicit trainable all-zero dictionary atom giving a structural abstention option (as opposed to entmax/sparsemax's incidental exact zeros from a fixed normalization map) combined with an ISTA-unrolled dictionary replacing V at every layer is a distinct mechanism-purpose pairing not directly shown in these papers. |
| 146 | ADJACENT | 3 | 4 | reflex-deliberate abstention gate | Dual-process-gating applied to attention-scores so that a query with no relevant keys can abstain from mixing values entirely rather than being forced to distribute mass over an irrelevant set. | 2306.12929; 2602.01203 | The specific dual-process architecture (a cheap query-only fast gate with a slower query+top-1-key MLP override on disagreement) is a distinct mechanistic twist not present in any cited gated-attention work, which use a single learned gate rather than a two-speed reflex/deliberate arbitration scheme. |
| 147 | ADJACENT | 3 | 4 | Submodular greedy expert selection | Greedy submodular-maximisation applied to moe-router so that top-k selection accounts for redundancy and complementarity between experts instead of scoring them independently. | 2510.13079; 2507.06567 | A twist could survive if the proposal is the first to actually replace top-k with a per-token greedy submodular loop carrying a provable (1-1/e) coverage guarantee computed from a jointly-trained expert-embedding table, rather than a redundancy-penalty heuristic (GatePro) or an offline placement problem (SlimCaching); this exact per-token in-router greedy-with-guarantee mechanism is not demonstrated in the retrieved set. |
| 148 | ADJACENT | 3 | 4 | self-distilled adaptive router | Online self-distillation applied to the moe-router so that a slow (EMA) copy of the router's implied expert-selection distribution teaches a fast copy in real time, giving the gate a feedback signal about which choices actually reduced loss. | 2604.13508; 2601.14797 | The specific combination of an EMA-smoothed shadow router as a mean-teacher-style target, gated by a per-token comparison of realized next-token loss between shadow and live expert choices (rather than KL-consistency or replay alone), appears novel relative to these adjacent stabilization/distillation approaches. |
| 149 | ADJACENT | 3 | 4 | Hebbian fast-weight router feedback | Hebbian outer-product fast weights applied to the moe-router gate so that the gate's expert scoring adapts within a sequence based on the expert's recent downstream error, without any gradient step. | 2510.14853; 2606.08814 | The specific mechanism of an outer-product Hebbian fast-weight matrix added directly onto the router's gate weight, driven explicitly by the chosen expert's downstream prediction-error signal (not self-supervised loss on generated text or GHA-based subspace tracking), applied per-token with decay/reset within a sequence, is not demonstrated by any of these; a rigorous comparison against STAR's GHA-router and Rewiring's self-supervised router-vector update would need to show this error-driven Hebbian credit-assignment step is a genuinely distinct update rule. |
| 150 | ADJACENT | 4 | 2 | learned-index dynamic tokeniser | A learned-index model that predicts merge-boundary positions applied to the tokeniser so that segmentation granularity adapts per-span at inference time. | 2506.18639; 2307.02092 | The proposal's specific twist of a causal, per-16-byte learned merge-boundary predictor trained jointly with LM loss (bootstrapped from BPE merges) that runs identically at train and inference to reshape the LM's own vocabulary/embedding table is not demonstrated in these text-LM-focused works, which mostly use entropy/attention heuristics or offline-trained assigners rather than a jointly-trained learned-index replacing the BPE merge lookup itself. |
| 151 | ADJACENT | 4 | 2 | Predictive-error-routed hierarchical tokens | Predictive coding applied to the tokeniser so that a coarse token level only passes a fine-grained byte-level correction signal upward when the coarse prediction fails, letting effective granularity be chosen by prediction error rather than fixed at pre-training time. | 2506.18639; 2605.30080 | The specific mechanism of using the coarse LM's top-1 probability as a threshold to conditionally inject a byte-level residual-error embedding into the main model at inference time (rather than fixing vocabulary/chunk boundaries at training time) is not demonstrated by any of these papers. |
| 152 | ADJACENT | 3 | 4 | Kalman-fused logit calibration | Kalman filtering applied to the output-head so that the preference-tuned logits (a noisy 'measurement') are fused at inference time with a pretrained-base-model prediction (the 'prior') in proportion to each source's estimated uncertainty, restoring calibration lost during preference tuning. | 2601.04277; 2503.22725 | The specific mechanism of running base and preference-tuned models in parallel and fusing their logits via a Kalman-style inverse-variance-weighted average (explicit variance/gain parameters, not just a scalar temperature) is not shown in 2601.04277, so a proposal demonstrating this exact variance-weighted fusion mechanism against that baseline could still be new. |
| 153 | ADJACENT | 3 | 4 | dual-path abstention gate | A dual-process gate applied to the output-head so that the model can emit an explicit abstain token when a slow verifier disagrees with the fast reflex logits. | 2607.04430; 2509.01455 | The specific mechanism—an extra transformer block reading the same final hidden state as a second 'slow' path, jointly trained with a threshold on fast/slow KL divergence to emit a first-class ABSTAIN token at the output head—is architecturally more specific than the cited calibration/refusal frameworks, so a proposal demonstrating this exact dual-path KL-gated architecture outperforms generic calibration heads could still be novel. |
| 154 | ADJACENT | 3 | 4 | fast-weight overwrite of committed token memory | delta-rule-memory applied to the sampler so that a committed token's contribution to a separate associative fast-weight state can be erased and overwritten by later context, without altering the emitted token sequence itself | 2606.26560; 2608.27763 | The specific framing as a frozen-base-LM graft where only a small linear read head and mixing gate are trained to blend causal KV attention with a separately-updated delta-rule associative state (explicitly preserving emitted-token causality while allowing 'semantic rollback' of meaning) is a narrower engineering variant not explicitly tested in these papers, but the core mechanism (delta-rule overwrite of key-value associative memory decoupled from the emitted sequence) is already established as the same operation on the same object. |
| 155 | ADJACENT | 3 | 4 | transactional multi-token commit sampler | transactions-and-rollback applied to the sampler so that a block of k tokens is generated speculatively as an uncommitted transaction and only committed atomically if a post-hoc verifier accepts the whole block, otherwise the entire block is discarded and regenerated | 2509.06982; 2608.14653 | The proposal's twist of strictly atomic, fixed-size k-token blocks with full KV-cache checkpoint/restore and a generic pluggable verifier (rule-based or trained classifier) applied uniformly to any downstream check, rather than a domain-specific guard (safety, compiler, uncertainty) or arbitrary-length rollback region, is a narrower engineering framing but not a clearly new capability beyond CARE/Hydra/uncertainty-rollback, which already establish generate-block-then-verify-then-rollback-or-commit at the sampler with an auxiliary trained or rule-based checker. |
| 156 | ADJACENT | 3 | 4 | Speculative layer-truncated prefill | Speculate-and-verify applied to prefill so that time-to-first-token is bounded by a cheap shallow-network pass instead of the full-depth exact pass, while exact hidden states are restored before generation. | 2605.06105; 2410.08391 | The specific speculate-and-verify-with-rollback loop—emitting token 1 from an approximate shallow/draft KV cache and then exactly reconciling/rolling back per-layer against the full pass before continuing generation—is not what any of these do; they either permanently truncate depth (SPEED), replace the cache once without verification (KV Prediction), or prune tokens rather than depth (SpecPrefill), so a verified, correctable approximate-prefill mechanism could still be new. |
| 157 | ADJACENT | 3 | 4 | PID-controlled adaptive depth loop | A PID feedback controller applied to the layer-schedule so that a block's output error signal (e.g. residual-norm change or a learned halting score) drives repeated re-application of that block until the error is driven below threshold, instead of the fixed single pass. | 2603.01914; 2312.10193 | The specific twist of using an explicit trained PID (proportional-integral-derivative) control law—with separate P/I/D gains reacting to residual-norm error dynamics rather than a single learned scalar halting probability—is a different controller parameterization, but since ACT/AdaPonderLM/ACM already achieve the identical capability (data-dependent iterate count via error/halting signal) at the identical component, this is a controller-design variant rather than a new capability. |
| 158 | ADJACENT | 3 | 4 | Test-time-trained FFN adapter | Test-time-training applied to the FFN so that a small adapter subset of FFN parameters is updated by gradient descent on a self-supervised loss computed from the incoming stream itself, before being used for prediction. | 2602.05085; 2604.14048 | The specific twist of grafting a frozen-base rank-r LoRA solely onto the FFN down-projection with a strictly causal masked reconstruction of past FFN hidden activations (not full weight TTT or offloaded memory blocks) in an autoregressive LM stream is not exactly what Locas or the video/3D TTT papers do, so a tightly scoped causal-LM FFN adapter variant could still be new, though it is a narrow delta. |
| 159 | ADJACENT | 3 | 4 | LRU-gated FFN slot replacement | an adaptive replacement policy applied to FFN key-value slots so that the memory can swap which associations it stores as the stream's topic shifts | 2512.13751; 2501.18356 | The specific combination of treating FFN up-projection columns as an ARC-scored slot table with rank-1 gated rewrites trained via synthetic topic-shift sequences, applied to FFN memory (not attention KV cache) for non-causal topic tracking within a single context, is not directly instantiated in any of these papers. |
| 160 | ADJACENT | 3 | 4 | delta-rule closed-loop LR controller | a delta-rule fast-weight associative memory (write key = recent loss-curve statistics, value = a learning-rate multiplier, with erase-then-write updates so stale readings are overwritten) applied to the lr-schedule so that the learning rate is set by a closed-loop readout of the training run's own loss trajectory instead of a fixed pre-registered curve. | 2411.16975; 2506.13274 | A genuinely new angle could survive if the delta-rule's causal, in-place erase-then-write update (with formal fast-weight-programmer guarantees) is shown to give qualitatively different stability/capacity properties than the heuristic or search-based controllers in ExpTest/AdaLRS/POLA-style memories, but as stated the capability and placement are already covered. |
| 161 | ADJACENT | 3 | 4 | Contrastive multi-horizon auxiliary loss | A contrastive objective applied to hidden states at position t so that representations are pulled toward the true encoding of position t+k and pushed away from other positions' encodings, is applied to the training objective so that intermediate representations carry information predictive of tokens several steps ahead. | 2410.18160; 2510.14751 | The specific mechanism—an InfoNCE contrastive loss pulling h_t toward a stop-gradient h_{t+k} and pushing apart in-batch negatives, as opposed to auxiliary generative/predictive heads or cross-attention decoders used in the cited works—is a genuine mechanistic difference that could survive as a narrower contribution, though the capability and placement are already well-explored. |
| 162 | ADJACENT | 3 | 4 | fast-weight mixture rewriter | Delta-rule fast-weight memory applied to the data-mixture controller so that the mixture-selection key for each domain/source is overwritten (erase-then-write) by a running estimate of the model's current per-domain error, letting the mixture adapt continuously instead of being fixed in advance. | 2606.18663; 2410.11820 | The specific framing as an associative-memory slot per domain updated by an explicit delta-rule erase-then-write (w += eta*(loss-w)) rather than scaling-law fits, gradient-conflict estimators, or velocity heuristics is a narrow mechanistic distinction, but since several papers already implement the same 'per-domain running loss -> cheap online reweighting' capability, only a demonstrated difference in dynamics/stability or theoretical link to fast-weight memory models would be new. |
| 163 | ADJACENT | 3 | 4 | Streaming teacher-student self-distillation | Online self-distillation applied to weights-at-inference so that a fast low-rank adapter continually absorbs the slow base model's own predictions on the live input stream | 2604.06169; 2307.05014 | The specific combination of a frozen base model as teacher and a small LoRA adapter as student, updated via periodic KL-distillation solely on the live inference stream (no labels, no architectural fast-weight redefinition, base weights literally untouched), is not exactly what In-Place TTT does (which redefines MLP projections as fast weights with a bespoke NTP-aligned loss rather than explicit teacher-student KL distillation), so a version emphasizing the LoRA/teacher-student KL framing specifically could still be distinguishable, though it is a narrow twist. |
| 164 | ADJACENT | 3 | 4 | learned position-predictor attention router | learned-index applied to attention-topology so that a small model predicts which past positions are worth attending to, replacing a fixed structural mask with a trained lookup | 2505.00315; 2603.20997 | A twist could survive if the index model is explicitly framed as a rank/score predictor trained to approximate a learned-index CDF-style lookup (as in learned index structures) rather than a differentiable gate/router, or if it demonstrates the joint-training co-adaptation pitfalls identified in 2603.02227 are avoided by a specific architectural choice—but as stated the mechanism and placement match MoSA and the routing-absorption/content-based-routing literature closely. |
| 165 | ADJACENT | 3 | 4 | Hebbian gated topology adapter | Hebbian fast-weight outer-product traces applied to attention-topology so that the mask itself is perturbed by recent content rather than being fixed before any token is seen. | 2102.11174; 2510.21908 | The specific twist of keeping the standard causal QK^T mask intact and adding a separate learned-decay Hebbian trace strictly as a gated additive bias to logits (rather than replacing attention with the fast-weight computation or adding a parallel memory module) is not explicitly shown in these papers, though it is a small step from them. |
| 166 | ADJACENT | 3 | 4 | Delta-rule rewrite lane for KV | Delta-rule associative overwrite applied to a side attention-topology lane so that later context can erase-and-replace an earlier position's stored value while the main causal path stays untouched. | 2607.02303; 2412.06464 | A genuinely new angle would require showing the side lane specifically decouples read causality (queries at position t reading only past-written overwrites without ever seeing future tokens) in a way that differs functionally from HOLA's dual-memory or Gated DeltaNet's hybrid gating, e.g. a formal proof/ablation isolating the 'meaning revision without future leakage' property as distinct from simply improving recall; as stated the placement (delta-rule lane alongside causal attention) is already explored. |
| 167 | ADJACENT | 3 | 4 | Hebbian fast-weight causal patch | Hebbian fast-weights applied to attention-topology so that recent token associations formed after a position was written can still influence that position's downstream reads for a few steps, approximating revision without violating causality. | 2102.11174; 2608.27763 | A genuinely new contribution would need to show that the dual-path read (causal KV cache + separate short-horizon fast-weight matrix, trained jointly with matched parameter budget by trading off attention heads) produces a measurable causality-compliant 'revision' effect—i.e., that later tokens change how earlier positions are effectively read downstream—as a targeted ablation/capability claim, rather than just presenting fast-weights as an efficient attention substitute or memory augmentation as prior work does. |
| 168 | ADJACENT | 3 | 3 | Kalman-fused KV entries | Kalman-filtering applied to the kv-cache so that each cached key/value is treated as a noisy estimate that gets fused with a new 'measurement' derived from later context, weighted by learned uncertainty, rather than being fixed forever. | 2406.05317; 2403.09636 | The explicit Kalman-filter formalism—maintaining a learned scalar/low-rank uncertainty per slot and updating both the KV estimate and its uncertainty via a Kalman-gain-weighted fusion with a periodic 'measurement' derived from later context—is a more principled and reversible (non-discarding, uncertainty-tracked) mechanism than LoCoCo's fixed convolutional mixing or DMC's compression-ratio learning, so a version demonstrating this specific recursive uncertainty-tracked fusion (not mere blending or ratio selection) would still be a genuine, if narrow, contribution. |
| 169 | ADJACENT | 3 | 3 | MDL-driven adaptive KV budget | minimum-description-length applied to kv-cache so that each entry's stored precision is chosen to minimize total code length (model bits + residual bits) rather than fixed uniformly, letting redundant entries collapse to near-zero cost | 2604.04722; 2607.01520 | A genuinely new twist would be the explicit online two-part MDL code-length computation (model bits for quantization level choice + residual bits under a fitted prior) reported and minimized per-entry as a measurable quantity, rather than a learned/heuristic/task-performance-driven importance score—none of the found papers compute or optimize this explicit code-length decomposition. |
| 170 | ADJACENT | 3 | 3 | Importance-weighted KV allocation | Importance-sampling applied to kv-cache allocation so that bit-width/slot-count per entry is set by an estimated attention-mass reweighting rather than uniform allocation. | 2603.10899; 2602.03203 | A genuine self-normalized importance-sampling correction (explicit proposal q vs. target p reweighting, debiased rather than just predicted) mapped to a continuous bit-width/precision tier rather than binary evict/keep could still be new, but the core capability of estimating future attention mass to reallocate cache resources is already claimed by LookaheadKV and ForesightKV. |
| 171 | ADJACENT | 3 | 3 | MDL-gated attention abstention | A minimum-description-length code-length penalty applied to attention-scores so that a token can allocate near-zero total attention mass when no key shortens its predictive code, instead of being forced to mix a full unit of value. | 2309.17453; 2605.08453 | The specific MDL/code-length gating mechanism—an auxiliary head estimating bits-saved per key via ablation, used to penalize logits before softmax over {keys, null}—is not described in any of these papers, which use fixed sink tokens, geometric/threshold-based sparsity, or oversmoothing analysis rather than an information-theoretic per-key description-length cost; if the team can show the MDL auxiliary-loss induced abstention differs functionally (e.g. context-dependent code-length rather than static/thresholded sparsity) that twist could survive. |
| 172 | ADJACENT | 3 | 3 | Choquet head-interaction aggregation | Choquet-integral applied to attention-scores so that multi-head outputs are combined via a fuzzy measure capturing pairwise redundancy/complementarity instead of a fixed linear (concat+project) combination. | 2606.02341; 2006.16362 | A genuinely new contribution could survive if it shows the discrete Choquet integral (with explicit 2-additive capacity encoding signed redundancy/complementarity beyond the sum, applied per-layer at the head-combination projection) yields interaction structure or interpretability that shared-projection or gating fusion methods cannot, but no paper in the set does exactly this operation at exactly this component for this purpose, making it at most a close follow-up to the Choquet fusion paper and the collaborative-head paper. |
| 173 | ADJACENT | 3 | 3 | Bayesian posterior-weighted head mixture | Bayesian-model-averaging applied to attention-scores so that head outputs are combined by an online-estimated posterior over 'which head is currently reliable' rather than a fixed learned linear combination, letting the mixture weights shift per-token based on evidence of each head's recent predictive accuracy. | 2210.05144; 2009.09364 | The specific mechanism of maintaining an explicit running per-head log-likelihood (via auxiliary per-head next-token heads) and converting it into a causal Bayesian posterior strictly from past prediction accuracy—rather than a router or composition function conditioned on the current query—appears novel relative to MoA/DCMHA/Repulsive Attention, none of which track online predictive reliability as the weighting signal. |
| 174 | ADJACENT | 3 | 3 | MDL-regularized attention codebook | A minimum-description-length objective over a shared query/key codebook is applied to attention-scores so that the model is pressured to route mass through a small reusable set of transport 'hubs' rather than bilinear all-pairs comparison. | 2604.03850; 2407.12820 | The specific combination of an in-line VQ bottleneck on keys with an explicit MDL loss (reconstruction error plus log2(C) times codebook usage-entropy) trained jointly with the LM loss specifically to measure and prune attention structure per layer/head is not exactly instantiated by any single paper, though close analogues (prototype codebooks with diversity+reconstruction loss, PQ on keys, top-k landmark routing) already cover the mechanism and placement; the surviving novelty would be the explicit description-length-based pruning diagnostic rather than the routing/quantization mechanism itself. |
| 175 | ADJACENT | 3 | 3 | Bayesian-averaged expert aggregation | Bayesian model averaging applied to moe-router so that expert outputs are combined by posterior-mass weighting over interacting hypotheses rather than a hard top-k selection that ignores inter-expert dependence. | 2606.01509; 2510.16411 | A twist could survive if the explicit Bayesian/Dirichlet posterior formalism combined with an online EMA-updated low-rank pairwise correlation matrix used specifically to correct for non-independence in the aggregation weights (rather than just soft routing or graph-based interaction) yields measurably different calibration/uncertainty behavior than ProbMoE's exact-marginal surrogate or the graph-based SymphonySMoE, but this is a narrow technical distinction rather than a new capability. |
| 176 | ADJACENT | 3 | 3 | verified router rollback | speculate-and-verify applied to the moe-router so that the top-1 expert's output is checked against a cheap proxy of the top-2 expert's disagreement and, on disagreement above a threshold, both are computed and the exact top-2-mixture result is substituted | 2605.00342; 2608.02989 | The proposal's specific twist—using a cheap distilled/low-rank proxy to predict top-2 disagreement before deciding to compute the real second expert, framed explicitly as speculate-and-verify with exact top-2 recovery guarantee on the disagreement subset—is not clearly present in CARE (which uses the router's own softmax mass/disagreement directly rather than a separate trained proxy network) or the speculative-decoding papers (which operate on draft-token trees, not single-token top-1-vs-top-2 disagreement), so a version centered on the proxy-network verification-with-exact-fallback guarantee could still be new. |
| 177 | ADJACENT | 3 | 3 | speculative expert routing with rollback | Branch-prediction applied to the moe-router so that the top-1 expert choice is treated as a speculative guess validated against a cheap secondary signal, allowing misrouted tokens to be caught and rerouted before the FFN output is committed. | 2609.09241; 2410.17954 | The specific mechanism of speculatively computing both top-1 and top-2 FFN outputs and using a trained lightweight verifier scorer (conditioned on an EMA of historical per-expert hit-rate) to choose which to commit before writing to the residual stream is not explicitly implemented in any of these papers, which instead do cache-miss correction, distributional recalibration, or heuristic expert-count adjustment. |
| 178 | ADJACENT | 3 | 3 | ARC-policy positional cache aging | The adaptive replacement (ARC) cache policy applied to positional-encoding so that position indices assigned to KV-cache slots are dynamically remapped based on recency-and-frequency of attention use rather than fixed absolute index, letting long streams reuse a bounded range of trained positions. | 2502.11101; 2606.26631 | The specific ARC-style dual-list (recency T1 + frequency T2) eviction policy with a balance parameter governing which cache entries get position-recycled is a genuinely different remapping heuristic than the length/attention-based reallocation in CacheFocus or PRCR's coordinate-based rebinding, so a rigorous ARC-policy-driven remap plus a trained boundary-correction adapter could still be a novel instantiation if it demonstrably outperforms these existing recency/frequency-agnostic or content-coordinate-based reassignment schemes. |
| 179 | ADJACENT | 3 | 3 | test-time adaptive merge head | Test-time-training applied to the tokeniser's merge-decision head so that a small set of boundary parameters is updated on the incoming stream via a self-supervised next-byte loss, letting granularity adapt per-document without touching the base LM. | 2506.01084; 2512.23675 | The specific twist of using gradient-based test-time-training (per-k-byte online SGD on a self-supervised next-byte loss) restricted to a small frozen-base-LM merge-head, rather than an online compression algorithm or a purely feed-forward adaptive gate, appears novel relative to zip2zip's LZW-based hypertoken mechanism and TTT-E2E's whole-model weight updates. |
| 180 | ADJACENT | 3 | 3 | ARC-policy adaptive merge cache | An adaptive-replacement cache policy (balancing recency and frequency of byte n-grams) applied to the tokeniser's merge table so that merge granularity adapts online to the actual input stream instead of being frozen after BPE training. | 2411.18553; 2410.08481 | The specific ARC (recency+frequency with ghost lists) cache-replacement policy governing promotion/eviction of merges, as opposed to hypernetwork-based or simple frequency/pruning-based dynamic vocabularies, is a genuine mechanistic twist not explicitly present in the closest papers. |
| 181 | ADJACENT | 3 | 3 | Multiresolution byte-wavelet embedding | Wavelet-multiresolution decomposition applied to the tokeniser's input embedding stream so that a single forward pass carries both coarse (merged-span) and fine (byte-level) representations simultaneously, letting downstream layers pick resolution per span. | 2509.20581; 2509.21153 | The specific claim that both resolutions are kept as simultaneous additive channels at every sequence position (not merely a coarser pooled stream or an adaptive-exit mechanism), letting downstream layers pick resolution per-span without any explicit chunk boundary or re-tokenisation, is a finer distinction not clearly demonstrated in HRT or WAVECLIP, which still produce a single resolution-selected representation per position/stage rather than co-resident multi-resolution channels. |
| 182 | ADJACENT | 3 | 3 | Energy-based candidate refinement head | An energy-based refinement procedure applied to the output-head so that the final token distribution is produced by iterative descent over a learned energy on (context, candidate) pairs rather than a single softmax read, yielding better-calibrated aggregate probabilities than the raw post-trained logits. | 2009.13267; 2009.07177 | A genuine twist would be demonstrating that this refinement specifically improves calibration (top-k probability mass matching empirical correctness rates) rather than task accuracy/BLEU/quality, since none of the closest papers target calibration as the explicit objective of energy-based candidate refinement at the LM output head. |
| 183 | ADJACENT | 2 | 5 | arithmetic-coding calibration probe | Arithmetic coding applied to the output-head so that miscalibration is exposed and quantified as excess coding length, giving a verified, recoverable calibration diagnostic and correction pass. | 1906.05664; 2511.11966 | A genuinely new contribution would need the specific coupling of exact, losslessly-invertible arithmetic-coding codelength (not just entropy-rate proxies) with a provable, parameter-light rescaling map guaranteed to drive codelength to the entropy bound while preserving exact logit recovery for at-will exact/calibrated decoding—none of the closest papers combine reversibility guarantees with a provable optimality bound in this exact form. |
| 184 | ADJACENT | 3 | 3 | energy-descent candidate refinement sampler | Energy-based-refinement applied to the sampler so that, instead of sampling a token once from the softmax, the system samples a small candidate set, scores it with a learned scalar energy function conditioned on the causal context, and performs a few gradient or hill-climbing steps over an embedding relaxation before snapping to the nearest vocabulary token, still emitting one committed token per position. | 2604.08302; 2601.08808 | The proposal's specific twist — a contrastively-trained scalar energy head conditioned on causal hidden state that refines a small top-k candidate list via gradient descent before snapping to one committed autoregressive token, explicitly targeting correction of locally high-probability but globally incoherent token choices — is not demonstrated at this exact combination (causal LM sampler + top-k candidate energy refinement + one-token commit) in any single paper, so a tightly scoped implementation could still be novel. |
| 185 | ADJACENT | 3 | 3 | energy-descent block resampling | energy-based-refinement applied to the sampler so that a committed block of tokens can be revised before being finalized rather than accepted token-by-token forever | 2608.19567; 2410.09675 | The specific twist of using an explicit trained energy function (Langevin/iterative denoising descent over soft embeddings or discrete neighborhoods, contrastively trained MLP head) as the revision criterion, rather than confidence scores, order-agnostic reconstruction, or speculative rejection, is not directly shown in these papers and could still be new if it demonstrably outperforms these simpler revision signals. |
| 186 | ADJACENT | 3 | 3 | Test-time-trained sampling head | Test-time training of a small per-position controller applied to the sampler so that temperature/top-k adapt online via a self-supervised loss computed purely from the incoming generated stream. | 2603.18428; 2411.09661 | A genuinely self-supervised, purely inference-time (no reward, no preference data, no RL loop) per-document controller trained online via SGD on a reconstruction/entropy-prediction loss computed only from the emitted stream would be a distinct mechanism from the RL/policy-learning and preference-optimization approaches seen here, though the capability and placement are already well-explored. |
| 187 | ADJACENT | 3 | 3 | dual-path override sampler | Dual-process gating applied to the sampler so that a fast default top-k/temperature reflex is overridden by a slower, more expensive per-position deliberation path only when a cheap uncertainty signal flags the position as high-stakes, giving genuinely per-position sampling control at bounded average cost. | 2601.07353; 2512.13194 | The proposal is distinctive in gating specifically the token sampling distribution itself (top-k/temperature/renormalization/lookahead at the logit level) via a fixed deterministic entropy/margin function computed on ordinary autoregressive decoding, independent of any draft-verify or tree-search infrastructure; if implemented as a lightweight, model-architecture-agnostic wrapper around standard sampling (not requiring a separate draft model or speculative verification), this concrete instantiation is not directly shown by the closest papers, which embed adaptive compute allocation inside speculative-decoding tree structures or reasoning-mode selection. |
| 188 | ADJACENT | 3 | 3 | MDL-selected prefill cache codebook | A minimum-description-length criterion applied to prefill so that the KV cache is replaced by the shortest joint code (a small learned codebook plus per-token indices) that still lets the model reconstruct next-token predictions, trading exact per-token K/V storage for a compressed code chosen by two-part-code length rather than a fixed compression ratio. | 2607.01520; 2606.08382 | The specific two-part MDL code-length criterion (codebook bits + index bits + residual bits) used to dynamically select codebook size per prompt at prefill, as opposed to risk-bound, thresholding, or importance-based adaptive schemes, is not directly instantiated in any candidate paper. |
| 189 | ADJACENT | 3 | 3 | RG-coarsened prefill hierarchy | Renormalisation-group coarse-graining applied to prefill so that the prompt is summarized into a multi-scale cache whose coarse levels can be reused across prompts sharing structure. | 2406.02657; 2311.04934 | The specific combination of a trained hierarchical block-pooling (dyadic-tree) summarizer applied to KV pairs across multiple scales, jointly optimized to minimize KL divergence in attention outputs, and reused/hashed at the block level across prompts, is not directly instantiated by Block Transformer (which doesn't do cross-prompt cache reuse) or Prompt Cache (which doesn't do multi-scale coarse-graining or learned pooling)—so a proposal emphasizing the learned RG-style pooling MLPs trained jointly for cross-prompt coarse reuse could still be distinguishable, though it is a close combination of existing pieces. |
| 190 | ADJACENT | 3 | 3 | Barrier-constrained draft divergence bound | Control-barrier-functions applied to speculative-drafting so that the draft model's per-step KL divergence from the target model is constrained online to stay inside a safety set, throttling draft aggressiveness before verification would reject it | 2411.18462; 2606.30265 | The specific formalism of a control-barrier-function with a certified safety set (rather than a heuristic entropy/margin threshold) could survive if it provides provable non-violation guarantees or formal safety-margin certificates rather than just an empirical stopping heuristic, but the core capability (proxy-triggered preemptive throttling of draft aggressiveness before verification) is already covered. |
| 191 | ADJACENT | 3 | 3 | multiresolution layer-schedule | Wavelet multiresolution decomposition applied to the layer-schedule so that every token is processed at several depth-resolutions simultaneously and difficulty determines which resolution's output is read out, instead of committing to one fixed depth. | 2412.05225; 2412.01455 | The specific wavelet-coefficient-hierarchy training objective (coarse readout capturing bulk signal, deeper readouts trained as residual detail conditioned on not-yet-stopping, akin to a DWT decomposition) is a structurally different loss/parameterization from standard independent or jointly-optimized early-exit heads, and if shown to yield different accuracy/compute tradeoffs than vanilla multi-exit training, that specific residual-coefficient formulation could be a genuine (if narrow) contribution. |
| 192 | ADJACENT | 3 | 3 | Sinkhorn-balanced FFN key routing | Optimal-transport Sinkhorn matching applied to the FFN key-value memory so that tokens are routed to keys under a balanced-marginal constraint instead of independently top-k scored, guaranteeing every key gets bounded utilization across a batch. | 2511.08972; 2606.01666 | The proposal's specific twist—treating the FFN's key-value memory itself (not discrete MoE experts) as the doubly-stochastic-routed object, with capacity constraints per key/neuron in an otherwise dense, non-MoE FFN—is closest to DOT-MoE's neuron-level Sinkhorn assignment, so genuine novelty would require showing the guarantee/inspection benefit holds for a standard dense-FFN key-value view distinct from any expert partitioning, which none of the papers directly frame that way. |
| 193 | ADJACENT | 3 | 3 | Hopfield energy-descent FFN read | Modern Hopfield energy-descent retrieval applied to the FFN key-value memory so that the per-token FFN output is computed by iterative content-addressed convergence toward a stored pattern instead of a single dense feed-forward score-and-sum, giving one-shot associative cleanup. | 2605.08143; 2406.12220 | The proposal's specific twist is training the pattern set/beta end-to-end from scratch as a direct multi-step-iteration replacement for the FFN's single matmul at matched FLOPs (rather than as a post-hoc editing/interpretability wrapper as in HoReN), which is not demonstrated in these papers. |
| 194 | ADJACENT | 3 | 3 | Information-bottleneck compressed moment memory | Information-bottleneck applied to optimiser-state so that the per-parameter second-moment estimate is replaced by a small learned per-module summary that is explicitly trained to retain only the statistics predictive of the next gradient's magnitude, discarding the rest. | 2606.13894; 2307.02047 | The explicit information-bottleneck training objective (minimizing I(z; raw grads) while maximizing an auxiliary decoder's predictive power for next-step gradient magnitude) as a provable, tunable compression criterion is not what Gefen, CAME, or Adafactor do—they use heuristic sharing/confidence/factorization rather than an IB loss with a trained encoder/decoder, so a rigorous IB-based ablation of exactly how much state is necessary would be a genuine (if narrow) methodological twist. |
| 195 | ADJACENT | 3 | 3 | speculative branch-predicted LR jumps | branch-prediction-style speculative LR adjustment applied to the lr-schedule so that the trainer predicts the next beneficial LR change from recent loss-curve history and speculatively applies it, rolling back on misprediction | 2411.16975; 1910.08525 | The specific speculative-execution-style mechanism of immediately applying a predicted LR jump, checkpointing, and rolling back on a measured misprediction (verification window vs. no-jump control estimate) is not implemented by any of these controller/hypergradient/statistical-test approaches, so that exact rollback-based speculation mechanism could still be novel. |
| 196 | ADJACENT | 3 | 3 | Predictive-coding error-only horizon loss | Predictive coding applied to the training objective so that each layer is trained only against the residual error of its own multi-step prediction (not the raw next-token target), is applied so that lower layers stop re-learning what upper layers already predict correctly at longer horizons. | 2404.19737; 2112.10048 | The specific twist of making each layer's loss the residual error unexplained by the next layer's top-down (detached) prediction at a longer horizon—an explicit hierarchical decomposition of multi-horizon prediction across depth rather than independent parallel heads or reconstruction—is not directly instantiated by any of these papers, though it is a natural combination of MTP heads (Gloeckle et al.) and classic Rao-Ballard top-down error correction. |
| 197 | ADJACENT | 3 | 3 | Offline sleep-phase consolidation replay | Sleep-replay-consolidation applied to data-order so that periodic offline phases reactivate and compress a compact episodic memory of recent examples into consolidated gradient updates, rather than every example being seen once and discarded. | 2303.10725; 2401.08623 | Applying this exact mechanism to standard LLM pretraining data-order (not continual-learning/task-incremental settings) with a surprise-based top-k buffer and token-masked perturbation, integrated into single-pass corpus training rather than multi-task CL, is a narrower framing but not clearly distinct from SIESTA/WSCL's wake-sleep consolidation design. |
| 198 | ADJACENT | 3 | 3 | gated fast/slow replay override | Dual-process gating applied to data-order so that a fast reflex scheduler (fixed random order) is overridden by a slow deliberate scheduler (a small auxiliary loss-predictor) whenever the deliberate path's confidence that an example needs replay exceeds a threshold, giving on-demand revisiting without changing the default streaming order. | 2512.02333; 2202.01838 | The specific dual-process framing—an always-on cheap random-order path overridden only when a jointly-trained auxiliary loss-delta gate exceeds a calibrated threshold, defaulting to zero-overhead streaming—is not shown; a paper demonstrating exactly this gate-triggered FIFO mid-epoch reinjection with negligible added params trained jointly from scratch would still be new. |
| 199 | ADJACENT | 3 | 3 | test-time adapted mask gates | test-time-training applied to attention-topology so that a small set of gating parameters controlling which positions attend to which are updated online via a self-supervised loss on the incoming stream, letting the mask adapt to the current document rather than staying fixed from pretraining | 2606.06906; 2512.23675 | A twist could survive if the gate is explicitly a per-head low-rank bias trained once and then adapted purely to trigger connectivity/window changes (not evidence localization or general context compression) with an explicit widening-on-loss-spike mechanism validated as such, since EASE-TTT targets evidence alignment and E2E-TTT updates general fast weights rather than an explicit topology-widening gate. |
| 200 | ADJACENT | 3 | 3 | sparse-coded attention dictionary | sparse-coding applied to attention-topology so that the mask for each query is derived as a sparse combination of a small learned dictionary of reusable attention patterns (local, sink, periodic, etc.) rather than one fixed hand-designed pattern | 2508.02124; 2303.08810 | The specific twist of using a small fixed dictionary of hand-designed structural topology atoms (local/sink/strided) combined via OMP/top-k sparse coding, rather than a fully learned dynamic mask or generic content dictionary, could still be novel if it demonstrably preserves interpretable structural priors and compute bound in a way DMA/BiFormer/sparse-coding-transformer do not. |
| 201 | ADJACENT | 3 | 3 | Hopfield rewrite of KV cache | Hopfield-style energy descent over a buffered window of past key-value entries applied to attention-topology so that earlier representations can be pulled toward a content-addressed attractor once later context arrives, without violating causal emission order. | 2606.26560; 2503.00979 | A genuinely new contribution would need to show that Hopfield energy-descent rewriting (multi-step, content-addressed, attractor-based) yields qualitatively different long-range coherence gains than the existing delta-rule erase-write or attention-guided iterative refinement schemes at matched budget, since the mechanism class (in-place causal-safe KV rewrite triggered by later context) is already occupied by EDA and MorphKV/GRKV for closely related purposes. |
| 202 | ADJACENT | 3 | 2 | Graph message-passing KV rewrite | Graph-message-passing applied to the kv-cache so that cached entries are nodes in an explicit sparse graph (built from attention-score thresholds or coreference links) and get updated by aggregating messages from newly-arrived neighbor nodes, instead of staying fixed once written. | 2505.16950; 2509.00388 | The proposal's twist of learned MLP message-passing that rewrites the actual K/V content (not just importance/eviction scores) along attention-derived edges is functionally very close to Bottlenecked Transformers' non-causal in-place KV rewrite via an auxiliary network; a genuinely new contribution would need to show the sparse-graph edge structure (vs. dense/windowed Transformer processor) yields a distinct capability such as long-range coreference propagation at strictly O(edges) cost with provable savings over the dense Cache Processor. |
| 203 | ADJACENT | 3 | 2 | distilled shared prefix cache | online self-distillation applied to the kv-cache so that a slow reference cache teaches a fast compressed cache to represent shared prefixes across concurrent requests without recomputation | 2607.28069; 2608.01247 | A genuinely new twist would require the student to be continuously updated online as an unbounded stream of distinct prefixes arrives (true online/continual distillation rather than an offline-compiled or per-eviction-budget writer), and to serve as a drop-in replacement for exact prefix-cache lookup across many arbitrary unseen prefixes rather than one document/context type — SemPIC already covers the core mechanism-at-component-for-purpose overlap, so this delta is narrow. |
| 204 | ADJACENT | 3 | 2 | Spectral-domain causal attention | A spectral-transform reparameterization applied to attention-scores so that long-range query-key similarity is computed as a local product in a learned frequency basis instead of a global bilinear dot product over raw positions. | 2607.07478; 2111.15473 | A genuinely causal, chunk-local multi-resolution DCT/Hadamard transform with cumulative low-frequency summaries carried across chunks (mimicking a causal multi-scale memory) is not shown in FourierQK's bilateral FFT or MDFWA's non-causal Fourier-wavelet mixing, so a working causal chunked-DCT variant with the multi-resolution carry-over could still be new if it demonstrably preserves strict causality while matching FLOPs. |
| 205 | ADJACENT | 2 | 4 | conformal expert-set routing | conformal prediction applied to the moe-router so that top-k selection is replaced by a variable-size expert set with a distribution-free coverage guarantee on containing the correct expert | 2403.07652; 2608.04401 | None of these papers derive a nonconformity-score threshold from held-out calibration data to give a formal, distribution-free 1-alpha coverage guarantee on containing the oracle-optimal expert (nor do they flag tokens where no small set can achieve the target coverage), so a rigorous conformal calibration step with certified coverage semantics would still be a genuine addition over these heuristic dynamic-k methods. |
| 206 | ADJACENT | 3 | 2 | energy-descent expert selection | energy-based refinement applied to the moe-router so that expert-combination weights are found by a few steps of gradient descent on a learned joint energy over (token, expert-subset) rather than by a single softmax-and-threshold pass | 2606.01062; 2605.08322 | The specific mechanism of a joint energy function E(h_token, w) over continuous relaxed expert-inclusion vectors, optimized by K explicit gradient/mean-field descent steps at inference (an iterative denoising-like rewrite of the routing weights themselves, trained with a contrastive real-vs-corrupted-subset loss) is not instantiated by any of these papers, which use either static structural aggregation, message-passing among already-selected experts, or differentiable-but-single-pass gates; a genuine implementation of iterative energy descent for expert-subset selection itself would still be new. |
| 207 | ADJACENT | 2 | 4 | PID-controlled residual norm | Feedback-control applied to the residual-stream so that each block's write gain is driven by a PID loop tracking a target activation-norm error rather than being a fixed scalar per depth. | 2605.18832; 2606.16112 | A genuine PID loop with explicit proportional-integral-derivative terms tracking a target norm (not just an adaptive/Kalman gain or magnitude-direction split) that trains only 3 scalar gains per layer while leaving the rest of the model architecture untouched could still be a distinct, narrower mechanism than BFT's Kalman-update framing, but the core capability (feedback-driven, error-tracking dynamic residual-write scaling) is already claimed by the Bayesian Filtering Transformer paper. |
| 208 | ADJACENT | 2 | 4 | content-predicted positional offsets | a small learned-index submodule applied to positional-encoding so that position signals can be warped by content-dependent predicted offsets rather than a fixed lookup | 2507.23083; 2511.17388 | A twist restricted to a tiny jointly-trained MLP predicting bounded additive offsets to the integer index itself (rather than directly to rotation angles/frequencies) applied uniformly across all heads from a pooled hidden-state summary could still be marginally different, but CARoPE and Selective RoPE already cover input-dependent warping of RoPE angles at every layer for the same compression/expansion-of-effective-spacing purpose. |
| 209 | ADJACENT | 2 | 4 | proper-scoring calibrated position gate | a small auxiliary head that predicts relative-position-dependent attention-decay confidence, trained with a proper scoring rule against realized next-token loss at each distance, applied to positional-encoding so that the model down-weights positions whose encoding is known (from training-time evidence) to be miscalibrated at test lengths. | 2605.10414; 2108.12409 | The specific twist of training the gate via an explicit proper scoring rule (CRPS/Brier) against realized next-token correctness bucketed by distance, yielding a calibrated confidence signal rather than a heuristically learned or fixed decay, is not demonstrated in GAPE, ALiBi, or AdaRoPE and could be the differentiator if calibration-quality (not just accuracy) is the deliverable. |
| 210 | ADJACENT | 2 | 4 | Importance-weighted position reweighting | Importance sampling applied to positional-encoding so that attention scores computed under the training-length position distribution are reweighted, at inference, to estimate what scores would have been under a target long-context position distribution, without retraining. | 2502.02659; 2608.10021 | The specific formalization as an importance-sampling ratio p_target(d)/p_train(d) with a proof of convergence to the exact in-distribution answer as effective sample overlap improves, plus an explicit effective-sample-size diagnostic for flagging unreliable extrapolation, is not present in GALI or the survey, which use heuristic interpolation/outlier removal without a probabilistic correctness guarantee or degeneracy diagnostic. |
| 211 | ADJACENT | 3 | 2 | distilled dynamic re-merging teacher | Online self-distillation applied to the tokeniser boundary predictor so that a fast student network can propose per-span merge/split decisions at inference that track a slow teacher's byte-level loss landscape. | 2411.18553; 2506.01084 | A genuinely new contribution would need the EMA-teacher-vs-fast-student distillation specifically targeting byte-level merge/split boundary decisions tied to the teacher's own per-token loss landscape (not vocabulary/compression-based adaptivity and not representation-level self-distillation), which none of the closest papers implement. |
| 212 | ADJACENT | 3 | 2 | spectral byte-span merging | A frequency-domain locality transform applied to the raw byte stream so that merge boundaries are chosen dynamically per input instead of fixed at training time. | 2412.09871; 2603.03583 | The specific spectral/frequency-domain locality criterion (short-time DCT/wavelet smoothness score) as the boundary-decision signal, as opposed to entropy or coding-rate criteria, is not directly shown in these papers, so a proposal isolating that mechanism's effect could still be a genuine (if narrow) contribution. |
| 213 | ADJACENT | 3 | 2 | information-bottleneck adaptive chunking | An information-bottleneck objective that explicitly trades off compression of the byte stream against prediction of the next token, applied to the tokeniser, so that chunk boundaries are placed to keep only what predicts the target and discard the rest per-span. | 2603.06351; 2501.03120 | The explicit dual mutual-information IB objective (I(Z;Y) - beta*I(Z;X)) applied specifically to byte-level LM tokenizer boundary placement, with a Gumbel-softmax boundary scorer trained jointly with a from-scratch nano LM and an inference-time beta dial, is not demonstrated by any of these; the closest works use content/complexity predictors or router losses rather than a stated rate term against raw bytes for next-token prediction, so a rigorous IB formalization at the tokenizer for language modeling would still be a distinguishable contribution if it makes the rate-distortion tradeoff explicit and provably tied to I(Z;X) rather than an implicit token-count penalty. |
| 214 | ADJACENT | 2 | 4 | importance-sampled logit recalibration | Importance sampling applied to the output-head so that samples drawn from the miscalibrated post-training distribution are reweighted to estimate expectations (e.g. token frequencies, downstream metrics) under the true pretraining-calibrated distribution, without ever renormalizing or retraining the head. | 2507.15507; 2607.04728 | A twist could survive if the proposal specifically targets pure post-hoc estimation of held-out evaluation metrics (perplexity, entropy, reward-model score) from a frozen preference-tuned model using exact base-vs-tuned per-token probability ratios, never touching gradients or training objectives, which none of the closest papers do since they all fold importance weights into training/model-correction rather than treating it as a diagnostic estimator with explicit ESS monitoring at eval time only. |
| 215 | ADJACENT | 2 | 4 | Mixture-head post-hoc recalibration | Mixture-density heads applied to the output-head so that post-preference-training logits recover calibrated probability mass instead of a single sharpened point estimate. | 2606.30037; 2604.08627 | The proposal's specific twist—grafting a K-component mixture-density head post-hoc onto preference-trained LLM next-token logits to recover multimodal calibration collapsed by RLHF/DPO—has not been directly shown; ETN establishes the post-hoc frozen-base LLM logit-recalibration setup but with a Dirichlet/evidential parameterization rather than an explicit mixture-of-softmaxes, so demonstrating that mixture-density heads specifically recover multimodal token-level uncertainty lost to preference-training collapse (not just scalar confidence calibration) would still be a novel contribution. |
| 216 | ADJACENT | 2 | 4 | EMA-stabilized logit trajectory sampler | EMA-averaging applied to the sampler so that, within a short lookahead window of draft tokens produced by repeated re-forward-passes with perturbed inputs, the exponential moving average of the resulting logit trajectory (rather than the single final logit vector) is used to pick the committed token, still emitting exactly one token at a time. | 2607.03236; 2412.18299 | The specific combination of noised repeated re-forward-passes (embedding dropout perturbations or KV variants) generating a logit trajectory, combined with true EMA (not simple averaging) with a tuned decay beta, applied specifically to standard autoregressive token-by-token sampling for variance reduction/calibration (not diffusion commit-gating, not multi-prompt ensembling) is not exactly demonstrated, but the core idea of EMA-over-logit-trajectory at the sampler is already used in TACG and simple ensembling-to-pick-one-token is in M-Ped, making the delta narrow. |
| 217 | ADJACENT | 3 | 2 | checksum-verified token repair | error-correcting-codes applied to the sampler so that redundant parity tokens are embedded in the generation stream, letting corrupted or low-confidence committed tokens be detected and repaired a few steps later instead of staying wrong forever | 2605.26436; 2608.00531 | Yes: none of the closest works train a dedicated auxiliary parity/syndrome-prediction head (a compressed hash/low-rank checksum of a token block) jointly with the LM and use a mismatch between predicted and recomputed syndrome as the trigger signal — they all rely on confidence/uncertainty or external verifiers rather than an ECC-style in-band cryptographic-style checksum, so the specific error-correcting-code mechanism at the sampler is not yet demonstrated. |
| 218 | ADJACENT | 2 | 4 | Speculative temperature branch predictor | Branch-prediction applied to the sampler so that the decoder predicts, from recent token-type history, whether the upcoming position is a 'high-certainty' or 'high-entropy' branch point and pre-selects a temperature/top-k regime before computing the logits, correcting on misprediction. | 2510.26697; 2603.17942 | The specific twist of a lightweight offline classifier that predicts a discrete temperature/top-k regime from token-history features before logits are used, explicitly logging a 'misprediction rate' analogous to CPU branch mispredictions, is not demonstrated in AutoDeco (which jointly predicts continuous temperature/top-p via learned heads trained end-to-end with the model) nor in the speculative-decoding papers (which predict tokens, not sampling regimes); a proposal emphasizing the branch-prediction-style discrete regime classification and misprediction diagnostic as a separate, model-untouched auxiliary module could still be novel enough to survive as a follow-up rather than a duplicate, if it is framed as clearly distinct from joint decoding-head learning already published in 2510.26697. |
| 219 | ADJACENT | 3 | 2 | Learned-index prefill position router | A learned-index model applied to prefill so that token positions are routed directly to their approximate KV-cache slot via a small predictor instead of via full quadratic self-attention over all prior prompt tokens. | 2406.09827; 2609.01925 | A trained small MLP that predicts a concrete offset-range window per token (learned-index style, analogous to Kraska et al.) plus an explicit realized-mass verification threshold triggering fallback to exact attention is a more specific mechanism than HiP's tree-search top-k estimation or CRISP's structural-proxy routing, so a paper demonstrating this exact train-once offset-predictor-with-verification-fallback design would still be new, but the core capability (sub-quadratic prefill by exploiting attention locality to skip full attention) is already occupied by HiP and CRISP. |
| 220 | ADJACENT | 3 | 2 | Energy-descent draft refinement | Energy-based refinement applied to speculative drafting so that the draft model produces a block of candidate tokens by descending a learned energy landscape over the block, rather than sampling once autoregressively, before the target verifies the block. | 2608.02438; 2608.20530 | A genuinely new twist would be explicit iterative energy-descent (gradient or Gibbs coordinate steps at inference time) trained via NCE against target log-probs, as opposed to the single learned refiner/correlation pass or latent-mixture reparameterization used by xPress, LiLiCorr, and DBLAST — but this is a mechanism-level distinction on the same component for the same purpose, not a new capability. |
| 221 | ADJACENT | 2 | 4 | conformal adaptive draft length | conformal prediction applied to the draft model's per-step confidence sequence so that draft length is chosen to guarantee a target expected acceptance rate rather than fixed by a hyperparameter | 2405.19715; 2506.01979 | Yes: SpecDec++ uses a trained MDP-derived threshold on a learned acceptance-prediction head (a new trained component), whereas the proposal uses distribution-free conformal calibration directly on raw draft-model confidence with an explicit finite-sample coverage guarantee on acceptance rate and periodic non-parametric recalibration—if that formal coverage guarantee and lack of any auxiliary trained head is the emphasized twist, it is a meaningfully different mechanism class (distribution-free quantile calibration vs. learned threshold policy). |
| 222 | ADJACENT | 2 | 4 | proper-scoring-rule draft stopping | proper scoring rules applied to the draft model's stopping decision so that the draft's self-reported confidence is trained to be a calibrated predictor of the target's actual accept/reject outcome | 2508.15371; 2605.02888 | A twist could survive if the proposal specifically proves calibration (not just correlation/ranking) via a strict proper scoring rule (Brier/log score) trained on labeled accept/reject outcomes with theoretical calibration guarantees, distinguishing it from the heuristic entropy/margin or acceptance-length-reweighting objectives used in the closest papers, but none of the cited works explicitly frame the auxiliary head's objective as a proper scoring rule for calibrated P(accept), so this specific formalization is not clearly TAKEN. |
| 223 | ADJACENT | 2 | 4 | PID-controlled speculative draft length | Feedback-control applied to speculative-drafting so that draft length is continuously adjusted by a PID loop driving the observed acceptance-rate error to a target setpoint. | 2504.00030; 2405.19715 | The specific twist of a closed-loop PID controller that explicitly drives observed acceptance rate to a fixed target setpoint via integral/derivative error terms (rather than a heuristic switch, MDP threshold, or entropy bound) is not demonstrated in these papers, so a PID-specific formulation with formal setpoint tracking and offline-tuned gains could still be a novel contribution if framed as such. |
| 224 | ADJACENT | 2 | 4 | ARC-scheduled draft length | A cache-replacement-arc-style dual-list policy applied to speculative-drafting so that draft length is chosen per-step by balancing recent accept-rate (recency) against long-run accept-rate (frequency) rather than a single fixed or purely-instantaneous threshold. | 2504.00030; 2607.03876 | The specific ARC-style dual-list (short-window recency vs long-run frequency) weighting mechanism with an explicit meta-controller that shifts weight based on which estimate predicted better last round is a more structured non-stationarity-tracking scheme than GammaTune's simple heuristic switching, so a rigorous ablation showing gains specifically under abrupt easy-then-hard regime shifts (which single-EMA/entropy-threshold methods provably lag on) could still be a narrow but genuine contribution. |
| 225 | ADJACENT | 2 | 4 | Importance-weighted draft-length calibration | Importance-sampling reweighting applied to speculative-drafting so that the draft model's per-token confidence is recalibrated against the true target-acceptance distribution before being used to choose draft length, correcting for the draft's miscalibration under distribution shift. | 2406.16858; 2504.00030 | A twist could survive if the proposal's isotonic-regression map built specifically from logged importance weights p_target/q_draft is shown to give a provably unbiased acceptance-probability estimator for setting draft length, unlike GammaTune's heuristic acceptance-rate tracking or EAGLE-2's uncorrected calibration claim, but this is a narrow theoretical refinement of existing acceptance-rate-based length calibration rather than a new capability. |
| 226 | ADJACENT | 3 | 2 | energy-descent block iteration | energy-based-refinement applied to the layer-schedule so that a block is re-applied a variable number of times, each step taking a gradient step on a learned scalar energy of its own output, until the energy stops decreasing or a step budget is hit | 2502.11646; 2607.20519 | The proposal's specific twist—an explicit scalar contrastive energy head whose gradient with respect to the block's own output drives a variable-length per-instance refinement loop with an early-stopping threshold on energy delta, trained jointly with LM loss at matched FLOPs—is not clearly instantiated in any single paper, since Hyper-SET's recurrence isn't input-adaptive-halted via a learned energy head and the looped-transformer halting work uses probability gates rather than energy-gradient descent on the representation. |
| 227 | ADJACENT | 2 | 4 | Product-quantised FFN key memory | Product quantisation applied to the FFN's key vectors so that per-token key scoring is done against compact codebook centroids instead of the full dense key matrix, cutting the score-computation cost from O(d_ff) dense dot-products to O(m*k) codebook lookups. | 2512.13751; 2503.15798 | The specific twist of applying classic product-quantisation (multi-subspace k-means codebooks with ADC reconstruction, m*k lookups) directly to the dense FFN key matrix trained end-to-end from scratch, as opposed to product-key memory's disjoint two-subspace Cartesian product used mainly as an added memory layer, is not clearly demonstrated in these papers and could still be a distinguishable contribution if PQ's compositional multi-way structure and residual coding are shown to give capacity/quality advantages over product-key memory's simpler two-way key composition. |
| 228 | ADJACENT | 3 | 2 | kNN retrieval FFN memory | kNN-retrieval applied to the FFN so that the key-value lookup is replaced by nearest-neighbor search over an external memory bank instead of scoring every stored key. | 2501.00823; 2503.15798 | The specific mechanism of exact top-k nearest-neighbor search over a frozen/refreshed (key,value) activation bank feeding attention-like mixing into the FFN down-projection, trained/matched at FLOPs parity as in Memorizing Transformers/kNN-LM but applied inside the FFN sublayer rather than at the output layer or via cross-attention, is not directly shown in these papers. |
| 229 | ADJACENT | 2 | 4 | predictive-coding learning-rate feedback | predictive-coding-style hierarchical error feedback applied to the lr-schedule so that the learning rate is set by a running estimate of loss-prediction error rather than a fixed curve | 2605.20005; 2107.03331 | The specific twist of maintaining an explicit two-scalar (level+slope) EMA forecaster of the loss and using the loss-prediction error itself (predicted vs actual loss) as a bounded multiplicative correction on a base LR curve is not exactly instantiated in KOALA (which models parameter/gradient dynamics via Kalman filtering) or FINCH (which reacts to raw loss magnitude, not forecast error); a paper isolating forecast-error-driven, hierarchical predictive-coding-style correction for general pretraining schedules would still be distinguishable. |
| 230 | ADJACENT | 2 | 4 | Loss-prioritised replay buffer | Experience-replay applied to data-order so that high-loss examples are revisited on a prioritised schedule instead of being seen exactly once. | 2602.17066; 2604.16918 | A genuine ring-buffer over fresh pretraining shards with running-average per-example loss priority^alpha resampling, matched FLOPs budget, and no auxiliary predictor network, applied specifically to single-pass LM pretraining data-order (rather than RL rollouts, continual-learning task memory, or a proxy loss predictor) has not been directly shown; PBS is the closest but avoids actual per-example loss tracking and buffer replay, which is exactly the mechanism claimed here. |
| 231 | ADJACENT | 2 | 4 | self-distillation curriculum control | online-self-distillation applied to data-order so that a slow EMA teacher's per-domain loss gap against the fast student is used to reweight the sampling mixture toward domains the student is currently failing relative to its own recent past | 2411.14318; 2606.25178 | The specific mechanism of using an EMA copy of the same model (rather than a scaling-law target or advantage/transfer signal) and its per-domain loss gap against the live student as the literal softmax reweighting signal for pretraining data-mixture control, retrained from scratch at matched FLOPs, is not exactly instantiated in Velocitune or TAC, so a paper explicitly ablating EMA-teacher-gap-driven mixture reweighting against these nearest baselines would still be a meaningful, non-trivial follow-up rather than a duplicate. |
| 232 | ADJACENT | 2 | 4 | PID-controlled attention span | feedback-control applied to attention-topology so that a PID controller drives the per-head attention span (window width, sink count) to track a target error signal (e.g. local loss or entropy) instead of using a fixed pre-set span | 2601.15305; 2302.10377 | A genuine PID closed-loop control law (explicit error-tracking with proportional-integral-derivative gains against a measured entropy/loss error, tuned per-head via grid search rather than learned end-to-end) is a different control mechanism than the learned/gated adaptive controllers in GSA or the dynamic-span network in the speech paper, so a strict claim about closed-loop feedback control (vs. learned/heuristic adaptation) could still be a narrow but real twist. |
| 233 | ADJACENT | 3 | 2 | MDL-selected attention mask per sequence | minimum-description-length applied to attention-topology so that the sparsity pattern used for a given input is the one that minimizes total coding cost (model bits + residual bits) rather than a fixed a-priori mask | 2607.21752; 2510.24606 | The explicit MDL formulation—computing an actual two-part code (log topology-code + cross-entropy bits under each candidate mask on a held-out subsegment) and choosing the minimizer, over a small enumerable menu of named topologies (dense/windowed/sink/strided) shared-trained at matched budget—is a more principled, exactly-computed criterion than compression-ratio heuristics or learned importance scores, so a rigorous MDL-vs-heuristic comparison could still be a genuine contribution. |
| 234 | ADJACENT | 2 | 4 | Speculative step with rollback check | branch-prediction applied to the gradient step so that a cheap predicted update is applied speculatively and only fully computed/committed if a low-cost verification confirms loss decrease | 1905.01549; 2603.17942 | The specific closed-loop speculative-execution structure—cheap momentum/Nystrom-predicted gradient applied speculatively, verified on a small probe batch, and rolled back to exact computation on misprediction to amortize backward-pass cost below one full pass per step—is not shown in these papers; CG predict-and-recompute targets communication-hiding in a different algorithm class and lacks the probe-based verify/rollback and skip-backward capability. |
| 235 | ADJACENT | 2 | 3 | Hopfield draft-block retrieval | Hopfield energy-descent retrieval over a memory bank of stored n-gram/hidden-state patterns applied to the sampler so that a block of draft tokens can be produced in one associative-recall step instead of one forward pass per token | 2311.08252; 2502.15572 | A genuine twist would be showing that Hopfield energy-descent convergence (multi-step, non-causal, iterative denoising toward an attractor) produces materially better/faster multi-token draft blocks than existing dense/sparse nearest-neighbor retrieval drafters (REST/DReSD), e.g. via provable capacity/robustness advantages of the attractor dynamics rather than just swapping the retrieval index. |
| 236 | ADJACENT | 2 | 3 | Kalman-fused draft correction | Kalman-style fusion of a fast low-fidelity token-block predictor (prediction) with a slower exact forward pass (measurement), weighted by their calibrated uncertainties, applied to the sampler so that multiple tokens can be committed per exact forward pass without waiting for full sequential confirmation | 2603.01581; 2601.06100 | A twist restricted to fusing draft-model logits with exact-model logits at the sampler for generic autoregressive LLM decoding (not robot kinematics, not model-internal state/memory, not ICL adaptation), with a trained variance head calibrating the Kalman gain per-token, is not exactly covered by KERV's kinematic-domain action correction. |
| 237 | ADJACENT | 2 | 3 | evolutionary beam mutation decoding | evolutionary-population applied to the sampler so that a population of candidate continuations is mutated and selected across generations, allowing a previously committed span to be replaced by a mutated descendant that scores better under a fitness function | 2411.07112; 2311.03758 | A genuine evolutionary-population loop that maintains a population across explicit generations, mutates arbitrary spans of already-committed autoregressive output via resampling, and selects survivors by a fitness function—applied to standard autoregressive LLM decoding rather than code-specific backtracking, diffusion order-search, or offline reranking—is not directly instantiated by these papers, so a careful comparison and demonstration against ROCODE-style single-shot backtracking and best-of-N/beam baselines could still be a new empirical contribution. |
| 238 | ADJACENT | 2 | 3 | multi-draft consensus commit sampler | consensus-protocols applied to the sampler so that a token is only committed once multiple independent stochastic drafts (from perturbed decoding rounds of the same model) agree via a voting round, otherwise the position is re-drawn, giving each commit an explicit agreement quorum instead of a single irrevocable sample | 2502.18779; 2606.06695 | The specific twist of running the same frozen model under different temperature/dropout seeds (no separate draft model) purely as an agreement quorum with an explicit re-draw-on-disagreement rule and entropy-gated cost amortization is not quite the framing of multi-draft speculative decoding (which uses a separate drafter model and target verification for distributional fidelity, not per-token quorum confidence); this narrower framing could still be new if benchmarked as a calibrated confidence/abstention mechanism rather than a speedup technique. |
| 239 | ADJACENT | 2 | 3 | submodular prefill block selection | Greedy submodular-maximisation applied to prefill so that the attention computation over the prompt uses a guaranteed near-optimal sparse subset of key/value blocks instead of full quadratic attention. | 2608.01169; 2602.08722 | The proposal's twist is porting the exact facility-location submodular greedy algorithm with its formal (1-1/e) coverage guarantee (already used for video/token compression) specifically onto prefill KV-block attention selection, which none of the prefill sparse-attention papers do (they use similarity/oracle/learned scoring without a submodular optimality certificate) — this is a narrow but plausible gap. |
| 240 | ADJACENT | 2 | 3 | Arithmetic-coding-gated prefill skip | Arithmetic coding applied to prefill so that prompt spans whose model-predicted code length is near-zero (i.e. highly predictable continuations of already-cached prefixes) skip full attention computation and reuse a compressed cache entry instead. | 2606.13126; 2606.10537 | The specific gating mechanism—using the model's own next-token distribution fed through an actual arithmetic coder to measure per-span code length as the compressibility criterion for splicing cached KV—is not what any of these papers do (they use hashing of identical prefixes, attention-based relevance, or importance probing instead of an entropy/code-length threshold), so a paper demonstrating that this specific online entropy-coding signal yields better precision/recall for cache-splice decisions than existing heuristics would still be a new contribution. |
| 241 | ADJACENT | 2 | 3 | Self-paced draft-length curriculum | Self-paced curriculum scheduling applied to speculative-drafting so that the draft length used during a training/calibration phase is ordered by the draft model's own current loss on upcoming spans, letting the draft-length policy learn to predict its own future reliability before deployment. | 2411.18462; 2608.30135 | The specific self-paced curriculum ordering of training examples by the draft model's own current loss (mastering easy spans before hard ones) to reduce deployment-time variance is not tested by any of these papers, which either use training-free entropy signals or standard i.i.d. training of the auxiliary head—so demonstrating that curriculum ordering specifically lowers variance versus i.i.d. training of the same head would be a genuine addition. |
| 242 | ADJACENT | 2 | 3 | self-paced depth curriculum | Self-paced curriculum ordering applied to the layer-schedule so that the number of blocks a token is routed through is set by the model's own current per-token loss signal, allocated at training time rather than fixed architecture time. | 2603.01914; 2404.02258 | The proposal's distinguishing twist is using an explicit self-paced curriculum mechanism (an adaptively-updated per-epoch loss threshold that governs the pacing function itself, evolving as a curriculum-learning schedule rather than a differentiable learned gate/router) applied only during training with cached gating vectors reused on next occurrence — none of the closest papers implement pacing-function-style curriculum scheduling of depth tied to token re-occurrence rather than a learned per-forward-pass router, so a careful comparison against AdaPonderLM/MoD emphasizing the self-paced scheduling (vs. gated/routed) mechanism could still be novel. |
| 243 | ADJACENT | 2 | 3 | PID-controlled curriculum scheduler | feedback-control applied to data-order so that a PID controller drives each domain's measured loss-improvement error to a target trajectory by adjusting that domain's sampling weight every evaluation interval | 2310.13260; 2411.14318 | The proposal's twist—explicit per-domain P/I/D terms with integral windup correction and derivative damping applied directly to LM pretraining domain-mixture logits, rather than a single PID controller regularizing one aggregate objective (MoRec) or a velocity-based heuristic without explicit integral/derivative control (Velocitune)—could still be new if it demonstrably fixes steady-state lag/oscillation that these nearest works don't address per-domain. |
| 244 | ADJACENT | 2 | 3 | SMC-weighted micro-batch gradients | sequential-monte-carlo applied to the gradient step so that a small population of candidate updates (from different microbatch splits) is weighted by their measured loss-reduction and resampled, replacing a single noisy gradient with a weighted-average survivor | 2601.22003; 2012.06951 | The specific twist of splitting a single step's batch into K microbatches, taking tentative steps, weighting by loss-reduction on a shared eval batch, and resampling+averaging survivors as an implicit uncertainty estimate on step direction (rather than as a Bayesian posterior sampler or per-example reweighting) is not directly instantiated in any of these, though it is a fairly incremental combination of existing SMC-for-optimization and loss-based reweighting ideas. |
| 245 | ADJACENT | 2 | 3 | evolutionary gradient-step selection | evolutionary-population applied to the gradient step so that several mutated variants of the candidate update are evaluated on a cheap held-out microbatch and the best-scoring survivor is applied, instead of the raw single-sample gradient | 1810.06773; 2605.05284 | The specific twist of generating explicit mutant gradient variants scaled to per-layer norm and selecting/blending via a cheap held-out microbatch fitness check (rather than alternating population evolution steps or step-size search) is a narrower, more surgical mechanism than ESGD's population-based training loop, so a tightly scoped ablation showing this exact per-step mutate-and-select filter catches bad steps that ESGD's periodic evolution phase would miss could still be novel. |
| 246 | ADJACENT | 2 | 2 | Wavelet multiresolution prefill cache | Wavelet multiresolution decomposition applied to prefill so that the prompt's key/value sequence is represented as a coarse approximation plus sparse detail coefficients, letting the model skip computing/storing detail where it is negligible. | 2210.01989; 2604.22808 | A twist that ties wavelet detail-coefficient thresholding specifically to skipping/recomputing KV-cache entries during LLM prefill (with an inverse-transform reconstruction used directly as the cached representation and exact on-demand recomputation of discarded detail) is not explicitly done by WavSpA or FreqFormer, so a version concretely instantiated as a prefill KV-cache sparsification/allocation scheme (rather than a general attention-in-wavelet-space layer or diffusion video attention router) could still be new. |
| 247 | TAKEN | 4 | 4 | delta-rule residual overwrite gate | Delta-rule associative memory applied to the residual stream so that each block can erase-and-replace the component of the residual along a learned key direction instead of only adding to it. | 2601.00417; 2606.20743 | Nothing distinct remains: DDL already implements a gated rank-1 read-compare-overwrite delta-rule update directly on the residual stream at each layer, matching the proposal's mechanism, placement, and purpose almost exactly. |
| 248 | UNSCOPED | 4 | 4 | reflex-deliberate head override | dual-process gating applied to multi-head attention so that a cheap fixed 'reflex' subset of heads always fires while a slow learned controller can selectively override with the full head-set only when its own uncertainty signal exceeds a threshold. |  |  |
| 249 | TAKEN | 4 | 3 | windowed denoising KV rewrite | masked-diffusion-denoising applied to the KV cache so that a bounded trailing window of past key/value entries is iteratively refined by later context before being frozen. | 2505.16950; 2601.02076 | Only a genuinely different twist (e.g., a formal masked-diffusion training objective for the denoiser distinct from Bottlenecked Transformer's in-place rewrite, or proof the mechanisms differ mathematically rather than just terminologically) could survive, but per the stated equivalence rule (non-causal in-place rewrite = denoising rewrite) this proposal is already covered. |
| 250 | TAKEN | 4 | 3 | TTT-rewrite of KV entries | Test-time-training applied to the kv-cache so that stored key/value entries are revised in place by a small self-supervised update as later context arrives, instead of remaining frozen once written. | 2505.16950; 2608.30692 | A genuinely new angle would need to show the learned adapter operates strictly on cached K/V projections in fixed windows with an offline-learned update rule under matched-FLOPs, and demonstrate gains specifically from local self-supervised correction rather than full replacement of the cache with fast weights, but this is a narrow scope difference rather than a new capability. |
| 251 | TAKEN | 4 | 3 | Sleep-consolidation cache compression | Sleep-replay-consolidation applied to the kv-cache so that during an offline pause (between documents or on an idle scheduler slot) recent cache traces are reactivated and distilled into a compact, permanent summary slot, instead of being kept append-only until eviction. | 2603.14517; 2505.16950 | Only a genuinely distinct twist (e.g., proving a different theoretical guarantee, a training-free variant, or targeting a capability neither interference-resolution nor generalized reasoning addresses) would separate this from SleepGate and Bottlenecked Transformers, which already implement periodic sleep-inspired consolidation modules that compress/merge KV cache entries into permanent summaries. |
| 252 | TAKEN | 4 | 3 | Transactional KV rewrite windows | Transactions-and-rollback applied to the kv-cache so that a bounded window of recent entries can be atomically rewritten by later context before being committed to the immutable cache | 2505.16950; 2605.06997 | Little; the twist would need to be something beyond periodic non-causal in-place KV rewrites via an auxiliary/adapter module before commit, which Bottlenecked Transformers already frames via consolidation/reconsolidation theory with the same staging-then-commit structure. |
| 253 | TAKEN | 4 | 3 | information-bottleneck KV pruning | information-bottleneck applied to kv-cache so that each entry is compressed to the minimal sufficient statistic for predicting future tokens, discarding the rest explicitly rather than storing full-precision keys/values uniformly | 2505.16950; 2604.25975 | Only a genuinely per-entry, target-driven variable-rate allocation measured explicitly in nats via a VIB reparameterized-Gaussian encoder (rather than eviction/rewriting at fixed or heuristic budgets) could still be new, but 2505.16950 already frames KV rewriting explicitly as IB-driven compression-vs-prediction tradeoff at the same component for the same purpose. |
| 254 | TAKEN | 4 | 3 | information-bottleneck KV compression | an explicit information-bottleneck objective applied to the kv-cache so that eviction is replaced by a trained compression that provably keeps only what predicts future tokens | 2505.16950; 2604.25975 | Little remains new: the core twist (frozen base model, trained per-layer side network replacing about-to-be-evicted KV blocks with a fixed-size summary optimized via an explicit rate+task IB loss) is essentially what Bottlenecked Transformers already implements as IB-justified, non-causal, in-place KV cache rewrites via an auxiliary Cache Processor. |
| 255 | TAKEN | 4 | 3 | Sinkhorn-balanced head-to-value matching | Optimal-transport-sinkhorn applied to attention-scores so that queries and keys are matched under a transport cost with marginal constraints instead of independent per-query softmax, redistributing mass to avoid multiple queries collapsing onto the same few keys. | 2002.11296; 2509.23436 | A version that provably preserves strict causality with per-row cumulative column marginals varying by position (rather than fixed target vectors) and demonstrates measurable prevention of attention-sink pathologies specifically versus Sparse Sinkhorn Attention's efficiency framing and LOTFormer's low-rank linear-time framing could still be a narrow contribution, but the core claimed mechanism-placement-purpose triple is already published. |
| 256 | TAKEN | 4 | 3 | Sinkhorn-transported causal attention | Optimal-transport Sinkhorn matching applied to attention-scores so that query-key alignment is computed as a balanced transport plan under an explicit cost rather than an unconstrained bilinear dot product. | 2605.12879; 2605.20433 | Only the causal, sliding-window streaming variant with a running column-mass budget under strict autoregressive masking is not explicitly tested in these papers, but this is a scale/setting variant of an already-established mechanism (Sinkhorn/OT replacing softmax to enforce balanced marginals and prevent attention collapse), not a new capability. |
| 257 | TAKEN | 3 | 5 | Conformal recalibration of post-trained logits | Conformal prediction applied to the output head so that after preference post-training, the model emits a calibrated prediction set with a distribution-free coverage guarantee instead of a miscalibrated point-softmax. | 2604.01170; 2502.12601 | Nothing distinctive survives: applying split conformal calibration to a frozen post-trained model's softmax scores to yield per-step coverage-guaranteed token sets for abstention/verification is exactly what ORCA, COPU, and VACP already do; only the specific 'paperkiln events.jsonl' logging source is new, which is not a methodological contribution. |
| 258 | TAKEN | 3 | 5 | Conformal abstention token | Conformal prediction applied to the output-head so that the model can refuse to commit to any single token when no calibrated prediction set of bounded size achieves target coverage. | 2512.22682; 2302.08703 | Nothing genuinely new survives: 2512.22682 already computes conformal nonconformity scores from softmax at each decoding step, calibrates a threshold for target coverage, and explicitly addresses the case where the set would be too large (their whole 'efficiency' contribution is about shrinking oversized sets), which functionally subsumes the proposal's abstain-when-too-large behavior; only the explicit token name 'ABSTAIN' differs from suppressing/refusing to sample. |
| 259 | TAKEN | 4 | 3 | diffusion patch-repair over committed spans | masked-diffusion-denoising applied to the sampler so that a fixed trailing window of already-committed tokens can be jointly re-masked and re-denoised using bidirectional context from tokens generated after them, before being permanently frozen | 2606.01026; 2602.06161 | Little room remains: the specific fixed cadence (k=16/w=8), the small side-network sharing embeddings while the base LM stays frozen, and the paperkiln KV-lane implementation are engineering variants, not a new capability beyond visible-token revision already demonstrated by D3IM and COVER. |
| 260 | TAKEN | 4 | 3 | renormalized coarse-to-fine attention topology | renormalisation-group applied to attention-topology so that the mask is built hierarchically: coarse-grained token blocks attend first, and only blocks that survive a relevance threshold get refined into fine-grained fixed attention, instead of applying one fixed-scale mask everywhere | 2603.28458; 2605.18753 | Nothing genuinely new survives: the RG framing is just relabeling the same coarse-filter-then-fine-attend block-sparse mechanism already implemented (with joint training and matched compute) in HISA and DashAttention. |
| 261 | UNSCOPED | 4 | 3 | sleep-phase KV consolidation | offline reactivation-and-compression of old KV segments applied to the context window so that stale positions are replaced by a compact consolidated trace instead of being kept at full per-token cost |  |  |
| 262 | UNSCOPED | 4 | 3 | Sinkhorn head-token routing | Optimal-transport Sinkhorn matching applied to multi-head attention so that heads are assigned to tokens (or token-groups) via a balanced doubly-stochastic routing plan instead of every head attending to every token uniformly. |  |  |
| 263 | UNSCOPED | 4 | 3 | MDL-selected head subset | minimum-description-length applied to multi-head attention so that the set of heads actually used per layer is chosen to minimize total code length (model bits + residual bits) rather than being fixed a priori. |  |  |
| 264 | TAKEN | 3 | 4 | Attention-weighted PQ cache | Product-quantisation applied to the kv-cache so that entries' storage precision (codebook granularity) is allocated in proportion to their running attention mass rather than uniformly | 2407.12820; 2605.08317 | Only a genuinely novel twist—e.g. online k-means codebook updates jointly trained with an EMA-attention-mass threshold policy that dynamically re-tiers entries between full precision and PQ during training itself (not just post-hoc/inference-time)—might be new, but this is a training-recipe nuance rather than a new capability beyond PQCache/RDKV's attention-weighted precision allocation. |
| 265 | TAKEN | 4 | 2 | Sleep-phase KV consolidation | Sleep-replay-consolidation applied to the kv-cache so that an offline, periodic 'sleep' pass reactivates recently evicted cache traces and compresses them into a stable long-term store, then discards the raw traces. | 2603.14517; 2505.16950 | Only if the sleep pass is framed as literally reconstructing attention outputs via replay-trained autoencoding (rather than gating/summarizing or routed accumulation) with a strict discard-after-consolidate buffer, but this is a training-detail variation on mechanisms already published. |
| 266 | TAKEN | 3 | 4 | product-quantised shared KV store | product quantisation applied to the kv-cache so that KV vectors are stored as compact codebook indices in a shared cross-request store, letting identical or near-identical prefixes be looked up by code rather than recomputed | 2601.10155; 2504.03661 | The specific claim of sub-vector code matching across paraphrased/partially-overlapping prefixes (rather than just per-request compression or exact prefix reuse) is not explicitly tested by these papers, but it's a fairly incremental combination of existing PQ-KV compression (LOOKAT/MILLION) with existing shared-cache reuse (PolyKV/prefix caching systems), so any novelty would rest narrowly on the cross-request code-level deduplication claim rather than the core mechanism. |
| 267 | TAKEN | 3 | 4 | Hebbian erase-write buffer | hebbian fast-weights applied to the residual stream so that recently-written associations can be actively overwritten by new outer-product updates within a short window, rather than only accumulated | 2412.06464; 2606.26560 | Only a genuinely distinct twist—e.g. applying this exact erase-write fast-weight state as an additive term directly into the transformer residual stream (rather than as the recurrent state of a standalone linear-attention layer) with a short fixed forget horizon—could be new, but this is a placement/framing detail, not a new mechanism or capability. |
| 268 | TAKEN | 3 | 4 | contrastive multi-horizon head | A contrastive objective applied to the output-head so that hidden states are trained to be predictive of representations several tokens ahead, not just the immediate next token. | 2607.12277; 2411.16695 | Only a genuinely different twist (e.g., proving the InfoNCE negatives specifically prevent representation collapse or degenerate shortcut solutions that BYOL/summary losses do not, or showing gains transfer specifically to re-ranking tasks not covered by prior work) would be new; the core mechanism-placement-purpose triple is already demonstrated by NONTP's TCL. |
| 269 | TAKEN | 3 | 4 | SMC particle decoding | sequential-monte-carlo applied to the sampler so that multiple candidate continuations are tracked and reweighted in parallel, letting low-weight hypotheses be dropped and replaced rather than committing to a single irrevocable token | 2404.17546; 2306.03081 | Nothing genuinely new remains; the exact mechanism (particle population at the sampler, importance weights from lookahead/reward, periodic resampling, optional trained twist via self-normalized importance sampling) is already established by Lew et al. and the twisted-SMC line, matching the generator's own nearest-known guess. |
| 270 | TAKEN | 3 | 4 | Thompson-sampled decoding temperature | Bandit posterior sampling applied to the sampler so that the temperature/top-k value at each position is drawn from a posterior over which value would maximize a reward proxy, rather than fixed a priori. | 2603.09065; 2004.10874 | A genuinely new contribution would need to show Thompson-sampling-specific posterior exploration (vs. RL/policy-gradient adapters) gives measurably better exploration-exploitation tradeoffs than the contextual-bandit/POMDP decoding policy already demonstrated in 2603.09065, but the core capability (per-position/per-prompt online selection of decoding hyperparameters via bandit-style reward feedback) is already claimed there. |
| 271 | TAKEN | 3 | 4 | cross-prompt kNN prefill cache | kNN-retrieval applied to prefill so that instead of recomputing K/V from scratch for every prompt, the model looks up cached K/V (or hidden-state) representations from structurally similar previously-seen prompts and blends them in, amortising the quadratic cost across a corpus of prompts. | 2311.04934; 2606.13361 | A genuinely new angle would require approximate (non-exact-prefix) retrieval via embedding-based kNN over non-identical but structurally similar blocks with a trained adapter correcting for context mismatch, rather than exact-match prefix/document caching as in the cited systems—but this is already anticipated by adapter-based cross-cache correction work (e.g., C2KV, cross-model KV mapping) applied to the same reuse-at-prefill goal, so the novelty margin is thin. |
| 272 | TAKEN | 3 | 4 | Conformal coverage speculative drafting | Conformal prediction applied to speculative drafting so that the draft model proposes calibrated prediction sets with a guaranteed coverage level instead of single-token guesses, and the target verifies set membership rather than exact match. | 2510.09942; 2606.30265 | A conformal-specific twist with a formal (1-alpha) marginal coverage guarantee on the draft set (rather than heuristic top-k/multi-draft or KL-margin certificates) could still be distinct, but 2510.09942 already applies online conformal prediction to shape the draft/retained token set in speculative decoding, closely matching the proposed mechanism and placement. |
| 273 | TAKEN | 3 | 4 | SMC-weighted multi-branch drafting | Sequential Monte Carlo applied to speculative-drafting so that the draft stage maintains a weighted population of candidate continuations instead of a single greedy path, resampled by target-model likelihood before verification | 2604.15672 | No clear twist survives; the proposal's specific schedule details (K=4-8, resample threshold, extension length k tuned on validation) are hyperparameter choices within the same mechanism already published as SMC-SD. |
| 274 | TAKEN | 3 | 4 | test-time-trained stopping threshold | test-time training applied to the draft model's stopping head so that a small parameter set is updated via a self-supervised loss on the incoming stream itself, letting draft length adapt within a single long generation without external labels | 2411.18462; 2405.19715 | A twist restricted to updating only a small stopping-threshold adapter (not the whole draft distribution) via self-supervised accept/reject pseudo-labels rather than teacher-logit distillation might be marginally different, but SpecDec++ and SVIP already cover threshold/length adaptation from in-stream signals, and TTS already covers verification-driven online adaptation of the draft model itself, so little room remains. |
| 275 | TAKEN | 3 | 4 | Thompson-sampled draft length | Bandits-thompson applied to speculative-drafting so that draft length is chosen by sampling from a posterior over expected accepted-tokens-per-call for each candidate k, rather than fixed. | 2406.03853; 2505.15141 | Only a genuinely different posterior parameterization or action-set structure (e.g., continuous k via Gaussian process bandits) not covered by these discretized bandit/Thompson approaches would be new, but the core claim is already realized. |
| 276 | TAKEN | 3 | 4 | PID-controlled learning rate | PID feedback-control applied to the lr-schedule so that the learning rate is driven by proportional, integral, and derivative terms on a measured error signal (deviation of observed loss-decrease-rate from a target decay trajectory) rather than following a fixed predetermined curve. | 1911.07710; 2605.21968 | Nothing substantive: PID-controlled LR schedules driven by loss-decrease error against a target trajectory are already established (PID optimizers, PD-control LR schedules); a genuinely new twist would need a different error signal or control target not yet explored (e.g. controlling against a theoretically derived optimal trajectory with formal stability guarantees tied to transformer-specific loss curvature). |
| 277 | TAKEN | 4 | 2 | predictive-coding gradient error routing | predictive-coding applied to the backward pass so that each layer propagates a locally-verified prediction error (top-down prediction minus bottom-up activation) rather than a single unverified end-to-end gradient signal | 2508.08762; 2605.31022 | Only the specific gating rule (skipping/scaling a layer's update when its local error is already small) is a minor addition not explicitly tested in these papers, but it is a straightforward inference-time thresholding on the already-standard PC local error signal, not a new mechanism or placement. |
| 278 | UNSCOPED | 3 | 4 | PQ-compressed KV cache lanes | product-quantisation applied to the context-window's stored key/value vectors so that positions contributing little to attention outputs occupy far less memory than high-information positions |  |  |
| 279 | UNSCOPED | 3 | 4 | Spectral context compression | spectral-transform applied to the context-window's cached representations so that long-range structure becomes localized in a frequency basis, allowing far coarser truncation of high-frequency (locally-redundant) components without losing long-range (low-frequency) dependencies |  |  |
| 280 | UNSCOPED | 3 | 4 | generational KV garbage collection | age-partitioned, differentially-swept eviction applied to the KV cache so that young (recent) positions are re-evaluated for relevance often while old positions are checked rarely, giving non-uniform maintenance cost matched to expected contribution |  |  |
| 281 | UNSCOPED | 3 | 4 | ARC-policy KV eviction | an adaptive-replacement-cache policy (dynamically balancing recency and frequency lists) applied to the KV cache so that eviction decisions adapt online to the actual access/attention pattern instead of a fixed recency-only window |  |  |
| 282 | UNSCOPED | 4 | 2 | RG coarse-graining of stale context | A renormalisation-group coarse-graining step applied to the context window so that older KV blocks are iteratively merged into fewer, coarser summary tokens whose surviving degrees of freedom are exactly those relevant at longer range, instead of every position costing the same regardless of age. |  |  |
| 283 | UNSCOPED | 3 | 4 | Importance-sampled context reweighting | Importance sampling applied to the context window so that positions are drawn/attended to with probability proportional to their estimated contribution to the next-token loss under a cheap proxy distribution, then reweighted to remain an unbiased estimate of full dense attention, instead of paying uniform cost to attend to every position. |  |  |
| 284 | UNSCOPED | 3 | 4 | Hebbian fast-weight context compression | Hebbian outer-product fast-weight memory applied to the context window so that low-contribution positions decay out of an associative store within a few steps instead of occupying a fixed KV slot for their full lifetime in the window. |  |  |
| 285 | UNSCOPED | 3 | 4 | sparse-dictionary head activation | sparse coding applied to multi-head attention so that each token's head-mixture is expressed as a sparse combination of a learned head-dictionary rather than always summing all heads. |  |  |
| 286 | UNSCOPED | 4 | 2 | Hopfield cross-request cache recall | Hopfield-style energy-descent retrieval applied to the serving-batch's aggregate KV history so that a new request's attention query can pull in relevant K/V patterns previously computed for other concurrent or recent requests instead of only its own cache. |  |  |
| 287 | UNSCOPED | 4 | 2 | multi-scale coarse-grained shared cache | renormalisation-group applied to serving-batch so that KV states from many independent requests are coarse-grained into a hierarchy of shared summaries at increasing context-scale, and requests read the coarsest summary that still changes their output within tolerance |  |  |
| 288 | UNSCOPED | 3 | 4 | LSH-bucketed KV cache sharing | Locality-sensitive hashing applied to serving-batch request states so that similar prompt/context prefixes across concurrent requests can share cached KV computation instead of recomputing independently. |  |  |
| 289 | TAKEN | 3 | 3 | IB-consolidated KV summaries | Information-bottleneck applied to the kv-cache so that groups of old entries are compressed into a small sufficient-statistic summary trained explicitly to preserve only what predicts future tokens | 2505.16950; 2604.25975 | Only a genuinely different twist—such as a formal variational IB bound with an epsilon-constrained frozen predictor loss guarantee, or joint training with the base model rather than periodic in-place rewrites—would be new, but this is a training-recipe/framing difference, not a new capability. |
| 290 | TAKEN | 3 | 3 | Sparse dictionary KV atoms | Sparse-coding applied to the kv-cache so that each entry is represented as a sparse combination of a small shared dictionary of atoms rather than a dense per-token vector | 2412.08890; 2412.11741 | The interpretability side-channel (inspecting which atoms compose an entry) and joint from-scratch training at matched FLOPs are not emphasized in Lexico/CSR (which are largely post-hoc/training-light on pretrained models), so a genuinely new angle would be exploiting atom co-activation patterns as an interpretability tool rather than just compression, but the core mechanism-at-component-for-compression claim is already taken. |
| 291 | TAKEN | 3 | 3 | Shared cross-request KV retrieval | kNN-retrieval applied to the kv-cache so that identical or near-identical prefixes across requests are served from a shared cache instead of recomputed. | 2402.15220; 2504.11765 | Only a genuinely new twist—e.g. proving tight guarantees on approximate (non-exact) KV splicing with logit-verified tolerance across arbitrary non-prefix overlaps rather than exact/prefix-tree matching—would be new, but the core capability (shared, persistent, cross-request KV cache retrieval to skip prefill) is already established by ChunkAttention, RadixAttention-style systems, and disk/object KV sharing works. |
| 292 | TAKEN | 3 | 3 | RG-coarsened attention gating | A renormalisation-group coarse-graining step applied to attention-scores so that a query first attends to a coarse-grained summary of key-blocks and only 'refines' (spends mass) on blocks whose coarse signal survives a learned relevance threshold, letting irrelevant blocks be dropped entirely rather than mixed at full resolution. | 2605.18753; 2603.28458 | Only if the RG framing contributes a genuinely different mathematical mechanism (e.g. a multi-scale flow with provable fixed-point/relevance criteria across many coarsening levels, not just one coarse-then-fine pass) rather than being a relabeling of existing coarse-block-gate-then-refine designs. |
| 293 | TAKEN | 3 | 3 | Thompson-sampled expert selection | Thompson sampling applied to the moe-router so that expert selection during training is drawn from a posterior over each expert's per-token value rather than taken as a deterministic top-k of point-estimate scores | 2609.13058; 2509.23830 | A count-based Beta/Gaussian per-expert-token-cluster posterior updated from per-token training loss as reward, with explicit sample-at-train/mean-at-inference switching framed strictly as a bandit exploration-exploitation solution replacing load-balancing losses, is a narrow variant but not clearly distinct from the Bayesian-routing and expert-space-exploration frameworks already proposed for the identical purpose at the identical component. |
| 294 | TAKEN | 3 | 3 | Sinkhorn balanced-assignment routing | Optimal-transport Sinkhorn matching applied to the moe-router so that expert assignment is a balanced transport plan computed jointly for the batch, removing the need for an auxiliary load-balancing loss. | 2511.08972; 2604.07298 | Nothing distinguishes the proposal from SSR (2511.08972), which already implements Sinkhorn-derived gating scores as a drop-in replacement for aux-loss balancing in language modeling MoEs; the proposed gpt2-nano/llama-nano ablation would be a direct scale-down follow-up rather than a new mechanism or purpose. |
| 295 | TAKEN | 3 | 3 | self-distilled position extrapolation | online self-distillation applied to positional-encoding so that a slow teacher copy processing shorter, in-training-distribution windows teaches a fast student copy operating at longer test-time positions, transferring calibrated behavior from seen to unseen position ranges without new labels. | 2604.17535; 2604.06070 | Only a genuinely new twist—continuous online EMA-teacher updating during streaming inference itself (not a separate fine-tuning phase) with adapter-only backprop on position-dependent projections—could differentiate it, but this is a minor implementation variant of the same mechanism already covered by OPSDL and the RoPE-distillation papers. |
| 296 | TAKEN | 2 | 5 | Proper-scoring-rule post-hoc recalibration head | A proper scoring rule (CRPS-style / Brier-style loss on a held-out calibration split) applied to the output-head so that the post-preference-trained logits recover calibrated probabilities without touching the base weights. | 2409.19817; 2601.23096 | Using a strictly proper scoring rule (Brier/CRPS-style) as the specific fitting objective for the small post-hoc map, rather than NLL/temperature-only, is a minor variant already implicitly covered since temperature/Platt scaling under log-loss is itself a proper scoring rule; no genuinely new mechanism or placement remains. |
| 297 | TAKEN | 3 | 3 | particle-filter multi-horizon decoding | Sequential Monte Carlo applied to the output-head so that a population of weighted multi-token continuations is maintained and resampled during generation instead of committing to a single greedy/sampled token at each step. | 2604.16453; 2007.11936 | Little room remains; a genuinely new twist would need a mechanism distinct from reward-weighted SMC resampling over LM continuations, e.g., a fundamentally different weighting signal not reducible to a lookahead reward/value model, or joint training of the base LM with the particle dynamics rather than leaving it untouched. |
| 298 | TAKEN | 2 | 5 | PID-controlled entropy sampler | Feedback-control applied to the sampler so that temperature is adjusted per-token to drive measured output entropy to a target trajectory rather than being fixed globally. | 2603.03310; 2602.13035 | Only the specific PID (proportional-integral-derivative) control law with offline-tuned gains might be a minor implementation detail difference, but the core capability—closed-loop per-token entropy regulation via adaptive temperature at the sampler—is already demonstrated in 2603.03310 and related works. |
| 299 | TAKEN | 3 | 3 | PQ-compressed key prefill cache | Product-quantisation applied to the K/V vectors produced during prefill so that the cache built from a long prompt is stored and attended to in compressed codebook space, cutting the memory/bandwidth cost that drives quadratic prefill cost at large context. | 2407.12820 | Only a genuinely different twist (e.g. training codebooks jointly end-to-end for exact ADC attention scoring rather than approximate top-k retrieval, or provable full-attention equivalence rather than sparsified retrieval) could separate this from PQCache, since PQ-on-prefill-KV for memory/bandwidth reduction is already claimed there. |
| 300 | TAKEN | 2 | 5 | History-conditioned draft-length prediction | Branch-prediction applied to speculative-drafting so that the number of tokens speculated per round is predicted from a small per-position history table rather than fixed, extending further when past predictions at similar contexts were reliably accepted | 2405.19715; 2411.18462 | A concrete twist could be an explicit hashed saturating-counter table keyed on quantized entropy-history (literal branch-predictor structure with online table updates rather than a trained head or entropy-threshold rule), but this is a narrow implementation variant on an already crowded, well-covered capability. |
| 301 | TAKEN | 3 | 3 | self-distilled adaptive drafter | online self-distillation applied to the draft model so that a slow, larger 'teacher' copy continuously updates the fast draft's stopping/confidence behavior from the live token stream, adapting draft length to distribution shift within a session | 2605.09329; 2603.12617 | Restricting the online update to only a small stopping/confidence head (rather than the draft backbone) and running distillation asynchronously off a replay buffer in a background thread is a minor implementation variant, not a new capability, since TTS/OnlineSPEC/OSD already achieve on-the-fly adaptive drafting via online distillation from target feedback. |
| 302 | TAKEN | 3 | 3 | self-distilled early exit gate | online-self-distillation applied to the layer-schedule so that a shallow-copy student predicts when the full-depth teacher's output has already stabilized, letting easy tokens exit early while the same weights keep training on the live stream | 2209.08625; 2206.12420 | Only if the 'live stream / trained from scratch matched budget' online-continual aspect (rather than post-hoc fine-tuning of a pretrained model) were shown to matter mechanistically, but as stated this is the same joint self-distillation early-exit recipe already published. |
| 303 | TAKEN | 3 | 3 | gated reflex-deliberate depth | dual-process-gating applied to the layer-schedule so that easy tokens exit after a shallow reflex path while a learned gate routes hard tokens through additional deliberate blocks | 2605.05222; 2603.01914 | Nothing distinguishes this proposal from TSA/AdaPonderLM/MoR beyond superficial framing ('reflex vs deliberate') and a specific FLOPs-budget penalty, which are not novel twists. |
| 304 | TAKEN | 2 | 5 | Conformal coverage-calibrated token sets | Conformal prediction applied to the token-level output distribution so that the model can emit a prediction set with a distribution-free, finite-sample coverage guarantee instead of an uncalibrated softmax probability. | 2512.22682; 2309.03797 | No clear twist remains; the proposal's exact mechanism (nonconformity score = 1 - softmax probability, calibration quantile threshold, per-position token sets with coverage guarantee) is already implemented and evaluated at the token level in 2512.22682. |
| 305 | TAKEN | 3 | 3 | Delta-rule fast-weight inference memory | Delta-rule associative memory applied to weights-at-inference so that the model overwrites stale key-value associations from the current stream instead of only accumulating them | 2102.11174; 2605.22791 | Nothing distinguishes this proposal from DeltaNet/Gated DeltaNet-family delta-rule fast-weight memories already trained end-to-end to overwrite stale KV associations at inference; a genuinely new angle would need a different erase/write decoupling or object (e.g. erasing along the query direction as in QED) beyond vanilla delta rule. |
| 306 | TAKEN | 2 | 5 | PID-controlled activation steering | Proportional-integral-derivative feedback control applied to weights-at-inference so that a measured behavioral error (e.g. drift in output calibration or repetition rate) is driven to zero by continuously adjusting a small steering vector rather than leaving generation uncorrected | 2510.04309; 2506.18831 | Nothing distinguishes the proposal's generic error signal (entropy/repetition rate) and PID-on-steering-vector formalism from 2510.04309's general PID steering framework or 2506.18831's closed-loop PID steering; a new twist would need a fundamentally different control target or correction mechanism beyond swapping the probed statistic. |
| 307 | TAKEN | 3 | 3 | distilled fast-mask predictor | online self-distillation applied to attention-topology so that a fast per-token mask-predictor learns to open/close attention edges from a slow teacher's realized attention patterns | 2410.13276; 2603.02227 | The specific slow-EMA-teacher-vs-fast-student online self-distillation loop (rather than joint training or frozen-model distillation) is a minor recipe variant, not a new mechanism/placement, so little survives as genuinely new. |
| 308 | TAKEN | 3 | 3 | Predictive-coding cache correction | Predictive coding applied to attention-topology so that every k tokens each cache entry is corrected not by overwriting its value but by adding a learned residual proportional to the prediction error between what the entry implied and what later tokens actually needed, propagated only forward into future reads. | 2508.09001; 2604.15356 | Little room remains unless the predictive-coding framing yields a demonstrably different training/inference behavior (e.g. explicit top-down generative prediction rather than a learned residual/delta), but as an in-place additive correction to cached values driven by prediction error it is functionally covered by RetroAttention's retrospective revision and delta-coding/residual-correction approaches already in the pool. |
| 309 | UNSCOPED | 3 | 3 | Hopfield recall over training trajectory | hopfield-retrieval applied to checkpoints so that any past weight configuration along the training trajectory becomes content-addressable and can be recalled by energy descent from a partial or noisy cue instead of only being resumable from an exact stored file |  |  |
| 310 | UNSCOPED | 3 | 3 | prioritized replay of checkpoint states | experience-replay applied to checkpoints so that past weight states (not just past data) are revisited on a prioritized schedule during training, instead of being discarded once superseded |  |  |
| 311 | UNSCOPED | 3 | 3 | Sparse dictionary context compression | sparse-coding applied to the context-window's hidden-state history so that each position's contribution to future attention is represented as a sparse combination of a small learned atom dictionary instead of a full dense vector |  |  |
| 312 | UNSCOPED | 3 | 3 | entropy-adaptive position budgeting | arithmetic-coding-style entropy accounting applied to the context window so that each position's storage cost in the KV cache is scaled inversely to its measured predictive-surprise contribution, giving low-entropy (predictable) spans a compressed representation and high-entropy spans full fidelity |  |  |
| 313 | UNSCOPED | 3 | 3 | IB-gated KV compression | An information-bottleneck objective applied to the KV cache so that each cached position is compressed to the minimum statistic sufficient to predict future tokens, giving variable bit-cost per position instead of uniform cost. |  |  |
| 314 | UNSCOPED | 3 | 3 | wavelet-banded context allocation | Multiresolution wavelet decomposition applied to the context window so that distant positions are stored at coarser resolution and near positions at full resolution, giving variable effective cost per position instead of uniform cost. |  |  |
| 315 | UNSCOPED | 3 | 3 | Choquet head aggregation | Choquet-integral aggregation with pairwise interaction terms applied to multi-head attention output combination so that heads are weighted by a learned fuzzy measure capturing redundancy/complementarity instead of being summed independently. |  |  |
| 316 | UNSCOPED | 3 | 3 | Submodular greedy head selection | Greedy submodular maximization applied to multi-head attention so that only a diminishing-returns-guaranteed subset of heads is activated per token/layer instead of always running all heads. |  |  |
| 317 | UNSCOPED | 3 | 3 | Thompson-sampled head router | Thompson sampling applied to multi-head attention so that each token position routes to a stochastically-sampled subset of heads chosen from a posterior over per-head value rather than always activating all heads deterministically. |  |  |
| 318 | UNSCOPED | 3 | 3 | PQ-shared KV cache pool | Product-quantisation applied to the serving-batch's per-request KV caches so that structurally similar prefixes across concurrent requests share compressed cache entries instead of storing full-precision K/V independently. |  |  |
| 319 | UNSCOPED | 3 | 3 | kNN cross-batch cache sharing | kNN-retrieval applied to the serving-batch's pooled KV history so that each request's attention can mix in nearest-neighbour K/V entries drawn from any concurrent request's cache, not only its own. |  |  |
| 320 | UNSCOPED | 3 | 3 | offline cache consolidation across requests | sleep-replay-consolidation applied to serving-batch so that recurring prefix/KV patterns across independent requests are compressed into a shared reusable store |  |  |
| 321 | TAKEN | 2 | 4 | Spectral KV cache compression | Spectral-transform applied to the kv-cache so that stored entries are represented in a frequency basis (e.g. DCT over the time axis per feature) where long-range slowly-varying structure collapses onto a few low-frequency coefficients | 2505.00570; 2506.11886 | Nothing substantive remains new: per-block DCT over rolling time windows with top-f coefficient retention and inverse-DCT reconstruction at read time is essentially what FreqKV and FourierAttention already implement and evaluate. |
| 322 | TAKEN | 2 | 4 | ARC-policy KV eviction | cache-replacement-arc applied to kv-cache so that entries are evicted by an adaptive recency/frequency balance instead of a fixed window or static top-k rule | 2606.21238; 2608.18098 | The specific ARC ghost-list bookkeeping (tracking recency-evicted vs frequency-evicted shadow entries to adapt the split point) is a distinct implementation detail, but the capability and placement it targets are already claimed by existing recency/frequency adaptive KV caching work. |
| 323 | TAKEN | 2 | 4 | LSH-bucketed KV prefix sharing | LSH-hashing applied to the kv-cache so that requests with locality-similar prefixes collide into shared buckets and reuse cached KV blocks instead of recomputing prefill. | 2509.24832; 2503.05530 | Nothing substantive survives; SemShareKV already implements LSH-bucketed fuzzy prefix matching on token embeddings to reuse KV cache blocks across semantically similar but lexically different prompts, which is the same mechanism at the same component for the same purpose. |
| 324 | TAKEN | 2 | 4 | posterior-weighted expert averaging | Bayesian model averaging applied to the moe-router so that expert outputs are combined by posterior mass over a small ensemble of gate hypotheses rather than a single argmax/top-k choice, so that the effective gate value reflects a calibrated mixture weight. | 2603.09453; 2509.23830 | Nothing distinguishes the proposal's specific 'last N router checkpoints or K dropout passes + per-expert temperature/prior fit on a calibration split' recipe from VMoER's amortised variational/temperature routing or Bayesian-MoE's thesis-level exploration of weight/logit/selection-space uncertainty, so no genuine twist survives. |
| 325 | TAKEN | 2 | 4 | mixture-density MoE router | Mixture-density-heads applied to the moe-router so that gate outputs become calibrated probability estimates of expert correctness rather than uninterpreted scores. | 2603.09453; 2509.23830 | Little room remains unless the mixture-density-of-Gaussians parameterization trained against realized downstream loss (rather than variational/Laplace/Bayesian posteriors over routing logits) yields materially different calibration behavior or enables new abstention/compute-triggering mechanisms not demonstrated in VMoER or Bayesian-MoE. |
| 326 | TAKEN | 2 | 4 | spectral-domain long-range position code | spectral-transform applied to positional-encoding so that position is represented in a frequency basis chosen so long-range dependencies become local, rather than fixed local index | 2607.10134; 2607.07678 | Nothing distinguishes the proposal from LeRoPE's learned-per-frequency RoPE at matched budget; a genuinely new angle would need a different parameterization (e.g. explicit small discrete spectral basis with sparsity/selection over coefficients rather than a continuous learned scalar per existing frequency slot) or a distinct evaluation targeting periodic long-range retrieval specifically. |
| 327 | TAKEN | 2 | 4 | wavelet multiresolution position code | wavelet multiresolution decomposition applied to positional-encoding so that position is encoded as a fixed orthogonal basis of localized, multi-scale wavelets instead of a single global sinusoidal frequency bank | 2302.08647; 2602.12189 | Using a fixed, non-learned orthogonal discrete wavelet basis (e.g., Haar/Daubechies dyadic scales) evaluated at integer positions and substituted directly for RoPE rotation angles, rather than a learned/continuous Morlet or adaptive DWT variant, is a narrow parameterization choice already covered in spirit by WavePE's fixed orthogonal wavelet PE. |
| 328 | TAKEN | 2 | 4 | transactional multi-token commit blocks | transactions-and-rollback applied to the sampler so that groups of speculatively generated tokens commit atomically or abort as a whole, exposing an explicit commit boundary the model can condition future speculation width on | 2412.18910; 2411.18462 | Framing the accept/abort bitmap as an explicit 'transaction log' with atomic commit/abort semantics is just terminology over the same accept-then-predict-next-K loop already implemented by AdaEAGLE/SVIP/AdaEDL, so no new capability survives unless the transactional rollback itself (e.g., multi-step abort/rollback across cache state beyond simple prefix truncation) does something these lack. |
| 329 | TAKEN | 3 | 2 | sparse dictionary prefill compression | Sparse-coding applied to the stream of prefill hidden states so that the prompt's K/V cache is represented as a small number of active dictionary atoms per token instead of a dense vector, giving compute and memory that scale with the sparsity level rather than the raw prompt length. | 2412.08890 | Only a narrow twist survives: explicit claims/evidence that shared atoms specifically exploit repeated/boilerplate prompt structure so effective compute scales with intrinsic content complexity rather than token count, which Lexico does not frame or measure, but the core mechanism-placement is already taken. |
| 330 | TAKEN | 2 | 4 | LSH-bucketed prefill sparsification | Locality-sensitive hashing applied to prefill so that attention during prompt encoding is restricted to same-bucket tokens, replacing full quadratic prefill with sparse bucketed attention. | 2607.19368; 2510.18413 | Only a genuinely new twist—such as jointly learned hash functions co-trained with causal windowing that provably preserve exactness guarantees beyond SimHash-based macro-token aggregation—would be distinct from Spectral-LSH's already-published LSH-bucketed sub-quadratic prefill compression. |
| 331 | TAKEN | 2 | 4 | Kalman-gated adaptive draft length | Kalman-filtering applied to speculative-drafting so that draft length is chosen per-step from a fused estimate of the draft model's confidence trajectory rather than fixed a priori. | 2411.18462; 2410.18351 | Only the specific Kalman recursion (explicit process/measurement noise scalars fit offline) as opposed to entropy-threshold or rolling-average fusion could be new, but this is a filtering-method substitution on the same object for the same purpose, not a new capability. |
| 332 | TAKEN | 2 | 4 | confidence-gated draft length | dual-process-gating applied to speculative-drafting so that the draft model emits a variable number of tokens per round, chosen by a fast reflex head, before target verification | 2405.19715; 2410.18351 | Nothing distinguishes the proposal from SpecDec++'s trained acceptance-prediction head with threshold-based stopping; a genuinely new twist would need a different signal source, joint drafting/verification co-adaptation, or a different object being rewritten, none of which is present here. |
| 333 | TAKEN | 2 | 4 | Branch-predicted layer skipping | Branch-prediction applied to the layer-schedule so that the router's continue/exit decision at each layer is predicted from a short history of recent tokens' continue/exit outcomes and speculatively executed, with cheap correction if the prediction is wrong. | 2206.04685; 2206.07269 | A hardware-branch-predictor-style, non-learned, hashed-history counter table specifically overlapping the router latency with computation (rather than replacing the exit decision itself or using a full draft/verify pass) is a narrow implementation twist, but the capability (predict exit/continue ahead of time and speculatively proceed, correcting on misprediction) is already covered by Predictive Exit and Exit Predictor combined with LayerSkip's speculative-verify-correct pattern. |
| 334 | TAKEN | 2 | 4 | BMA ensemble logit averaging head | Bayesian model averaging applied to the training objective so that the model's output distribution is a posterior-weighted mixture over an ensemble of lightweight parameter samples rather than a single point estimate | 2405.14438; 2310.00035 | Only a genuinely different twist—such as using the ensemble disagreement specifically to localize under-trained data regions for active data collection rather than just calibration—would be new, but this is not clearly claimed as distinct from existing calibration-focused LoRA-ensemble/BMA work. |
| 335 | TAKEN | 2 | 4 | Thompson-sampled curriculum bandit | Bandits-thompson applied to data-order so that the choice of which data shard/difficulty-bucket to sample from next is drawn from a posterior over each bucket's expected training value, rather than fixed random ordering. | 2102.03662; 2606.25178 | Only a genuinely different posterior parameterization or reward signal (e.g., provably optimal regret bounds tailored to LM pretraining loss dynamics, or explicit Beta/Gaussian per-token loss-reduction posteriors validated at pretraining scale) would be new, since bandit-driven bucket/domain curriculum sampling for LLM training is already demonstrated in multiple works. |
| 336 | TAKEN | 2 | 4 | importance-weighted replay buffer | Importance-sampling applied to data-order so that examples are effectively revisited by upweighting the loss contribution of high-current-loss examples drawn from a fixed-order stream, correcting for the mismatch between the arrival distribution and the model's current error distribution. | 2502.06733; 2006.13169 | Only a genuinely different correction mechanism, unbiasedness guarantee, or a different placement (not loss-based instance reweighting during single-pass pretraining) would remain novel; the proposal's core (loss-triggered importance-weighted reservoir replay for unbiased gradient correction in pretraining) is already covered by 2502.06733. |
| 337 | TAKEN | 2 | 4 | homeostatic mixture normalization | homeostatic-plasticity applied to data-order so that each domain's sampling rate is continuously rescaled to hold a fixed target statistic (e.g. per-domain gradient-norm contribution or per-domain average token loss) constant across training, rather than letting naturally easy domains dominate gradient signal | 2411.14318; 2502.06733 | Little; the homeostatic multiplicative gain toward an explicit shared target statistic is essentially the same continuous loss-based domain-reweighting loop Velocitune and DomainPilot already implement, so only a genuinely different target statistic (e.g. gradient-norm share rather than loss) with demonstrated distinct dynamics could still be new. |
| 338 | TAKEN | 2 | 4 | Loss-tilted importance data order | Importance-sampling reweighting applied to data-order selection so that the effective training distribution is corrected toward the model's current error distribution while remaining an unbiased estimator of the original data distribution's gradient. | 1602.02283; 2607.05806 | Little room remains beyond combining smoothed per-example loss scores with explicit IS-ratio gradient scaling in a large-scale/production LLM training loop, which is an engineering variant rather than a new capability given 1602.02283 already established unbiased loss/gradient-informed importance sampling for minibatches. |
| 339 | TAKEN | 3 | 2 | Kalman-fused causal cache update | Kalman filtering applied to attention-topology so that each cached key/value is treated as a noisy state estimate that gets fused with a new 'measurement' derived from later context, weighted by learned uncertainty, instead of being either frozen or fully overwritten. | 2609.07816 | Only a narrow implementation difference (explicit per-slot KV cache in a standard transformer attention topology vs. linear-attention recurrent state) could remain novel, but the core mechanism—Kalman-gain-weighted fusion of new evidence into a cached state representing uncertainty—is already claimed. |
| 340 | UNSCOPED | 2 | 4 | streaming teacher from own past | online self-distillation applied to checkpoints so that the continuous trajectory between saved snapshots is compressed into a running teacher signal instead of being discarded |  |  |
| 341 | UNSCOPED | 3 | 2 | sleep-phase checkpoint consolidation | Sleep-replay-consolidation applied to checkpoints so that the discarded inter-checkpoint trajectory is periodically replayed offline and compressed into the next checkpoint rather than being thrown away. |  |  |
| 342 | UNSCOPED | 2 | 4 | learned prefix-index for cache lookup | learned-index applied to serving-batch so that a small model predicts the position of a matching cached KV segment for an incoming request prefix, replacing exact hash-lookup with a learned approximate retrieval that shares computation across requests |  |  |
| 343 | TAKEN | 2 | 3 | EMA-consolidated KV trajectory | ema-averaging applied to the KV cache so that each cached value is replaced by an exponential moving average over its own revision trajectory (computed from repeated local re-encodes) rather than the raw single-pass endpoint. | 2602.04268; 2605.08840 | The only defensible twist is doing the EMA over multiple perturbed re-encodes of the same position during prefill (variance reduction via repeated forward passes) rather than EMA over sequential time-steps of a single trajectory, but this is a minor procedural variant of an already-published in-place EMA smoothing of cached K/V vectors. |
| 344 | TAKEN | 2 | 3 | MDL-regularized expert set selection | Minimum-description-length selection applied to moe-router so that top-k picks the expert subset that minimizes total code length (token reconstruction cost plus routing description cost) rather than maximizing independent per-expert scores. | 2606.26287; 2607.20427 | A concrete greedy KL-based conditional-redundancy penalty computed from expert output projections (rather than gating entropy or Huffman-code framing) could be a distinguishing implementation detail, but the core claim—MDL-based selection at the router to penalize redundant expert sets—is already proposed in GeMoE and explicitly called for in the Huffman-coding paper. |
| 345 | TAKEN | 2 | 3 | Self-distilling FFN fast copy | Online self-distillation applied to the FFN so that a fast, cheaply-updated shadow copy of the FFN weights tracks the stream in real time while the slow copy stays fixed as a stable teacher. | 2608.01672 | Little room remains beyond minor recipe differences (LoRA-rank-8 parameterization, KL-on-logits vs hidden-state discrepancy, explicit reset-to-slow-copy framing), none of which change the core mechanism-at-component-for-purpose match. |
| 346 | TAKEN | 2 | 3 | EMA-smoothed key-value trace | EMA averaging applied to attention-topology's stored keys/values so that each cache entry represents a smoothed trajectory estimate rather than a single noisy endpoint, letting later tokens implicitly 'denoise' earlier representations without violating causality. | 2602.04268; 2609.03515 | A learned per-head alpha trained end-to-end at matched FLOPs as a first-class architectural component (rather than a training-free inference-time smoothing heuristic) is the only remaining distinction, and it is a minor scale/training-recipe difference already anticipated by the proposal's own exclusion criteria. |
| 347 | UNSCOPED | 2 | 3 | quantized checkpoint trajectory codebook | product-quantisation applied to checkpoints so that the full sequence of intermediate weight snapshots between saves can be stored and later retrieved at a fraction of the cost, instead of being thrown away |  |  |
| 348 | UNSCOPED | 2 | 3 | checkpoint-trajectory kNN retrieval | kNN-retrieval applied to the sequence of training checkpoints so that inference can mix in parameter-space neighbours from the optimization trajectory instead of using only the final weights. |  |  |
| 349 | UNSCOPED | 2 | 3 | information-bottleneck checkpoint distillation | Information-bottleneck applied to the inter-checkpoint trajectory so that only the trajectory information predictive of future validation loss is explicitly compressed into an auxiliary checkpoint summary, discarding the rest. |  |  |
| 350 | UNSCOPED | 2 | 3 | RG coarse-graining of checkpoints | renormalisation-group coarse-graining applied to the sequence of training checkpoints so that the trajectory between snapshots is summarised into a compact multi-scale record instead of discarded |  |  |
| 351 | UNSCOPED | 2 | 3 | MDL-adaptive position budgeting | A minimum-description-length criterion applied to the context window so that each position's KV entry is allotted a code-length proportional to how much it shortens the code of future tokens, replacing uniform per-position storage with a two-part code (model + residual). |  |  |
| 352 | UNSCOPED | 2 | 3 | hot-path tracing across shared prompts | jit-tracing applied to serving-batch so that a computation path repeatedly triggered by structurally similar requests (e.g. identical system prompt, repeated few-shot template) is recorded once and specialised, and the specialised trace is shared across all requests that hit that path |  |  |
| 353 | TAKEN | 1 | 4 | Entropy-coded optimiser state checkpoints | Arithmetic-coding applied to optimiser-state so that the per-parameter moment tensors are stored/transmitted at their empirical entropy rather than fixed-width floats, exploiting the fact that most moments are structurally similar within a module. | 2506.12000; 2009.13003 | Nothing distinguishes this proposal from 2506.12000/2009.13003/ExCP, which already quantize+entropy-code optimizer moment/gradient tensors per empirical distribution purely as an I/O-path checkpoint codec; a genuinely new twist would need a different guarantee (e.g. provable bit-exact bounded-loss reconstruction certificates or exploiting cross-step redundancy in a way none of these do). |
| 354 | TAKEN | 2 | 2 | wavelet-banded moment memory | Wavelet multiresolution decomposition applied to optimiser-state so that per-parameter second-moment trajectories are stored as a few low-frequency wavelet coefficients instead of one running scalar, giving the optimiser explicit access to curvature at multiple timescales simultaneously. | 2501.07237; 2510.05361 | The proposal's twist of using explicit per-parameter multiresolution (approximation+detail) coefficients with globally tuned per-level damping weights is essentially what GWT already implements (multilevel Haar transform maintaining moment states on the compact coefficients), so no clearly distinct capability remains. |
| 355 | UNSCOPED | 2 | 2 | hot-path checkpoint specialization tracing | JIT-tracing applied to checkpoints so that a frequently-recurring loss-landscape trajectory pattern (a 'hot path' between two snapshots) is recorded once and reused as a specialized update instead of being recomputed and discarded every time it recurs. |  |  |

## Top OPEN, in full

### Hebbian fast-weight FFN overlay  (splash 4, feasibility 4, id 67ed619219)

**Claim.** outer-product Hebbian fast weights applied to the FFN so that recent token associations are stored and read back within the same forward pass, decaying over a short horizon

**New capability.** the model can bind and recall arbitrary token-pair associations introduced mid-sequence (e.g. a novel name-to-fact mapping) without any gradient update, which a fixed FFN cannot do by construction

**Placement.** Add a low-rank fast-weight matrix A_t to one FFN layer's weight, updated at every token by A_t = decay*A_{t-1} + eta * (v_t outer k_t) where k_t,v_t are learned linear projections of the current residual stream; the FFN forward pass uses W + A_t. Only the projections (k,v,eta,decay) and a mixing gate are trained end-to-end on a synthetic associative-recall task (introduce a key-value pair, query it later in-context); the static FFN weights W stay frozen or are trained jointly at matched budget, stated as from-scratch at matched budget.

**Invariant.** outputs stay causal

**Falsifier.** H: adding Hebbian fast weights to one FFN layer improves in-context associative recall accuracy at delay > vanilla FFN's implicit capacity, at matched FLOPs/params Metric: exact-match accuracy on held-out key-value recall probes at varying delay lengths Baseline: matched-FLOPs nano-gpt2 with static FFN only, same total parameter budget (fast-weight params subtracted from FFN width) Protocol: nano-gpt2 preset, 12 seeds, synthetic recall dataset with delay in {8,32,128,512} tokens, matched FLOPs via reduced FFN hidden dim to compensate for fast-weight overhead GPU-hours: 22

**Nearest (generator's guess).** Fast Weight Programmers (Schmidhuber 1992) / Linear Transformers as fast weight systems (Schlag et al. 2021)

**Scoper.** closest: [2607.11614] Extending LLM Context via Associative Recurrent Memory: uses associative recurrent memory for long-context extension but as a recurrent state across segments/layers, not an outer-product Hebbian fast-weight rewrite of the FFN matrix itself within a single forward pass.; [2605.11196] Variational Linear Attention: Stable Associative Memory for Long-Context Transformers: implements an outer-product-like associative memory update but placed in the attention/linear-attention mechanism, not as an additive low-rank overlay on FFN weights.; [2406.03068] Distributional Associations vs In-Context Reasoning: A Study of Feed-forward and Attention Layers: studies FFN vs attention roles in associative/in-context tasks but does not propose a Hebbian fast-weight overlay added to FFN weights. | survives: None of the retrieved papers place a per-token outer-product Hebbian update directly onto the FFN weight matrix (W + A_t) with decay, read back within the same forward pass; the closest work does associative memory either as a separate recurrent module across segments or inside linear attention, so the specific FFN-weight fast-weight overlay for mid-sequence binding remains untested. | confidence 0.62

### atomic commit fast-weight edits  (splash 4, feasibility 4, id 8d53aebded)

**Claim.** transactions-and-rollback applied to weights-at-inference so that a batch of proposed within-sequence weight edits is validated against a held-out probe before being committed, with automatic rollback to the frozen base weights if the probe check fails

**New capability.** the system can attempt speculative per-sequence weight adaptation (e.g. from a few in-context examples) with a guarantee that adaptation never degrades output quality below the frozen baseline, since bad edits are provably discarded

**Placement.** Every k tokens, accumulate a candidate ΔW (Hebbian outer-product or gradient-based) in a staging buffer; before committing, run the last m tokens of context through both frozen weights and staged (W+ΔW) and compare loss; commit ΔW into the live forward pass only if staged loss <= frozen loss - margin, else discard and reset staging buffer to zero. The comparator threshold and staging update rule are trained/tuned; base weights are never modified, only ever read exactly or ΔW-perturbed.

**Invariant.** exactness is recoverable by verification

**Falsifier.** H: transactional commit of fast-weight edits achieves strictly non-negative delta-perplexity vs always-frozen baseline on a mixed easy/adversarial-context stream, while naive always-commit fast-weights sometimes regresses Metric: per-token perplexity delta vs frozen baseline, fraction of committed transactions, worst-case regression Baseline: frozen weights (no adaptation) and always-commit Hebbian fast-weights (no rollback) at matched extra FLOPs Protocol: 12 seeds, nano-llama preset, mixed in-context-learning + distribution-shift synthetic stream, report worst-seed regression GPU-hours: 25

**Nearest (generator's guess).** Fast weight programmers with gating; model souping / speculative decoding-style accept-reject (but applied to weight edits rather than tokens, which is the novel part)

**Scoper.** closest: [2503.11842] Test-Time Training Provably Improves Transformers as In-context Learners: Nearest concept of per-sequence weight adaptation from in-context examples, but it has no staged commit/rollback against a probe with quality guarantee.; [2601.21012] Order-Aware Test-Time Adaptation: Leveraging Temporal Dynamics for Robust Streaming Inference: Uses a gate that reverts to a frozen base predictor when adaptation evidence is weak, structurally similar to rollback-on-failure, but at the prediction/output level rather than committing a ΔW into weights via staged buffer comparison.; [2609.15887] The Model Proposes, the Code Disposes: A Pre-Registered Ablation of a Verifier-and-Acceptance Stage in an LLM-Orchestrated Offensive-Security Agent: Accept/reject gating pattern analogous in spirit, but applied to agent report outputs, not to in-place weight edits with a frozen-weight fallback. | survives: Nothing in the set stages a candidate ΔW in a buffer, compares held-out probe loss of frozen vs staged weights, and commits/rolls back the weight perturbation itself (as opposed to gating tokens, predictions, or reports); that specific transactional weight-edit mechanism with a provable non-degradation guarantee at the weight level remains undemonstrated. | confidence 0.72

### delta-rule fast-weight router  (splash 4, feasibility 3, id d92399e1b2)

**Claim.** Delta-rule fast-weight memory applied to the moe-router so that the gate's expert-scoring key-value memory is overwritten in-context based on observed downstream error, closing the feedback loop within a single sequence.

**New capability.** The system can adapt its routing decisions within a single forward pass over a long sequence in response to which experts turned out to help or hurt, without any gradient step or extra training data, because the router carries a fast associative memory updated by the delta rule as the sequence streams.

**Placement.** Augment the router with a small fast-weight matrix M (per-layer, size d_model x n_experts) updated at each token via the delta rule: M_t = M_{t-1} + eta * (target_t - M_{t-1} k_t) k_t^T, where target_t is a one-step-lagged correction signal derived from the realized loss delta attributable to the previously chosen expert, and k_t is the token's router key. The gate score becomes linear-projection + q_t^T M_t. Trained from scratch at matched budget; the delta-rule update itself uses no additional learned parameters beyond eta (fixed or small scalar learned).

**Invariant.** outputs stay causal; the fast-weight update at position t only uses the loss signal from position t-1's expert choice (already emitted), so nothing depends on future tokens.

**Falsifier.** H: Delta-rule fast-weight augmented routing improves perplexity on long sequences (>2k tokens) relative to a static router at matched FLOPs/params, with gains growing with sequence length. Metric: perplexity vs. sequence position (bucketed), and total held-out perplexity Baseline: static linear router MoE, same total params (fast-weight matrix budget subtracted from expert width) Protocol: nano llama-MoE preset, sliding-window lane disabled to isolate effect, 12 seeds, sequences up to 4k tokens, matched FLOPs GPU-hours: 35

**Nearest (generator's guess).** Fast weight programmers / DeltaNet (Schlag et al., Yang et al. 2023) applied to routing rather than attention

**Scoper.** closest: [2609.14859] N/A (irrelevant match): not relevant, excluded from real ranking; [2609.04895] Cache-Aware Joint Router Adaptation for Memory-Efficient MoE Inference: touches MoE routers and cache/history state but the mechanism is trained retention prediction for expert-weight caching, not an in-context delta-rule associative memory updated from realized loss to change expert scoring.; [2609.08189] Do Dynamic Routers Need Memory? HeRo: History-Aware Routing for Efficient LLM Inference: closest in spirit (fast associative memory augmenting a router, updated in-context) but it is for layer-skip/depth routing, aggregates via linear attention (not delta-rule error-correction), and is not MoE expert-choice routing driven by realized downstream loss feedback.; [2601.00417] Deep Delta Learning: applies delta-rule-style target-seeking rewrites, but to the residual stream, not to a MoE router's expert-scoring memory, and not driven by an error signal from realized expert performance. | survives: Nothing in the set closes the loop of a delta-rule fast-weight memory embedded specifically in the MoE gate, updated in-context token-by-token from a lagged realized-loss/regret signal attributable to the chosen expert, to change future expert scoring within the same sequence; HeRo does router memory but for depth-skip routing via linear-attention aggregation rather than delta-rule error-correective overwrite tied to observed expert-attributable loss, so a router-specific credit-assigned delta-rule fast-weight mechanism remains open. | confidence 0.72

### Barrier-constrained expert routing  (splash 4, feasibility 3, id c427a1d0b8)

**Claim.** Control-barrier-functions applied to the moe-router so that gate logits are projected at inference onto the nearest point of a safety set (e.g. minimum per-expert utilization over a sliding window, or maximum logit gap bound) whenever the raw top-k choice would leave that set, guaranteeing the constraint holds every token without retraining.

**New capability.** The system can guarantee, by construction and verifiable at runtime, that no expert is starved or overloaded within any window of W tokens, a property the open-loop router cannot certify because it has no mechanism to correct a choice before it is committed.

**Placement.** Define h(state) = per-expert utilization over the last W tokens minus a floor epsilon; at each step compute the unconstrained top-k logits, then if projected utilization would violate h>=0, solve a small QP (closed-form for top-k, since it's just a logit bias) that minimally perturbs logits to keep h>=0 for the next step. No new trainable parameters; W and epsilon are hyperparameters; runs every token, O(num_experts) extra work.

**Invariant.** exactness recoverable by verification

**Falsifier.** H: CBF-constrained routing eliminates expert starvation events (zero-utilization windows) entirely while degrading perplexity by <1% vs. matched-FLOPs unconstrained router. Metric: count of starvation windows, held-out perplexity Baseline: standard top-k router with aux load-balance loss only, matched FLOPs Protocol: nano gpt2 MoE, 8 experts, 12 seeds, inject adversarial input distribution shift mid-eval to stress starvation, paperkiln events.jsonl logging GPU-hours: 26

**Nearest (generator's guess).** safe-RL control barrier functions (Ames et al.); no published application to MoE routing

**Scoper.** closest: [2608.14205] FreeBalance: Pre-Routing Online Moe Load Balancing via Residual Workload Prediction: Closest in purpose (per-token online load balancing of MoE experts) but achieves it via predictive pre-routing migration planning, not a runtime CBF-based projection/QP correction of gate logits with a certified safety guarantee.; [2604.09474] SafeMind: A Risk-Aware Differentiable Control Framework for Adaptive and Safe Quadruped Locomotion: Uses the exact CBF-QP mechanism the proposal wants, but applied to robot locomotion control, not to MoE router logits.; [2509.23830] Bayesian Mixture-of-Experts: Towards Making LLMs Know What They Don't Know: Intervenes on router logits/selection for a different purpose (calibration/uncertainty), not per-token certified utilization-bound safety via CBF projection. | survives: Nothing in the set places a CBF/QP safety-set projection directly on MoE gate logits to give a per-window, per-token certifiable no-starvation/no-overload guarantee without retraining; the nearest work (FreeBalance) solves load balancing predictively/statistically rather than via a barrier-certified logit correction, so this specific mechanism-at-component-for-purpose combination remains open. | confidence 0.72

### PID-controlled expert capacity  (splash 3, feasibility 5, id 9fdb7032de)

**Claim.** Proportional-integral-derivative feedback control applied to the moe-router so that per-expert capacity thresholds are continuously adjusted to drive measured load imbalance to zero, replacing the static aux-loss weight.

**New capability.** The system can maintain expert load balance as a closed-loop control target that adapts online during training (and optionally at inference) instead of relying on a fixed-weight auxiliary loss whose gradient fights the LM loss.

**Placement.** Track per-expert load error e_i(t) = target_load - actual_load (measured over a sliding window of B batches); compute a PID signal u_i(t) = Kp*e_i + Ki*sum(e_i) + Kd*(e_i - e_i_prev) and add u_i(t) as a per-expert bias directly to the router logits (not as a loss term) before top-k selection, updated every training step. No aux loss gradient is backpropagated for balancing; only Kp/Ki/Kd (few scalars, tuned or fixed) and the router weights via LM loss are involved. Tested on gpt2-nano MoE, matched FLOPs vs. aux-loss baseline.

**Invariant.** matched FLOPs/params; the PID bias only depends on past load measurements (t' < t), so it is a causal, purely inference-time-computable correction with no gradient interference with the quality loss.

**Falsifier.** H: PID-biased routing achieves equal or better validation perplexity than aux-loss balancing at equal or better load-balance CV, and converges faster in wall-clock/steps. Metric: validation perplexity, load CV, steps-to-balance Baseline: standard aux-loss top-k router, matched FLOPs/params Protocol: gpt2-nano, 4-8 experts, 12 seeds, sweep Kp/Ki/Kd on a small grid, matched training steps GPU-hours: 25

**Nearest (generator's guess).** Expert-choice routing / capacity-factor heuristics (Zhou et al. 2022); no direct PID-controller precedent in MoE literature that we know of

**Scoper.** closest: [2605.15403] φ-Balancing for Mixture-of-Experts Training: Uses an online feedback adjustment to router logits/bias to correct load balance, but via mirror descent on a convex potential rather than an explicit PID (proportional-integral-derivative) control law with tunable Kp/Ki/Kd gains.; [2604.26340] Adaptive and Fine-grained Module-wise Expert Pruning for Efficient LoRA-MoE Fine-Tuning: Tracks per-expert utilization dynamically but changes expert count/structure rather than applying a closed-loop feedback bias to logits.; [2602.14159] Synergistic Intra- and Cross-Layer Regularization Losses for MoE Expert Specialization: Still uses auxiliary loss-based mechanisms rather than a gradient-free PID bias term added directly to router logits. | survives: None of the candidates implement an explicit PID (proportional-integral-derivative) feedback controller with distinct P/I/D terms added as a non-gradient bias directly to router logits in place of an aux-loss; the nearest work (φ-Balancing) uses mirror-descent/EMA-based adjustment, which is a different control law without explicit derivative/integral gain structure, so a genuine PID formulation with Kp/Ki/Kd terms tested against matched-FLOPs aux-loss baselines would still be new. | confidence 0.6

### periodic bidirectional residual denoising  (splash 4, feasibility 3, id bed3b457c2)

**Claim.** Masked-diffusion-denoising applied to the residual stream so that a bounded lookback window of past residual writes can be revised (partially erased/overwritten) by later context, instead of only ever receiving additive updates.

**New capability.** The system can correct or retract information written into the residual stream by earlier blocks/tokens using later context, something an additive-only residual stream cannot do by construction.

**Placement.** Every k tokens (e.g. k=32), take the last W residual-stream states (e.g. W=64, already causally computed and cached), run a small bidirectional denoising transformer (2-4 layers, separately trained) over them with a fraction masked/noised, and write the denoised output back into a side-buffer that future reads consult preferentially over the original residual entries. Downstream blocks at future timesteps read from this rewritten buffer; because the write only ever affects entries that are strictly in the past relative to any token being currently generated (the edit happens at step t but is only consulted by decoding at step >t+k), causality of emitted outputs is preserved. The denoiser is trained separately (masked reconstruction loss on frozen residual snapshots) with the base model untouched, or jointly at matched budget if from-scratch.

**Invariant.** outputs remain causal; the denoising step only rewrites cache entries for positions already emitted, and rewritten entries are only ever consulted by future token predictions, so no output at position t depends on any input at position >t.

**Falsifier.** H: Periodic residual denoising reduces perplexity on long-range dependency tasks (e.g. induction-style or needle-in-haystack recall at nano-scale) versus a matched-FLOPs baseline with no denoising step, because it can correct stale/wrong early representations. Metric: validation perplexity on long-context nano benchmark + synthetic recall accuracy Baseline: same-size decoder-only model, no denoising pass, matched FLOPs (denoiser FLOPs added as extra baseline layers instead) Protocol: gpt2-nano, sliding-window lane in paperkiln, W=64, k=32, 12 seeds, pre-register recall accuracy delta threshold GPU-hours: 35

**Nearest (generator's guess).** KV-cache compression/rewriting schemes (e.g. H2O, cache-merging) and non-causal patch-level diffusion LMs; genuinely novel combination for the residual stream specifically

**Scoper.** closest: [2207.02495] Improving Streaming End-to-End ASR on Transformer-based Causal Models with Encoder States Revision Strategies: This is the nearest analog: a bounded-lookback in-place revision of cached hidden states after later context arrives, but it targets encoder states for ASR via a revision network, not masked-diffusion-denoising on the residual stream of an LM.; [2601.16971] Auto-Regressive Masked Diffusion Models: Combines masked diffusion with causal/block generation but operates on output token distributions, not on rewriting cached residual-stream states with a separate bidirectional denoiser.; [2603.06642] SR-TTT Does Not Learn Retrieval: A Correction and Mechanistic Post-Mortem of Surprisal-Aware Residual Test-Time Training: Shares the theme of revising cached state with later information for correction, but the mechanism is fast-weight TTT/attention routing rather than diffusion-denoising rewrites of the residual stream, and the paper is a negative result about retrieval, not a capability claim about correction via denoising. | survives: Nothing in the set applies masked-diffusion-style bidirectional denoising specifically to residual-stream cache entries (as opposed to encoder states in ASR, token logits, or fast-weight memories) to allow non-causal in-place erasure/overwrite of past residual writes while preserving causal output emission; that specific placement, mechanism, and purpose combination remains open. | confidence 0.62

### TTT gate on residual  (splash 4, feasibility 3, id 4fa44608b8)

**Claim.** test-time-training applied to the residual stream so that a block can attenuate or overwrite directions written by earlier blocks rather than only add to them

**New capability.** the network can suppress or invert an earlier layer's contribution based on later context, instead of being restricted to superposition of all past writes

**Placement.** Insert a small per-block gating module (a low-rank linear map, rank 16-64, plus a scalar per-channel gate) that is updated by a few steps of gradient descent on a self-supervised reconstruction loss (predict the block's own input from a masked/noised version of the current residual) computed only from tokens up to and including position t; the fast weights are reset every k tokens (k=32-128) or decay with a fixed half-life. Trained jointly with the base model at nano-scale (~50-150M params) on gpt2 preset in paperkiln; the outer loop trains the initialization and learning rate of the inner TTT update, the inner loop runs at both train and inference time.

**Invariant.** outputs stay causal

**Falsifier.** H: adding a TTT-gated erase path to the residual stream reduces loss on tasks requiring overwriting stale bound information (e.g. variable-reassignment / state-tracking synthetic tasks) more than a matched-FLOPs deeper/wider baseline Metric: perplexity and task accuracy on a synthetic state-tracking benchmark (sequences with repeated variable rebinding) plus held-out validation loss Baseline: matched-FLOPs/params gpt2-nano with extra depth or width absorbing the TTT module's compute, no gating Protocol: nano-scale (~50-125M params), 12 seeds, 40 GPU-hour budget, pre-registered accuracy threshold on state-tracking task, ablate gate reset period k in {32,64,128} GPU-hours: 36

**Nearest (generator's guess).** Test-Time-Training layers (Sun et al. 2024) / TTT-Linear, applied here as a gating mechanism on residual writes rather than as a full sequence-mixing layer replacement

**Scoper.** closest: [2608.07110] Modular TTT: Rethinking Test-Time Training as Composable Modules: This is the closest TTT paper but treats TTT as a sequence-mixing layer replacement and studies gating/residuals as minor architectural knobs within the fast-weight network, not as a mechanism for a block to attenuate/overwrite earlier blocks' residual-stream writes.; [2605.03953] Transformers with Selective Access to Early Representations: This gates access to an early-layer contribution via a context-dependent gate on the residual pathway, but it is a static learned gate for selective reuse of V_1, not an online test-time-trained update that can invert/suppress arbitrary earlier writes.; [2609.05309] How Does mHC Use Its Residual Streams? Selective Routing and Near-Identity Mixing: Analyzes learned routing/mixing of multi-stream residual writes but is a post-hoc empirical study of a fixed trained architecture, not a test-time-training gating mechanism enabling suppression/inversion of earlier writes. | survives: No paper combines test-time (inference-time gradient) updated fast weights with a gating/rewrite operation specifically placed on the residual stream to let later blocks suppress or invert earlier blocks' contributions; Modular TTT explicitly finds residual/gating additions unhelpful within the fast-weight network itself rather than testing a TTT-driven gate on the stream, so the specific combination (online TTT-updated gate performing in-place attenuation/inversion of the residual stream) remains untested. | confidence 0.62

### Delta-rule content-adaptive positions  (splash 4, feasibility 3, id 739f773c4f)

**Claim.** Delta-rule associative overwriting applied to positional encoding so that a token's effective position code is a content-conditioned, overwritable memory entry rather than a fixed function of its integer index.

**New capability.** The model can re-key its notion of 'position' when content signals a structural boundary (e.g. new sentence, repeated motif), collapsing or stretching the effective index instead of being locked to raw token count.

**Placement.** Maintain a small associative memory M keyed by a hash/embedding of local content; at each position, before adding the usual base positional vector, query M with the current token's content embedding, and overwrite the entry with a delta-rule update (erase-then-write) using the same content key, producing a position-adjustment vector added to (or replacing part of) the standard sinusoidal/RoPE code. Trained end-to-end from scratch: the read/write projections for M are learned, M itself is not a parameter but a per-sequence state reset at sequence start.

**Invariant.** outputs remain causal; M at step t is only ever written using tokens up to t, and reads for position t use only that state, so no future information leaks in.

**Falsifier.** H: Content-adaptive delta-rule position adjustment improves performance on tasks with variable-length repeated substructure (e.g. list/table copying, indentation-sensitive code) versus fixed positional encoding at matched FLOPs/params. Metric: exact-match / edit-distance on synthetic structured-copy benchmark plus standard LM perplexity as a control Baseline: gpt2-nano with RoPE, matched params (extra M read/write heads offset by narrower FFN) Protocol: 12 seeds, nano preset, synthetic structured tasks generated in-repo, paperkiln sliding-window lane held fixed for fair comparison GPU-hours: 26

**Nearest (generator's guess).** DeltaNet (Yang et al. 2024) applied to content, but not previously targeted at positional encoding specifically

**Scoper.** closest: [2605.10414] Remember to Forget: Gated Adaptive Positional Encoding: This is content-aware modulation of positional encoding, but it gates attention decay/salience rather than performing a delta-rule erase-and-write overwrite of a positional memory keyed by content.; [2511.19279] MapFormer: Self-Supervised Learning of Cognitive Maps with Input-Dependent Positional Embeddings: This makes position encoding input-dependent (content-conditioned) but via Lie-algebra matrix exponentials, not a delta-rule associative memory overwrite mechanism, and doesn't frame it as re-keying/collapsing index via erase-then-write.; [2607.20062] Solar Open 2 Technical Report: This applies a delta-rule mechanism in a no-PE hybrid attention architecture, but the delta rule governs the linear-attention state/token mixing, not a dedicated positional-encoding memory keyed by content for re-indexing purposes. | survives: The proposal's specific twist—an explicit per-sequence associative memory M keyed by content embeddings, updated via delta-rule erase-then-write, whose output is injected specifically as a positional-adjustment vector to let the model collapse/stretch its effective index at structural boundaries—is not demonstrated by any candidate; GAPE and MapFormer make positional encoding content-aware but through gating or Lie-algebra transforms rather than an explicit delta-rule memory-overwrite keyed by content and dedicated to re-indexing. | confidence 0.62

### Redundant-code cache repair  (splash 4, feasibility 3, id 250371bee8)

**Claim.** Error-correcting-code redundancy applied to attention-topology so that cache entries are stored with a learned parity/checksum projection, and every k tokens a syndrome computed from later hidden states is used to detect and repair drift in earlier cache entries before future reads.

**New capability.** The model gains a mechanism to detect *which* earlier cache entries have become stale or corrupted (high syndrome magnitude) and selectively repair only those, rather than uniformly rewriting or fusing all entries, giving sparse, targeted, auditable revision with an explicit corruption signal in events.jsonl.

**Placement.** Augment each cached value vector with a small learned parity vector p = P(v) (fixed linear code, e.g. random projection with known pseudo-inverse) computed at write time. Every k tokens, recompute an expected parity from the current hidden state via a trained decoder, compare to stored parity to get a syndrome; if syndrome exceeds a threshold, replace the value with P_inverse(current-context-informed correction), else leave untouched. Threshold, projection P, and decoder are trained jointly with the LM loss from scratch at matched params (P is a fixed random matrix, not counted since it's non-parametric, decoder replaces part of an FFN layer).

**Invariant.** exactness recoverable by verification; the syndrome check is exactly the verification step — entries below threshold are provably left as originally emitted (identity), so the cache is only altered when a certificate of drift is present, and one can always fall back to the unrepaired cache to recover the exact baseline computation.

**Falsifier.** H: Selective syndrome-triggered repair reduces perplexity on long documents with mid-sequence topic shifts more than either always-repair or never-repair baselines, at lower average repair-FLOPs than full periodic rewrite. Metric: perplexity, fraction of slots repaired, FLOPs spent on repair vs perplexity gain Baseline: matched-param nano-llama, no repair; and always-repair (dense rewrite) variant Protocol: nano-llama, 12 seeds, seq len 2048 with synthetic topic-shift documents, k=32, threshold swept over 3 values, 40 GPU-hour budget GPU-hours: 37

**Nearest (generator's guess).** No direct precedent combining ECC syndromes with transformer KV caches; nearest are checksum-based memory verification in neural Turing machines and anomaly-gated memory writes in differentiable neural computers

**Scoper.** closest: [2608.28293] A Probabilistic Interpretation of KV Cache Eviction: Also targets selective correction of degraded KV cache entries during decoding, but via probabilistic expectation-estimation for eviction, not ECC-style parity/syndrome detection-and-repair.; [2602.06161] Stop the Flip-Flop: Context-Preserving Verification for Fast Revocable Diffusion Decoding: Uses a cache-drift-aware scoring signal to selectively decide which cached states to override, analogous in spirit to a corruption signal gating selective repair, but for diffusion decoding verification not ECC syndromes.; [2609.12090] Does Video Memory Use What It Retrieves? A Causal Audit of Memory Specificity: Performs selective read-time replacement of memory/cache values, related to selective repair but as an ablation/audit tool, not a trained ECC detect-and-repair mechanism. | survives: The specific mechanism of a learned parity/checksum projection with a syndrome-threshold trigger for selective, auditable cache repair (an explicit ECC-style detect-then-correct loop trained end-to-end) is not implemented by any candidate, which instead use probabilistic correction, drift-aware verification scores, or causal substitution audits rather than a parity-check code. | confidence 0.7

### Kalman-fused router logits  (splash 3, feasibility 4, id 342139d049)

**Claim.** Kalman filtering applied to the moe-router so that expert selection fuses a prior gate score with a noisy downstream-error measurement from the previous token's routing outcome, in proportion to their estimated uncertainties.

**New capability.** The router can down-weight its own prediction for a token when recent routing has been measurably unreliable, instead of always trusting the raw gate score.

**Placement.** At each token position t, maintain a per-expert scalar state (mean, variance) updated online: prediction = router logit at t; measurement = a cheap proxy for expert t-1's usefulness (e.g. drop in next-token loss or gradient-norm signal from the auxiliary loss, computed causally using only tokens up to t-1); Kalman gain blends the two into the logit actually used for top-k selection at t. The gate and a learned per-expert measurement-noise estimate are trained jointly with the LM; the filter itself has no extra trainable weights beyond noise scalars, so the compute cost is one scalar update per expert per token.

**Invariant.** outputs stay causal

**Falsifier.** H: Kalman-fused routing reduces expert-choice regret (measured as loss of the greedily-best-in-hindsight expert vs. chosen expert) by >=10% relative to a matched-FLOPs top-k MoE baseline, without hurting perplexity. Metric: held-out perplexity and per-token expert regret Baseline: standard top-k linear router, matched params/FLOPs, same expert count Protocol: nano gpt2 MoE (4-8 experts, top-2) on paperkiln, 12 seeds, 40M-token budget, matched FLOPs via reduced hidden size on the KF variant to offset the state buffers GPU-hours: 30

**Nearest (generator's guess).** expert-choice routing / adaptive gating with EMA load-balancing (Zhou et al. 2022); no published Kalman-specific router

**Scoper.** closest: [2606.00761] Confidence-Adaptive SwiGLU for Mixture-of-Experts: adjusts gating behavior based on routing confidence but modulates gate sharpness, not a Kalman-style temporal fusion of prior logit with a downstream-error measurement.; [2511.08277] X-IONet: Cross-Platform Inertial Odometry Network: uses a Kalman filter for state estimation fusion, but applied to inertial odometry displacement, not to MoE router logits.; [2607.26052] Spend Experts Where You Are Unsure: Confidence-Adaptive Routing for Mixture-of-Experts LoRA: uses router confidence to adapt expert budget per token, but does not maintain temporal per-expert noise/variance state or blend prior/measurement via Kalman gain. | survives: Nothing in the set maintains a per-expert online (mean, variance) state updated causally across tokens and blends the current router logit with a downstream-error measurement via an explicit Kalman gain before top-k selection; the nearest papers either use EKF for unrelated sensor fusion or adapt routing sharpness/budget from static per-token router confidence without any temporal filtering state. | confidence 0.72

### Transactional block commit  (splash 3, feasibility 4, id 35f9b215c9)

**Claim.** transactions-and-rollback applied to the residual stream so that a block's write is either fully committed or fully discarded based on a learned validity check, giving the stream an erase-like veto it lacks by default

**New capability.** the model can detect that a block's proposed update conflicts with or is redundant/harmful given the current stream state and abstain from writing it, something pure addition cannot express

**Placement.** Each block computes its usual delta d_l, then a small learned scalar 'commit gate' c_l in [0,1] (a 2-layer MLP on [residual, d_l]) trained end-to-end via a straight-through estimator; the effective write is c_l * d_l, and c_l can hard-round to 0 at inference (rollback = full discard) checked against a validation KL-divergence budget so aggregate behavior stays close to the always-commit baseline unless discarding strictly helps. Trained from scratch at matched FLOPs/params on paperkiln gpt2-nano preset, gate parameters counted against the parameter budget.

**Invariant.** matched FLOPs/params

**Falsifier.** H: learned commit-gating reduces catastrophic interference on tasks with distractor context (irrelevant earlier tokens that a block would otherwise blend in) versus a matched-budget baseline with no gating Metric: validation perplexity and accuracy on a distractor-insertion synthetic task where correct answer requires ignoring a spurious earlier write Baseline: matched-FLOPs gpt2-nano, same depth/width, gate replaced by an always-1 (always commit) constant Protocol: nano-scale, 12 seeds, 40 GPU-hour budget, measure fraction of gates that saturate near 0 vs 1, report ablation with gate frozen at 1 GPU-hours: 30

**Nearest (generator's guess).** Highway networks / GRU-style gated residual (Srivastava et al. 2015), reframed as commit/rollback

**Scoper.** closest: [1710.04773] Residual Connections Encourage Iterative Inference: Analyzes residual blocks as iterative refinement but does not introduce any learned gate to veto/discard a block's write.; [2412.05768] Uncovering Uncertainty in Transformer Inference: Studies iterative refinement dynamics in the residual stream but proposes no commit/rollback mechanism to conditionally block a write.; [2409.14199] Loop Neural Networks for Parameter Sharing: Uses residual connections for iterative refinement/looping, not a learned scalar gate that can hard-discard a block's entire update. | survives: A learned per-block scalar gate trained with a straight-through estimator that can hard-round to zero at inference (full discard of a block's write, checked against a KL budget) is not implemented by any candidate; all found papers either study residual-stream refinement dynamics passively or use residual streams/connections for unrelated compression, steering, or distillation purposes. | confidence 0.68

### graph-propagated position state  (splash 4, feasibility 2, id ed07b1373e)

**Claim.** graph-message-passing applied to positional-encoding so that each token's position representation is updated by passing messages along an explicit sequence graph (edges to recent tokens, sink tokens, and cache-retained tokens) instead of being a fixed function of its integer index

**New capability.** the model can give two tokens the 'same effective position' when the graph collapses paths between them (e.g. after a deletion/edit in a streaming edit-buffer), something impossible when position is index-only and edits shift every later index

**Placement.** define an explicit DAG over the sequence: each token connects to its immediate predecessor, to sink tokens, and to any tokens marked 'retained' after a KV-cache eviction; run 2 rounds of GNN message passing per layer over this DAG at every generation step to produce a positional feature that is added to RoPE angle before attention; the GNN (2-layer, shared across positions) is trained jointly from scratch at matched budget.

**Invariant.** outputs stay causal; the DAG only contains edges from later to earlier tokens (message passing respects generation order), so no node aggregates information from a future token

**Falsifier.** H: graph-based position recovers coherent continuations after simulated mid-stream edits (insertions/deletions) better than fixed-index RoPE, which must renumber and thus disrupts learned position-content associations Metric: perplexity/accuracy on a constructed edit-continuation task where a chunk is deleted mid-context and generation must continue coherently Baseline: gpt2-nano fixed RoPE re-indexed after edit Protocol: nano from-scratch, 12 seeds, sliding-window lane with synthetic edit insertion, matched FLOPs (GNN cost budgeted against extra attention layers removed) GPU-hours: 38

**Nearest (generator's guess).** graph-based/relative position schemes for edited or tree-structured sequences (e.g. Graphformer, structural position encodings for code); no direct nano precedent for streaming edit-buffers

**Scoper.** closest: [2606.26631] Position Rebinding Cache Reuse: Replay-Free Visual Revisiting for Interleaved Multimodal Reasoning: closest analog of rewriting position assignment for retained cache entries, but it reassigns positions rather than computing them via graph message-passing over an explicit sequence DAG; [2605.05696] Irminsul: MLA-Native Position-Independent Caching for Agentic LLM Serving: addresses position-independence for cache reuse but via closed-form rotation correction, not a learned GNN over a token-connectivity graph; [2110.07875] Graph Neural Networks with Learnable Structural and Positional Representations: uses GNNs to learn positional encodings but for static graph-structured data, not for streaming token sequences with edit/eviction-induced position collapse | survives: None of the retrieved papers define positions via message passing over an explicit sequence-token DAG (predecessor/sink/retained edges) that lets edited/evicted tokens collapse to shared effective positions during autoregressive generation; the nearest work either rewrites/rebinds cached positional coordinates by direct reassignment (PRCR) or corrects RoPE angles in closed form (Irminsul/MiniPIC) rather than computing positions via learned graph propagation, so a GNN-based positional state trained jointly for streaming edit-buffers remains a distinct, unclaimed mechanism-purpose pairing. | confidence 0.62

### test-time position adapter  (splash 3, feasibility 4, id 4f5c2656aa)

**Claim.** test-time-training applied to positional-encoding so that a small set of position-dependent parameters (e.g. RoPE frequency scaling factors or ALiBi slopes) are updated via a self-supervised next-token loss on the incoming long stream itself, adapting the effective position function to the actual lengths encountered at inference.

**New capability.** the model can recalibrate its notion of 'distance' online as it streams past its training length, rather than being frozen into whatever position function was fixed at train time.

**Placement.** expose a handful of scalar parameters (per-head RoPE base frequency, or per-head ALiBi slope) as the only trainable tensors at inference; every k tokens (e.g. every 256), compute a self-supervised loss (next-token cross-entropy on the last window) and take a few gradient steps on just these scalars, holding all other weights frozen; reset or persist state depending on session. Training signal is the stream's own continuation, no external labels.

**Invariant.** base model untouched (only a handful of scalar position parameters are updated, all other weights frozen); outputs remain causal since updates at step t only ever use tokens ≤ t and are applied to future predictions.

**Falsifier.** H: TTT-adapted position scalars reduce perplexity at long context vs frozen positional encoding, matched FLOPs (the TTT gradient steps counted against budget). Metric: perplexity at 2x/4x training length with vs without TTT updates; wall-clock overhead per token Baseline: frozen RoPE/ALiBi at same lengths, and a matched-FLOPs baseline that spends the extra TTT compute on additional forward-pass ensembling instead Protocol: nano-llama preset with RoPE, enable TTT only on frequency/slope scalars, update every 256 tokens, 12 seeds, cap at 40 GPU-hours GPU-hours: 25

**Nearest (generator's guess).** TTT (test-time training) layers of Sun et al.; NTK-aware RoPE scaling adapted online

**Scoper.** closest: [2607.07678] How Data Shapes RoPE Frequency Usage: From Positional Scale Matching to Length Generalization: This uses test-time frequency scaling of RoPE for length generalization but as a static/analytical rescaling derived from data structure, not a gradient-based self-supervised TTT update loop on the live stream.; [2504.18857] Effective Length Extrapolation via Dimension-Wise Positional Embeddings Manipulation: Adjusts RoPE dimension-wise position indices at inference for extrapolation, but via a deterministic detection heuristic, not via gradient descent on a self-supervised next-token loss.; [2309.10400] PoSE: Efficient Context Window Extension of LLMs via Positional Skip-wise Training: Manipulates positional indices to extend context, but this is a train-time fine-tuning scheme, not test-time gradient updates on incoming stream. | survives: Yes: none of these perform actual gradient-based test-time training (online backprop on a self-supervised next-token loss computed on the live stream) restricted to a handful of positional scalar parameters (RoPE frequencies/ALiBi slopes); they use static rescaling rules, heuristic dimension selection, or offline training, so an online TTT loop specifically on positional parameters remains novel. | confidence 0.72

### Kalman-fused positional estimate  (splash 4, feasibility 2, id 763d1f4c36)

**Claim.** Kalman filtering applied to positional-encoding so that the position signal fed to attention is a fused estimate combining the nominal index (a noisy 'prediction' that becomes unreliable past train length) with a measurement derived from content-based cues (e.g. induction-head-style repeat detection or attention-entropy signals), weighted inversely to each source's estimated uncertainty at that distance.

**New capability.** the model can maintain a usable notion of relative position past the training length by trusting content-derived position cues more heavily exactly where the parametric position encoding's uncertainty (learned from training data) is known to be high.

**Placement.** at each layer, alongside RoPE angle theta_nominal(t), compute a content-derived position estimate theta_hat(t) from a lightweight learned function of recent attention patterns (e.g. a small linear probe on attention-entropy/repeat statistics); maintain a scalar uncertainty schedule sigma_nominal(d) fit at train time (from validation loss vs distance) and sigma_measurement fit similarly; fuse via standard Kalman update to get theta_fused, used in place of theta_nominal for rotary application. Trained once at pretraining time (probe + uncertainty schedules), frozen at inference, applied every token.

**Invariant.** matched FLOPs/params (probe is small, added to both baseline and treatment as dead weight for fairness); outputs remain causal since theta_hat(t) only depends on attention statistics up to t.

**Falsifier.** H: Kalman-fused positions yield lower perplexity beyond training length than nominal RoPE alone, matched params, and the fusion weight shifts toward the content estimate as distance grows past train length (verifiable from logged sigma schedules). Metric: perplexity at 1x/2x/4x train length; fraction of Kalman gain attributed to measurement term vs distance Baseline: matched-param nano-llama with plain RoPE, and an ablation with the probe present but gain fixed at 0 (pure nominal) Protocol: nano-llama preset, 12 seeds, log gain/sigma schedules to events.jsonl for falsification, 40 GPU-hour cap GPU-hours: 35

**Nearest (generator's guess).** no direct match; adjacent to ALiBi/xPos decay-based extrapolation and induction-head literature, but explicit Kalman fusion of two position estimates is novel here

**Scoper.** closest: [2506.00920] Position as Probability: Self-Supervised Transformers that Think Past Their Training for Length Extrapolation: Closest in spirit as it maintains a probabilistic/uncertainty-aware position estimate for extrapolation, but uses a histogram filter over implicit positions rather than a Kalman fusion of an explicit nominal RoPE angle with a content-derived measurement weighted by fitted uncertainty schedules.; [2405.14722] DAPE: Data-Adaptive Positional Encoding for Length Extrapolation: Also fuses a content-adaptive signal with a fixed prior positional encoding for length extrapolation, but does not use an explicit Kalman update with per-distance uncertainty schedules distinguishing nominal vs. measurement noise.; [2401.07004] Extending LLMs' Context Window with 100 Samples: Uses attention-entropy signals to modify RoPE for extrapolation, overlapping with the proposal's content-cue signal, but does not fuse two position estimates via a Kalman-style uncertainty-weighted update. | survives: The specific mechanism of an explicit Kalman-style fusion combining a nominal RoPE angle (with a learned, distance-dependent uncertainty schedule from validation loss) and a content-derived positional measurement (from induction-head/attention-entropy probes), fused per-token via inverse-variance weighting, is not implemented in any of these papers, which instead use histogram filters, static/learned biases, or entropy-based rescaling without explicit dual-source uncertainty fusion. | confidence 0.62

### mixture-density multi-horizon head  (splash 3, feasibility 4, id 25bba07001)

**Claim.** Mixture-density heads applied to the output-head so that the model predicts a small mixture of distributions over tokens at offsets {1,2,4} rather than a single point next-token distribution.

**New capability.** The model can report calibrated multi-modal uncertainty about what token will appear several steps ahead, and this multi-horizon mixture can be read off in one forward pass to drive downstream speculative proposals or early-exit decisions, which a plain softmax next-token head cannot produce.

**Placement.** Add K=3 extra small MLP heads on top of the final hidden state, each outputting a mixture of M=4 categorical components (mixture weights + per-component logits over vocab, or over a reduced sub-word cluster vocab to keep FLOPs matched) for offsets 1, 2, and 4 tokens ahead. Trained jointly with the standard next-token loss via an auxiliary mixture negative-log-likelihood term computed against the actual tokens at those future offsets in the training sequence; only training data plumbing changes, no change to attention or base transformer. Runs at every training step; at inference the extra heads are optional side outputs, not on the critical path unless used for speculation.

**Invariant.** outputs remain causal; the auxiliary heads only consume the causal hidden state at position t and predict strictly future tokens t+1, t+2, t+4, never conditioning on them, so nothing emitted depends on a future token.

**Falsifier.** H: Adding multi-horizon mixture-density auxiliary heads improves next-token perplexity and downstream speculative-decoding acceptance rate versus a matched-FLOPs single-head baseline at nano-scale. Metric: next-token validation perplexity; acceptance rate when the offset-2/4 mixture mode is used as a speculative draft Baseline: gpt2-nano with identical parameter count, extra head FLOPs compensated by removing an equivalent number of MLP hidden units in the base model Protocol: nano gpt2 preset on paperkiln, 12 seeds, train to matched token budget (e.g. 200M tokens), report mean+/-std perplexity and speculative acceptance rate, pre-registered thresholds for 'improves' GPU-hours: 30

**Nearest (generator's guess).** multi-token prediction (Gloeckle et al. 2024, 'Better & Faster LLMs via Multi-token Prediction'); MDN heads (Bishop 1994)

**Scoper.** closest: [2608.05454] Hybrid Probabilistic Zonotopes for Identifiable and Refinable Predictive Uncertainty: This is a multi-step probabilistic output head producing calibrated multi-step uncertainty in one forward pass, but it uses zonotope generators rather than mixture-density heads and is not shown applied to next-token LM heads at fixed offsets {1,2,4}.; [2412.10418] Constrained Decoding with Speculative Lookaheads: Related to using future-token proposals to drive speculative decoding, but relies on a separate draft model rather than an auxiliary mixture-density head on the same backbone's output layer.; [2603.24844] Reaching Beyond the Mode: RL for Distributional Reasoning in Language Models: Shares the goal of exposing multi-modal uncertainty in one forward pass, but targets multiple final answers via RL rather than a mixture-density auxiliary head predicting tokens at future offsets. | survives: Yes: no paper places a jointly-trained mixture-density (multi-component categorical) auxiliary head specifically at offsets {1,2,4} on top of the standard next-token LM head to produce calibrated multi-modal, multi-horizon token distributions in a single forward pass for speculative/early-exit use; the closest works either use single-mode multi-token prediction, non-mixture uncertainty representations (zonotopes), or separate draft models instead of auxiliary mixture heads on the same output layer. | confidence 0.68

### fast-weight sampling memory  (splash 3, feasibility 4, id 19dba99fb8)

**Claim.** Hebbian outer-product fast-weights applied to the sampler so that recently sampled tokens transiently bias per-position top-k/temperature decisions for the next few steps, giving the decoder a short adaptive memory instead of one static global knob.

**New capability.** The sampler can locally suppress or boost token probabilities based on very recent sampling history (e.g. avoid immediate repetition or lock onto a recently established register) without any change to the frozen model weights or a hand-tuned repetition penalty, and this bias decays automatically within a few steps by construction of the fast-weight update rule.

**Placement.** Maintain a small fast-weight matrix F (vocab x vocab or embedding x embedding) initialized to zero at sequence start. After sampling token x_t, update F += eta * outer(embed(x_t), embed(x_t)) then decay F *= gamma each step (gamma<1, few-step memory). At each step, add F applied to the current hidden state (or a lookup bias on logits) before top-k/temperature truncation. Only eta, gamma, and the projection matrix (if any) are trained/tuned on a small held-out set; the base model is untouched.

**Invariant.** base model untouched; the fast-weight bias is an additive term computed and applied entirely at decode time on top of frozen logits, with F reset per sequence so it cannot leak information across examples or violate causality (F at step t depends only on tokens 1..t).

**Falsifier.** H: Fast-weight-biased sampling reduces degenerate repetition (measured by repetition rate / self-BLEU) relative to a matched-entropy static top-k/temperature baseline without degrading perplexity beyond a preregistered margin. Metric: repetition rate (n-gram recurrence), self-BLEU, and perplexity delta vs baseline Baseline: fixed top-k=40/temperature=0.8, and separately a standard repetition-penalty baseline Protocol: nano-gpt2, 12 seeds, generations of length 512, matched decode FLOPs (fast-weight update cost budgeted), sweep eta/gamma on 3 seeds held out from evaluation GPU-hours: 15

**Nearest (generator's guess).** repetition penalty / frequency penalty decoding, and fast-weight programmer architectures (Schmidhuber, Ba et al.) applied at train time rather than decode time

**Scoper.** closest: [2503.08057] Odysseus Navigates the Sirens' Song: Dynamic Focus Decoding for Factual and Diverse Open-Ended Text Generation: Nearest match in adjusting sampling behavior adaptively at decode time without retraining, but uses layer-wise distributional signals rather than a Hebbian fast-weight trace of recently sampled tokens.; [2606.23601] Semantic-guided Diverse Decoding for Large Language Model: Also biases sampling using embedding-space history/repulsion signals, but mechanism is explicit repulsion/orthogonality across parallel candidates, not a decaying Hebbian outer-product memory over sequential sampling history.; [2603.18428] Adaptive Decoding via Test-Time Policy Learning for Self-Improving Generation: Shares the goal of adapting sampler parameters per-step without touching base weights, but uses an RL-trained policy rather than a Hebbian fast-weight trace with automatic decay. | survives: None of the candidates implement a literal Hebbian outer-product fast-weight matrix over token embeddings that is written after each sampled token and exponentially decayed to bias the next few steps' logits before top-k/temperature truncation; this specific mechanism-object pairing (fast-weight cache as the sampler's short-term memory, with built-in decay dynamics rather than learned policy or layer-contrast heuristics) remains undemonstrated. | confidence 0.68

### multi-lane consensus prefill  (splash 3, feasibility 4, id 3733172871)

**Claim.** consensus-protocols applied to prefill so that several cheap, differently-approximated prefill lanes (e.g. sliding-window, sink, low-rank) vote on the token-1 distribution and the vote itself is used as the emission signal, with the exact full-attention prefill running as an arbiter only when lanes disagree

**New capability.** the system can emit the first token as soon as a quorum of cheap approximate lanes agree, skipping full exact prefill entirely on the (measurable) fraction of prompts where lanes concur, while retaining the exact pass as a fallback whenever they don't — giving average-case TTFT below the exact-prefill lower bound without ever silently returning a wrong first token.

**Placement.** Run 3-5 cheap prefill lanes in parallel (paperkiln's existing sliding-window and sink attention lanes plus a truncated-rank lane), each producing a token-1 logit distribution; compare pairwise KL/agreement across lanes; if all lanes agree within threshold, emit the majority-vote token and continue generation, deferring exact full-attention prefill to run in the background and only used to verify/replace if a later check (e.g. exact prefill result compared post-hoc) reveals disagreement, triggering a corrective re-emission. No component is retrained beyond calibrating the agreement threshold on a held-out prompt set.

**Invariant.** exactness is recoverable by verification; the exact full-attention prefill always eventually runs and its result is compared against the emitted token, so any consensus error is caught and corrected (base model untouched, lanes are pre-existing attention modes in paperkiln).

**Falsifier.** H: quorum agreement across cheap lanes predicts exact-prefill token-1 output with high precision, giving a TTFT reduction on the agreement fraction of prompts without increasing wrong-first-token rate versus exact baseline Metric: TTFT (agreement-fraction-weighted), quorum precision (P(exact token = vote | agreement)), corrective re-emission rate Baseline: standard exact prefill only, matched FLOPs summed across all lanes vs single exact pass Protocol: paperkiln nano gpt2/llama presets, exact/sliding-window/sink lanes as-is plus one added low-rank lane, 12 seeds, prompts 128-1024 tokens, sweep agreement threshold GPU-hours: 22

**Nearest (generator's guess).** ensemble/self-consistency early-exit and mixture-of-attention-lanes, closest to speculative decoding's draft-verify structure but voting-based rather than single-draft

**Scoper.** closest: [2608.20397] Nexus: Depth-Adaptive KV-Cache Splicing and Retrieval-Decoupled Tool Routing for Agentic LLMs on Unified Memory: Shares the never-silently-wrong verify/fallback structure for TTFT reduction, but via KV splicing/RoPE repair rather than multi-lane voting across differently-approximated attention variants.; [2503.03588] PowerAttention: Exponentially Scaling of Receptive Fields for Effective Sparse Attention: Uses cheap approximate attention lanes (sparse/windowed) at prefill but as the sole mechanism, not as a voting ensemble with an exact arbiter triggered by disagreement.; [2410.08391] KV Prediction for Improved Time to First Token: Similar idea of cheap approximate prefill substituting for exact prefill to cut TTFT, but single-approximator substitution, not multi-lane consensus voting with exact-prefill-as-arbiter-on-disagreement. | survives: The specific mechanism of running several differently-approximated prefill lanes in parallel and using cross-lane agreement/voting itself as the emission trigger (with exact prefill deferred as a post-hoc arbiter only on disagreement) is not described in any candidate; nearest works either guarantee exactness via splice-and-repair (Nexus) or substitute a single cheap approximator (KV Prediction) rather than consensus among multiple lanes. | confidence 0.72

### spectral prefill attention  (splash 4, feasibility 2, id 5adb570da9)

**Claim.** A spectral-transform applied to the prefill sequence so that long-range dependencies in the prompt appear as sparse/local coefficients in frequency space, allowing prefill attention to be computed with a truncated spectral mixer instead of full O(n^2) pairwise attention.

**New capability.** The system can prefill very long prompts by mixing tokens via a truncated global transform (e.g. keep top-r frequency components) at O(n log n) or O(nr) cost, capturing long-range structure that fixed-window/sink lanes miss, while local attention still handles short-range detail.

**Placement.** During prefill only, replace (or augment) a subset of attention layers with a token-mixing block that applies an FFT (or learned orthogonal transform) along the sequence axis per channel, truncates to the top-r magnitude coefficients, applies a small learned filter, and inverse-transforms; trained from scratch alongside the rest of the nano model at matched parameter count by removing an equivalent number of attention FLOPs elsewhere. Sink/local attention layers remain for near-token precision; the spectral layer only replaces long-range mixing, and it operates only over the already-seen prefix so causality is preserved via causal-masked/prefix-only FFT (e.g. using a causal variant like FNet-with-cumulative-transform or restricting to blockwise causal chunks).

**Invariant.** outputs stay causal; the spectral mixer is restricted to attend only over tokens up to and including the current position (via chunked causal FFT or prefix-sum formulation), so nothing depends on future tokens.

**Falsifier.** H: Replacing a fraction of long-range attention layers with a causal spectral mixer matches perplexity on long-range-dependency benchmarks (e.g. long-range copy, induction) at equal FLOPs, while being faster on prompts >4k tokens. Metric: perplexity on long-context nano benchmarks (induction heads, long copy), prefill wall-clock vs length Baseline: matched-FLOPs sliding-window/sink attention baseline in paperkiln Protocol: nano gpt2/llama, prompt lengths 512 to 8k, 12 seeds, from-scratch training at matched param/FLOP budget GPU-hours: 35

**Nearest (generator's guess).** FNet, GFNet, and other spectral token-mixing transformers; Hyena/long-conv architectures

**Scoper.** closest: [2502.00585] Converting Transformers into DGNNs Form: closest conceptually as a spectral/Fourier alternative to self-attention token-mixing, but it is a general architecture replacement not specifically a causal prefill-only truncated spectral mixer with local attention retained for near-token precision.; [2607.19368] Spectral-LSH: Sub-Quadratic Prompt Compression via Krylov-Projected Locality-Sensitive Hashing: targets prefill sub-quadratic cost via spectral/eigenspace approximation, but compresses tokens into macro-tokens via hashing rather than replacing attention with an FFT-based truncated frequency mixer.; [2603.28114] Attention Frequency Modulation: Training-Free Spectral Modulation of Diffusion Cross-Attention: uses Fourier-domain manipulation of attention but for diffusion cross-attention control, not for replacing prefill self-attention token-mixing with a truncated spectral operator. | survives: Yes: no candidate implements a causal, prefill-only FFT/learned-orthogonal-transform token mixer that truncates to top-r frequency coefficients as a direct O(n log n) replacement for a subset of self-attention layers while retaining local/sink attention for near-token detail, trained end-to-end at matched parameter count; the nearest works either do general spectral transformer replacement (not prefill-specific, not causal-truncated) or spectral approximation for token compression/hashing rather than direct spectral token-mixing attention replacement. | confidence 0.68

### hebbian fast-weight depth gate  (splash 3, feasibility 4, id ae20f33a42)

**Claim.** Hebbian fast-weight outer-product updates applied to the layer-schedule controller so that recently-seen token/context patterns transiently lower or raise the depth a token is routed through within a sequence, without any slow-weight (backprop) update.

**New capability.** Within a single streaming sequence, the model can adapt its per-token compute allocation based on very recent within-sequence repetition (e.g. a repeated entity or phrase gets progressively shallower routing as its fast-weight association strengthens) without retraining or altering base parameters — a static layer schedule cannot exhibit this within-sequence adaptivity.

**Placement.** Maintain a small fast-weight matrix A (outer-product accumulator, decayed each step: A <- lambda*A + eta*h_t h_t^T over a low-dim projection of hidden state) updated at every token during the forward pass only, read by a gating scalar g_t = sigmoid(h_t^T A h_t) that determines how many of the top blocks to skip for token t; A is reset at sequence start (streaming-compatible) and never touched by the optimizer. Base model is untouched (graft-trained): only the low-rank projection matrices feeding into A are trained, at matched budget, on top of a frozen pretrained nano-gpt2.

**Invariant.** base model untouched; the fast-weight gate and its small projection matrices are the only trained/adapted component, frozen base weights are never modified, and streaming holds because A is causal and sequence-local (reset per sequence, updated only from past tokens).

**Falsifier.** H: The Hebbian fast-weight depth gate reduces average blocks-executed by >=15% on sequences with repeated spans (e.g. synthetic repeated-entity benchmark) at <=0.5% perplexity increase versus frozen full-depth baseline. Metric: average depth used vs perplexity on repetition-heavy vs non-repetitive held-out sets Baseline: frozen nano-gpt2 full fixed depth, no gate Protocol: nano-gpt2 preset frozen base, 12 seeds, pre-registered decay lambda and learning rate eta grid, 40 GPU-hour budget GPU-hours: 26

**Nearest (generator's guess).** fast weight programmers (Schmidhuber); Hebbian fast weights for attention (Ba et al. 2016); early-exit with recurrent state

**Scoper.** closest: [2404.02258] Mixture-of-Depths: Dynamically allocating compute in transformer-based language models: Same component (layer-level compute routing) and same capability (per-token depth allocation), but the router is a static backprop-trained gate, not a Hebbian fast-weight matrix updated at inference without any slow-weight update.; [2605.05222] Adaptive Computation Depth via Learned Token Routing in Transformers: Same purpose (per-token depth gating) but the gate is a trained differentiable MLP, not a training-free Hebbian outer-product accumulator that adapts within a sequence via repetition.; [2606.24756] Adaptive Hebbian Memory Routing in Vision Transformers for Few-Shot Learning: Uses Hebbian fast-weights for adaptive memory routing, but the Hebbian mechanism gates memory contribution, not layer-depth/block-skipping, and the router itself is a trained MLP rather than the fast-weight matrix directly gating depth. | survives: None of the retrieved papers place a Hebbian outer-product fast-weight accumulator (updated only via decay/Hebbian rule, never touched by the optimizer) directly as the gating signal controlling per-token layer-skip depth within a streaming sequence; the closest works either use backprop-trained MLP gates for depth (MoD, TSA) or Hebbian fast-weights for a different purpose (memory contribution in few-shot learning), so a Hebbian-computed depth gate exhibiting within-sequence adaptation to repeated tokens without any slow-weight update remains unaddressed. | confidence 0.72

### Kalman-gated block re-entry  (splash 4, feasibility 2, id ca627b8ae2)

**Claim.** kalman-filtering applied to the layer-schedule so that a block's re-application is triggered and weighted by fusing the block's own output (measurement) with a running prediction of the token's hidden state (prior), re-applying the block only when the fused uncertainty stays high

**New capability.** the model can decide, per token and per pass, how much to trust a fresh block application versus its prior running estimate, and loop the block only while uncertainty (residual innovation) remains large, giving a principled data-driven halting signal rather than a fixed schedule

**Placement.** at one designated block, maintain a running estimate mu_t and uncertainty sigma_t^2 of the token's hidden state; each pass, treat the block's raw output as a noisy measurement with a learned observation-noise head R(h), compute the Kalman gain K = sigma_t^2/(sigma_t^2+R), update mu_{t+1} = mu_t + K*(measurement-mu_t) and shrink sigma_t^2, and stop re-applying the block when sigma_t^2 falls below a threshold or a max-pass cap is reached; the observation-noise head and threshold are trained jointly with the LM loss, from scratch at matched budget with max-pass capped to match baseline FLOPs.

**Invariant.** outputs stay causal; the filter only fuses the current token's own successive measurements over passes, never reading future tokens, so causality is preserved.

**Falsifier.** H: Kalman-gated re-entry reduces val loss vs matched-FLOPs fixed-depth baseline, with fewer passes spent on low-loss (easy) tokens Metric: val loss, mean passes per token vs per-token baseline loss correlation Baseline: fixed-depth gpt2-nano at matched average FLOPs (max-pass cap tuned to match) Protocol: nano preset, 12 seeds, max-pass cap in {1,2,3}, 40 GPU-hour budget GPU-hours: 30

**Nearest (generator's guess).** Adaptive Computation Time; no direct Kalman-filter-as-halting-signal precedent found in transformer depth literature

**Scoper.** closest: [2605.05222] Adaptive Computation Depth via Learned Token Routing in Transformers: closest prior art on per-token adaptive depth/halting for transformer blocks, but uses a learned MLP gate on residual magnitude, not a Kalman filter fusing measurement and prior with an explicit gain/uncertainty update.; [2607.20519] Adaptive Depth in Looped Transformers: Diagnosing Learned Halting Gates and Trajectory Readouts: same component/capability space (looped block re-application with learned halting) but halting is via exit-distribution/confidence readouts, not Kalman gain/innovation-based uncertainty tracking.; [2312.06937] Can a Transformer Represent a Kalman Filter?: shows transformers can represent Kalman filtering internally, but this is a representational/theoretical result about attention approximating KF dynamics, not a mechanism for gating block re-application via a fused gain-weighted halting signal. | survives: Yes: no candidate paper uses an explicit Kalman gain (fusing a learned observation-noise head with a running variance estimate) as the halting/reapplication signal for a transformer block, which is a mechanistically distinct data fusion rule from confidence-MLP gates or exit-distribution readouts used in the adaptive-depth literature found here. | confidence 0.72

### Delta-rule FFN memory edits  (splash 3, feasibility 4, id 43e1b3d069)

**Claim.** Delta-rule associative writes applied to the FFN so that the per-token key-value memory is erased-and-rewritten online instead of staying fixed after pretraining.

**New capability.** The model can overwrite a specific fact/association stored in an FFN slot mid-stream (e.g. correct a stated error at token t and have token t+50 read the corrected value) without any gradient step or backward pass.

**Placement.** Replace (or augment) one FFN block's up-projection with a fast-weight matrix S_t (same shape as W_up) updated at every token by S_t = S_{t-1} - beta*(S_{t-1}k_t - v_t)k_t^T + write-in-new-value term, where k_t,v_t are the FFN's own hidden pre-activation and target derived from a small trained value head; the down-projection reads from S_t as usual. Trained end-to-end from scratch at nano-scale (gpt2-nano preset) alongside baseline, with beta and the value-head as the only new learned parameters, matched FLOPs by shrinking hidden dim slightly to compensate for the S_t update cost.

**Invariant.** outputs remain causal; S_t is updated only from k_t,v_t at or before position t, and read at position t or later, so nothing depends on future tokens.

**Falsifier.** H: A delta-rule FFN memory recovers an injected mid-sequence fact-edit (measured by next-token loss on the edited fact downstream) significantly better than a matched-FLOPs static-FFN baseline, without harming general LM loss. Metric: post-edit token loss on synthetic key-value recall probe; overall val perplexity delta Baseline: matched-FLOPs static FFN gpt2-nano, same seeds Protocol: 12 seeds, synthetic associative-recall dataset (à la RWKV/DeltaNet recall benchmarks) injected into gpt2-nano training stream, matched param/FLOP budget, pre-registered threshold: >20% loss reduction on edited-fact tokens vs baseline GPU-hours: 30

**Nearest (generator's guess).** DeltaNet / linear-attention fast-weight programmers (Schlag et al., Yang et al. DeltaNet)

**Scoper.** closest: [2301.04213] Does Localization Inform Editing? Surprising Differences in Causality-Based Localization vs. Knowledge Editing in Language Models: Addresses editing facts stored in FFN/MLP weights but via offline weight-editing methods (e.g. ROME-style), not an online per-token delta-rule fast-weight update happening every token during a forward pass.; [2405.16720] Large Scale Knowledge Washing: Also rewrites FFN/MLP weights to erase knowledge, but via gradient-based training-time unlearning, not a gradient-free online fast-weight delta-rule update within inference.; [2503.18216] Adaptive Rank Allocation: Speeding Up Modern Transformers with RaNA Adapters: Modifies FFN linear layers with learned adapters, but for compute efficiency rather than online per-token memory rewriting. | survives: None of the retrieved papers place a DeltaNet-style per-token online erase-and-rewrite fast-weight update inside the FFN's own up-projection to let the FFN act as a continuously overwritable associative memory without gradients; the nearest works either localize/edit facts offline via backprop (ROME/LAW) or use static/learned adapters for efficiency, so the specific mechanism-object-purpose triple (delta-rule fast weight replacing FFN weight matrix for gradient-free mid-stream fact overwrite) remains open. | confidence 0.72

### Graph-message-passing FFN memory  (splash 4, feasibility 2, id 76a6c40498)

**Claim.** Graph message-passing applied to the FFN so that its key-value memory slots are nodes that exchange updates with each other (not just with the token stream) at each step, letting related memory slots co-adapt.

**New capability.** The model can propagate a single new piece of information into multiple related memory slots simultaneously (e.g. updating one fact updates its semantic neighbors), which a per-token, per-slot-independent FFN cannot do by construction.

**Placement.** Treat the FFN's N hidden units (key-value memory slots) as nodes of a fixed sparse graph (top-k cosine-similarity among key vectors, computed once at init or periodically every E tokens); after each token's FFN write (e.g. via the delta-rule update from 43e1b3d069, or a simple additive Hebbian write), run one message-passing round where each slot's value is updated as a weighted average with its graph neighbors' values, gated by a learned mixing coefficient. Trained from scratch at nano-scale; the graph structure and mixing gate are the only new learned components.

**Invariant.** outputs remain causal; message passing only propagates information already written by tokens <= t, and executes before token t+1 is read, so no future leakage.

**Falsifier.** H: Graph-propagated FFN memory generalizes a single injected fact to semantically related probes (zero-shot) better than an equivalent per-slot-independent memory-write baseline at matched FLOPs/params. Metric: accuracy on held-out semantically-related probe set after single fact injection, vs. unrelated control probes (should NOT improve, checking for spurious leakage) Baseline: matched-FLOPs delta-rule FFN without the message-passing round (i.e. same base mechanism, graph step ablated) Protocol: 12 seeds, synthetic knowledge-graph-structured associative recall task (facts + paraphrase/neighbor probes), matched param/FLOP budget via slightly smaller hidden dim to pay for message-passing cost GPU-hours: 32

**Nearest (generator's guess).** Hopfield-network / graph-structured fast weights; Memory networks with relational propagation (e.g. Graph Memory Networks); not standard in transformer FFNs

**Scoper.** closest: [2605.08143] HoReN: Normalized Hopfield Retrieval for Large-Scale Sequential Model Editing: Uses key-value memory entries wrapping an MLP with pattern-completion dynamics, but retrieval-time convergence for editing/retrieval, not graph-structured co-propagation of writes across neighboring slots during normal FFN computation.; [2007.10637] Distributed Associative Memory Network with Memory Refreshing Loss: Distributes memory across blocks updated simultaneously, but blocks are independent (no inter-slot message passing) and purpose is relational reasoning representation, not propagating a single write to semantic neighbors.; [2412.08193] Mixture of Experts Meets Decoupled Message Passing: Towards General and Adaptive Node Classification: Combines message-passing with FFN/expert-like blocks, but for node classification on external graphs, not for propagating in-place writes among a transformer's own FFN memory slots. | survives: Nothing in the set makes FFN hidden units into graph nodes that exchange value updates with each other after each token write so that one write propagates to semantic neighbors; nearest work (HoReN) does pattern-completion retrieval over codebook entries via Hopfield dynamics for editing, not per-token neighbor-to-neighbor message passing among key-value slots during normal forward computation, so the specific mechanism-object pairing remains open. | confidence 0.72

### spectral cross-sequence normalisation  (splash 3, feasibility 4, id 8fc4b86001)

**Claim.** computing normalisation statistics in a frequency basis over a sliding window of token activations applied to normalisation so that low-frequency (long-range/slowly-varying) components are whitened jointly instead of per-token

**New capability.** the model can suppress sequence-level drift (e.g. slow topic or scale shifts across a document) that per-token LayerNorm cannot see because it normalises each token independently

**Placement.** at each block, before the existing LayerNorm, take the last W token activations (W=128-512, causal window) per channel, apply a real DCT along the sequence axis, divide the low-frequency coefficients by their running RMS (causal, computed only from positions <= current), inverse-transform, then apply standard per-token LayerNorm on top. Only the causal-window DCT statistics are new parameters-free machinery; the surrounding network trains from scratch at matched FLOPs/params by allocating the DCT's extra multiply-adds against a slightly smaller FFN.

**Invariant.** outputs remain causal; the DCT window only ever reads positions up to and including the current token, and the transform is recomputed identically at every step so nothing depends on future tokens

**Falsifier.** H: spectral cross-token normalisation reduces validation loss and improves long-range copy/retrieval tasks vs matched-FLOPs LayerNorm baseline Metric: val perplexity and synthetic long-range associative-recall accuracy Baseline: gpt2-nano with standard LayerNorm, same param count (FFN width reduced to match FLOPs) Protocol: nano gpt2 preset, 12 seeds, 40M token budget, sliding-window W in {128,256}, report mean+-std, pre-registered stop at fixed step count GPU-hours: 30

**Nearest (generator's guess).** FNet / spectral mixing layers; RMSNorm variants with windowed statistics

**Scoper.** closest: [2306.16999] Spectral Batch Normalization: Normalization in the Frequency Domain: closest prior work normalizes activations in a frequency domain, but it operates per-sample over channel/spatial dimensions to control feature-map explosion, not causally over a sliding token window to whiten long-range sequence drift for autoregressive LMs.; [2109.08780] Long-Range Modeling of Source Code Files with eWASH: addresses long-range/sequence-level context but via syntax-hierarchy windowing, not spectral normalization statistics.; [1907.04003] Mean Spectral Normalization of Deep Neural Networks for Embedded Automation: uses 'spectral' normalization terminology and addresses a drift phenomenon, but this is spectral norm of weight matrices, unrelated to frequency-transformed token activations or causal windows. | survives: Nothing in this set performs a causal DCT over a sliding token window to jointly whiten low-frequency (long-range/document-level drift) components before per-token LayerNorm in a sequence model; the nearest analog (spectral batch norm) normalizes feature-map spectra per-sample for training stability, not sequence-position drift suppression, so the specific placement/purpose remains unclaimed. | confidence 0.75

### Thompson-sampled learning-rate bandit  (splash 3, feasibility 4, id b07d67e750)

**Claim.** Thompson sampling over a discrete bank of candidate learning rates applied to the lr-schedule so that step-size choice is made by posterior sampling over observed loss-improvement reward rather than a fixed curve

**New capability.** the trainer can allocate more steps to whichever LR value empirically reduces loss fastest at the current phase of training, adaptively reallocating exploration as the loss landscape changes, which a fixed schedule cannot do

**Placement.** define a small fixed arm-set of LR multipliers (e.g. 6-8 log-spaced values relative to the base schedule); maintain a Beta or Normal-Gamma posterior per arm over the reward = negative loss delta over the next window of N steps; every N steps sample from each posterior, pick the argmax arm, apply that LR multiplier for the next window, and update the posterior with the observed reward; only the bandit's tiny posterior state is trained/updated, the base model trains normally.

**Invariant.** matched FLOPs/params; the bandit adds only scalar bookkeeping every N steps, no change to model compute

**Falsifier.** H: Thompson-sampled LR bandit reaches lower loss at fixed token budget than a matched-FLOPs fixed cosine/warmup schedule Metric: final validation loss at fixed budget; also tokens-to-target Baseline: cosine schedule tuned to same total budget, same arm-range as bandit's max/min Protocol: gpt2-nano, 12 seeds, N=50-200 step windows, matched total steps, report arm-selection trace alongside loss curve GPU-hours: 14

**Nearest (generator's guess).** bandit-based hyperparameter schedulers (e.g. Hyperband/PBT-style online LR adaptation; bandit-based learning rate control, e.g. Daniel et al. 2016)

**Scoper.** closest: [1912.06910] Adapting Behaviour for Learning Progress: Uses a bandit to adapt training hyperparameters based on learning-progress reward, but targets RL exploration/data-generation parameters rather than the LR schedule specifically.; [1709.04047] Learning-based Control of Unknown Linear Systems with Thompson Sampling: Applies Thompson sampling to an online control/step-size-like problem but in LQ control, not to neural network training LR schedules.; [2510.14717] Seesaw: Accelerating Training by Balancing Learning Rate and Batch Size Scheduling: Also adapts LR scheduling for training speed but via a deterministic theoretically-derived rule, not bandit posterior sampling over discrete LR arms. | survives: No paper here applies Thompson sampling (or an equivalent posterior-sampling bandit) directly over a discrete bank of LR multipliers during neural network training to adaptively reallocate step-size exploration based on observed loss-improvement reward; the nearest bandit-for-training-hyperparameter work targets RL exploration parameters, and nearest Thompson-sampling work targets control systems, not LR schedules, so the specific placement (LR-schedule arm-selection via TS posterior updates each window) remains open. | confidence 0.72

### Information-bottleneck auxiliary future head  (splash 3, feasibility 4, id 5307f07552)

**Claim.** An information-bottleneck penalty applied to the training objective so that hidden states are compressed toward only what predicts a token ten steps ahead, in addition to next-token loss.

**New capability.** The model can be probed for a representation that explicitly discards information useless for a fixed k-step-ahead target, giving a controllable compression-vs-horizon tradeoff not present in vanilla CE training.

**Placement.** At each layer-L hidden state h_t, add a small auxiliary head predicting token t+k (k=10) trained jointly with next-token CE; add a variational IB penalty (KL of h_t against a learned marginal, or a stochastic bottleneck with reparameterization noise on h_t) weighted by beta, tuned on a held-out set. Trained from scratch alongside the base model at matched FLOPs (auxiliary head adds <1% params). Ablate beta=0 as the matched-FLOPs baseline.

**Invariant.** matched FLOPs/params; the auxiliary head and IB noise are only added at train time on hidden states that already only depend on past tokens, so causality of the emitted distribution is unaffected.

**Falsifier.** H: Adding IB-regularized k-step auxiliary loss improves k-step-ahead prediction accuracy and downstream long-range task performance at fixed next-token perplexity, versus CE-only training. Metric: perplexity at t+1 (held fixed within noise) vs accuracy of held-out k=10 induction/copy task and long-context QA at nano-scale Baseline: matched-FLOPs GPT2-nano trained with CE only, no auxiliary head Protocol: nano-scale (~30M-50M param llama/gpt2 preset), 12 seeds, sweep beta in {0, 1e-4, 1e-3, 1e-2}, matched total FLOPs including auxiliary head cost, report mean+CI GPU-hours: 30

**Nearest (generator's guess).** Deep Variational Information Bottleneck (Alemi et al.) combined with multi-token prediction (Gloeckle et al. 2024 'Better & Faster LLMs via Multi-token Prediction')

**Scoper.** closest: [2310.17284] Learning to Abstract with Nonparametric Variational Information Bottleneck: Applies a VIB-style compression penalty to transformer hidden states, but for layer-wise abstraction/robustness, not for a k-step-ahead auxiliary prediction target.; [2605.23960] Learning Protein Structure-Function Relationships through Knowledge-guided Representation Decomposition: Uses an IB-inspired auxiliary objective on hidden representations for a downstream task, but not tied to a k-steps-ahead LM token prediction head.; [2007.00339] Multi-Task Variational Information Bottleneck: Combines VIB with an auxiliary multi-task head, structurally similar to adding an IB-penalized auxiliary prediction head, but not applied to next-token/future-token LM prediction. | survives: None of the candidates combine a per-layer VIB penalty with a fixed k-step-ahead auxiliary LM head trained jointly with next-token CE at matched FLOPs; the specific placement (future-token auxiliary head + IB bottleneck at hidden states of an autoregressive LM, ablated against a matched-FLOPs beta=0 baseline) is not addressed by any paper here, which either apply VIB to non-LM domains or to LM layers without a future-token prediction target. | confidence 0.72
