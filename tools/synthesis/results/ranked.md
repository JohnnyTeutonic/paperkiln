# Synthesis search: ranked candidates

357 candidates scored; verdicts: ADJACENT 197, OPEN 65, TAKEN 95

Rank = novelty (OPEN 30 / ADJACENT 10 / TAKEN 0) + 2*splash + feasibility. Novelty is the scoper's verdict against arXiv; read the closest paper before believing any OPEN.

## Controls
- ctl-hebbffn expected TAKEN got ADJACENT: **PASS**; closest 2102.11174; 2212.02475; 2608.27763
- ctl-choquet expected ADJACENT got ADJACENT: **PASS**; closest 2607.26164; 2510.13079; 2606.09886
- ctl-kvdiff expected TAKEN got TAKEN: **PASS**; closest 2505.16950; 2601.02076; 2510.14973
- ctl-pid expected TAKEN got TAKEN: **PASS**; closest 2408.15664; 2512.03915; 2502.15451

## Ranked

| # | verdict | splash | feas | name | claim | closest | survives |
|---|---|---|---|---|---|---|---|
| 1 | OPEN | 4 | 4 | atomic commit fast-weight edits | transactions-and-rollback applied to weights-at-inference so that a batch of proposed within-sequence weight edits is validated against a held-out probe before being committed, with automatic rollback to the frozen base weights if the probe check fails | 2605.28053; 2512.23675 | Yes: none of these papers implement a staged ΔW buffer that is accept/reject-gated by comparing frozen-vs-staged loss on a held-out probe window before committing into the live forward pass, which is the proposal's specific transactional guarantee against a frozen baseline; nearest work either handles multi-tenant serving correctness (RW-TTT) or unconditional TTT without any validation/rollback gate (TTT-E2E), or transactional rollback for non-weight agent state (DeltaBox). |
| 2 | OPEN | 4 | 3 | Barrier-constrained expert routing | Control-barrier-functions applied to the moe-router so that gate logits are projected at inference onto the nearest point of a safety set (e.g. minimum per-expert utilization over a sliding window, or maximum logit gap bound) whenever the raw top-k choice would leave that set, guaranteeing the constraint holds every token without retraining. | 2504.03038; 2509.23830 | Nothing in the set applies control-barrier-function-style constrained projection of router logits at inference to guarantee per-expert utilization/load bounds within a sliding window without retraining; existing MoE load-balancing work uses training-time losses or capacity heuristics rather than a certified per-token QP projection onto a safety set. |
| 3 | OPEN | 4 | 3 | TTT gate on residual | test-time-training applied to the residual stream so that a block can attenuate or overwrite directions written by earlier blocks rather than only add to them | 2510.13876; 2409.19606 | Yes: no candidate uses inner-loop gradient descent on a self-supervised reconstruction loss to produce a fast-weight gate that attenuates/inverts specific earlier blocks' residual contributions conditioned on context up to position t; existing gating/hyper-connection work uses static learned parameters rather than test-time training. |
| 4 | OPEN | 3 | 4 | Kalman-fused router logits | Kalman filtering applied to the moe-router so that expert selection fuses a prior gate score with a noisy downstream-error measurement from the previous token's routing outcome, in proportion to their estimated uncertainties. | 2509.23830; 2605.11800 | The specific mechanism—maintaining an explicit per-expert (mean, variance) Kalman state that computes a gain from estimated prediction vs. measurement noise to blend the raw gate logit with a causal downstream-usefulness proxy from the previous token—is not implemented by any candidate; the nearest works either impose static/Bayesian distributions over routing or recalibrate logits without an online uncertainty-weighted fusion rule. |
| 5 | OPEN | 3 | 4 | Transactional block commit | transactions-and-rollback applied to the residual stream so that a block's write is either fully committed or fully discarded based on a learned validity check, giving the stream an erase-like veto it lacks by default | 2409.19606; 1909.12117 | Yes: none of the candidates implement a per-block scalar commit gate trained end-to-end (straight-through) that can hard round to zero at inference to fully discard a conflicting/redundant update while bounding aggregate KL drift from an always-commit baseline; the Hyper-Connections family reweights/mixes continuous streams rather than learning a discrete abstain-from-writing veto conditioned on write-write conflict detection. |
| 6 | OPEN | 4 | 2 | graph-propagated position state | graph-message-passing applied to positional-encoding so that each token's position representation is updated by passing messages along an explicit sequence graph (edges to recent tokens, sink tokens, and cache-retained tokens) instead of being a fixed function of its integer index | 2401.17791; 2305.17589 | None of the papers apply message-passing PE to an explicit dynamically-edited sequence DAG (predecessor + sink + cache-retained edges) specifically to let post-deletion tokens share collapsed effective positions in a streaming edit-buffer/KV-cache setting; this specific mechanism-purpose pairing for streaming edits appears absent. |
| 7 | OPEN | 3 | 4 | test-time position adapter | test-time-training applied to positional-encoding so that a small set of position-dependent parameters (e.g. RoPE frequency scaling factors or ALiBi slopes) are updated via a self-supervised next-token loss on the incoming long stream itself, adapting the effective position function to the actual lengths encountered at inference. | 2606.21803; 2512.12167 | Yes: no paper in the set actually performs online gradient updates on RoPE/ALiBi scalar parameters using a self-supervised next-token loss computed on the live inference stream itself (i.e., true test-time-training restricted to positional parameters, updated every k tokens during a session), as opposed to fixed/learned-at-train-time scaling functions or fast-weight cache rewrites elsewhere in the architecture. |
| 8 | OPEN | 4 | 2 | Kalman-fused positional estimate | Kalman filtering applied to positional-encoding so that the position signal fed to attention is a fused estimate combining the nominal index (a noisy 'prediction' that becomes unreliable past train length) with a measurement derived from content-based cues (e.g. induction-head-style repeat detection or attention-entropy signals), weighted inversely to each source's estimated uncertainty at that distance. | 2505.22842; 2506.00920 | The specific mechanism of explicit two-source Kalman fusion (nominal RoPE angle as noisy prediction + content-derived induction/attention-entropy measurement, combined via inverse-uncertainty weighting fit from validation loss vs distance) is not directly instantiated in any candidate; the closest works either use a fixed probabilistic prior (BAM) or a self-supervised histogram filter (PRISM) rather than fusing two distinct estimator sources with a Kalman-style gain, so a proposal specifically demonstrating this dual-source fusion with learned uncertainty schedules would still be new. |
| 9 | OPEN | 3 | 4 | fast-weight sampling memory | Hebbian outer-product fast-weights applied to the sampler so that recently sampled tokens transiently bias per-position top-k/temperature decisions for the next few steps, giving the decoder a short adaptive memory instead of one static global knob. | 2307.03214; 2604.00018 | The specific mechanism of an outer-product Hebbian fast-weight matrix over sampled-token embeddings, updated and exponentially decayed step-by-step to bias top-k/temperature at the sampler with automatic few-step forgetting, is not implemented by any candidate; nearest works use static prompt contrast, entropy-triggered branching, or heuristic temperature schedules rather than a self-decaying associative memory keyed on recent sampling history. |
| 10 | OPEN | 3 | 4 | multi-lane consensus prefill | consensus-protocols applied to prefill so that several cheap, differently-approximated prefill lanes (e.g. sliding-window, sink, low-rank) vote on the token-1 distribution and the vote itself is used as the emission signal, with the exact full-attention prefill running as an arbiter only when lanes disagree | 2503.18076; 2503.05330 | No paper places multiple cheap differently-approximated prefill lanes (sliding-window, sink, low-rank) in parallel, uses cross-lane agreement/KL as a quorum gate to emit token-1 without ever running exact full-attention prefill on the agreeing fraction of prompts, and falls back to exact prefill only as a post-hoc arbiter/corrective mechanism — this specific consensus-gated skip-or-fallback structure at the prefill component for TTFT reduction is not covered by the nearest classification-consensus, multi-sample-consensus, or token-pruning-prefill papers. |
| 11 | OPEN | 3 | 4 | hebbian fast-weight depth gate | Hebbian fast-weight outer-product updates applied to the layer-schedule controller so that recently-seen token/context patterns transiently lower or raise the depth a token is routed through within a sequence, without any slow-weight (backprop) update. | 2503.23798; 2602.01219 | No candidate combines a genuinely non-backprop, per-sequence-reset Hebbian outer-product accumulator (A <- lambda*A + eta*h h^T) as the actual gating signal for layer-skip depth; all depth-adaptive/layer-skip works here use slow-weight trained routers/gates (even when noting repetition lowers required depth empirically), so the specific mechanism-purpose pairing (fast-weight Hebbian trace driving a depth gate, reset per sequence, no optimizer touch) is not demonstrated. |
| 12 | OPEN | 4 | 2 | Kalman-gated block re-entry | kalman-filtering applied to the layer-schedule so that a block's re-application is triggered and weighted by fusing the block's own output (measurement) with a running prediction of the token's hidden state (prior), re-applying the block only when the fused uncertainty stays high | 2603.01914; 2605.23872 | The specific mechanism—explicit Bayesian fusion (Kalman gain) of the block's raw output as a noisy measurement against a running mean/variance prior, with a learned observation-noise head and innovation-based halting threshold—is not instantiated in any candidate; all found methods use scalar confidence/halting gates or ODE-style damping rather than a two-moment (mu, sigma^2) recursive estimator, so this remains a live, non-duplicated idea. |
| 13 | OPEN | 4 | 2 | Graph-message-passing FFN memory | Graph message-passing applied to the FFN so that its key-value memory slots are nodes that exchange updates with each other (not just with the token stream) at each step, letting related memory slots co-adapt. | 2605.08143; 2412.08193 | Nothing in the set makes FFN hidden units into graph nodes that exchange value updates with each other after each token write; the nearest work (HoReN) only does content-addressed retrieval/denoising over a codebook for offline sequential edits, not per-token online message-passing between memory slots during normal forward passes, so the proposed simultaneous propagation to semantic neighbors during training-time inference remains untested. |
| 14 | OPEN | 3 | 4 | spectral cross-sequence normalisation | computing normalisation statistics in a frequency basis over a sliding window of token activations applied to normalisation so that low-frequency (long-range/slowly-varying) components are whitened jointly instead of per-token | 2504.11513; 2406.19770 | Nothing in this set implements a causal windowed-DCT whitening of low-frequency coefficients inserted before LayerNorm at each transformer block to suppress sequence-level drift that per-token normalization cannot see; the nearest analog (FLN) is a domain-adaptation frequency-preserving normalization for fault diagnosis, not a causal in-place spectral rewrite of a sliding activation window for language/sequence models. |
| 15 | OPEN | 3 | 3 | Mixture-density attention abstention | A mixture-density-head applied to attention-scores so that each query predicts a small mixture of candidate attention distributions (including a degenerate 'attend-to-nothing' component) instead of one fixed softmax, letting the model represent abstention as one mixture mode with nonzero learned weight. | 2502.00281; 2606.18283 | None of the papers implement a per-query mixture of several full softmax-over-keys distributions (K>=2) with one component pinned to pure null/abstain and a router predicting mixture weights trained jointly with the LM loss; the closest works either use mixture-of-experts as a theoretical attention-analysis tool, replace attention with Gaussian-mixture routing for efficiency, or add a single scalar gate for sink suppression, so the proposed multi-modal discrete mixture with an explicit abstain mode as one of several attention distributions appears genuinely novel. |
| 16 | OPEN | 3 | 3 | Multiresolution residual channels | Wavelet-multiresolution decomposition applied to the residual stream so that each token's representation is carried at several explicit bandwidths simultaneously, letting cheap tokens use only coarse channels while hard tokens engage fine channels. | 2606.18246; 2510.13876 | The specific mechanism of a fixed wavelet/dyadic multiresolution decomposition of the residual channel dimension, with per-band causal gating so tokens selectively read/write coarse vs fine bands, is not implemented by any candidate; nearest works vary depth-wise width or gate whole-layer/whole-update magnitude rather than partitioning and selectively activating explicit frequency bands within the residual width. |
| 17 | OPEN | 3 | 3 | renormalized multiscale position code | a renormalization-group coarse-graining procedure applied to positional-encoding so that position is represented as a hierarchy of scales where only the frequency components that survive block-averaging at each scale are kept, letting extrapolation reuse low-frequency (coarse-scale) structure unseen-length-independent | 2607.07678; 2302.08647 | The explicit dyadic RG coarse-graining procedure with a trained scale-to-scale renormalization map and length-dependent scale-weighting for graceful fallback to coarse scales is not implemented in any candidate; nearest work either analyzes frequency usage post hoc or builds multiresolution PE for a different domain/purpose. |
| 18 | OPEN | 3 | 3 | Hebbian fast-weight position refresh | Hebbian fast-weight outer-product updates applied to positional-encoding so that a short-lived, rapidly-decaying associative memory binds recent absolute positions to content, letting the model 'relearn' local position-content correlations online at any length rather than relying solely on the frozen, out-of-range static encoding. | 2608.27763; 2503.03046-DAPE | None of the retrieved papers place a decaying Hebbian outer-product trace specifically between content vectors and bounded relative-position tags, added as a low-rank bias to the value projection and reset per sequence, purely to refresh position-content locality beyond trained length; the nearest works either adapt positional bias via learned networks (DAPE) or use fast-weights for general content/state memory (Fast Weight Attention, REFINE), so a mechanism whose sole state is a position-tag outer product for locality restoration remains open. |
| 19 | OPEN | 3 | 3 | PID-controlled dynamic retokenisation | Feedback control applied to the tokeniser's merge table so that vocabulary composition tracks live model prediction error instead of being frozen from pre-training frequency stats. | 2603.03583; 2506.18831 | Nothing in the set closes a PID (proportional/integral/derivative) feedback loop specifically over BPE merge-candidate promotion/demotion keyed to live per-merge cross-entropy error during LM pretraining; that specific mechanism-object pairing (control-theoretic error signal steering the tokenizer's active merge table mid-training) remains undemonstrated. |
| 20 | OPEN | 3 | 3 | Mixture-density boundary prediction | Mixture-density heads applied to the tokeniser's segmentation decision so that token boundaries are drawn from a learned multimodal distribution over segmentation points instead of committed once via greedy frequency merges. | 2303.08888; 2603.03583 | No paper places a mixture-density head directly on the tokenizer's boundary decision to yield a sampleable, entropy-scored multimodal segmentation distribution jointly trained with the LM; the nearest works either learn adaptive/tokenizer-free segmentation without multimodal distributional heads (ByteFlow) or apply mixture/diffusion-based multimodal prediction to image segmentation, not text tokenization, so this specific combination remains open. |
| 21 | OPEN | 3 | 3 | predictive-coding residual head | Predictive coding applied to the output-head so that upper layers pass down only the prediction error against the current top-down next-token guess, rather than the layers being trained purely bottom-up to a single softmax target. | 2410.18160; 2601.15286 | Nothing in the set implements a predictive-coding-style iterative top-down/bottom-up error correction loop inside the final block that refines the same-position next-token logits via explicit error units before the loss; the nearest works either predict multiple future tokens with separate heads/decoders in one pass, or perform iterative refinement using external critics/VLMs at a different granularity, so a genuine within-forward-pass error-unit correction mechanism at the output head remains unclaimed. |
| 22 | OPEN | 3 | 3 | chunked atomic prefill commit | transactions-and-rollback applied to prefill so that the prompt is processed in committed chunks, each of which speculatively unlocks token emission for a prefix boundary before the whole prompt is consumed, with rollback if a later chunk changes the meaning of already-committed KV entries | 2603.13358; 2510.08055 | The core untaken mechanism is speculative unlocking of decode after only a prefix chunk of the prompt, paired with an explicit rollback-and-regenerate protocol triggered by a trained divergence gate when later chunks retroactively invalidate the shallow encoding; none of the retrieved systems commit to emitting tokens before the full prompt is seen and then visibly roll back generation. |
| 23 | OPEN | 3 | 3 | retrieval-mixed normalisation statistics | computing per-token normalisation statistics as a mixture of the token's own stats and the stats of its k nearest neighbours retrieved from a cached activation store applied to normalisation so that normalisation is informed by cross-sequence and cross-batch structure instead of being purely local to one token | 2606.06363; 2512.20420 | Nothing found computes LayerNorm mean/variance as a learned mixture of the token's own statistics and those of cached kNN-retrieved past activations; this specific placement (retrieval-conditioned normalization statistics rather than retrieval-conditioned features/outputs/rewards) is untouched by the candidates. |
| 24 | OPEN | 3 | 3 | MDL-selected normalisation window | choosing, per layer and online, the normalisation aggregation window (single token vs. causal running window of length L) that minimises a two-part code length (bits to describe the chosen L plus bits of the residual after normalisation) applied to normalisation so that the model adaptively normalises over the sequence extent that actually compresses activations best, instead of always at the single-token granularity | 2603.25025; 1902.10658 | A genuinely new contribution would be the specific placement: using an MDL two-part code (bits for L plus residual entropy) to select among discrete causal normalization window lengths per layer online, since no candidate paper combines MDL model selection with the normalization-window/aggregation-granularity choice at a neural network layer. |
| 25 | OPEN | 3 | 3 | Energy-refined example replay | Energy-based refinement applied to data-order so that examples are not merely resampled by a fixed schedule but are iteratively refined (via gradient or MCMC steps on an auxiliary energy score) into harder or more informative variants before being re-shown to the model | 1511.05952; 1905.11475 | Yes: no candidate combines a loss-indexed replay buffer with inference-only, model-own-loss-as-energy Langevin/gradient refinement in embedding space to produce harder near-miss variants re-injected into training batches as a curriculum signal; this specific placement (data-order component, energy-refinement mechanism, replay-buffer object) is not covered by prioritization, adversarial-training, or curriculum-learning papers found here. |
| 26 | OPEN | 3 | 3 | Generational age-partitioned replay | Generational-garbage-collection applied to data-order so that recently-seen examples are revisited frequently from a 'young generation' buffer while older examples are promoted to a rarely-swept 'old generation' buffer, rather than being seen exactly once uniformly. | 2202.01838; 1511.05952 | The specific mechanism of a two-tier (young/old) ring-buffer with GC-style age-based promotion and differential sweep probabilities, applied to LM pretraining data order under a matched token budget, is not described by any candidate; the nearest work either studies static permutation orderings or importance/TD-error-based replay prioritization in RL, not a generational-GC-style tiered buffer for pretraining data order. |
| 27 | OPEN | 3 | 3 | graph-propagated fast-weight patch | graph-message-passing applied to weights-at-inference so that a per-sequence parameter patch is computed by propagating token states over a small induced graph before being written into a low-rank slice of the FFN weights | 2512.23675; 2504.10646 | The specific mechanism—building a per-window token-similarity graph, running message passing, and aggregating into a rank-r outer-product ΔW injected into an FFN weight matrix for the rest of the sequence—does not appear in the retrieved set; nearest works either do gradient-based weight compression (TTT) or graph message-passing for unrelated purposes (anomaly detection, weight-space reasoning), so the proposed graph-to-fast-weight-patch pathway remains open. |
| 28 | OPEN | 3 | 3 | Speculative gradient rollback | Speculate-and-verify applied to the gradient so that a cheap proxy step is taken immediately and only retroactively verified against the expensive exact gradient, rolling back if it would have increased loss. | 2510.05421; 2209.00361 | The proposal's core novelty—applying weights optimistically from a cheap/stale gradient every step and then verifying against the exact gradient every k steps with an explicit rollback-and-replay to recover exact-gradient guarantees—is not present in any candidate; nearest works either refresh estimators periodically without rollback or verify tokens/rewards, not weight updates, so this remains open. |
| 29 | OPEN | 3 | 3 | Loss-barrier constrained update filter | control-barrier-functions applied to the gradient step so that the update is projected onto the set of directions that keep a safety margin on validation loss (and gradient-norm blowup) never violated | 2503.10384; 2204.01930 | Yes: no paper in the set applies a discrete-time CBF-QP projection directly to the raw gradient/parameter update in neural network training to hold a validation-probe-loss (or gradient-norm) safety margin invariant step-by-step; the closest (2503.10384) uses a relaxed barrier penalty term added to the SGD objective rather than a CBF-style nearest-feasible-update projection with a formal invariance condition h(theta_next) >= (1-alpha) h(theta). |
| 30 | OPEN | 3 | 3 | Hopfield recall over training trajectory | hopfield-retrieval applied to checkpoints so that any past weight configuration along the training trajectory becomes content-addressable and can be recalled by energy descent from a partial or noisy cue instead of only being resumable from an exact stored file | 2210.11803; 2206.05314 | Nothing in the set stores checkpoint weight sketches as Hopfield memory patterns and retrieves the nearest checkpoint by energy descent from a probe state for content-based (index-free) rollback; the nearest work instead selects/averages checkpoints by index or validation score, so the specific content-addressable retrieval-for-rollback mechanism remains open. |
| 31 | OPEN | 3 | 3 | prioritized replay of checkpoint states | experience-replay applied to checkpoints so that past weight states (not just past data) are revisited on a prioritized schedule during training, instead of being discarded once superseded | 2306.03241; 2507.07451 | Nothing found treats stored past weight checkpoints themselves (rather than data, trajectories, or averaging) as a prioritized-replay buffer whose sampled states are turned back into fresh gradients mixed into the current update; that specific mechanism-object pairing (priority-scored checkpoints -> replayed gradient injection) remains undemonstrated. |
| 32 | OPEN | 3 | 2 | learned-index KV lookup | learned-index applied to kv-cache so that a small predictive model replaces explicit per-token key storage for entries the model can regenerate positionally, cutting cost for predictable entries while keeping exact storage for the rest | 2604.15356; 2502.04077 | Yes: no candidate trains a per-layer positional/n-gram predictor whose output replaces the stored key/value itself (with a pointer and threshold-gated residual correction) analogous to a learned-index structure; the nearest paper (PLT-based predictive delta coding) predicts KV residuals from the model's own sequence prediction rather than a tiny auxiliary MLP keyed on position+local n-gram context with explicit store-nothing-else pointer semantics, so a concrete implementation of position-indexed KV lookup with learned-index-style sparse residual storage is not shown to exist. |
| 33 | OPEN | 2 | 4 | Barrier-constrained attention mass | A control-barrier-function constraint applied to attention-scores so that the total mass assigned to any single value vector is dynamically capped to keep a defined 'safety set' (e.g. bounded influence of any one token on the residual stream) from ever being exited, allowing the softmax to abstain by pushing the remaining, unconstrained mass onto an explicit null output. | 2601.15305; 2608.21174 | No paper applies a control-barrier-function-style per-forward-pass certificate directly to softmax attention scores to bound any single value's influence while routing freed mass to an explicit null output; this specific per-query CBF projection with formal safety-set guarantee and null-slot routing appears novel. |
| 34 | OPEN | 3 | 2 | Consensus-voted attention abstention | A consensus-protocol applied to attention-scores so that multiple independent 'voter' sub-heads must agree a key is relevant before it receives nonzero mass, and if no quorum is reached across voters the query's output defaults to a null/abstain vector rather than a forced mixture. | 2410.02703; 2601.16397 | Nothing in the set implements a multi-voter quorum gate on attention scores that forces a null/abstain output per query when independent sub-head votes disagree, so the specific mechanism (thresholded voting sub-projections + quorum-based abstention vector) remains open. |
| 35 | OPEN | 3 | 2 | atomic expert-group commit | Transactions-and-rollback applied to the moe-router so that a token's routing decision across a block of consecutive MoE layers is grouped and only committed to the residual stream if a post-hoc consistency check across the whole group passes, otherwise the group falls back to a dense (all-expert-averaged) path. | 2609.05228; 2608.11699 | Nothing in this set groups several consecutive MoE layers into a speculative transaction with a post-hoc cross-layer consistency check that triggers rollback to a dense/averaged path; existing work does per-token/per-expert skipping or single-layer uncertainty gating, not multi-layer speculative commit-or-fallback, so the proposal's cross-layer atomicity guarantee is still open. |
| 36 | OPEN | 2 | 4 | PID-controlled expert gate | PID feedback control applied to the moe-router so that the gate logit for each expert is corrected online by proportional, integral, and derivative terms computed from a causal running error signal (e.g. expert load deviation or realized loss residual), not just the raw linear score. | 2510.14853; 2509.23830 | A demonstration that a causal, per-token running PID correction (with explicit integral/derivative terms on a load or loss-residual error) added directly to router logits before top-k outperforms both static auxiliary load-balancing losses and the periodic/offline logit-adaptation schemes found here, none of which implement a continuously updated closed-loop controller at every token. |
| 37 | OPEN | 3 | 2 | ARC-policy residual-channel eviction | Adaptive-replacement-cache policy applied to residual-stream channels so that each token allocates its fixed write-budget only to the subset of channels currently balancing recency and frequency of use, instead of writing uniformly to all channels. | 2510.13876; 2606.16112 | The specific combination of ARC's two-list (recency vs frequency) admit/evict balance implemented as a per-token, per-channel router over residual-stream slots (not KV cache entries, not whole-block skipping) giving genuinely variable effective residual width per token is not directly instantiated by any of these; a paper doing exactly this per-channel ARC transplant would still be new. |
| 38 | OPEN | 2 | 4 | PID-controlled merge rate | Feedback-control applied to the tokeniser's merge-rate parameter so that a PID loop drives the measured per-window compression ratio (or loss) to a target setpoint, continuously retuning granularity as local text difficulty changes. | 2605.30080; 2506.01084 | Yes: no paper closes a PID feedback loop specifically on a tokenizer's merge-threshold parameter using rolling per-byte loss as the error signal to hold compression ratio at a fixed setpoint; the closest matches either use PID for a different component (activation steering) or adjust chunking/compression via curriculum schedules or compression heuristics rather than explicit closed-loop control. |
| 39 | OPEN | 3 | 2 | MDL-selected merge depth | A minimum-description-length criterion that scores candidate segmentations by total code length (model bits + data bits) applied to the tokeniser so that merge depth is chosen per-span to minimize total code length rather than by fixed frequency rank. | 2506.01084; 2411.18553 | The specific mechanism of scoring per-span segmentation choice by explicit model-bits-plus-data-bits MDL code length across multiple precomputed granularities (byte/BPE-4k/BPE-32k) sharing one LM, used both to build a curriculum and to select online at inference, is not directly instantiated by any candidate. |
| 40 | OPEN | 3 | 2 | Contrastive merge-embedding co-training | A contrastive objective applied to candidate subword merges during vocabulary construction so that merge selection is driven by which spans the model can predict well jointly with their contexts, rather than by raw co-occurrence frequency. | 2506.18639; 1910.13267 | None of the candidates use a contrastive (pull-toward-true-continuation, push-from-sampled-negative-continuations) objective specifically to re-rank/select BPE merge candidates in an iterative merge-then-score loop before freezing a vocabulary; ByteSpan and ByteFlow use predictability/compression signals, not contrastive separability, so the specific contrastive-merge-scoring mechanism at this component appears open. |
| 41 | OPEN | 2 | 4 | arithmetic-coded reversible sampler | arithmetic-coding applied to the sampler so that committed tokens can be losslessly re-expressed as a shorter code and re-expanded under a revised model without changing the emitted token distribution | 2604.06055; 2511.04684 | None of the retrieved papers place an arithmetic-coding bijection at the LM sampler step to store per-token code-stream receipts that let a different (recalibrated) model deterministically re-derive alternate token choices from the same bits; this specific bit-exact-replay/counterfactual-resampling capability at the sampler is not addressed by any candidate, so the twist of exact CDF-to-interval bijection enabling off-line re-ranking under a different top-p/temperature remains open. |
| 42 | OPEN | 2 | 4 | Prioritized replay for sampling controller | Prioritized experience replay over a buffer of (hidden-state, realized-surprisal) pairs applied to the sampler's temperature/top-k controller so that per-position decisions are trained on the sequence positions where past predictions were most miscalibrated. | 1511.05952; 2309.02772 | The specific mechanism of maintaining a ring buffer of (hidden-state, temperature, realized-surprisal) triples and training a small online controller MLP via TD-error-prioritized replay sampling (rather than a heuristic or fixed schedule) does not appear in any candidate paper, so this exact combination remains open. |
| 43 | OPEN | 3 | 2 | SMC-staged incremental prefill | Sequential Monte Carlo applied to prefill so that a population of partial-cache hypotheses at increasing prompt-prefixes is weighted and resampled, letting the first token be emitted from the highest-weight partial-prefill particle before the full prompt is exactly encoded. | 2502.02789; 2410.08391 | None of the candidates maintain a weighted, resampled population of partial-prefill KV-cache hypotheses (SMC) that lets a token be emitted before exact full-prompt encoding and then collapses to the exact cache; the nearest work either prunes/approximates the cache once (SpecPrefill, KV Prediction) or streams context without particle-based resampling (Stream2LLM), so the SMC-specific mechanism at prefill is unaddressed. |
| 44 | OPEN | 2 | 4 | PID-controlled early-exit depth | A PID controller applied to the layer-schedule so that each token's exit layer is set by driving a measured per-token confidence-error to zero rather than by a fixed or single-threshold rule. | 2510.14211; 2604.14612 | None of the retrieved early-exit or layer-skipping papers implement an explicit PID (proportional-integral-derivative) feedback loop with accumulated error and derivative terms driving a per-token depth bias to correct oscillation/overshoot; they use static/adaptive thresholds or learned gates instead, so a genuine closed-loop PID controller on the confidence error remains a distinct mechanism. |
| 45 | OPEN | 2 | 4 | EMA-trajectory layer folding | ema-averaging applied to the layer-schedule so that a block's output is replaced by an exponential moving average of its own repeated-application trajectory, giving a cheap proxy for depth-adaptive refinement without an explicit halting decision | 2605.23872; 2604.08556 | Yes: none of the found papers average the T repeated-application intermediate states of a single block via an EMA and feed that average (not the final iterate) forward as the block's output at both train and inference time; the closest looped-transformer work either blends via damped ODE steps or uses learned/confidence-based halting rather than a fixed-decay EMA over the unrolled trajectory. |
| 46 | OPEN | 2 | 4 | Graph-propagated optimiser moments | Graph-message-passing applied to optimiser-state so that per-parameter moment estimates are updated using averaged/gated signals from structurally adjacent parameters (same layer, same head, residual-stream neighbors) instead of being estimated in isolation. | 2405.18144; 2509.03378 | Nothing in the set actually message-passes optimizer moments across a learned/static parameter-adjacency graph (same-head, same-layer, residual neighbors) at each step; the nearest works (Shampoo/SOAP/KFAC) share statistics only within a single parameter tensor's own dimensions via Kronecker factorization, not across distinct tensors via graph propagation, so the specific mechanism-placement pairing remains open. |
| 47 | OPEN | 2 | 4 | quantised curvature codebook | Product-quantisation applied to optimiser-state so that the final per-parameter second-moment vectors are compressed into a small codebook that can be shipped and reloaded to warm-start new training runs, instead of being thrown away at checkpoint time. | 2606.13894; 2406.11257 | None of the papers build a reusable, retrievable PQ codebook of final second-moment statistics specifically intended to warm-start optimizer state (v) in a *new, separate* training run (fine-tune or post-prune restart) rather than compressing state within/for the same run's memory or checkpoint size; that specific artifact-and-reuse-across-runs mechanism appears genuinely open. |
| 48 | OPEN | 3 | 2 | Hopfield-recalled curvature memory | Hopfield content-addressed retrieval applied to optimiser-state so that a bank of stored final second-moment patterns from prior training runs can be recalled by energy descent, keyed on the current model's early-training moment pattern, to warm-start optimiser state on a new but related run. | 2607.20769; 2410.17413 | Nothing in the set applies Hopfield/modern-associative-memory energy-descent retrieval specifically to recall and blend Adam's second-moment (curvature) state across distinct training runs keyed on early partial moment patterns; the nearest work either does generic offline-memory warm-starts for convex optimization or retrieves for attribution, not curvature initialization via Hopfield dynamics. |
| 49 | OPEN | 2 | 4 | Conformal gradient-step gate | Conformal-prediction applied to the gradient so that each proposed optimizer step carries a distribution-free guarantee on the probability it increases held-out loss, before it is committed. | 2402.05203; 2208.08401 | None of the retrieved papers apply conformal prediction directly to the optimizer's gradient/step commit decision with a distribution-free guarantee on the probability of a loss-increasing update; the nearest works apply conformal calibration to forecasting intervals or reward correction, not to gating SGD/Adam steps, so a genuine implementation of per-step conformal gradient gating for optimizer commits appears novel. |
| 50 | OPEN | 2 | 4 | energy-verified gradient acceptance | energy-based-refinement applied to the optimizer step so that each backward-pass update is scored against a cheap scalar energy before being committed, rejecting or damping steps predicted to raise loss | 2011.04803; 2012.12418 | Yes: none of the retrieved papers implement a runtime gate that computes a cheap held-out energy on the tentative post-update parameters and conditionally halves/rejects the step before committing (an inference-time trust-region-like veto on the optimizer step), so the specific mechanism (energy-checked commit/reject/damp per step) appears open. |
| 51 | OPEN | 2 | 4 | Atomic micro-batch commit groups | transactions-and-rollback applied to the gradient step so that a group of micro-batch gradients only updates weights if the group's aggregate loss-delta is verified non-increasing, otherwise the whole group is discarded | 2512.03610; 2608.14774 | Yes: no paper implements an atomic transaction over accumulated micro-batch gradients that tentatively commits, evaluates on a probe set, and rolls back to a pre-group checkpoint with re-sampling on failure—this specific checkpoint-based accept/reject gate on the optimizer step itself (as opposed to model merging, sharpness regularization, or architectural stability) remains a distinct, unclaimed mechanism. |
| 52 | OPEN | 2 | 4 | learned prefix-index for cache lookup | learned-index applied to serving-batch so that a small model predicts the position of a matching cached KV segment for an incoming request prefix, replacing exact hash-lookup with a learned approximate retrieval that shares computation across requests | 2512.15550; 2605.06046 | A trained embedding-to-slot-index model that replaces exact hash/radix-tree prefix matching with an approximate learned lookup, retrained periodically on logged (embedding, slot) pairs, is not directly instantiated by any candidate; the nearest works either do approximate indexing within a single context (CTkvr) or improve scheduling/eviction with learning but keep exact prefix matching for cache retrieval. |
| 53 | OPEN | 2 | 3 | multiresolution logit sculpting | Wavelet-multiresolution decomposition applied to the per-step logit vector so that top-k/temperature truncation acts differently on coarse (broad semantic) versus fine (lexical-choice) components of the distribution, yielding a genuinely position- and shape-dependent sampling rule instead of one scalar knob. | 2505.07893... n/a; 2309.02772 | The specific mechanism of applying a discrete wavelet transform to the sorted per-step logit vector to separately shrink high-frequency (rank-jitter) detail coefficients while temperature-scaling coarse coefficients, then inverse-transforming before softmax, is not implemented in any candidate paper; FiGKD applies wavelet decomposition to logits but for cross-model distillation, not single-model sampling control. |
| 54 | OPEN | 2 | 3 | submodular greedy FFN key selection | Greedy submodular-maximization over the FFN's key set applied to the FFN memory read so that per-token key activation is sparsified with a provable approximation ratio to the dense score instead of an ad-hoc top-k. | 2607.27591; 2605.28283 | None of the retrieved papers replace top-k FFN key selection with a submodular greedy surrogate (facility-location/coverage objective) that yields a per-token (1-1/e)-style certified approximation to the dense activation; the closest works either use magnitude ranking (Prox) or cumulative probability mass (PrunePath) as the selection rule, which lack the diminishing-returns submodular guarantee that is the proposal's core claim. |
| 55 | OPEN | 2 | 3 | Contrastive cross-sequence normalisation stats | a contrastive objective applied to normalisation so that per-token scale/shift statistics are pulled toward a batch-level cluster of 'similar-context' tokens and pushed away from dissimilar ones, injecting cross-sequence structure into an otherwise purely local statistic | 2106.04051; 2309.12931 | None of the retrieved papers apply an InfoNCE-style contrastive loss specifically to the pre-normalization activations of LayerNorm to pull same-local-context tokens across different training sequences into a shared statistical regime while leaving inference-time normalization unchanged; this specific placement (auxiliary contrastive head on LayerNorm's gain/bias, removed at inference) is not covered. |
| 56 | OPEN | 2 | 3 | Wavelet cross-token normalisation | Wavelet multiresolution decomposition applied to per-token activation normalisation so that normalisation statistics are computed jointly across nearby tokens at multiple temporal resolutions rather than per single token. | 2606.26487; 2512.06929 | Nothing in the set replaces LayerNorm's per-token mean/variance computation itself with a causal, incrementally-updated wavelet multiresolution tree (trained filter taps) inside a standard transformer block; the nearest works either apply multi-scale decomposition to embeddings/retrieval or use trend-extraction blocks for series-level normalization, so a causal wavelet-based normalization statistic at the LayerNorm site appears genuinely unaddressed. |
| 57 | OPEN | 2 | 3 | curvature-retrieval second-order warmstart | kNN-retrieval over a saved bank of per-parameter Adam moment vectors (indexed by parameter identity plus a compressed loss-curvature signature) applied to optimiser-state so that a freshly initialised training run can warm-start its second-moment estimates from the nearest historical run instead of the standard zero/bias-corrected init. | 2410.21316; 2410.17413 | Yes: none of the candidates perform cross-run kNN retrieval over a bank of saved per-parameter Adam (m,v) states indexed by a curvature signature to warm-start a fresh run's second-moment estimates; the nearest works address intra-run offloading, attribution, or objective-balancing of optimizer state, not this cross-run retrieval-based transfer mechanism. |
| 58 | OPEN | 2 | 3 | reflex/override dual-rate controller | dual-process gating applied to the lr-schedule so that a fast per-step reflex adjustment (cheap, reactive to instantaneous grad-noise) is overridden by a slow deliberate controller (reactive to a windowed loss trend) whenever the two disagree, producing closed-loop lr control with two timescales instead of one fixed curve. | 2506.08419; 2003.03977 | None of the papers implement two simultaneous feedback controllers at different timescales (per-step grad-noise reflex vs windowed loss-trend deliberation) with an explicit override-on-disagreement rule; that dual-rate closed-loop gating on the lr schedule appears genuinely absent. |
| 59 | OPEN | 2 | 3 | Kalman-fused logit calibration | Kalman filtering applied to the training objective so that per-position logits are treated as noisy measurements fused with a running state estimate of the true token-frequency distribution, producing a calibrated posterior rather than a raw softmax point estimate. | 2409.19817; 2111.14610 | A paper that explicitly treats per-position logits as noisy Bernoulli/Gaussian measurements of a latent token-frequency state and fuses them via a running Kalman mean+covariance (rather than a single scalar temperature or generic model-parameter EnKF) for online, retraining-free LM calibration would still be new. |
| 60 | OPEN | 2 | 3 | PID-controlled replay rate | Feedback-control applied to data-order so that the rate of revisiting past examples is driven by a PID loop tracking a measured training-error signal instead of a fixed schedule. | 2603.28611; 2603.09892 | A genuine PID controller (proportional+integral+derivative gains explicitly tuned and logged) driving replay-buffer sampling fraction from a loss-slope error signal is not shown in any candidate; the nearest works use threshold triggers, bandits, or heuristic loss-driven scheduling rather than an explicit closed-loop PID control law, so implementing and demonstrating the PID-specific dynamics (windup, derivative kick, gain tuning) at matched token budget would still be new. |
| 61 | OPEN | 2 | 3 | Kalman-fused mixture weights | Kalman filtering applied to data-order so that the per-domain sampling probability is a running fused estimate of predicted vs measured loss-improvement, weighted inversely by each domain's estimate uncertainty rather than by a fixed EMA rate | 2510.19099; 2506.11300 | Nothing in the set applies a per-domain Kalman filter (state=loss-improvement rate, variance=uncertainty) to set LM data-mixture sampling weights each eval interval; nearest work only uses uncertainty as a static difficulty metric or applies Kalman/EnKF mixtures to unrelated estimation problems, so the specific placement (curriculum/data-order component, KF-based adaptive fusion mechanism) remains open. |
| 62 | OPEN | 2 | 3 | information-bottleneck checkpoint distillation | Information-bottleneck applied to the inter-checkpoint trajectory so that only the trajectory information predictive of future validation loss is explicitly compressed into an auxiliary checkpoint summary, discarding the rest. | 2011.10127; 2009.13003 | Nothing found applies an explicit variational IB objective to per-step gradient/loss trajectory logs between checkpoints to produce a compact auxiliary summary vector optimized to predict next-checkpoint validation loss for warm-starting or diagnosing training; the closest works either do IB-style trajectory compression in a different domain (molecular dynamics) or compress checkpoints via quantization/averaging without an IB objective tied to future validation loss. |
| 63 | OPEN | 2 | 3 | RG coarse-graining of checkpoints | renormalisation-group coarse-graining applied to the sequence of training checkpoints so that the trajectory between snapshots is summarised into a compact multi-scale record instead of discarded | 2210.11803; 2412.12153 | Nothing in the set builds a recursive block-spin-style pyramid over checkpoints that explicitly separates kept coarse (persistent) modes from discarded fine-scale residuals at multiple scales specifically to let one query post-hoc which weight directions/dynamics were scale-invariant versus transient during training without rerunning it; existing work either averages/merges checkpoints for performance gains or does one-shot low-rank projection for merging, not a multi-scale trajectory-compression record for dynamics analysis. |
| 64 | OPEN | 2 | 2 | hot-path checkpoint specialization tracing | JIT-tracing applied to checkpoints so that a frequently-recurring loss-landscape trajectory pattern (a 'hot path' between two snapshots) is recorded once and reused as a specialized update instead of being recomputed and discarded every time it recurs. | 2606.23521; 2403.19390 | Yes: no paper detects recurring gradient/optimizer trajectory signatures between checkpoint intervals and substitutes a cached specialized parameter-delta sequence (verified periodically) to skip fresh gradient computation during ongoing training; existing work either JIT-compiles checkpoint handlers for fault tolerance, merges/averages checkpoints post-hoc, or replays full RL/agent trajectories for sample efficiency rather than optimizer-step reuse keyed on trajectory-signature matching. |
| 65 | OPEN | 1 | 3 | LSH-bucketed curvature memory bank | hashing-LSH over per-parameter (m,v) Adam state vectors, bucketing by locality-sensitive hash of a compressed curvature fingerprint, applied to optimiser-state so that curvature information from many past training runs can be pooled and cheaply retrieved without an exact nearest-neighbour scan. | 2111.07839; 2410.17413 | Nothing in the set hashes per-parameter Adam (m,v)/curvature fingerprints into LSH buckets across many past runs specifically to retrieve an approximate warm-start init for a new run's optimizer state; the nearest papers apply LSH bucketing to different objects (video features, network traffic, log lines) or apply curvature/optimizer-state correction within a single run for a different purpose (influence attribution, incremental gradient acceleration), so a genuine cross-run curvature-archive warm-start via LSH remains open. |
| 66 | ADJACENT | 4 | 4 | Choquet-capacity expert router | A 2-additive Choquet integral over a fuzzy measure with learned pairwise interaction terms is applied to the moe-router so that top-k expert selection can explicitly favor complementary experts and penalize redundant ones, instead of ranking experts by independent linear scores. | 2607.26164; 2607.28308 | The specific 2-additive Choquet-integral formalism with a learned low-rank pairwise interaction capacity, combined subset-scoring via greedy/beam search and a pairwise co-selection balancing loss, is not shown in any of these papers, so a rigorous instantiation and ablation of this exact mechanism at the router would still be new relative to the graph-interaction and Choquet-as-aggregator precedents. |
| 67 | ADJACENT | 4 | 4 | reflex-deliberate head override | dual-process gating applied to multi-head attention so that a cheap fixed 'reflex' subset of heads always fires while a slow learned controller can selectively override with the full head-set only when its own uncertainty signal exceeds a threshold. | 2410.11842; 2210.05144 | The specific two-tier design—an always-on fixed reflex subset combined with a separate uncertainty-thresholded controller that escalates to the full head-set (rather than a learned router picking among all heads each time)—is a narrower architectural commitment not explicitly instantiated in MoH/MoA's per-token top-k routing, so a version emphasizing that asymmetric fixed-vs-full escalation structure and its FLOP-logging analysis could still be new. |
| 68 | ADJACENT | 4 | 3 | Predictive-error KV cache | Predictive-coding applied to the kv-cache so that each layer stores a prediction-error residual against a top-down prediction from later/higher layers instead of the raw key/value, letting later context implicitly rewrite what the entry 'means' at read time. | 2604.15356; 2605.06472 | The proposal's distinctive twist—training a per-layer top-down predictor conditioned on the current (later) decoding state so that the SAME stored residual yields a DIFFERENT effective K/V as later context grows, i.e. genuine reinterpretation without mutation—is not demonstrated in the nearest paper (2604.15356), which uses predictive residual coding purely for compression with a fixed (not read-time-recomputed-per-later-context) prediction; if the mechanism is shown to implement content-dependent revision under append-only storage rather than just entropy reduction, it would remain new. |
| 69 | ADJACENT | 4 | 3 | RG coarse-graining KV cache | Renormalisation-group coarse-graining applied to the kv-cache so that groups of old, low-salience entries are merged into fewer effective entries whose attention-relevant statistics are preserved. | 2602.16284; 2402.06082 | The specific RG-flavored iterative coarse-graining across scales (repeated block-merging with a size-preserving auxiliary projector plus local KL/L2 refit at each step, applied recursively so cache shrinks with a formal renormalization schedule as context grows) is a more principled/iterative version than the single-shot attention-matching or clustering methods, but the core mechanism (fit synthetic KV pairs to match block attention output, training-free) is already demonstrated by Attention Matching/Cartridges-style work. |
| 70 | ADJACENT | 4 | 3 | Slow-fast self-distilled cache consolidation | Online self-distillation applied to the kv-cache so that a slowly-updated teacher summary distills evicted entries into a compact consolidated representation before they are dropped by the fast cache. | 2606.07878; 2608.01247 | The specific two-tier fast/slow cache architecture with a continuously EMA-stabilized teacher and incremental every-k-token online gradient consolidation (rather than one-shot amortized synthesis or per-context optimization) is a narrower architectural variant that these papers do not explicitly instantiate, so a proposal centered on that exact online slow-teacher/fast-student consolidation loop could still be marginally new, though the core mechanism is already well covered. |
| 71 | ADJACENT | 3 | 5 | EMA-consolidated KV summary | EMA-averaging applied to the kv-cache so that evicted entries are folded into an exponentially-decayed running summary slot rather than discarded outright. | 2606.01563; 2605.29873 | The proposal's specific twist—an explicit extra 'summary slot' per head that receives exactly the evicted entry via alpha*summary+(1-alpha)*evicted_kv and then participates in attention like a normal KV pair (rather than smoothing all live entries or using moment statistics for score correction)—is a narrower, more literal instantiation than what MomentKV or KVSmooth do, so a tightly scoped version could survive, but it is very close to MomentKV's mechanism and purpose. |
| 72 | ADJACENT | 4 | 3 | RG-coarsened KV summaries | a renormalisation-group style coarse-graining applied to the kv-cache so that old KV blocks are iteratively merged into coarser-scale summaries whose surviving degrees of freedom are exactly the ones later attention still uses | 2406.02069; 2407.08454 | The specific RG framing—recursive, scale-boundary-triggered merging into a log-depth pyramid where the merge operator is explicitly trained to preserve exactly the attention outputs later queries would use (an information-theoretic renormalization criterion), applied purely at cache-write time as blocks age past scale boundaries—is a more principled and recursive construction than the single-level or layer-wise merging in KVMerger/PyramidKV/LightVLM, so a version that demonstrates genuine multi-scale (log-depth) recursive coarsening with RG-consistent training could still be new. |
| 73 | ADJACENT | 4 | 3 | RG coarse-grained attention cascade | Renormalisation-group coarse-graining is applied to attention-scores so that the model computes attention at multiple explicit scales, keeping only what survives block-averaging, instead of a single bilinear similarity at token resolution. | 2607.15449; 2512.04025 | The specific enforced fixed-point/consistency loss (requiring level L+1 attention weights equal the block-sum of level-L weights, i.e. a literal RG coarse-graining constraint rather than just multi-level pooled KV or post-hoc RG analysis) is not present in PSA or the RG-analysis paper, so a proposal built specifically around that trained consistency constraint as the mechanism generating the hierarchy would still be novel. |
| 74 | ADJACENT | 4 | 3 | wavelet multiresolution key-value bank | A wavelet multiresolution decomposition is applied to attention-scores so that keys and values are represented simultaneously at multiple frequency bands, letting a single attention operation mix content at both fine detail and coarse trend scales instead of one bilinear score per pair. | 2303.12398; 2506.11454 | A genuinely causal, streaming-safe wavelet transform applied specifically to the KV-cache of an autoregressive LM (enabling per-band inspection/ablation of cache entries during generation) is not demonstrated in these vision/PDE/segmentation-focused non-causal wavelet-attention works, so the causal KV-cache-native framing and interpretability angle could still be new if not merely an application-domain relabeling. |
| 75 | ADJACENT | 4 | 3 | delta-rule fast-weight router | Delta-rule fast-weight memory applied to the moe-router so that the gate's expert-scoring key-value memory is overwritten in-context based on observed downstream error, closing the feedback loop within a single sequence. | 2510.14853; 2609.08189 | The specific twist of using a one-step-lagged, loss-delta-derived error signal (an internal credit-assignment target rather than self-supervised generation quality or purely history-based context) as the delta-rule target for updating router key-value memory is not demonstrated in these papers, so a version centered on that credit-assignment mechanism could still be novel. |
| 76 | ADJACENT | 4 | 3 | test-time-trained router | Test-time-training applied to the moe-router so that a small subset of router parameters is updated via a self-supervised loss on the incoming token stream at inference time, closing the downstream-error feedback loop that a frozen open-loop gate lacks. | 2510.14853; 2502.20395 | The proposal's twist of updating only a small subset of router parameters via a hindsight-teacher-forced next-token loss (rather than additive router-logit vectors or output-side re-weighting) is a narrow implementation difference, not a new capability, so it would need a demonstrably different self-supervised objective or theoretical guarantee to survive as new. |
| 77 | ADJACENT | 4 | 3 | periodic bidirectional residual denoising | Masked-diffusion-denoising applied to the residual stream so that a bounded lookback window of past residual writes can be revised (partially erased/overwritten) by later context, instead of only ever receiving additive updates. | 2604.18567; 2207.06937 | The proposal would survive only if the exact instantiation matters: applying masked-diffusion denoising specifically to a transformer's residual-stream cache (not video buffers or generic time-series residuals) with a periodic-every-k-tokens side-buffer overwrite scheme that downstream blocks preferentially read, is not shown by any paper here operating on LLM residual streams with this bidirectional-diffusion mechanism, whereas the closest paper (LPSR) achieves a similar retraction capability via a different (steering-vector/rollback) mechanism rather than masked denoising. |
| 78 | ADJACENT | 4 | 3 | Per-token delta-rule width | delta-rule-memory applied to the residual stream so that each token can allocate an effective write capacity (erase-then-write at a token-specific key) proportional to its own need, instead of every token being forced through the same fixed-width additive channel | 2601.00417; 2609.07816 | The specific framing of adding a small-dimension (d/4) delta-rule memory bank in parallel with the residual stream at every block, explicitly evaluated as a token-adaptive capacity control (vs. sequence-mixing) at matched FLOPs/params against a fixed-width baseline, is not quite what DDL or the delta-rule linear-attention papers test, but DDL's gated rank-1 residual overwrite with explicit capacity/width separation is functionally the same mechanism-at-component-for-purpose, so only a very narrowly scoped empirical distinction (e.g. isolating capacity-allocation effects independent of sequence-mixing benefits) would be new. |
| 79 | ADJACENT | 4 | 3 | Delta-rule content-adaptive positions | Delta-rule associative overwriting applied to positional encoding so that a token's effective position code is a content-conditioned, overwritable memory entry rather than a fixed function of its integer index. | 2501.00712; 2405.14722 | The specific mechanism of a delta-rule (erase-then-write) associative memory keyed by content hash, explicitly borrowed from DeltaNet's linear-attention state-update math and applied as an overwritable per-sequence positional memory (vs. TAPE/DAPE/MapFormer's layer-contextualization, learned biases, or Lie-algebra transforms), is a distinct enough mechanism-object pairing to remain novel if the delta-rule's specific erase-then-write algebra and capacity properties are shown to matter for the boundary-collapsing behavior claimed. |
| 80 | ADJACENT | 4 | 3 | windowed masked-diffusion decoding lane | Masked-diffusion denoising applied to the sampler so that a block of upcoming positions is filled in parallel over a few bidirectional refinement passes instead of one autoregressive token at a time, with the result verified against the exact causal model before being committed. | 2510.02358; 2601.16971 | The specific combination of a trained small denoising adapter sharing the base backbone at matched FLOPs, plus a hard per-token exact-causal-probability threshold triggering rollback to strict autoregressive re-decoding (rather than soft lattice search or another model's revision), is a narrower verification mechanism not explicitly instantiated in DiffuSpec or Speculative Correction, so a paper demonstrating exact-distribution-preserving rollback under this specific adapter setup could still be new. |
| 81 | ADJACENT | 3 | 5 | conformal per-token abstention bands | conformal-prediction applied to the sampler so that each committed token carries a distribution-free guaranteed-coverage confidence set, letting downstream consumers or a rollback controller detect exactly which committed tokens are statistically unreliable | 2512.22682; 2509.00461 | The specific coupling of a calibrated conformal per-token set (not just entropy/confidence) to a rule-based rollback controller that triggers windowed re-decoding only on non-conformal spans, logging set-size events for downstream consumers, is not demonstrated in any single cited work. |
| 82 | ADJACENT | 4 | 3 | Information-bottleneck prefill cache pruning | An explicit information-bottleneck objective applied to prefill so that the KV cache built during prompt processing is compressed to the minimal sufficient statistic for predicting the continuation, discarding redundant prompt structure rather than caching every token's K/V. | 2509.19228; 2607.01520 | The specific instantiation as an explicit rate-distortion IB objective (cross-entropy relevance term + KL-to-prior rate term) with a trainable cross-attention query bottleneck compressing the entire prefill KV cache, trained from scratch/grafted and ablated against a matched-budget nano model, is not directly shown in any single paper, so a rigorous from-scratch IB-vs-fixed-scheme ablation could still be a genuine (if narrow) contribution. |
| 83 | ADJACENT | 4 | 3 | Predictive-coding depth iteration | Predictive-coding message passing applied to the layer-schedule so that each layer receives only the prediction-error residual from the layer below and iteratively refines its estimate over several internal steps before the token advances, producing a genuine loop where a block's own error signal determines re-application. | 2112.03378; 2006.04182 | The proposal's specific instantiation as a drop-in replacement for the transformer residual stream at nano-scale with matched-FLOPs comparison and adaptive-T convergence-gated re-application (rather than as a biologically-motivated alternative to backprop or a generic feedforward-to-PC translation) is a narrower systems/architecture claim not directly tested in the cited PC papers, so a demonstration that this gating yields a genuinely new capability (early-exit-like convergence detection at inference within a causal transformer) could still be novel. |
| 84 | ADJACENT | 4 | 3 | slow-teacher horizon distillation | online self-distillation applied to the training objective so that a slowly-updated (EMA) copy of the model provides an auxiliary target on its own future hidden state, adding a term that rewards representations for what they will be useful for several tokens ahead, not just the immediate next token. | 2509.24317; 2210.02077 | The specific twist of using the EMA teacher's hidden state at a fixed future token offset (t+k) as a longer-horizon auxiliary target for autoregressive LM training, via a delayed activation buffer, rather than same-position/same-frame consistency or masked-region prediction, is not explicitly instantiated in these papers and would need to be shown as the distinguishing contribution. |
| 85 | ADJACENT | 4 | 3 | content-built dynamic attention graph | graph-message-passing applied to attention-topology so that the connectivity pattern is constructed from a cheap first pass over content rather than fixed in advance | 2003.05997; 2505.00315 | A genuinely new angle would be the causal, layer-local KNN-graph-construction-plus-one-message-passing-step specifically as a pre-attention rewiring module trained jointly with a causal LM at matched FLOPs against a sliding-window+sink baseline, since the closest works either use k-means/expert routing instead of explicit graph message passing (Routing Transformer, MoSA) or apply dynamic graph message passing non-causally in vision rather than as a causal attention-topology precursor in autoregressive LLMs. |
| 86 | ADJACENT | 4 | 3 | Redundant-code cache repair | Error-correcting-code redundancy applied to attention-topology so that cache entries are stored with a learned parity/checksum projection, and every k tokens a syndrome computed from later hidden states is used to detect and repair drift in earlier cache entries before future reads. | 2607.21604; 2607.14107 | The specific mechanism of a fixed linear parity/checksum code with an explicit syndrome magnitude as a corruption signal (versus drift/attention/residual heuristics) and its logged auditability could be a genuine twist, but the capability of selective, syndrome/drift-triggered cache repair is already established by these training-free and training-based drift-detection methods. |
| 87 | ADJACENT | 4 | 3 | Transactional buffered rewrite window | Transactions-and-rollback applied to attention-topology so that a bounded window of past representations can be atomically rewritten once later context arrives, without breaking causal decoding. | 2608.08086; 2511.01266 | The proposal's twist—an explicit all-or-nothing learned commit gate that atomically accepts/discards a bidirectional rewrite pass over a bounded trailing buffer in a text-token autoregressive transformer, trained from scratch at matched FLOPs—is not demonstrated by Archer (training-free KV cache reuse for already-bidirectional DLMs) or the video-streaming window/knot papers (smoothing/sink mechanisms without atomic commit gating or rollback semantics); a genuinely novel contribution would need to show this gated atomic transaction mechanism itself, not just windowed revision or cache reuse. |
| 88 | ADJACENT | 4 | 3 | sleep-phase KV consolidation | offline reactivation-and-compression of old KV segments applied to the context window so that stale positions are replaced by a compact consolidated trace instead of being kept at full per-token cost | 2511.14694; 2502.14280 | A genuinely new twist would need to show the periodic (every-k-token) offline consolidation with joint distillation-trained cross-attention pooling replacing raw KV in place is mechanistically distinct from FOCUS's progressive cross-layer summary-token compression—e.g. demonstrating that the specific replay+evict+distill loop yields properties (asynchronous 'sleep phase' scheduling, no architectural summary-token insertion) that FOCUS's continuous compression does not achieve. |
| 89 | ADJACENT | 4 | 3 | Sinkhorn head-token routing | Optimal-transport Sinkhorn matching applied to multi-head attention so that heads are assigned to tokens (or token-groups) via a balanced doubly-stochastic routing plan instead of every head attending to every token uniformly. | 2210.05144; 2410.11842 | The specific mechanism of running Sinkhorn iterations to produce a doubly-stochastic token-head routing matrix with hard balanced marginals (as opposed to softmax/top-k gating or auxiliary balancing losses) at the multi-head attention component appears not directly instantiated in these papers, so a proposal emphasizing the OT-Sinkhorn balanced-transport mechanism specifically (not just token-to-head routing) could still be a genuine mechanistic novelty if none of these actually use Sinkhorn normalization for heads. |
| 90 | ADJACENT | 4 | 3 | MDL-selected head subset | minimum-description-length applied to multi-head attention so that the set of heads actually used per layer is chosen to minimize total code length (model bits + residual bits) rather than being fixed a priori. | 2410.11842; 2606.19150 | The explicit two-part MDL objective (explicit bits-to-specify-parameters term plus residual coding cost, with a verification pass certifying no degradation at a stated bit budget) is a distinct formalism from L0/MoE-style gating or heuristic importance pruning seen in the closest papers, so a version emphasizing certified bit-budget selection and exact-model verification could still be novel. |
| 91 | ADJACENT | 4 | 3 | Hebbian fast-weight FFN memory | hebbian-fast-weights applied to the ffn so that the key-value memory it implements can be rewritten by the current stream within a context window instead of staying fixed post-training | 2102.11174; 2212.02475 | The specific placement as a low-rank additive fast-weight term on the FFN's W1 (treating the FFN as the rewritable key-value memory, rather than augmenting the attention/linear-attention mechanism) is a narrower architectural choice not directly demonstrated in these closest papers, so a proposal that shows this component-specific placement matters (vs. equivalent attention-based fast-weights) could still be new. |
| 92 | ADJACENT | 3 | 4 | Choquet-capacity expert selection | the choquet-integral applied to the MoE router so that top-k expert selection accounts for pairwise redundancy/complementarity instead of ranking experts by independent scores. | 2607.26164; 2510.13079 | A genuinely novel contribution would be the specific formalism of a learned 2-additive Choquet capacity used at inference time per-token to select the joint-utility-maximizing k-subset (not aggregation, not post-hoc pruning, not a heuristic diversity penalty), trained jointly with the router from scratch and shown to beat both redundancy-heuristics (GatePro) and independent top-k scoring at matched FLOPs. |
| 93 | ADJACENT | 3 | 4 | delta-rule KV overwrite | delta-rule-memory applied to the KV cache so that writing a new value at a previously-used key position first erases/interpolates the old value instead of purely appending. | 2607.02303; 2605.22884 | The proposal's specific twist—training the delta-memory strictly as a parallel, non-evicted auxiliary slot bank at matched param/FLOP budget purely to let later tokens overwrite stale content at matched-key positions while the full append-only KV remains untouched (rather than being fed by evictions, replacing state, or serving as the primary compressive path)—is a narrower combination not explicitly tested in HOLA, Tensor Cache, or FDM, but it is very close to all three and would likely read as a direct follow-up/ablation of this cluster. |
| 94 | ADJACENT | 4 | 2 | energy-descent KV refinement | energy-based-refinement applied to the KV cache so that cached key/value vectors are iteratively updated by gradient descent on a learned energy function conditioned on the growing context, instead of being fixed at write-time. | 2603.13875; 2505.15781 | The specific mechanism of a learned scalar energy function scored against a running context summary, iteratively refined every k tokens via gradient descent (not a single steering step or forward-pass caching schedule, and not tied to a diffusion denoising process or prefix-token compression), trained end-to-end as an inner-loop optimization is not demonstrated by any of these; this repeated energy-descent correction of frozen-key/updated-value pairs throughout ordinary autoregressive decoding remains a distinct proposal. |
| 95 | ADJACENT | 3 | 4 | Generational GC cache tiers | Generational-garbage-collection applied to the kv-cache so that entries are age-partitioned into a young (sliding-window, frequently re-evaluated) tier and an old (sink, rarely-touched) tier, and promotion between tiers triggers a consolidation rewrite instead of blind eviction. | 2505.16950; 2608.11701 | The specific generational-GC framing—two explicit age-based lanes (sliding-window young vs. sink old) with promotion triggering a trained consolidation rewrite specifically at the young→old boundary, matched-FLOPs against removed sink capacity—is a narrower instantiation than Bottlenecked Transformer's periodic whole-cache rewrite or Consolidator's STM→LTM routed transform, so a twist tying rewrite cost/trigger strictly to sliding-window aging dynamics could still be new, but the core mechanism (non-causal in-place KV rewrite at a tier transition) is already claimed. |
| 96 | ADJACENT | 4 | 2 | RG coarse-grained KV hierarchy | Renormalisation-group coarse-graining applied to the kv-cache so that entries are recursively merged into progressively coarser scales, keeping only the degrees of freedom that survive repeated blocking | 2605.06946; 2306.01070 | The specific mechanism of a trained recursive dyadic blocking/pooling transform applied jointly at pretraining to merge adjacent KV entries into coarser time-scale summaries (an explicit RG-like real-space decimation of cache entries over time, not layers or fixed frequency bands) is not clearly instantiated in any cited work, so a from-scratch model with this exact mergent operator and matched-FLOPs comparison could still be novel. |
| 97 | ADJACENT | 3 | 4 | Hebbian fast-weight KV overlay | Hebbian fast-weights applied to the kv-cache so that a short-lived outer-product overlay lets recent tokens' keys be nudged by later associations without touching the permanent stored K/V | 2605.22884; 2606.25342 | The specific framing of a bounded-norm, fast-decaying (lambda<1) additive correction term explicitly designed to let recent associations be transiently revised and then auto-forgotten without ever touching stored K/V—rather than as a compression/capacity mechanism (Tensor Cache) or a persistent state-tracking substrate (TTT/parametric attention)—is a narrower purpose not directly targeted by the closest papers, but it is a thin twist given how mechanically identical the update/read rule is. |
| 98 | ADJACENT | 3 | 4 | homeostatic KV precision allocator | homeostatic-plasticity applied to kv-cache so that each entry's bit-width adapts to keep its long-run attention-mass contribution near a fixed target instead of every entry costing the same | 2603.20616; 2502.13176 | The specific homeostatic control law that continuously re-quantizes each entry's bit-width (from a fixed set) to hold population-mean attention-mass-per-bit constant, without any profiling pass, budget target, or learned selector, is not shown in these papers; if that exact self-regulating EMA-to-quantization-level mapping is the twist, it could still be novel. |
| 99 | ADJACENT | 4 | 2 | Wavelet multiresolution KV cache | Wavelet multiresolution decomposition applied to the kv-cache so that each token's key/value is stored at multiple resolutions simultaneously, letting attention read coarse or fine detail depending on distance. | 2606.00724; 2606.10671 | The proposal's twist of a fixed invertible wavelet transform that keeps approximation coefficients permanently and reconstructs approximate keys/values via a learned inverse-projection head—rather than scoring tokens for eviction or merging adjacent entries—could still be new if it demonstrably differs from WaveFilter's use of wavelets purely as a token-importance/filtering signal. |
| 100 | ADJACENT | 3 | 4 | generational KV eviction tiers | generational garbage collection applied to the kv-cache so that eviction follows an age-partitioned survival policy instead of a single heuristic threshold | 2607.10582; 2608.05326 | The specific generational-GC mechanics (distinct sweep cadences per age tier, promotion between tiers based on attention-mass survival thresholds tuned to hit a cache-size budget, explicitly modeled on age-since-last-hit rather than semantic region or precision) is not directly instantiated by any single paper, so a system built precisely around age-partitioned differential sweep frequency with promotion thresholds could still be novel relative to MemDecay's region-based decay and QEvict's precision-tier promotion. |
| 101 | ADJACENT | 4 | 2 | Hopfield-addressed shared KV bank | Hopfield content-addressed retrieval applied to the kv-cache so that a shared cross-request bank of prefix KV patterns is recalled by energy descent from partial/noisy queries instead of requiring exact prefix matches | 2510.12872; 2606.09862 | The specific mechanism of energy-descent (modern-Hopfield) retrieval over a trained pooling/query projection against a bank of pooled prefix representations, with an explicit iterative attractor-dynamics formulation and partial-recompute verification, is more specific than KVCOMM's anchor-deviation estimation, so a paper demonstrating genuine Hopfield energy-descent convergence properties (not just nearest-anchor lookup) for KV approximation could still be new. |
| 102 | ADJACENT | 4 | 2 | Sparse-coded attention with null atom | Sparse-coding applied to attention-scores so that a query is represented as a sparse combination over a learned dictionary of value atoms including an explicit zero-atom, allowing exact abstention when no atom fits. | 2511.20194; 2605.21649 | The explicit trainable all-zero dictionary atom giving a structural abstention option (as opposed to entmax/sparsemax's incidental exact zeros from a fixed normalization map) combined with an ISTA-unrolled dictionary replacing V at every layer is a distinct mechanism-purpose pairing not directly shown in these papers. |
| 103 | ADJACENT | 3 | 4 | reflex-deliberate abstention gate | Dual-process-gating applied to attention-scores so that a query with no relevant keys can abstain from mixing values entirely rather than being forced to distribute mass over an irrelevant set. | 2306.12929; 2602.01203 | The specific dual-process architecture (a cheap query-only fast gate with a slower query+top-1-key MLP override on disagreement) is a distinct mechanistic twist not present in any cited gated-attention work, which use a single learned gate rather than a two-speed reflex/deliberate arbitration scheme. |
| 104 | ADJACENT | 3 | 4 | Submodular greedy expert selection | Greedy submodular-maximisation applied to moe-router so that top-k selection accounts for redundancy and complementarity between experts instead of scoring them independently. | 2510.13079; 2507.06567 | A twist could survive if the proposal is the first to actually replace top-k with a per-token greedy submodular loop carrying a provable (1-1/e) coverage guarantee computed from a jointly-trained expert-embedding table, rather than a redundancy-penalty heuristic (GatePro) or an offline placement problem (SlimCaching); this exact per-token in-router greedy-with-guarantee mechanism is not demonstrated in the retrieved set. |
| 105 | ADJACENT | 3 | 4 | self-distilled adaptive router | Online self-distillation applied to the moe-router so that a slow (EMA) copy of the router's implied expert-selection distribution teaches a fast copy in real time, giving the gate a feedback signal about which choices actually reduced loss. | 2604.13508; 2601.14797 | The specific combination of an EMA-smoothed shadow router as a mean-teacher-style target, gated by a per-token comparison of realized next-token loss between shadow and live expert choices (rather than KL-consistency or replay alone), appears novel relative to these adjacent stabilization/distillation approaches. |
| 106 | ADJACENT | 3 | 4 | Hebbian fast-weight router feedback | Hebbian outer-product fast weights applied to the moe-router gate so that the gate's expert scoring adapts within a sequence based on the expert's recent downstream error, without any gradient step. | 2510.14853; 2606.08814 | The specific mechanism of an outer-product Hebbian fast-weight matrix added directly onto the router's gate weight, driven explicitly by the chosen expert's downstream prediction-error signal (not self-supervised loss on generated text or GHA-based subspace tracking), applied per-token with decay/reset within a sequence, is not demonstrated by any of these; a rigorous comparison against STAR's GHA-router and Rewiring's self-supervised router-vector update would need to show this error-driven Hebbian credit-assignment step is a genuinely distinct update rule. |
| 107 | ADJACENT | 4 | 2 | learned-index dynamic tokeniser | A learned-index model that predicts merge-boundary positions applied to the tokeniser so that segmentation granularity adapts per-span at inference time. | 2506.18639; 2307.02092 | The proposal's specific twist of a causal, per-16-byte learned merge-boundary predictor trained jointly with LM loss (bootstrapped from BPE merges) that runs identically at train and inference to reshape the LM's own vocabulary/embedding table is not demonstrated in these text-LM-focused works, which mostly use entropy/attention heuristics or offline-trained assigners rather than a jointly-trained learned-index replacing the BPE merge lookup itself. |
| 108 | ADJACENT | 4 | 2 | Predictive-error-routed hierarchical tokens | Predictive coding applied to the tokeniser so that a coarse token level only passes a fine-grained byte-level correction signal upward when the coarse prediction fails, letting effective granularity be chosen by prediction error rather than fixed at pre-training time. | 2506.18639; 2605.30080 | The specific mechanism of using the coarse LM's top-1 probability as a threshold to conditionally inject a byte-level residual-error embedding into the main model at inference time (rather than fixing vocabulary/chunk boundaries at training time) is not demonstrated by any of these papers. |
| 109 | ADJACENT | 3 | 4 | Kalman-fused logit calibration | Kalman filtering applied to the output-head so that the preference-tuned logits (a noisy 'measurement') are fused at inference time with a pretrained-base-model prediction (the 'prior') in proportion to each source's estimated uncertainty, restoring calibration lost during preference tuning. | 2601.04277; 2503.22725 | The specific mechanism of running base and preference-tuned models in parallel and fusing their logits via a Kalman-style inverse-variance-weighted average (explicit variance/gain parameters, not just a scalar temperature) is not shown in 2601.04277, so a proposal demonstrating this exact variance-weighted fusion mechanism against that baseline could still be new. |
| 110 | ADJACENT | 3 | 4 | dual-path abstention gate | A dual-process gate applied to the output-head so that the model can emit an explicit abstain token when a slow verifier disagrees with the fast reflex logits. | 2607.04430; 2509.01455 | The specific mechanism—an extra transformer block reading the same final hidden state as a second 'slow' path, jointly trained with a threshold on fast/slow KL divergence to emit a first-class ABSTAIN token at the output head—is architecturally more specific than the cited calibration/refusal frameworks, so a proposal demonstrating this exact dual-path KL-gated architecture outperforms generic calibration heads could still be novel. |
| 111 | ADJACENT | 3 | 4 | mixture-density multi-horizon head | Mixture-density heads applied to the output-head so that the model predicts a small mixture of distributions over tokens at offsets {1,2,4} rather than a single point next-token distribution. | 2503.21801; 2608.00434 | The mixture-density parameterization (multiple weighted components per horizon capturing genuine multi-modal, calibrated uncertainty, verified against actual NLL) rather than a single point/categorical prediction per horizon is not demonstrated in any of the closest papers, so a proposal specifically validating calibrated multi-modal uncertainty via MDN heads at each horizon would still be new. |
| 112 | ADJACENT | 3 | 4 | fast-weight overwrite of committed token memory | delta-rule-memory applied to the sampler so that a committed token's contribution to a separate associative fast-weight state can be erased and overwritten by later context, without altering the emitted token sequence itself | 2606.26560; 2608.27763 | The specific framing as a frozen-base-LM graft where only a small linear read head and mixing gate are trained to blend causal KV attention with a separately-updated delta-rule associative state (explicitly preserving emitted-token causality while allowing 'semantic rollback' of meaning) is a narrower engineering variant not explicitly tested in these papers, but the core mechanism (delta-rule overwrite of key-value associative memory decoupled from the emitted sequence) is already established as the same operation on the same object. |
| 113 | ADJACENT | 3 | 4 | transactional multi-token commit sampler | transactions-and-rollback applied to the sampler so that a block of k tokens is generated speculatively as an uncommitted transaction and only committed atomically if a post-hoc verifier accepts the whole block, otherwise the entire block is discarded and regenerated | 2509.06982; 2608.14653 | The proposal's twist of strictly atomic, fixed-size k-token blocks with full KV-cache checkpoint/restore and a generic pluggable verifier (rule-based or trained classifier) applied uniformly to any downstream check, rather than a domain-specific guard (safety, compiler, uncertainty) or arbitrary-length rollback region, is a narrower engineering framing but not a clearly new capability beyond CARE/Hydra/uncertainty-rollback, which already establish generate-block-then-verify-then-rollback-or-commit at the sampler with an auxiliary trained or rule-based checker. |
| 114 | ADJACENT | 3 | 4 | Speculative layer-truncated prefill | Speculate-and-verify applied to prefill so that time-to-first-token is bounded by a cheap shallow-network pass instead of the full-depth exact pass, while exact hidden states are restored before generation. | 2605.06105; 2410.08391 | The specific speculate-and-verify-with-rollback loop—emitting token 1 from an approximate shallow/draft KV cache and then exactly reconciling/rolling back per-layer against the full pass before continuing generation—is not what any of these do; they either permanently truncate depth (SPEED), replace the cache once without verification (KV Prediction), or prune tokens rather than depth (SpecPrefill), so a verified, correctable approximate-prefill mechanism could still be new. |
| 115 | ADJACENT | 4 | 2 | spectral prefill attention | A spectral-transform applied to the prefill sequence so that long-range dependencies in the prompt appear as sparse/local coefficients in frequency space, allowing prefill attention to be computed with a truncated spectral mixer instead of full O(n^2) pairwise attention. | 2111.15473; 2505.00033 | The proposal's specific twist—causal/prefix-only FFT restricted to long-range mixing while explicitly retaining separate sink/local attention lanes for near-token precision, trained jointly at matched parameter count in a 'nano' LLM setting—is not demonstrated in these general spectral-replaces-attention papers, but the core mechanism (causal-masked Fourier token mixing replacing attention for long-range dependencies) is already claimed by MDFWA and the spectral dictionary paper. |
| 116 | ADJACENT | 3 | 4 | PID-controlled adaptive depth loop | A PID feedback controller applied to the layer-schedule so that a block's output error signal (e.g. residual-norm change or a learned halting score) drives repeated re-application of that block until the error is driven below threshold, instead of the fixed single pass. | 2603.01914; 2312.10193 | The specific twist of using an explicit trained PID (proportional-integral-derivative) control law—with separate P/I/D gains reacting to residual-norm error dynamics rather than a single learned scalar halting probability—is a different controller parameterization, but since ACT/AdaPonderLM/ACM already achieve the identical capability (data-dependent iterate count via error/halting signal) at the identical component, this is a controller-design variant rather than a new capability. |
| 117 | ADJACENT | 3 | 4 | Delta-rule FFN memory edits | Delta-rule associative writes applied to the FFN so that the per-token key-value memory is erased-and-rewritten online instead of staying fixed after pretraining. | 2102.11174; 2208.00399 | The specific twist of applying the delta rule as an erase-and-rewrite operation directly on the FFN's up-projection weight matrix (treating it as the classic Sukhbaatar/Geva key-value memory) to enable token-level factual correction and readout at a later token, evaluated with matched-FLOPs nano-scale ablation, is not demonstrated as such in any single paper—closest work either does delta-rule in attention (not FFN) or edits FFN memories via static/injected methods (not online per-token delta rule), so a paper combining both specifically at the FFN with this correction-capability framing would still be new. |
| 118 | ADJACENT | 3 | 4 | Test-time-trained FFN adapter | Test-time-training applied to the FFN so that a small adapter subset of FFN parameters is updated by gradient descent on a self-supervised loss computed from the incoming stream itself, before being used for prediction. | 2602.05085; 2604.14048 | The specific twist of grafting a frozen-base rank-r LoRA solely onto the FFN down-projection with a strictly causal masked reconstruction of past FFN hidden activations (not full weight TTT or offloaded memory blocks) in an autoregressive LM stream is not exactly what Locas or the video/3D TTT papers do, so a tightly scoped causal-LM FFN adapter variant could still be new, though it is a narrow delta. |
| 119 | ADJACENT | 3 | 4 | LRU-gated FFN slot replacement | an adaptive replacement policy applied to FFN key-value slots so that the memory can swap which associations it stores as the stream's topic shifts | 2512.13751; 2501.18356 | The specific combination of treating FFN up-projection columns as an ARC-scored slot table with rank-1 gated rewrites trained via synthetic topic-shift sequences, applied to FFN memory (not attention KV cache) for non-causal topic tracking within a single context, is not directly instantiated in any of these papers. |
| 120 | ADJACENT | 3 | 4 | delta-rule closed-loop LR controller | a delta-rule fast-weight associative memory (write key = recent loss-curve statistics, value = a learning-rate multiplier, with erase-then-write updates so stale readings are overwritten) applied to the lr-schedule so that the learning rate is set by a closed-loop readout of the training run's own loss trajectory instead of a fixed pre-registered curve. | 2411.16975; 2506.13274 | A genuinely new angle could survive if the delta-rule's causal, in-place erase-then-write update (with formal fast-weight-programmer guarantees) is shown to give qualitatively different stability/capacity properties than the heuristic or search-based controllers in ExpTest/AdaLRS/POLA-style memories, but as stated the capability and placement are already covered. |
| 121 | ADJACENT | 3 | 4 | Contrastive multi-horizon auxiliary loss | A contrastive objective applied to hidden states at position t so that representations are pulled toward the true encoding of position t+k and pushed away from other positions' encodings, is applied to the training objective so that intermediate representations carry information predictive of tokens several steps ahead. | 2410.18160; 2510.14751 | The specific mechanism—an InfoNCE contrastive loss pulling h_t toward a stop-gradient h_{t+k} and pushing apart in-batch negatives, as opposed to auxiliary generative/predictive heads or cross-attention decoders used in the cited works—is a genuine mechanistic difference that could survive as a narrower contribution, though the capability and placement are already well-explored. |
| 122 | ADJACENT | 3 | 4 | Information-bottleneck auxiliary future head | An information-bottleneck penalty applied to the training objective so that hidden states are compressed toward only what predicts a token ten steps ahead, in addition to next-token loss. | 2510.14751; 2404.19737 | The specific combination of a variational IB penalty (with explicit beta-controlled KL/stochastic bottleneck) applied jointly with a fixed k-step-ahead auxiliary CE head to yield a probeable, controllable compression-vs-horizon dial is not demonstrated as a single mechanism in any one paper; FSP gets closest on purpose but does not use an IB-style KL/noise bottleneck, and the IB papers get closest on mechanism but not on the k-ahead auxiliary target framing. |
| 123 | ADJACENT | 3 | 4 | Multi-horizon mixture-density prediction heads | Mixture-density heads applied to the training objective so that the model predicts a mixture of distributions over tokens at multiple future offsets (t+1, t+5, t+10) instead of a single point-distribution next-token target. | 2404.19737; 2410.17765 | A genuinely new contribution could still exist if the mixture-density heads are shown to produce calibrated, multimodal probability distributions (verified via calibration metrics) that let downstream code explicitly detect branching/forking futures—none of the cited MTP works evaluate or claim calibrated multimodal uncertainty as the capability, only auxiliary point predictions for training signal or inference speed. |
| 124 | ADJACENT | 3 | 4 | fast-weight mixture rewriter | Delta-rule fast-weight memory applied to the data-mixture controller so that the mixture-selection key for each domain/source is overwritten (erase-then-write) by a running estimate of the model's current per-domain error, letting the mixture adapt continuously instead of being fixed in advance. | 2606.18663; 2410.11820 | The specific framing as an associative-memory slot per domain updated by an explicit delta-rule erase-then-write (w += eta*(loss-w)) rather than scaling-law fits, gradient-conflict estimators, or velocity heuristics is a narrow mechanistic distinction, but since several papers already implement the same 'per-domain running loss -> cheap online reweighting' capability, only a demonstrated difference in dynamics/stability or theoretical link to fast-weight memory models would be new. |
| 125 | ADJACENT | 3 | 4 | Streaming teacher-student self-distillation | Online self-distillation applied to weights-at-inference so that a fast low-rank adapter continually absorbs the slow base model's own predictions on the live input stream | 2604.06169; 2307.05014 | The specific combination of a frozen base model as teacher and a small LoRA adapter as student, updated via periodic KL-distillation solely on the live inference stream (no labels, no architectural fast-weight redefinition, base weights literally untouched), is not exactly what In-Place TTT does (which redefines MLP projections as fast weights with a bespoke NTP-aligned loss rather than explicit teacher-student KL distillation), so a version emphasizing the LoRA/teacher-student KL framing specifically could still be distinguishable, though it is a narrow twist. |
| 126 | ADJACENT | 4 | 2 | submodular content-aware sparse mask | submodular-maximisation applied to attention-topology so that the attention mask for each query is chosen at inference time by greedy maximization of a coverage-diminishing-returns objective over key content, instead of being fixed by position before content is seen | 2003.05997; 2505.00315 | The specific claim of a certified (1-1/e)-approximation guarantee via submodular greedy selection (with lazy evaluation) for per-query mask construction—rather than heuristic or learned scoring—is not demonstrated in any of these; if the approximation guarantee and its empirical validation (not just the coverage-style objective) is the load-bearing contribution, that could still be new. |
| 127 | ADJACENT | 3 | 4 | learned position-predictor attention router | learned-index applied to attention-topology so that a small model predicts which past positions are worth attending to, replacing a fixed structural mask with a trained lookup | 2505.00315; 2603.20997 | A twist could survive if the index model is explicitly framed as a rank/score predictor trained to approximate a learned-index CDF-style lookup (as in learned index structures) rather than a differentiable gate/router, or if it demonstrates the joint-training co-adaptation pitfalls identified in 2603.02227 are avoided by a specific architectural choice—but as stated the mechanism and placement match MoSA and the routing-absorption/content-based-routing literature closely. |
| 128 | ADJACENT | 3 | 4 | Hebbian gated topology adapter | Hebbian fast-weight outer-product traces applied to attention-topology so that the mask itself is perturbed by recent content rather than being fixed before any token is seen. | 2102.11174; 2510.21908 | The specific twist of keeping the standard causal QK^T mask intact and adding a separate learned-decay Hebbian trace strictly as a gated additive bias to logits (rather than replacing attention with the fast-weight computation or adding a parallel memory module) is not explicitly shown in these papers, though it is a small step from them. |
| 129 | ADJACENT | 3 | 4 | Delta-rule rewrite lane for KV | Delta-rule associative overwrite applied to a side attention-topology lane so that later context can erase-and-replace an earlier position's stored value while the main causal path stays untouched. | 2607.02303; 2412.06464 | A genuinely new angle would require showing the side lane specifically decouples read causality (queries at position t reading only past-written overwrites without ever seeing future tokens) in a way that differs functionally from HOLA's dual-memory or Gated DeltaNet's hybrid gating, e.g. a formal proof/ablation isolating the 'meaning revision without future leakage' property as distinct from simply improving recall; as stated the placement (delta-rule lane alongside causal attention) is already explored. |
| 130 | ADJACENT | 3 | 4 | Hebbian fast-weight causal patch | Hebbian fast-weights applied to attention-topology so that recent token associations formed after a position was written can still influence that position's downstream reads for a few steps, approximating revision without violating causality. | 2102.11174; 2608.27763 | A genuinely new contribution would need to show that the dual-path read (causal KV cache + separate short-horizon fast-weight matrix, trained jointly with matched parameter budget by trading off attention heads) produces a measurable causality-compliant 'revision' effect—i.e., that later tokens change how earlier positions are effectively read downstream—as a targeted ablation/capability claim, rather than just presenting fast-weights as an efficient attention substitute or memory augmentation as prior work does. |
| 131 | ADJACENT | 3 | 4 | generational KV garbage collection | age-partitioned, differentially-swept eviction applied to the KV cache so that young (recent) positions are re-evaluated for relevance often while old positions are checked rarely, giving non-uniform maintenance cost matched to expected contribution | 2410.01805; 2509.04377 | The specific twist of generation-indexed differential sweep frequency (checking gen0 every step, gen1 every 8, gen2 every 64, with promotion/eviction as an amortized GC schedule) to bound average per-step compute is not explicitly present in any single paper; most works either use a single-pass or per-step scorer (Locret, SAGE-KV) or structural/head partitioning (PagedEviction, RedKnot) but none combine age-based generations with exponentially decreasing re-evaluation frequency as an amortized cost mechanism. |
| 132 | ADJACENT | 3 | 4 | sparse-dictionary head activation | sparse coding applied to multi-head attention so that each token's head-mixture is expressed as a sparse combination of a learned head-dictionary rather than always summing all heads. | 2210.05144; 2604.22583 | A genuinely new twist would require framing the head-mixture explicitly as sparse dictionary coding (ISTA-unrolled, L1-penalized) with the exposed per-token active-head-count as an interpretability/diagnostic signal rather than just an efficiency router, but as stated the mechanism and purpose duplicate MoA/BudgetFormer's per-token head selection. |
| 133 | ADJACENT | 4 | 2 | Hopfield cross-request cache recall | Hopfield-style energy-descent retrieval applied to the serving-batch's aggregate KV history so that a new request's attention query can pull in relevant K/V patterns previously computed for other concurrent or recent requests instead of only its own cache. | 2510.12872; 2405.16444 | A genuinely new twist would be the explicit modern-Hopfield energy-descent update rule (trained offline) operating over a rebuilt per-batch associative memory of K/V blocks as literal extra attention slots, rather than anchor-offset correction, selective recomputation, or Sparse-Q segment correction, plus demonstrating this energy-descent retrieval yields materially different reuse/robustness properties than the nearest correction-based systems. |
| 134 | ADJACENT | 4 | 2 | multi-scale coarse-grained shared cache | renormalisation-group applied to serving-batch so that KV states from many independent requests are coarse-grained into a hierarchy of shared summaries at increasing context-scale, and requests read the coarsest summary that still changes their output within tolerance | 2406.02657; 2604.10898 | The genuinely new piece is sharing one coarsened pyramid across many independent requests' unrelated contexts (common-domain background statistics amortized server-wide) with per-request KL-gated unrolling to decide how deep to read, rather than each request building/consulting its own hierarchy over its own cache — none of the closest papers do cross-request shared coarse-graining, so a proposal narrowly targeting that cross-request amortization would still be new. |
| 135 | ADJACENT | 3 | 3 | Kalman-fused KV entries | Kalman-filtering applied to the kv-cache so that each cached key/value is treated as a noisy estimate that gets fused with a new 'measurement' derived from later context, weighted by learned uncertainty, rather than being fixed forever. | 2406.05317; 2403.09636 | The explicit Kalman-filter formalism—maintaining a learned scalar/low-rank uncertainty per slot and updating both the KV estimate and its uncertainty via a Kalman-gain-weighted fusion with a periodic 'measurement' derived from later context—is a more principled and reversible (non-discarding, uncertainty-tracked) mechanism than LoCoCo's fixed convolutional mixing or DMC's compression-ratio learning, so a version demonstrating this specific recursive uncertainty-tracked fusion (not mere blending or ratio selection) would still be a genuine, if narrow, contribution. |
| 136 | ADJACENT | 3 | 3 | MDL-driven adaptive KV budget | minimum-description-length applied to kv-cache so that each entry's stored precision is chosen to minimize total code length (model bits + residual bits) rather than fixed uniformly, letting redundant entries collapse to near-zero cost | 2604.04722; 2607.01520 | A genuinely new twist would be the explicit online two-part MDL code-length computation (model bits for quantization level choice + residual bits under a fitted prior) reported and minimized per-entry as a measurable quantity, rather than a learned/heuristic/task-performance-driven importance score—none of the found papers compute or optimize this explicit code-length decomposition. |
| 137 | ADJACENT | 3 | 3 | Importance-weighted KV allocation | Importance-sampling applied to kv-cache allocation so that bit-width/slot-count per entry is set by an estimated attention-mass reweighting rather than uniform allocation. | 2603.10899; 2602.03203 | A genuine self-normalized importance-sampling correction (explicit proposal q vs. target p reweighting, debiased rather than just predicted) mapped to a continuous bit-width/precision tier rather than binary evict/keep could still be new, but the core capability of estimating future attention mass to reallocate cache resources is already claimed by LookaheadKV and ForesightKV. |
| 138 | ADJACENT | 3 | 3 | Conformal abstention mass in attention | Conformal-prediction applied to attention-scores so that a query token can allocate a calibrated 'null mass' to an abstention channel instead of forcing softmax weights to sum to one over content keys. | 2606.08105; 2309.17453 | The distribution-free, split-conformal calibration of a per-head threshold to guarantee a target false-abstention rate is not present in any sink/null-token paper, which use heuristic or learned thresholds/gates without a coverage guarantee — if that calibration-with-guarantee is the load-bearing claim (not just adding a null KV pair), it could survive as a genuinely new twist. |
| 139 | ADJACENT | 3 | 3 | MDL-gated attention abstention | A minimum-description-length code-length penalty applied to attention-scores so that a token can allocate near-zero total attention mass when no key shortens its predictive code, instead of being forced to mix a full unit of value. | 2309.17453; 2605.08453 | The specific MDL/code-length gating mechanism—an auxiliary head estimating bits-saved per key via ablation, used to penalize logits before softmax over {keys, null}—is not described in any of these papers, which use fixed sink tokens, geometric/threshold-based sparsity, or oversmoothing analysis rather than an information-theoretic per-key description-length cost; if the team can show the MDL auxiliary-loss induced abstention differs functionally (e.g. context-dependent code-length rather than static/thresholded sparsity) that twist could survive. |
| 140 | ADJACENT | 3 | 3 | Choquet head-interaction aggregation | Choquet-integral applied to attention-scores so that multi-head outputs are combined via a fuzzy measure capturing pairwise redundancy/complementarity instead of a fixed linear (concat+project) combination. | 2606.02341; 2006.16362 | A genuinely new contribution could survive if it shows the discrete Choquet integral (with explicit 2-additive capacity encoding signed redundancy/complementarity beyond the sum, applied per-layer at the head-combination projection) yields interaction structure or interpretability that shared-projection or gating fusion methods cannot, but no paper in the set does exactly this operation at exactly this component for this purpose, making it at most a close follow-up to the Choquet fusion paper and the collaborative-head paper. |
| 141 | ADJACENT | 3 | 3 | Bayesian posterior-weighted head mixture | Bayesian-model-averaging applied to attention-scores so that head outputs are combined by an online-estimated posterior over 'which head is currently reliable' rather than a fixed learned linear combination, letting the mixture weights shift per-token based on evidence of each head's recent predictive accuracy. | 2210.05144; 2009.09364 | The specific mechanism of maintaining an explicit running per-head log-likelihood (via auxiliary per-head next-token heads) and converting it into a causal Bayesian posterior strictly from past prediction accuracy—rather than a router or composition function conditioned on the current query—appears novel relative to MoA/DCMHA/Repulsive Attention, none of which track online predictive reliability as the weighting signal. |
| 142 | ADJACENT | 3 | 3 | MDL-regularized attention codebook | A minimum-description-length objective over a shared query/key codebook is applied to attention-scores so that the model is pressured to route mass through a small reusable set of transport 'hubs' rather than bilinear all-pairs comparison. | 2604.03850; 2407.12820 | The specific combination of an in-line VQ bottleneck on keys with an explicit MDL loss (reconstruction error plus log2(C) times codebook usage-entropy) trained jointly with the LM loss specifically to measure and prune attention structure per layer/head is not exactly instantiated by any single paper, though close analogues (prototype codebooks with diversity+reconstruction loss, PQ on keys, top-k landmark routing) already cover the mechanism and placement; the surviving novelty would be the explicit description-length-based pruning diagnostic rather than the routing/quantization mechanism itself. |
| 143 | ADJACENT | 3 | 3 | Bayesian-averaged expert aggregation | Bayesian model averaging applied to moe-router so that expert outputs are combined by posterior-mass weighting over interacting hypotheses rather than a hard top-k selection that ignores inter-expert dependence. | 2606.01509; 2510.16411 | A twist could survive if the explicit Bayesian/Dirichlet posterior formalism combined with an online EMA-updated low-rank pairwise correlation matrix used specifically to correct for non-independence in the aggregation weights (rather than just soft routing or graph-based interaction) yields measurably different calibration/uncertainty behavior than ProbMoE's exact-marginal surrogate or the graph-based SymphonySMoE, but this is a narrow technical distinction rather than a new capability. |
| 144 | ADJACENT | 3 | 3 | verified router rollback | speculate-and-verify applied to the moe-router so that the top-1 expert's output is checked against a cheap proxy of the top-2 expert's disagreement and, on disagreement above a threshold, both are computed and the exact top-2-mixture result is substituted | 2605.00342; 2608.02989 | The proposal's specific twist—using a cheap distilled/low-rank proxy to predict top-2 disagreement before deciding to compute the real second expert, framed explicitly as speculate-and-verify with exact top-2 recovery guarantee on the disagreement subset—is not clearly present in CARE (which uses the router's own softmax mass/disagreement directly rather than a separate trained proxy network) or the speculative-decoding papers (which operate on draft-token trees, not single-token top-1-vs-top-2 disagreement), so a version centered on the proxy-network verification-with-exact-fallback guarantee could still be new. |
| 145 | ADJACENT | 3 | 3 | speculative expert routing with rollback | Branch-prediction applied to the moe-router so that the top-1 expert choice is treated as a speculative guess validated against a cheap secondary signal, allowing misrouted tokens to be caught and rerouted before the FFN output is committed. | 2609.09241; 2410.17954 | The specific mechanism of speculatively computing both top-1 and top-2 FFN outputs and using a trained lightweight verifier scorer (conditioned on an EMA of historical per-expert hit-rate) to choose which to commit before writing to the residual stream is not explicitly implemented in any of these papers, which instead do cache-miss correction, distributional recalibration, or heuristic expert-count adjustment. |
| 146 | ADJACENT | 3 | 3 | Predictive-coding expert residual routing | Predictive coding applied to the moe-router so that the gate scores not the raw hidden state but the residual between the hidden state and a causally-predicted hidden state from the previous layer's chosen expert, so routing responds to what was *not* already explained. | 2605.00604; 2608.14205 | The specific causal predictive-coding formulation—predicting the current hidden state from the previous layer's chosen expert output via a trained predict head and routing on that residual, jointly trained end-to-end with matched FLOPs—is not exactly instantiated in 2605.00604 (which predicts next-token/next-state hidden states for transition detection rather than attributing residual to 'already explained by chosen expert') so a tightly scoped version emphasizing expert-attributed prediction (not just temporal/sequence prediction) could still be distinct, but the core mechanism and purpose are close enough that this must be argued explicitly against 2605.00604. |
| 147 | ADJACENT | 3 | 3 | Thompson-sampled expert selection | Thompson sampling applied to the moe-router so that expert selection is drawn from a posterior over each expert's expected quality-adjusted value rather than chosen by deterministic top-k on a point-estimate score, and load balance emerges from posterior uncertainty rather than an aux loss. | 2408.15664; 2603.09453 | A twist could survive if it specifically uses per-expert conjugate Bayesian bandit posteriors (Beta/Gaussian, online reward-based updates) with the explicit claim that load balance emerges purely from exploration-driven uncertainty with zero auxiliary loss and no explicit balancing bias term, contrasting with VMoER's variational/temperature-based stochasticity and Loss-Free's deterministic bias-correction mechanism — but this delta is narrow and mostly a tuning/parameterization difference rather than a new capability. |
| 148 | ADJACENT | 3 | 3 | dual-process router override | Dual-process gating applied to moe-router so that a fast unconditioned reflex route (trained only for balance) is overridden by a slow deliberate route (trained only for quality) exactly when the deliberate path's confidence exceeds a margin, decoupling the two objectives into separate parameters instead of one contested loss. | 2408.15664; 2607.26340 | The specific conditional-override architecture—two fully independent gates (parameter-free hash reflex vs. quality-only learned gate) arbitrated by a single trained per-layer confidence-margin threshold—is a distinct control-flow design not explicitly tested in Loss-Free Balancing's additive-bias approach, so a demonstration that margin-gated hard override outperforms bias-based decoupling would be new. |
| 149 | ADJACENT | 3 | 3 | Per-token TTT residual gate | Test-time-training applied to the residual-stream so that each token's effective write-width adapts on the fly via a self-supervised update rather than being fixed at train time. | 2608.27964; 2607.28669 | The specific mechanism of taking one causal, per-token gradient step on a low-rank residual-write adapter using a masked/noised reconstruction loss to adaptively expand/contract write-width at inference (rather than a fixed, offline-trained or merely selected/interpolated correction) is not demonstrated by any of these; a paper combining TTT's per-token online update rule with LARA/TALAN's residual-stream low-rank write target would still be new. |
| 150 | ADJACENT | 3 | 3 | test-time-trained content positions | test-time-training applied to positional-encoding so that position embeddings are updated online from the model's own predictions on the incoming stream rather than fixed as a function of integer index | 2507.06415; 2405.14722 | A genuine test-time-training loop (actual gradient steps on a self-supervised loss updating a positional-offset module during inference, every k tokens) rather than a purely feed-forward content-conditioned positional function would be a real mechanistic difference from DAPE/TAPE, though PERK already establishes TTT-via-LoRA-gradients as a known mechanism for a closely related purpose (context encoding), so the novelty rests narrowly on doing this specifically for the positional correction term rather than content memory. |
| 151 | ADJACENT | 3 | 3 | sparse dictionary position codes | sparse-coding applied to positional-encoding so that each position is represented as a sparse combination of a learned dictionary of position-atoms, chosen per-sequence by content, instead of one fixed function of index | 2405.18719; 2501.00712 | A sparse-dictionary/ISTA mechanism with explicit per-position k-sparse atom selection and joint dictionary learning is mechanistically distinct from CoPE's gating, TAPE's equivariant transform, or DAPE's adaptive bias, so if the k-sparse combinatorial reuse of shared atoms across documents is shown to produce qualitatively different generalization behavior (e.g. atom reuse for structurally analogous positions) that these methods provably lack, the proposal could survive as a genuinely new mechanism at the same component. |
| 152 | ADJACENT | 3 | 3 | hebbian fast-weight position cache | a hebbian outer-product fast-weight buffer applied to positional-encoding so that recently seen (position, content) pairings are stored as a decaying associative memory that biases the position code for the next few tokens | 2511.17388; 2605.06946 | The specific twist of routing the Hebbian fast-weight outer-product update to bias the positional/RoPE channel itself (rather than content KV association or decay-gating of attention), enabling within-sequence adaptive position codes for repeated templates, is not directly shown in these papers and could still be novel if demonstrated to matter empirically. |
| 153 | ADJACENT | 3 | 3 | delta-rule position rewrite memory | a delta-rule associative memory applied to positional-encoding so that position codes beyond the training length are looked up from a small adaptive memory that overwrites (rather than accumulates) stale key-position bindings, instead of extrapolating a fixed untrained function | 2511.09146; 2405.14722 | A genuinely new contribution would be the specific delta-rule (erase-then-write, beta-gated) fast-weight memory keyed on relative distance and trained end-to-end from scratch specifically for the position channel, as opposed to denoising/reparameterizing attention (DoPE), context-adaptive bias networks (DAPE), or discrete position-index overwriting (STRING) — none of which use a continuously-updated associative memory with the erase-then-write delta rule for positions. |
| 154 | ADJACENT | 3 | 3 | ARC-policy positional cache aging | The adaptive replacement (ARC) cache policy applied to positional-encoding so that position indices assigned to KV-cache slots are dynamically remapped based on recency-and-frequency of attention use rather than fixed absolute index, letting long streams reuse a bounded range of trained positions. | 2502.11101; 2606.26631 | The specific ARC-style dual-list (recency T1 + frequency T2) eviction policy with a balance parameter governing which cache entries get position-recycled is a genuinely different remapping heuristic than the length/attention-based reallocation in CacheFocus or PRCR's coordinate-based rebinding, so a rigorous ARC-policy-driven remap plus a trained boundary-correction adapter could still be a novel instantiation if it demonstrably outperforms these existing recency/frequency-agnostic or content-coordinate-based reassignment schemes. |
| 155 | ADJACENT | 3 | 3 | Mixture-density positional decoder | Mixture-density heads applied to positional-encoding so that instead of a single deterministic embedding per index, each position emits a mixture of Gaussian phase/frequency components whose weights are trained to generalize smoothly past the training length. | 2505.22842; 2506.00920 | The specific twist of using an explicit K-component Gaussian mixture-density head trained with NLL loss to expose a per-token variance-derived confidence scalar that gates interpolation between position-aware and position-agnostic (mean-pooled) attention is not demonstrated in these closest papers, which either use a single Gaussian-family prior (BAM) or a histogram/superposition representation (PRISM) without a learned downstream gating mechanism consuming calibrated uncertainty. |
| 156 | ADJACENT | 3 | 3 | test-time adaptive merge head | Test-time-training applied to the tokeniser's merge-decision head so that a small set of boundary parameters is updated on the incoming stream via a self-supervised next-byte loss, letting granularity adapt per-document without touching the base LM. | 2506.01084; 2512.23675 | The specific twist of using gradient-based test-time-training (per-k-byte online SGD on a self-supervised next-byte loss) restricted to a small frozen-base-LM merge-head, rather than an online compression algorithm or a purely feed-forward adaptive gate, appears novel relative to zip2zip's LZW-based hypertoken mechanism and TTT-E2E's whole-model weight updates. |
| 157 | ADJACENT | 3 | 3 | ARC-policy adaptive merge cache | An adaptive-replacement cache policy (balancing recency and frequency of byte n-grams) applied to the tokeniser's merge table so that merge granularity adapts online to the actual input stream instead of being frozen after BPE training. | 2411.18553; 2410.08481 | The specific ARC (recency+frequency with ghost lists) cache-replacement policy governing promotion/eviction of merges, as opposed to hypernetwork-based or simple frequency/pruning-based dynamic vocabularies, is a genuine mechanistic twist not explicitly present in the closest papers. |
| 158 | ADJACENT | 4 | 1 | RG coarse-graining tokeniser | A renormalisation-group procedure that iteratively coarse-grains adjacent tokens and asks what statistical dependencies survive rescaling, applied to the tokeniser, so that the effective granularity at each layer/position is set by what information survives coarse-graining rather than by a fixed static merge table. | 2511.14806; 2506.01084 | The explicit RG framing—iteratively testing which mutual information with the target survives block-averaging via a probe classifier, and feeding different survival levels to different transformer depths—is a more principled and information-theoretically justified selection criterion than MergeDNA's differentiable merging or zip2zip's compression heuristic, so a version that rigorously operationalizes the RG coarse-graining/rescaling equivalence (not just adaptive merging) could still be new. |
| 159 | ADJACENT | 3 | 3 | Multiresolution byte-wavelet embedding | Wavelet-multiresolution decomposition applied to the tokeniser's input embedding stream so that a single forward pass carries both coarse (merged-span) and fine (byte-level) representations simultaneously, letting downstream layers pick resolution per span. | 2509.20581; 2509.21153 | The specific claim that both resolutions are kept as simultaneous additive channels at every sequence position (not merely a coarser pooled stream or an adaptive-exit mechanism), letting downstream layers pick resolution per-span without any explicit chunk boundary or re-tokenisation, is a finer distinction not clearly demonstrated in HRT or WAVECLIP, which still produce a single resolution-selected representation per position/stage rather than co-resident multi-resolution channels. |
| 160 | ADJACENT | 3 | 3 | Energy-based candidate refinement head | An energy-based refinement procedure applied to the output-head so that the final token distribution is produced by iterative descent over a learned energy on (context, candidate) pairs rather than a single softmax read, yielding better-calibrated aggregate probabilities than the raw post-trained logits. | 2009.13267; 2009.07177 | A genuine twist would be demonstrating that this refinement specifically improves calibration (top-k probability mass matching empirical correctness rates) rather than task accuracy/BLEU/quality, since none of the closest papers target calibration as the explicit objective of energy-based candidate refinement at the LM output head. |
| 161 | ADJACENT | 3 | 3 | BMA ensemble logit calibration | Bayesian model averaging applied to the output-head so that final next-token probabilities are a posterior-weighted mixture over several preference-tuned checkpoints/heads instead of one point-estimate model, restoring calibration that a single post-trained model loses. | 2404.12715; 2601.04277 | The specific twist of using held-out-NLL-derived posterior weights to average logits specifically across multiple DPO/preference-tuned checkpoints (rather than heterogeneous base LLMs or modalities) to restore calibration lost specifically to preference optimization is not directly instantiated in any candidate, though DeePEn already establishes the core probability-space fusion mechanism at the output head. |
| 162 | ADJACENT | 2 | 5 | arithmetic-coding calibration probe | Arithmetic coding applied to the output-head so that miscalibration is exposed and quantified as excess coding length, giving a verified, recoverable calibration diagnostic and correction pass. | 1906.05664; 2511.11966 | A genuinely new contribution would need the specific coupling of exact, losslessly-invertible arithmetic-coding codelength (not just entropy-rate proxies) with a provable, parameter-light rescaling map guaranteed to drive codelength to the entropy bound while preserving exact logit recovery for at-will exact/calibrated decoding—none of the closest papers combine reversibility guarantees with a provable optimality bound in this exact form. |
| 163 | ADJACENT | 3 | 3 | Mixture-head null-component abstention | Mixture-density heads applied to the output-head so that one mixture component is trained to represent an explicit 'no confident token' null class, giving the model a first-class abstention option instead of forcing a point prediction. | 1901.09192; 2212.07328 | The specific mechanism of adding a (K+1)-th 'ABSTAIN' mixture component—no vocabulary logits, just a learned mixing weight trained via an auxiliary disagreement-based loss, jointly with a frozen trunk and K vocabulary components at the LM output head—does not appear to be instantiated in any retrieved paper; SelectiveNet and reject-option regression use a separate rejection head/threshold rather than an intrinsic mixture component competing for probability mass, so a genuinely novel contribution would need to demonstrate that this in-mixture null-component formulation (vs. auxiliary head) yields materially different calibration/behavior. |
| 164 | ADJACENT | 3 | 3 | energy-descent candidate refinement sampler | Energy-based-refinement applied to the sampler so that, instead of sampling a token once from the softmax, the system samples a small candidate set, scores it with a learned scalar energy function conditioned on the causal context, and performs a few gradient or hill-climbing steps over an embedding relaxation before snapping to the nearest vocabulary token, still emitting one committed token per position. | 2604.08302; 2601.08808 | The proposal's specific twist — a contrastively-trained scalar energy head conditioned on causal hidden state that refines a small top-k candidate list via gradient descent before snapping to one committed autoregressive token, explicitly targeting correction of locally high-probability but globally incoherent token choices — is not demonstrated at this exact combination (causal LM sampler + top-k candidate energy refinement + one-token commit) in any single paper, so a tightly scoped implementation could still be novel. |
| 165 | ADJACENT | 3 | 3 | energy-descent block resampling | energy-based-refinement applied to the sampler so that a committed block of tokens can be revised before being finalized rather than accepted token-by-token forever | 2608.19567; 2410.09675 | The specific twist of using an explicit trained energy function (Langevin/iterative denoising descent over soft embeddings or discrete neighborhoods, contrastively trained MLP head) as the revision criterion, rather than confidence scores, order-agnostic reconstruction, or speculative rejection, is not directly shown in these papers and could still be new if it demonstrably outperforms these simpler revision signals. |
| 166 | ADJACENT | 3 | 3 | safety-set barrier sampler | control-barrier-functions applied to the sampler so that the token distribution is projected at each step onto the largest subset of the simplex for which a learned barrier function's value is guaranteed non-decreasing, preventing the trajectory from ever entering an unsafe/incoherent region rather than needing to roll back after the fact | 2511.03121; 2603.13300 | The proposal's specific twist—learning a scalar barrier function on LLM hidden states and solving a per-step QP projection over the top-m token simplex with a formal CBF non-decreasing constraint (rather than a generic 'safety filter' or negative-guidance heuristic)—could still be novel if it demonstrates provable forward-invariance guarantees at the token level that 2511.03121 does not formalize. |
| 167 | ADJACENT | 3 | 3 | Test-time-trained sampling head | Test-time training of a small per-position controller applied to the sampler so that temperature/top-k adapt online via a self-supervised loss computed purely from the incoming generated stream. | 2603.18428; 2411.09661 | A genuinely self-supervised, purely inference-time (no reward, no preference data, no RL loop) per-document controller trained online via SGD on a reconstruction/entropy-prediction loss computed only from the emitted stream would be a distinct mechanism from the RL/policy-learning and preference-optimization approaches seen here, though the capability and placement are already well-explored. |
| 168 | ADJACENT | 3 | 3 | dual-path override sampler | Dual-process gating applied to the sampler so that a fast default top-k/temperature reflex is overridden by a slower, more expensive per-position deliberation path only when a cheap uncertainty signal flags the position as high-stakes, giving genuinely per-position sampling control at bounded average cost. | 2601.07353; 2512.13194 | The proposal is distinctive in gating specifically the token sampling distribution itself (top-k/temperature/renormalization/lookahead at the logit level) via a fixed deterministic entropy/margin function computed on ordinary autoregressive decoding, independent of any draft-verify or tree-search infrastructure; if implemented as a lightweight, model-architecture-agnostic wrapper around standard sampling (not requiring a separate draft model or speculative verification), this concrete instantiation is not directly shown by the closest papers, which embed adaptive compute allocation inside speculative-decoding tree structures or reasoning-mode selection. |
| 169 | ADJACENT | 3 | 3 | speculative layer-skip prefill | branch-prediction applied to prefill so that early layers speculatively predict the residual-stream state that deep layers would produce, letting the first token be emitted before full-depth prefill completes | 2410.08391; 2601.03700 | The specific claim that the predicted KV is provisional and gets silently overwritten in-place by the real background deep pass before token 2 (a true speculative/retroactive reconciliation rather than a permanent approximation as in KV Prediction) is the one twist not clearly covered, though it is a narrow delta. |
| 170 | ADJACENT | 3 | 3 | MDL-selected prefill cache codebook | A minimum-description-length criterion applied to prefill so that the KV cache is replaced by the shortest joint code (a small learned codebook plus per-token indices) that still lets the model reconstruct next-token predictions, trading exact per-token K/V storage for a compressed code chosen by two-part-code length rather than a fixed compression ratio. | 2607.01520; 2606.08382 | The specific two-part MDL code-length criterion (codebook bits + index bits + residual bits) used to dynamically select codebook size per prompt at prefill, as opposed to risk-bound, thresholding, or importance-based adaptive schemes, is not directly instantiated in any candidate paper. |
| 171 | ADJACENT | 3 | 3 | RG-coarsened prefill hierarchy | Renormalisation-group coarse-graining applied to prefill so that the prompt is summarized into a multi-scale cache whose coarse levels can be reused across prompts sharing structure. | 2406.02657; 2311.04934 | The specific combination of a trained hierarchical block-pooling (dyadic-tree) summarizer applied to KV pairs across multiple scales, jointly optimized to minimize KL divergence in attention outputs, and reused/hashed at the block level across prompts, is not directly instantiated by Block Transformer (which doesn't do cross-prompt cache reuse) or Prompt Cache (which doesn't do multi-scale coarse-graining or learned pooling)—so a proposal emphasizing the learned RG-style pooling MLPs trained jointly for cross-prompt coarse reuse could still be distinguishable, though it is a close combination of existing pieces. |
| 172 | ADJACENT | 3 | 3 | Barrier-constrained draft divergence bound | Control-barrier-functions applied to speculative-drafting so that the draft model's per-step KL divergence from the target model is constrained online to stay inside a safety set, throttling draft aggressiveness before verification would reject it | 2411.18462; 2606.30265 | The specific formalism of a control-barrier-function with a certified safety set (rather than a heuristic entropy/margin threshold) could survive if it provides provable non-violation guarantees or formal safety-margin certificates rather than just an empirical stopping heuristic, but the core capability (proxy-triggered preemptive throttling of draft aggressiveness before verification) is already covered. |
| 173 | ADJACENT | 3 | 3 | Thompson-sampled per-token depth | Thompson sampling applied to the layer-schedule so that each token's number of executed layers is chosen by sampling from a posterior over the value (accuracy-per-FLOP) of stopping at each depth, rather than a fixed depth or deterministic threshold. | 2406.03853; 2509.23666 | A twist could survive if the proposal's per-arm Beta/Normal posteriors over each individual candidate exit-layer (rather than a single adaptive threshold or joint layer-head bandit) and its budget-adjusted sampled-value stopping rule yield materially different exploration dynamics or provable regret/accuracy-per-FLOP guarantees not shown in UAT or EESD, but on mechanism-component-purpose grounds it is very close to existing bandit-controlled early-exit work. |
| 174 | ADJACENT | 3 | 3 | multiresolution layer-schedule | Wavelet multiresolution decomposition applied to the layer-schedule so that every token is processed at several depth-resolutions simultaneously and difficulty determines which resolution's output is read out, instead of committing to one fixed depth. | 2412.05225; 2412.01455 | The specific wavelet-coefficient-hierarchy training objective (coarse readout capturing bulk signal, deeper readouts trained as residual detail conditioned on not-yet-stopping, akin to a DWT decomposition) is a structurally different loss/parameterization from standard independent or jointly-optimized early-exit heads, and if shown to yield different accuracy/compute tradeoffs than vanilla multi-exit training, that specific residual-coefficient formulation could be a genuine (if narrow) contribution. |
| 175 | ADJACENT | 3 | 3 | Sinkhorn-balanced FFN key routing | Optimal-transport Sinkhorn matching applied to the FFN key-value memory so that tokens are routed to keys under a balanced-marginal constraint instead of independently top-k scored, guaranteeing every key gets bounded utilization across a batch. | 2511.08972; 2606.01666 | The proposal's specific twist—treating the FFN's key-value memory itself (not discrete MoE experts) as the doubly-stochastic-routed object, with capacity constraints per key/neuron in an otherwise dense, non-MoE FFN—is closest to DOT-MoE's neuron-level Sinkhorn assignment, so genuine novelty would require showing the guarantee/inspection benefit holds for a standard dense-FFN key-value view distinct from any expert partitioning, which none of the papers directly frame that way. |
| 176 | ADJACENT | 3 | 3 | Hopfield energy-descent FFN read | Modern Hopfield energy-descent retrieval applied to the FFN key-value memory so that the per-token FFN output is computed by iterative content-addressed convergence toward a stored pattern instead of a single dense feed-forward score-and-sum, giving one-shot associative cleanup. | 2605.08143; 2406.12220 | The proposal's specific twist is training the pattern set/beta end-to-end from scratch as a direct multi-step-iteration replacement for the FFN's single matmul at matched FLOPs (rather than as a post-hoc editing/interpretability wrapper as in HoReN), which is not demonstrated in these papers. |
| 177 | ADJACENT | 3 | 3 | Sparse dictionary FFN keys | Sparse-coding applied to the FFN key-value memory so that each token activates a small learned support of atoms instead of scoring the full key set. | 2305.13999; 2012.14913 | The specific mechanism of an explicit reconstruction-driven sparse code (top-k matching pursuit or ISTA-unrolled encoder optimizing a reconstruction objective over a dictionary, rather than a learned router/gating network selecting discrete experts) is a genuine mechanistic difference from MoE/PKM-style routing and is not directly instantiated in the cited S-FFN or MoE works, so a rigorous head-to-head comparing sparse-coding-based key selection against routing-based selection at matched FLOPs and matched dictionary/atom scale would still be new. |
| 178 | ADJACENT | 3 | 3 | Information-bottleneck compressed moment memory | Information-bottleneck applied to optimiser-state so that the per-parameter second-moment estimate is replaced by a small learned per-module summary that is explicitly trained to retain only the statistics predictive of the next gradient's magnitude, discarding the rest. | 2606.13894; 2307.02047 | The explicit information-bottleneck training objective (minimizing I(z; raw grads) while maximizing an auxiliary decoder's predictive power for next-step gradient magnitude) as a provable, tunable compression criterion is not what Gefen, CAME, or Adafactor do—they use heuristic sharing/confidence/factorization rather than an IB loss with a trained encoder/decoder, so a rigorous IB-based ablation of exactly how much state is necessary would be a genuine (if narrow) methodological twist. |
| 179 | ADJACENT | 3 | 3 | speculative branch-predicted LR jumps | branch-prediction-style speculative LR adjustment applied to the lr-schedule so that the trainer predicts the next beneficial LR change from recent loss-curve history and speculatively applies it, rolling back on misprediction | 2411.16975; 1910.08525 | The specific speculative-execution-style mechanism of immediately applying a predicted LR jump, checkpointing, and rolling back on a measured misprediction (verification window vs. no-jump control estimate) is not implemented by any of these controller/hypergradient/statistical-test approaches, so that exact rollback-based speculation mechanism could still be novel. |
| 180 | ADJACENT | 3 | 3 | Predictive-coding error-only horizon loss | Predictive coding applied to the training objective so that each layer is trained only against the residual error of its own multi-step prediction (not the raw next-token target), is applied so that lower layers stop re-learning what upper layers already predict correctly at longer horizons. | 2404.19737; 2112.10048 | The specific twist of making each layer's loss the residual error unexplained by the next layer's top-down (detached) prediction at a longer horizon—an explicit hierarchical decomposition of multi-horizon prediction across depth rather than independent parallel heads or reconstruction—is not directly instantiated by any of these papers, though it is a natural combination of MTP heads (Gloeckle et al.) and classic Rao-Ballard top-down error correction. |
| 181 | ADJACENT | 3 | 3 | Offline sleep-phase consolidation replay | Sleep-replay-consolidation applied to data-order so that periodic offline phases reactivate and compress a compact episodic memory of recent examples into consolidated gradient updates, rather than every example being seen once and discarded. | 2303.10725; 2401.08623 | Applying this exact mechanism to standard LLM pretraining data-order (not continual-learning/task-incremental settings) with a surprise-based top-k buffer and token-masked perturbation, integrated into single-pass corpus training rather than multi-task CL, is a narrower framing but not clearly distinct from SIESTA/WSCL's wake-sleep consolidation design. |
| 182 | ADJACENT | 3 | 3 | gated fast/slow replay override | Dual-process gating applied to data-order so that a fast reflex scheduler (fixed random order) is overridden by a slow deliberate scheduler (a small auxiliary loss-predictor) whenever the deliberate path's confidence that an example needs replay exceeds a threshold, giving on-demand revisiting without changing the default streaming order. | 2512.02333; 2202.01838 | The specific dual-process framing—an always-on cheap random-order path overridden only when a jointly-trained auxiliary loss-delta gate exceeds a calibrated threshold, defaulting to zero-overhead streaming—is not shown; a paper demonstrating exactly this gate-triggered FIFO mid-epoch reinjection with negligible added params trained jointly from scratch would still be new. |
| 183 | ADJACENT | 3 | 3 | Multiresolution error-band scheduler | Wavelet multiresolution decomposition applied to the per-example loss stream over training time so that data-order selection responds to error trends at multiple timescales simultaneously instead of only the instantaneous loss. | 2205.08625; 2609.15975 | The specific mechanism of applying a Haar/DWT decomposition to the per-example loss time series to separate coarse persistent-difficulty trend from fine transient noise, and using only the coarse band to drive replay probability, is not demonstrated in any retrieved paper for the data-order/curriculum component—existing trend-aware or frequency-domain curricula either use simple loss trends without multiscale decomposition or apply wavelet decomposition to a different signal (features/motion) rather than the loss stream for replay scheduling. |
| 184 | ADJACENT | 3 | 3 | test-time adapted mask gates | test-time-training applied to attention-topology so that a small set of gating parameters controlling which positions attend to which are updated online via a self-supervised loss on the incoming stream, letting the mask adapt to the current document rather than staying fixed from pretraining | 2606.06906; 2512.23675 | A twist could survive if the gate is explicitly a per-head low-rank bias trained once and then adapted purely to trigger connectivity/window changes (not evidence localization or general context compression) with an explicit widening-on-loss-spike mechanism validated as such, since EASE-TTT targets evidence alignment and E2E-TTT updates general fast weights rather than an explicit topology-widening gate. |
| 185 | ADJACENT | 3 | 3 | sparse-coded attention dictionary | sparse-coding applied to attention-topology so that the mask for each query is derived as a sparse combination of a small learned dictionary of reusable attention patterns (local, sink, periodic, etc.) rather than one fixed hand-designed pattern | 2508.02124; 2303.08810 | The specific twist of using a small fixed dictionary of hand-designed structural topology atoms (local/sink/strided) combined via OMP/top-k sparse coding, rather than a fully learned dynamic mask or generic content dictionary, could still be novel if it demonstrably preserves interpretable structural priors and compute bound in a way DMA/BiFormer/sparse-coding-transformer do not. |
| 186 | ADJACENT | 3 | 3 | Hopfield rewrite of KV cache | Hopfield-style energy descent over a buffered window of past key-value entries applied to attention-topology so that earlier representations can be pulled toward a content-addressed attractor once later context arrives, without violating causal emission order. | 2606.26560; 2503.00979 | A genuinely new contribution would need to show that Hopfield energy-descent rewriting (multi-step, content-addressed, attractor-based) yields qualitatively different long-range coherence gains than the existing delta-rule erase-write or attention-guided iterative refinement schemes at matched budget, since the mechanism class (in-place causal-safe KV rewrite triggered by later context) is already occupied by EDA and MorphKV/GRKV for closely related purposes. |
| 187 | ADJACENT | 3 | 3 | Choquet head aggregation | Choquet-integral aggregation with pairwise interaction terms applied to multi-head attention output combination so that heads are weighted by a learned fuzzy measure capturing redundancy/complementarity instead of being summed independently. | 2606.02341; 2210.05144 | The specific formalism of a 2-additive Choquet capacity (explicit monotonic singleton+pairwise fuzzy measure with softplus reparameterization) applied per-token to head-output aggregation, rather than router/gating or generic compose functions, is not demonstrated in these papers and could still be a genuine mechanistic novelty if it yields distinct interpretability or capacity guarantees. |
| 188 | ADJACENT | 3 | 3 | Submodular greedy head selection | Greedy submodular maximization applied to multi-head attention so that only a diminishing-returns-guaranteed subset of heads is activated per token/layer instead of always running all heads. | 2210.05144; 2606.19150 | A genuinely novel twist would be the explicit formal submodularity proof (diminishing-returns guarantee) coupled with per-token greedy selection at inference, trained end-to-end with a differentiable relaxation and exact recoverability to dense attention—none of the closest papers combine a certified submodular greedy algorithm with per-token dynamic head budgets and exactness fallback. |
| 189 | ADJACENT | 3 | 3 | Thompson-sampled head router | Thompson sampling applied to multi-head attention so that each token position routes to a stochastically-sampled subset of heads chosen from a posterior over per-head value rather than always activating all heads deterministically. | 2210.05144; 2604.22583 | The specific combination of a Beta/Gaussian posterior per head updated via EMA of gradient-magnitude contribution, sampled via Thompson sampling (not just any stochastic/learned router) with annealed temperature, jointly trained from scratch on a real transformer, is not exactly instantiated in MoA or BudgetFormer, so a rigorous ablation isolating Thompson-sampling-specific calibration benefits over those baselines could still be a genuine (if narrow) contribution. |
| 190 | ADJACENT | 3 | 3 | kNN cross-batch cache sharing | kNN-retrieval applied to the serving-batch's pooled KV history so that each request's attention can mix in nearest-neighbour K/V entries drawn from any concurrent request's cache, not only its own. | 2510.12872; 2609.06940 | The specific mechanism of a continuously rebuilt ANN index over pooled batch K/V with top-k retrieval and a learned gate blending non-contiguous, non-prefix nearest-neighbor entries (not exact prefix/offset-aligned matches) into attention at decode time is not demonstrated in these prefix-sharing or offset-alignment systems, so a proposal built specifically around approximate kNN fuzzy matching (rather than exact/aligned prefix reuse) could still be novel. |
| 191 | ADJACENT | 3 | 2 | Graph message-passing KV rewrite | Graph-message-passing applied to the kv-cache so that cached entries are nodes in an explicit sparse graph (built from attention-score thresholds or coreference links) and get updated by aggregating messages from newly-arrived neighbor nodes, instead of staying fixed once written. | 2505.16950; 2509.00388 | The proposal's twist of learned MLP message-passing that rewrites the actual K/V content (not just importance/eviction scores) along attention-derived edges is functionally very close to Bottlenecked Transformers' non-causal in-place KV rewrite via an auxiliary network; a genuinely new contribution would need to show the sparse-graph edge structure (vs. dense/windowed Transformer processor) yields a distinct capability such as long-range coreference propagation at strictly O(edges) cost with provable savings over the dense Cache Processor. |
| 192 | ADJACENT | 3 | 2 | distilled shared prefix cache | online self-distillation applied to the kv-cache so that a slow reference cache teaches a fast compressed cache to represent shared prefixes across concurrent requests without recomputation | 2607.28069; 2608.01247 | A genuinely new twist would require the student to be continuously updated online as an unbounded stream of distinct prefixes arrives (true online/continual distillation rather than an offline-compiled or per-eviction-budget writer), and to serve as a drop-in replacement for exact prefix-cache lookup across many arbitrary unseen prefixes rather than one document/context type — SemPIC already covers the core mechanism-at-component-for-purpose overlap, so this delta is narrow. |
| 193 | ADJACENT | 3 | 2 | Spectral-domain causal attention | A spectral-transform reparameterization applied to attention-scores so that long-range query-key similarity is computed as a local product in a learned frequency basis instead of a global bilinear dot product over raw positions. | 2607.07478; 2111.15473 | A genuinely causal, chunk-local multi-resolution DCT/Hadamard transform with cumulative low-frequency summaries carried across chunks (mimicking a causal multi-scale memory) is not shown in FourierQK's bilateral FFT or MDFWA's non-causal Fourier-wavelet mixing, so a working causal chunked-DCT variant with the multi-resolution carry-over could still be new if it demonstrably preserves strict causality while matching FLOPs. |
| 194 | ADJACENT | 2 | 4 | learned-index optimal-transport keys | A learned-index model that predicts each key's rank position under an optimal-transport-style sort is applied to attention-scores so that query-key similarity carries an explicit transport cost rather than raw bilinear dot-products. | 2607.18027; 2509.23436 | The specific twist of training a learned-index-style monotone rank predictor per token (rather than a raw distance in embedding/position space) to derive the transport coordinate, and combining it with an OT-cost interpretation rather than a generic learned distance penalty, is not exactly instantiated in the closest papers. |
| 195 | ADJACENT | 2 | 4 | conformal expert-set routing | conformal prediction applied to the moe-router so that top-k selection is replaced by a variable-size expert set with a distribution-free coverage guarantee on containing the correct expert | 2403.07652; 2608.04401 | None of these papers derive a nonconformity-score threshold from held-out calibration data to give a formal, distribution-free 1-alpha coverage guarantee on containing the oracle-optimal expert (nor do they flag tokens where no small set can achieve the target coverage), so a rigorous conformal calibration step with certified coverage semantics would still be a genuine addition over these heuristic dynamic-k methods. |
| 196 | ADJACENT | 3 | 2 | energy-descent expert selection | energy-based refinement applied to the moe-router so that expert-combination weights are found by a few steps of gradient descent on a learned joint energy over (token, expert-subset) rather than by a single softmax-and-threshold pass | 2606.01062; 2605.08322 | The specific mechanism of a joint energy function E(h_token, w) over continuous relaxed expert-inclusion vectors, optimized by K explicit gradient/mean-field descent steps at inference (an iterative denoising-like rewrite of the routing weights themselves, trained with a contrastive real-vs-corrupted-subset loss) is not instantiated by any of these papers, which use either static structural aggregation, message-passing among already-selected experts, or differentiable-but-single-pass gates; a genuine implementation of iterative energy descent for expert-subset selection itself would still be new. |
| 197 | ADJACENT | 2 | 4 | Kalman-fused router confidence | Kalman filtering applied to the moe-router so that each token's router score is fused with a running per-expert reliability estimate (a Kalman state tracking expert accuracy over recent tokens) weighted by their respective uncertainties, instead of used raw | 2406.02969; 2509.23830 | A genuinely new contribution would need a closed-form per-expert Kalman state (mean+variance) fused via inverse-variance weighting directly into the top-k router logits inside a transformer MoE layer, tracking online accuracy drift under distribution shift — the MoE-F paper does the analogous filtering but at the level of combining whole LLM predictions via HMM/Bayesian filtering rather than modifying token-level router logits pre-topk, so a demonstration that this specific in-router Kalman fusion changes expert selection and improves robustness to drift within a single trained MoE layer would still be a distinct, unclaimed result. |
| 198 | ADJACENT | 2 | 4 | self-paced expert admission curriculum | Self-paced curriculum ordering applied to moe-router training so that load balance emerges from delaying hard-to-route tokens rather than from an auxiliary loss term. | 2310.07188; 2212.07571 | None of the found papers use a self-paced, confidence-thresholded masking schedule specifically to make load balance an emergent side-effect that removes the need for an auxiliary balancing loss; that specific mechanism-purpose pairing (confidence-gated update masking as an implicit balancer replacing aux loss) is not directly shown, though the curriculum+MoE combination itself is well trodden. |
| 199 | ADJACENT | 2 | 4 | PID-controlled residual norm | Feedback-control applied to the residual-stream so that each block's write gain is driven by a PID loop tracking a target activation-norm error rather than being a fixed scalar per depth. | 2605.18832; 2606.16112 | A genuine PID loop with explicit proportional-integral-derivative terms tracking a target norm (not just an adaptive/Kalman gain or magnitude-direction split) that trains only 3 scalar gains per layer while leaving the rest of the model architecture untouched could still be a distinct, narrower mechanism than BFT's Kalman-update framing, but the core capability (feedback-driven, error-tracking dynamic residual-write scaling) is already claimed by the Bayesian Filtering Transformer paper. |
| 200 | ADJACENT | 2 | 4 | Contrastive position embedding structuring | A contrastive objective applied to positional encoding so that position embeddings are structured by learned content-position pairing rather than by a fixed a priori function of the index alone. | 2501.00712; 2405.14722 | The specific mechanism of an explicit contrastive (positive/negative sampled) loss pairing content embeddings with position embeddings to shape the position table's geometry is not demonstrated in any of these papers, so that concrete contrastive-training recipe for the position table would still be a novel instantiation even though the broader goal of content-adaptive positional encoding is well-trodden. |
| 201 | ADJACENT | 2 | 4 | content-predicted positional offsets | a small learned-index submodule applied to positional-encoding so that position signals can be warped by content-dependent predicted offsets rather than a fixed lookup | 2507.23083; 2511.17388 | A twist restricted to a tiny jointly-trained MLP predicting bounded additive offsets to the integer index itself (rather than directly to rotation angles/frequencies) applied uniformly across all heads from a pooled hidden-state summary could still be marginally different, but CARoPE and Selective RoPE already cover input-dependent warping of RoPE angles at every layer for the same compression/expansion-of-effective-spacing purpose. |
| 202 | ADJACENT | 2 | 4 | proper-scoring calibrated position gate | a small auxiliary head that predicts relative-position-dependent attention-decay confidence, trained with a proper scoring rule against realized next-token loss at each distance, applied to positional-encoding so that the model down-weights positions whose encoding is known (from training-time evidence) to be miscalibrated at test lengths. | 2605.10414; 2108.12409 | The specific twist of training the gate via an explicit proper scoring rule (CRPS/Brier) against realized next-token correctness bucketed by distance, yielding a calibrated confidence signal rather than a heuristically learned or fixed decay, is not demonstrated in GAPE, ALiBi, or AdaRoPE and could be the differentiator if calibration-quality (not just accuracy) is the deliverable. |
| 203 | ADJACENT | 2 | 4 | Posterior-averaged RoPE bases | Bayesian model averaging applied to positional-encoding so that extrapolation to unseen lengths is handled by mixing several rotary base frequencies weighted by their calibrated held-out likelihood instead of committing to one fixed base. | 2505.22842; 2505.12423 | The specific mechanism of maintaining K parallel RoPE-base hypotheses and mixing their attention outputs/logits via an online exponentially-decayed calibration-buffer likelihood (explicit Bayesian model averaging over discrete rotary bases, not a single learned prior or continuous scaling function) is not directly implemented in any of these papers, so a demonstration of smooth, predictable mixture-weight shift and non-catastrophic collapse could still be a genuine contribution. |
| 204 | ADJACENT | 2 | 4 | Importance-weighted position reweighting | Importance sampling applied to positional-encoding so that attention scores computed under the training-length position distribution are reweighted, at inference, to estimate what scores would have been under a target long-context position distribution, without retraining. | 2502.02659; 2608.10021 | The specific formalization as an importance-sampling ratio p_target(d)/p_train(d) with a proof of convergence to the exact in-distribution answer as effective sample overlap improves, plus an explicit effective-sample-size diagnostic for flagging unreliable extrapolation, is not present in GALI or the survey, which use heuristic interpolation/outlier removal without a probabilistic correctness guarantee or degeneracy diagnostic. |
| 205 | ADJACENT | 3 | 2 | distilled dynamic re-merging teacher | Online self-distillation applied to the tokeniser boundary predictor so that a fast student network can propose per-span merge/split decisions at inference that track a slow teacher's byte-level loss landscape. | 2411.18553; 2506.01084 | A genuinely new contribution would need the EMA-teacher-vs-fast-student distillation specifically targeting byte-level merge/split boundary decisions tied to the teacher's own per-token loss landscape (not vocabulary/compression-based adaptivity and not representation-level self-distillation), which none of the closest papers implement. |
| 206 | ADJACENT | 3 | 2 | spectral byte-span merging | A frequency-domain locality transform applied to the raw byte stream so that merge boundaries are chosen dynamically per input instead of fixed at training time. | 2412.09871; 2603.03583 | The specific spectral/frequency-domain locality criterion (short-time DCT/wavelet smoothness score) as the boundary-decision signal, as opposed to entropy or coding-rate criteria, is not directly shown in these papers, so a proposal isolating that mechanism's effect could still be a genuine (if narrow) contribution. |
| 207 | ADJACENT | 3 | 2 | information-bottleneck adaptive chunking | An information-bottleneck objective that explicitly trades off compression of the byte stream against prediction of the next token, applied to the tokeniser, so that chunk boundaries are placed to keep only what predicts the target and discard the rest per-span. | 2603.06351; 2501.03120 | The explicit dual mutual-information IB objective (I(Z;Y) - beta*I(Z;X)) applied specifically to byte-level LM tokenizer boundary placement, with a Gumbel-softmax boundary scorer trained jointly with a from-scratch nano LM and an inference-time beta dial, is not demonstrated by any of these; the closest works use content/complexity predictors or router losses rather than a stated rate term against raw bytes for next-token prediction, so a rigorous IB formalization at the tokenizer for language modeling would still be a distinguishable contribution if it makes the rate-distortion tradeoff explicit and provably tied to I(Z;X) rather than an implicit token-count penalty. |
| 208 | ADJACENT | 2 | 4 | importance-sampled logit recalibration | Importance sampling applied to the output-head so that samples drawn from the miscalibrated post-training distribution are reweighted to estimate expectations (e.g. token frequencies, downstream metrics) under the true pretraining-calibrated distribution, without ever renormalizing or retraining the head. | 2507.15507; 2607.04728 | A twist could survive if the proposal specifically targets pure post-hoc estimation of held-out evaluation metrics (perplexity, entropy, reward-model score) from a frozen preference-tuned model using exact base-vs-tuned per-token probability ratios, never touching gradients or training objectives, which none of the closest papers do since they all fold importance weights into training/model-correction rather than treating it as a diagnostic estimator with explicit ESS monitoring at eval time only. |
| 209 | ADJACENT | 2 | 4 | Mixture-head post-hoc recalibration | Mixture-density heads applied to the output-head so that post-preference-training logits recover calibrated probability mass instead of a single sharpened point estimate. | 2606.30037; 2604.08627 | The proposal's specific twist—grafting a K-component mixture-density head post-hoc onto preference-trained LLM next-token logits to recover multimodal calibration collapsed by RLHF/DPO—has not been directly shown; ETN establishes the post-hoc frozen-base LLM logit-recalibration setup but with a Dirichlet/evidential parameterization rather than an explicit mixture-of-softmaxes, so demonstrating that mixture-density heads specifically recover multimodal token-level uncertainty lost to preference-training collapse (not just scalar confidence calibration) would still be a novel contribution. |
| 210 | ADJACENT | 2 | 4 | EMA-stabilized logit trajectory sampler | EMA-averaging applied to the sampler so that, within a short lookahead window of draft tokens produced by repeated re-forward-passes with perturbed inputs, the exponential moving average of the resulting logit trajectory (rather than the single final logit vector) is used to pick the committed token, still emitting exactly one token at a time. | 2607.03236; 2412.18299 | The specific combination of noised repeated re-forward-passes (embedding dropout perturbations or KV variants) generating a logit trajectory, combined with true EMA (not simple averaging) with a tuned decay beta, applied specifically to standard autoregressive token-by-token sampling for variance reduction/calibration (not diffusion commit-gating, not multi-prompt ensembling) is not exactly demonstrated, but the core idea of EMA-over-logit-trajectory at the sampler is already used in TACG and simple ensembling-to-pick-one-token is in M-Ped, making the delta narrow. |
| 211 | ADJACENT | 3 | 2 | Spectral block decoding | A frequency-basis transform (e.g. short-time DCT/FFT over token-embedding trajectories) applied to the sampler so that a block of future token representations can be generated by extrapolating low-frequency spectral coefficients in parallel, instead of one autoregressive step per token | 2503.07630; 2501.09747 | A demonstration that spectral-coefficient extrapolation (versus direct parallel token/embedding prediction as in ParallelSpec, or Fourier mixing as in FourierNAT) yields higher acceptance rates or cheaper draft heads specifically within a verify-and-truncate speculative decoding loop for pretrained autoregressive LMs would be a genuine new contribution. |
| 212 | ADJACENT | 3 | 2 | checksum-verified token repair | error-correcting-codes applied to the sampler so that redundant parity tokens are embedded in the generation stream, letting corrupted or low-confidence committed tokens be detected and repaired a few steps later instead of staying wrong forever | 2605.26436; 2608.00531 | Yes: none of the closest works train a dedicated auxiliary parity/syndrome-prediction head (a compressed hash/low-rank checksum of a token block) jointly with the LM and use a mismatch between predicted and recomputed syndrome as the trigger signal — they all rely on confidence/uncertainty or external verifiers rather than an ECC-style in-band cryptographic-style checksum, so the specific error-correcting-code mechanism at the sampler is not yet demonstrated. |
| 213 | ADJACENT | 2 | 4 | Delta-rule per-token temperature memory | A delta-rule associative memory keyed on hidden state applied to the sampler so that temperature/top-k become a learned per-position readout instead of a fixed global knob. | 2603.18428; 2309.02772 | Yes: none of these use a trained, continuously overwritten (erase-then-write/delta-rule) associative memory keyed on hidden states to store and retrieve per-position dispersion targets; that specific mechanism (small trainable key-value memory bank updated online via delta rule, read via attention) at the sampler is not demonstrated by any close paper, only heuristic entropy functions or RL policies. |
| 214 | ADJACENT | 2 | 4 | Speculative temperature branch predictor | Branch-prediction applied to the sampler so that the decoder predicts, from recent token-type history, whether the upcoming position is a 'high-certainty' or 'high-entropy' branch point and pre-selects a temperature/top-k regime before computing the logits, correcting on misprediction. | 2510.26697; 2603.17942 | The specific twist of a lightweight offline classifier that predicts a discrete temperature/top-k regime from token-history features before logits are used, explicitly logging a 'misprediction rate' analogous to CPU branch mispredictions, is not demonstrated in AutoDeco (which jointly predicts continuous temperature/top-p via learned heads trained end-to-end with the model) nor in the speculative-decoding papers (which predict tokens, not sampling regimes); a proposal emphasizing the branch-prediction-style discrete regime classification and misprediction diagnostic as a separate, model-untouched auxiliary module could still be novel enough to survive as a follow-up rather than a duplicate, if it is framed as clearly distinct from joint decoding-head learning already published in 2510.26697. |
| 215 | ADJACENT | 2 | 4 | importance-corrected per-token sampling | Importance-sampling reweighting applied to the sampler so that per-position temperature/top-k choices can be made cheaply from a proposal distribution while an exact correction restores the true model distribution on demand. | 2604.16453; 2212.06276 | The genuinely new part would be the periodic self-normalized-importance-sampling buffer resampling specifically calibrated to correct drift from an aggressively varying per-position top-k/temperature proposal with a stored per-token receipt log for on-demand exact recovery, rather than reward-guided steering or a single global exactness kernel — but this is a narrow procedural twist on an established SMC-for-decoding pattern. |
| 216 | ADJACENT | 3 | 2 | Learned-index prefill position router | A learned-index model applied to prefill so that token positions are routed directly to their approximate KV-cache slot via a small predictor instead of via full quadratic self-attention over all prior prompt tokens. | 2406.09827; 2609.01925 | A trained small MLP that predicts a concrete offset-range window per token (learned-index style, analogous to Kraska et al.) plus an explicit realized-mass verification threshold triggering fallback to exact attention is a more specific mechanism than HiP's tree-search top-k estimation or CRISP's structural-proxy routing, so a paper demonstrating this exact train-once offset-predictor-with-verification-fallback design would still be new, but the core capability (sub-quadratic prefill by exploiting attention locality to skip full attention) is already occupied by HiP and CRISP. |
| 217 | ADJACENT | 3 | 2 | Energy-descent draft refinement | Energy-based refinement applied to speculative drafting so that the draft model produces a block of candidate tokens by descending a learned energy landscape over the block, rather than sampling once autoregressively, before the target verifies the block. | 2608.02438; 2608.20530 | A genuinely new twist would be explicit iterative energy-descent (gradient or Gibbs coordinate steps at inference time) trained via NCE against target log-probs, as opposed to the single learned refiner/correlation pass or latent-mixture reparameterization used by xPress, LiLiCorr, and DBLAST — but this is a mechanism-level distinction on the same component for the same purpose, not a new capability. |
| 218 | ADJACENT | 2 | 4 | conformal adaptive draft length | conformal prediction applied to the draft model's per-step confidence sequence so that draft length is chosen to guarantee a target expected acceptance rate rather than fixed by a hyperparameter | 2405.19715; 2506.01979 | Yes: SpecDec++ uses a trained MDP-derived threshold on a learned acceptance-prediction head (a new trained component), whereas the proposal uses distribution-free conformal calibration directly on raw draft-model confidence with an explicit finite-sample coverage guarantee on acceptance rate and periodic non-parametric recalibration—if that formal coverage guarantee and lack of any auxiliary trained head is the emphasized twist, it is a meaningfully different mechanism class (distribution-free quantile calibration vs. learned threshold policy). |
| 219 | ADJACENT | 2 | 4 | proper-scoring-rule draft stopping | proper scoring rules applied to the draft model's stopping decision so that the draft's self-reported confidence is trained to be a calibrated predictor of the target's actual accept/reject outcome | 2508.15371; 2605.02888 | A twist could survive if the proposal specifically proves calibration (not just correlation/ranking) via a strict proper scoring rule (Brier/log score) trained on labeled accept/reject outcomes with theoretical calibration guarantees, distinguishing it from the heuristic entropy/margin or acceptance-length-reweighting objectives used in the closest papers, but none of the cited works explicitly frame the auxiliary head's objective as a proper scoring rule for calibrated P(accept), so this specific formalization is not clearly TAKEN. |
| 220 | ADJACENT | 2 | 4 | PID-controlled speculative draft length | Feedback-control applied to speculative-drafting so that draft length is continuously adjusted by a PID loop driving the observed acceptance-rate error to a target setpoint. | 2504.00030; 2405.19715 | The specific twist of a closed-loop PID controller that explicitly drives observed acceptance rate to a fixed target setpoint via integral/derivative error terms (rather than a heuristic switch, MDP threshold, or entropy bound) is not demonstrated in these papers, so a PID-specific formulation with formal setpoint tracking and offline-tuned gains could still be a novel contribution if framed as such. |
| 221 | ADJACENT | 2 | 4 | ARC-scheduled draft length | A cache-replacement-arc-style dual-list policy applied to speculative-drafting so that draft length is chosen per-step by balancing recent accept-rate (recency) against long-run accept-rate (frequency) rather than a single fixed or purely-instantaneous threshold. | 2504.00030; 2607.03876 | The specific ARC-style dual-list (short-window recency vs long-run frequency) weighting mechanism with an explicit meta-controller that shifts weight based on which estimate predicted better last round is a more structured non-stationarity-tracking scheme than GammaTune's simple heuristic switching, so a rigorous ablation showing gains specifically under abrupt easy-then-hard regime shifts (which single-EMA/entropy-threshold methods provably lag on) could still be a narrow but genuine contribution. |
| 222 | ADJACENT | 2 | 4 | Importance-weighted draft-length calibration | Importance-sampling reweighting applied to speculative-drafting so that the draft model's per-token confidence is recalibrated against the true target-acceptance distribution before being used to choose draft length, correcting for the draft's miscalibration under distribution shift. | 2406.16858; 2504.00030 | A twist could survive if the proposal's isotonic-regression map built specifically from logged importance weights p_target/q_draft is shown to give a provably unbiased acceptance-probability estimator for setting draft length, unlike GammaTune's heuristic acceptance-rate tracking or EAGLE-2's uncorrected calibration claim, but this is a narrow theoretical refinement of existing acceptance-rate-based length calibration rather than a new capability. |
| 223 | ADJACENT | 3 | 2 | energy-descent block iteration | energy-based-refinement applied to the layer-schedule so that a block is re-applied a variable number of times, each step taking a gradient step on a learned scalar energy of its own output, until the energy stops decreasing or a step budget is hit | 2502.11646; 2607.20519 | The proposal's specific twist—an explicit scalar contrastive energy head whose gradient with respect to the block's own output drives a variable-length per-instance refinement loop with an early-stopping threshold on energy delta, trained jointly with LM loss at matched FLOPs—is not clearly instantiated in any single paper, since Hyper-SET's recurrence isn't input-adaptive-halted via a learned energy head and the looped-transformer halting work uses probability gates rather than energy-gradient descent on the representation. |
| 224 | ADJACENT | 2 | 4 | Product-quantised FFN key memory | Product quantisation applied to the FFN's key vectors so that per-token key scoring is done against compact codebook centroids instead of the full dense key matrix, cutting the score-computation cost from O(d_ff) dense dot-products to O(m*k) codebook lookups. | 2512.13751; 2503.15798 | The specific twist of applying classic product-quantisation (multi-subspace k-means codebooks with ADC reconstruction, m*k lookups) directly to the dense FFN key matrix trained end-to-end from scratch, as opposed to product-key memory's disjoint two-subspace Cartesian product used mainly as an added memory layer, is not clearly demonstrated in these papers and could still be a distinguishable contribution if PQ's compositional multi-way structure and residual coding are shown to give capacity/quality advantages over product-key memory's simpler two-way key composition. |
| 225 | ADJACENT | 3 | 2 | kNN retrieval FFN memory | kNN-retrieval applied to the FFN so that the key-value lookup is replaced by nearest-neighbor search over an external memory bank instead of scoring every stored key. | 2501.00823; 2503.15798 | The specific mechanism of exact top-k nearest-neighbor search over a frozen/refreshed (key,value) activation bank feeding attention-like mixing into the FFN down-projection, trained/matched at FLOPs parity as in Memorizing Transformers/kNN-LM but applied inside the FFN sublayer rather than at the output layer or via cross-attention, is not directly shown in these papers. |
| 226 | ADJACENT | 3 | 2 | Sparse dictionary optimiser moments | Sparse-coding applied to per-parameter optimiser moments so that each module's second-moment tensor is represented as a sparse combination of a small shared per-module-type dictionary of atoms, giving the optimiser state module-level structure instead of pure per-parameter independence. | 2607.19058; 2405.18144 | A genuinely new contribution would be the learned, online-updated sparse dictionary (matching-pursuit/proximal coding) shared across parameters of a module type that lets brand-new or rarely-updated parameters inherit non-zero second-moment estimates immediately from the dictionary rather than only compressing existing statistics via a fixed transform or factorization—none of the closest papers address this cold-start inheritance capability via sparse coding. |
| 227 | ADJACENT | 3 | 2 | Spectral optimiser-moment sharing | Spectral-transform applied to per-parameter optimiser moments so that second-moment statistics for weight matrices are tracked in a frequency basis (2D DCT/FFT over the weight matrix's row/column index) where long-range correlations between distant parameters in the same module become local and can be captured by a small number of low-frequency coefficients. | 2501.07237; 2509.03378 | A genuinely new contribution would need to show that a 2D-DCT/FFT basis (versus Haar wavelets or Kronecker/eigenbasis) with a blended elementwise+low-frequency reconstruction gives a real accuracy/curvature benefit distinct from compression-motivated wavelet transforms and from Kronecker-factored second-order methods, e.g. demonstrating captured long-range row/column correlations that Shampoo/GWT do not capture. |
| 228 | ADJACENT | 2 | 4 | Kalman-fused learning rate | Kalman filtering applied to the lr-schedule so that the learning rate is set by fusing a predicted trajectory (from the schedule's prior curve) with a noisy measurement (the observed loss/gradient-noise) in proportion to their estimated uncertainties, rather than following either alone. | 1807.08534; 2508.02660 | The specific mechanism of running an explicit 1D Kalman update that fuses a prior schedule curve (process model) with a windowed loss/gradient-noise measurement, weighted by estimated process/measurement variances, to produce the per-step LR appears novel; no paper implements this precise dual-uncertainty fusion for the LR schedule itself, though the Kalman-for-optimization and closed-loop LR-control literatures are close. |
| 229 | ADJACENT | 2 | 4 | predictive-coding learning-rate feedback | predictive-coding-style hierarchical error feedback applied to the lr-schedule so that the learning rate is set by a running estimate of loss-prediction error rather than a fixed curve | 2605.20005; 2107.03331 | The specific twist of maintaining an explicit two-scalar (level+slope) EMA forecaster of the loss and using the loss-prediction error itself (predicted vs actual loss) as a bounded multiplicative correction on a base LR curve is not exactly instantiated in KOALA (which models parameter/gradient dynamics via Kalman filtering) or FINCH (which reacts to raw loss magnitude, not forecast error); a paper isolating forecast-error-driven, hierarchical predictive-coding-style correction for general pretraining schedules would still be distinguishable. |
| 230 | ADJACENT | 2 | 4 | safety-set-bounded LR control | control-barrier-function constraints applied to the lr-schedule so that the learning rate is adjusted online to guarantee the loss (or its gradient-noise proxy) never leaves a pre-declared safety envelope, rather than following a fixed curve. | 2607.13348; 2407.06183 | The specific CBF formalism guaranteeing forward-invariance of a declared safety envelope (h_t staying above zero via the discrete CBF inequality) rather than just a stability-proved or curvature-aware feedback law is not demonstrated for the lr-schedule component in any of these papers; a genuine twist would be the explicit safety-set/barrier-certificate framing with formal never-leaves-envelope guarantees applied to lr, rather than general closed-loop stability. |
| 231 | ADJACENT | 2 | 4 | Loss-prioritised replay buffer | Experience-replay applied to data-order so that high-loss examples are revisited on a prioritised schedule instead of being seen exactly once. | 2602.17066; 2604.16918 | A genuine ring-buffer over fresh pretraining shards with running-average per-example loss priority^alpha resampling, matched FLOPs budget, and no auxiliary predictor network, applied specifically to single-pass LM pretraining data-order (rather than RL rollouts, continual-learning task memory, or a proxy loss predictor) has not been directly shown; PBS is the closest but avoids actual per-example loss tracking and buffer replay, which is exactly the mechanism claimed here. |
| 232 | ADJACENT | 2 | 4 | self-distillation curriculum control | online-self-distillation applied to data-order so that a slow EMA teacher's per-domain loss gap against the fast student is used to reweight the sampling mixture toward domains the student is currently failing relative to its own recent past | 2411.14318; 2606.25178 | The specific mechanism of using an EMA copy of the same model (rather than a scaling-law target or advantage/transfer signal) and its per-domain loss gap against the live student as the literal softmax reweighting signal for pretraining data-mixture control, retrained from scratch at matched FLOPs, is not exactly instantiated in Velocitune or TAC, so a paper explicitly ablating EMA-teacher-gap-driven mixture reweighting against these nearest baselines would still be a meaningful, non-trivial follow-up rather than a duplicate. |
| 233 | ADJACENT | 2 | 4 | ARC-policy data admission | Adaptive-replacement-cache (ARC) balancing of recency and frequency applied to data-order selection so that the training mixture is chosen online from a bounded working set instead of fixed in advance. | 1503.07624; 2009.09206 | No paper applies ARC's specific two-list recency/frequency controller with hit/miss-driven partition parameter p to select which training examples stay in a live pool for LM pretraining data order; the closest analog (ARC itself) is a generic cache algorithm and the closest LM data-ordering work (GraB, Velocitune) uses different mechanisms (gradient balancing, velocity-based reweighting) rather than an ARC-style adaptive cache replacement policy, so the specific mechanism-to-purpose mapping remains unclaimed. |
| 234 | ADJACENT | 2 | 4 | Hebbian error-curriculum fast weights | Hebbian outer-product fast weights applied to data-order so that the curriculum reacts to the model's own recent loss instead of being fixed in advance | 2411.14318; 2601.03725 | The specific mechanism of a decaying Hebbian outer-product associative memory over engineered feature/context embeddings (mapping feature patterns to recent loss, then scoring future candidates by matrix-vector readout) is a distinct implementation not described in these works, which use scalar velocity trackers, entropy estimators, or gradient-discrepancy balancing instead of an associative key-value memory; this could survive as a genuinely different mechanism for the same online curriculum capability. |
| 235 | ADJACENT | 2 | 4 | PID-controlled attention span | feedback-control applied to attention-topology so that a PID controller drives the per-head attention span (window width, sink count) to track a target error signal (e.g. local loss or entropy) instead of using a fixed pre-set span | 2601.15305; 2302.10377 | A genuine PID closed-loop control law (explicit error-tracking with proportional-integral-derivative gains against a measured entropy/loss error, tuned per-head via grid search rather than learned end-to-end) is a different control mechanism than the learned/gated adaptive controllers in GSA or the dynamic-span network in the speech paper, so a strict claim about closed-loop feedback control (vs. learned/heuristic adaptation) could still be a narrow but real twist. |
| 236 | ADJACENT | 3 | 2 | MDL-selected attention mask per sequence | minimum-description-length applied to attention-topology so that the sparsity pattern used for a given input is the one that minimizes total coding cost (model bits + residual bits) rather than a fixed a-priori mask | 2607.21752; 2510.24606 | The explicit MDL formulation—computing an actual two-part code (log topology-code + cross-entropy bits under each candidate mask on a held-out subsegment) and choosing the minimizer, over a small enumerable menu of named topologies (dense/windowed/sink/strided) shared-trained at matched budget—is a more principled, exactly-computed criterion than compression-ratio heuristics or learned importance scores, so a rigorous MDL-vs-heuristic comparison could still be a genuine contribution. |
| 237 | ADJACENT | 2 | 4 | Speculative step with rollback check | branch-prediction applied to the gradient step so that a cheap predicted update is applied speculatively and only fully computed/committed if a low-cost verification confirms loss decrease | 1905.01549; 2603.17942 | The specific closed-loop speculative-execution structure—cheap momentum/Nystrom-predicted gradient applied speculatively, verified on a small probe batch, and rolled back to exact computation on misprediction to amortize backward-pass cost below one full pass per step—is not shown in these papers; CG predict-and-recompute targets communication-hiding in a different algorithm class and lacks the probe-based verify/rollback and skip-backward capability. |
| 238 | ADJACENT | 3 | 2 | sleep-phase checkpoint consolidation | Sleep-replay-consolidation applied to checkpoints so that the discarded inter-checkpoint trajectory is periodically replayed offline and compressed into the next checkpoint rather than being thrown away. | 2210.06458; 2508.21296 | The proposal's specific twist — periodically pausing ordinary (non-continual, single-task) training to replay the last N minibatches and KD-consolidate against an ensemble of the last few live checkpoints so each checkpoint itself summarizes a trajectory window, rather than solving catastrophic forgetting across tasks or producing a separate compressed student — is not demonstrated in these papers, though it is very close to checkpoint-ensemble KD and wake-sleep continual learning mechanisms. |
| 239 | ADJACENT | 2 | 3 | Byzantine-agreement head consensus | Consensus-protocol voting rounds applied to attention-scores so that per-token mixing weights reflect cross-head agreement rather than independent softmaxes. | 2108.01377; 2105.14850 | The specific pairwise cosine-overlap voting protocol with explicit majority-agreement fraction scaling (rather than a single learned importance/gate score or latent variational interaction) is a distinguishable mechanism if it demonstrably captures corroboration-based down-weighting that differs functionally from importance gating or variational head interaction. |
| 240 | ADJACENT | 2 | 3 | Hopfield draft-block retrieval | Hopfield energy-descent retrieval over a memory bank of stored n-gram/hidden-state patterns applied to the sampler so that a block of draft tokens can be produced in one associative-recall step instead of one forward pass per token | 2311.08252; 2502.15572 | A genuine twist would be showing that Hopfield energy-descent convergence (multi-step, non-causal, iterative denoising toward an attractor) produces materially better/faster multi-token draft blocks than existing dense/sparse nearest-neighbor retrieval drafters (REST/DReSD), e.g. via provable capacity/robustness advantages of the attractor dynamics rather than just swapping the retrieval index. |
| 241 | ADJACENT | 2 | 3 | Kalman-fused draft correction | Kalman-style fusion of a fast low-fidelity token-block predictor (prediction) with a slower exact forward pass (measurement), weighted by their calibrated uncertainties, applied to the sampler so that multiple tokens can be committed per exact forward pass without waiting for full sequential confirmation | 2603.01581; 2601.06100 | A twist restricted to fusing draft-model logits with exact-model logits at the sampler for generic autoregressive LLM decoding (not robot kinematics, not model-internal state/memory, not ICL adaptation), with a trained variance head calibrating the Kalman gain per-token, is not exactly covered by KERV's kinematic-domain action correction. |
| 242 | ADJACENT | 2 | 3 | Predictive-coding residual sampler | A hierarchical predictive-coding loop that propagates only prediction errors between a fast shallow drafting stack and a slow deep verifying stack, applied to the sampler so that most decoding steps only need a cheap error-update rather than a full deep forward pass | 2603.04514; 2605.01106 | A genuinely new contribution would need the error-predictor to be trained specifically to regress shallow-vs-full logit divergence (a predictive-coding residual signal) and shown to outperform confidence-based or fixed-threshold early-exit/self-speculative baselines at equal compute, rather than just being a relabeled gating mechanism. |
| 243 | ADJACENT | 2 | 3 | evolutionary beam mutation decoding | evolutionary-population applied to the sampler so that a population of candidate continuations is mutated and selected across generations, allowing a previously committed span to be replaced by a mutated descendant that scores better under a fitness function | 2411.07112; 2311.03758 | A genuine evolutionary-population loop that maintains a population across explicit generations, mutates arbitrary spans of already-committed autoregressive output via resampling, and selects survivors by a fitness function—applied to standard autoregressive LLM decoding rather than code-specific backtracking, diffusion order-search, or offline reranking—is not directly instantiated by these papers, so a careful comparison and demonstration against ROCODE-style single-shot backtracking and best-of-N/beam baselines could still be a new empirical contribution. |
| 244 | ADJACENT | 2 | 3 | multi-draft consensus commit sampler | consensus-protocols applied to the sampler so that a token is only committed once multiple independent stochastic drafts (from perturbed decoding rounds of the same model) agree via a voting round, otherwise the position is re-drawn, giving each commit an explicit agreement quorum instead of a single irrevocable sample | 2502.18779; 2606.06695 | The specific twist of running the same frozen model under different temperature/dropout seeds (no separate draft model) purely as an agreement quorum with an explicit re-draw-on-disagreement rule and entropy-gated cost amortization is not quite the framing of multi-draft speculative decoding (which uses a separate drafter model and target verification for distributional fidelity, not per-token quorum confidence); this narrower framing could still be new if benchmarked as a calibrated confidence/abstention mechanism rather than a speedup technique. |
| 245 | ADJACENT | 2 | 3 | Streaming teacher-student sampling controller | Online self-distillation between a slow (EMA) copy and a fast copy of a small controller head applied to the sampler so that per-position temperature/top-k adapt to the local statistics of the stream the model is currently generating. | 2603.18428; 2606.13233 | The specific mean-teacher (EMA slow copy vs fast copy trained by online self-distillation KL) controller architecture applied purely to sampler hyperparameters is not explicitly instantiated in these papers, so a twist emphasizing the distillation-based dual-copy mechanism (rather than entropy heuristics or RL policy) could still be novel, but the core capability of online per-step sampler recalibration without labels is already covered. |
| 246 | ADJACENT | 2 | 3 | submodular prefill block selection | Greedy submodular-maximisation applied to prefill so that the attention computation over the prompt uses a guaranteed near-optimal sparse subset of key/value blocks instead of full quadratic attention. | 2608.01169; 2602.08722 | The proposal's twist is porting the exact facility-location submodular greedy algorithm with its formal (1-1/e) coverage guarantee (already used for video/token compression) specifically onto prefill KV-block attention selection, which none of the prefill sparse-attention papers do (they use similarity/oracle/learned scoring without a submodular optimality certificate) — this is a narrow but plausible gap. |
| 247 | ADJACENT | 2 | 3 | Arithmetic-coding-gated prefill skip | Arithmetic coding applied to prefill so that prompt spans whose model-predicted code length is near-zero (i.e. highly predictable continuations of already-cached prefixes) skip full attention computation and reuse a compressed cache entry instead. | 2606.13126; 2606.10537 | The specific gating mechanism—using the model's own next-token distribution fed through an actual arithmetic coder to measure per-span code length as the compressibility criterion for splicing cached KV—is not what any of these papers do (they use hashing of identical prefixes, attention-based relevance, or importance probing instead of an entropy/code-length threshold), so a paper demonstrating that this specific online entropy-coding signal yields better precision/recall for cache-splice decisions than existing heuristics would still be a new contribution. |
| 248 | ADJACENT | 2 | 3 | Self-paced draft-length curriculum | Self-paced curriculum scheduling applied to speculative-drafting so that the draft length used during a training/calibration phase is ordered by the draft model's own current loss on upcoming spans, letting the draft-length policy learn to predict its own future reliability before deployment. | 2411.18462; 2608.30135 | The specific self-paced curriculum ordering of training examples by the draft model's own current loss (mastering easy spans before hard ones) to reduce deployment-time variance is not tested by any of these papers, which either use training-free entropy signals or standard i.i.d. training of the auxiliary head—so demonstrating that curriculum ordering specifically lowers variance versus i.i.d. training of the same head would be a genuine addition. |
| 249 | ADJACENT | 2 | 3 | Mixture-density draft-length head | A mixture-density output applied to the speculative-drafting confidence signal so that draft length is chosen from a predicted mixture over accept-run-length rather than a point-estimate confidence threshold, capturing multimodal reliability (e.g. bimodal 'easy vs. hard token' regimes). | 2605.02888; 2511.02017 | The specific mechanism of modeling accept-run-length as an explicit mixture density (capturing bimodal/heavy-tailed reliability rather than a single scalar or bandit-selected value) and choosing k via a quantile of that mixture is not shown in any candidate paper, which all use point-estimate regressors, thresholds, or bandit/heuristic policies. |
| 250 | ADJACENT | 2 | 3 | traced hot-path layer skipping | JIT hot-path tracing applied to the layer-schedule so that recurring token/context patterns get a cached, specialised (shorter) block sequence instead of running the full fixed depth. | 2608.09444; 2606.09514 | The specific mechanism of hashing a k-token context signature into a persistent trace table that memoizes and replays previously-verified shorter layer subsets for recurring/repeated spans (n-grams, boilerplate, copy spans) — as opposed to per-token difficulty/confidence routing or temporal reuse of the immediately preceding token's state — is not directly shown in any single paper, so a proposal emphasizing content-addressed cross-occurrence trace reuse (not just adjacent-step or confidence-based adaptive depth) could still be novel. |
| 251 | ADJACENT | 2 | 3 | self-paced depth curriculum | Self-paced curriculum ordering applied to the layer-schedule so that the number of blocks a token is routed through is set by the model's own current per-token loss signal, allocated at training time rather than fixed architecture time. | 2603.01914; 2404.02258 | The proposal's distinguishing twist is using an explicit self-paced curriculum mechanism (an adaptively-updated per-epoch loss threshold that governs the pacing function itself, evolving as a curriculum-learning schedule rather than a differentiable learned gate/router) applied only during training with cached gating vectors reused on next occurrence — none of the closest papers implement pacing-function-style curriculum scheduling of depth tied to token re-occurrence rather than a learned per-forward-pass router, so a careful comparison against AdaPonderLM/MoD emphasizing the self-paced scheduling (vs. gated/routed) mechanism could still be novel. |
| 252 | ADJACENT | 2 | 3 | renormalised optimiser moments | Renormalisation-group coarse-graining applied to optimiser-state so that per-layer moment statistics are pooled across a scale hierarchy of parameter blocks instead of kept fully per-parameter. | 2501.07237; 2607.19058 | None of the found papers combine a per-parameter/block-average convex blend with an explicit annealed alpha schedule keyed to parameter activation events (unmasking, LoRA rank growth) for the specific purpose of giving newly active or rarely-updated parameters a warm-started second moment; that activation-triggered inheritance mechanism, framed as RG coarse-graining, appears to be a genuinely new twist relative to the memory-compression or fixed block-factorization purposes seen in the closest papers. |
| 253 | ADJACENT | 2 | 3 | ARC-style recency/frequency LR adaptation | an adaptive-replacement-cache-style recency/frequency balance applied to the lr-schedule so that the effective step size is set by an online balance between a 'recency list' (recent loss volatility) and a 'frequency list' (sustained long-run improvement rate), with the balance parameter itself adapted online | 2506.08419; 2310.11291 | The specific ARC-inherited mechanism—two exponential trackers at different timescales plus a single online-adapted mixing parameter p updated by a hit/predictive-success rule mirroring ARC's ghost-list adjustment—is not explicitly instantiated in any candidate; if that precise dual-tracker/self-tuned-blend-ratio structure (rather than gradient alignment, curvature, or delta-bar-delta signals) is essential and demonstrably different in behavior, it could still be novel. |
| 254 | ADJACENT | 2 | 3 | Prioritized replay for horizon errors | Experience replay applied to the training objective so that training re-visits token positions with high historical multi-horizon prediction error on a prioritized schedule, is applied so that the model allocates more gradient updates to positions whose far-future predictability was previously poor. | 2010.03934; 2007.09569 | The specific combination of an EMA-tracked k-token-ahead auxiliary loss (not TD-error) as the priority signal, applied per (sequence,position) in a from-scratch single-pass LM pretraining run with importance-weighted replay mixed into fresh data, is not directly instantiated in the RL-replay papers (which prioritize by TD-error over transitions/levels, not multi-horizon LM prediction error over token positions) nor in the MTP papers (which add auxiliary heads but no replay/curriculum), so a paper combining exactly the LM-token multi-horizon-error prioritization mechanism at the training-objective/data-sampling component would still be a novel synthesis, though it is a fairly direct compositional adaptation of Prioritized Level Replay's method to a new keying signal and domain. |
| 255 | ADJACENT | 2 | 3 | PID-controlled curriculum scheduler | feedback-control applied to data-order so that a PID controller drives each domain's measured loss-improvement error to a target trajectory by adjusting that domain's sampling weight every evaluation interval | 2310.13260; 2411.14318 | The proposal's twist—explicit per-domain P/I/D terms with integral windup correction and derivative damping applied directly to LM pretraining domain-mixture logits, rather than a single PID controller regularizing one aggregate objective (MoRec) or a velocity-based heuristic without explicit integral/derivative control (Velocitune)—could still be new if it demonstrably fixes steady-state lag/oscillation that these nearest works don't address per-domain. |
| 256 | ADJACENT | 2 | 3 | SMC-weighted micro-batch gradients | sequential-monte-carlo applied to the gradient step so that a small population of candidate updates (from different microbatch splits) is weighted by their measured loss-reduction and resampled, replacing a single noisy gradient with a weighted-average survivor | 2601.22003; 2012.06951 | The specific twist of splitting a single step's batch into K microbatches, taking tentative steps, weighting by loss-reduction on a shared eval batch, and resampling+averaging survivors as an implicit uncertainty estimate on step direction (rather than as a Bayesian posterior sampler or per-example reweighting) is not directly instantiated in any of these, though it is a fairly incremental combination of existing SMC-for-optimization and loss-based reweighting ideas. |
| 257 | ADJACENT | 2 | 3 | evolutionary gradient-step selection | evolutionary-population applied to the gradient step so that several mutated variants of the candidate update are evaluated on a cheap held-out microbatch and the best-scoring survivor is applied, instead of the raw single-sample gradient | 1810.06773; 2605.05284 | The specific twist of generating explicit mutant gradient variants scaled to per-layer norm and selecting/blending via a cheap held-out microbatch fitness check (rather than alternating population evolution steps or step-size search) is a narrower, more surgical mechanism than ESGD's population-based training loop, so a tightly scoped ablation showing this exact per-step mutate-and-select filter catches bad steps that ESGD's periodic evolution phase would miss could still be novel. |
| 258 | ADJACENT | 2 | 3 | quantized checkpoint trajectory codebook | product-quantisation applied to checkpoints so that the full sequence of intermediate weight snapshots between saves can be stored and later retrieved at a fraction of the cost, instead of being thrown away | 2306.11800; 2511.12376 | The specific twist of per-layer-block product quantisation with online k-means-trained codebooks refreshed only at checkpoint intervals, enabling much finer-grained (every N<<checkpoint-interval steps) trajectory reconstruction than these delta/cluster-based approaches, could still be new if it demonstrably operates at a much higher temporal resolution than prior cluster-based or delta-quantization checkpoint compressors. |
| 259 | ADJACENT | 2 | 3 | MDL-adaptive position budgeting | A minimum-description-length criterion applied to the context window so that each position's KV entry is allotted a code-length proportional to how much it shortens the code of future tokens, replacing uniform per-position storage with a two-part code (model + residual). | 2604.25975; 2502.13176 | The specific two-part MDL formalism—jointly optimizing code length of quantized KV entries plus the residual description length of held-out future-token prediction error via a trained Gumbel-softmax gate, yielding a provable per-position bit certificate—is not what any of these papers do; they use attention/mutual-information/energy heuristics or profiling rather than an explicit MDL two-part code with a residual/future-token proof term. |
| 260 | ADJACENT | 2 | 3 | hot-path tracing across shared prompts | jit-tracing applied to serving-batch so that a computation path repeatedly triggered by structurally similar requests (e.g. identical system prompt, repeated few-shot template) is recorded once and specialised, and the specialised trace is shared across all requests that hit that path | 2505.17694; 2511.22333 | The specific twist of an explicit JIT-style trace-and-compile step triggered by a k-hit rolling-window threshold on prefix statistics (as opposed to a pre-built specialized kernel or scheduler reshuffling) is not clearly demonstrated in these papers, so a system that treats prefix detection as a genuine online compilation trigger (with trace caching/invalidation semantics) could still be distinct if that JIT-specific online-compilation angle is developed rather than just kernel design or scheduling. |
| 261 | ADJACENT | 2 | 2 | Wavelet multiresolution prefill cache | Wavelet multiresolution decomposition applied to prefill so that the prompt's key/value sequence is represented as a coarse approximation plus sparse detail coefficients, letting the model skip computing/storing detail where it is negligible. | 2210.01989; 2604.22808 | A twist that ties wavelet detail-coefficient thresholding specifically to skipping/recomputing KV-cache entries during LLM prefill (with an inverse-transform reconstruction used directly as the cached representation and exact on-demand recomputation of discarded detail) is not explicitly done by WavSpA or FreqFormer, so a version concretely instantiated as a prefill KV-cache sparsification/allocation scheme (rather than a general attention-in-wavelet-space layer or diffusion video attention router) could still be new. |
| 262 | ADJACENT | 2 | 2 | Learned-index FFN lookup | Learned-index applied to the FFN memory so that a small predictor model maps a token's query directly to the position(s) of relevant keys instead of scoring every key. | 2010.03881; 2503.15798 | A genuinely learned, jointly-trained index model that predicts a bucket via an auxiliary regression/classification loss (rather than a fixed hash/product-key factorization or ID-based LUT) could still be new if it demonstrably outperforms or behaves differently from PKM-style fixed structures at equal capacity, but no paper here implements that specific learned-index training scheme for FFN keys. |
| 263 | TAKEN | 4 | 4 | delta-rule residual overwrite gate | Delta-rule associative memory applied to the residual stream so that each block can erase-and-replace the component of the residual along a learned key direction instead of only adding to it. | 2601.00417; 2606.20743 | Nothing distinct remains: DDL already implements a gated rank-1 read-compare-overwrite delta-rule update directly on the residual stream at each layer, matching the proposal's mechanism, placement, and purpose almost exactly. |
| 264 | TAKEN | 4 | 4 | Hebbian fast-weight FFN overlay | outer-product Hebbian fast weights applied to the FFN so that recent token associations are stored and read back within the same forward pass, decaying over a short horizon | 2102.11174; 2212.02475 | Little: applying the fast-weight outer-product update specifically to gate/mix into a standard FFN's weight matrix (rather than as a separate linear-attention layer) is a placement detail already covered by the FWP/linear-transformer equivalence and FWL's training-time fast-weight layers; a genuinely new twist would need a mechanism qualitatively different from outer-product Hebbian/delta updates, not just relocating it inside the FFN. |
| 265 | TAKEN | 4 | 3 | windowed denoising KV rewrite | masked-diffusion-denoising applied to the KV cache so that a bounded trailing window of past key/value entries is iteratively refined by later context before being frozen. | 2505.16950; 2601.02076 | Only a genuinely different twist (e.g., a formal masked-diffusion training objective for the denoiser distinct from Bottlenecked Transformer's in-place rewrite, or proof the mechanisms differ mathematically rather than just terminologically) could survive, but per the stated equivalence rule (non-causal in-place rewrite = denoising rewrite) this proposal is already covered. |
| 266 | TAKEN | 4 | 3 | TTT-rewrite of KV entries | Test-time-training applied to the kv-cache so that stored key/value entries are revised in place by a small self-supervised update as later context arrives, instead of remaining frozen once written. | 2505.16950; 2608.30692 | A genuinely new angle would need to show the learned adapter operates strictly on cached K/V projections in fixed windows with an offline-learned update rule under matched-FLOPs, and demonstrate gains specifically from local self-supervised correction rather than full replacement of the cache with fast weights, but this is a narrow scope difference rather than a new capability. |
| 267 | TAKEN | 4 | 3 | Sleep-consolidation cache compression | Sleep-replay-consolidation applied to the kv-cache so that during an offline pause (between documents or on an idle scheduler slot) recent cache traces are reactivated and distilled into a compact, permanent summary slot, instead of being kept append-only until eviction. | 2603.14517; 2505.16950 | Only a genuinely distinct twist (e.g., proving a different theoretical guarantee, a training-free variant, or targeting a capability neither interference-resolution nor generalized reasoning addresses) would separate this from SleepGate and Bottlenecked Transformers, which already implement periodic sleep-inspired consolidation modules that compress/merge KV cache entries into permanent summaries. |
| 268 | TAKEN | 4 | 3 | Transactional KV rewrite windows | Transactions-and-rollback applied to the kv-cache so that a bounded window of recent entries can be atomically rewritten by later context before being committed to the immutable cache | 2505.16950; 2605.06997 | Little; the twist would need to be something beyond periodic non-causal in-place KV rewrites via an auxiliary/adapter module before commit, which Bottlenecked Transformers already frames via consolidation/reconsolidation theory with the same staging-then-commit structure. |
| 269 | TAKEN | 4 | 3 | information-bottleneck KV pruning | information-bottleneck applied to kv-cache so that each entry is compressed to the minimal sufficient statistic for predicting future tokens, discarding the rest explicitly rather than storing full-precision keys/values uniformly | 2505.16950; 2604.25975 | Only a genuinely per-entry, target-driven variable-rate allocation measured explicitly in nats via a VIB reparameterized-Gaussian encoder (rather than eviction/rewriting at fixed or heuristic budgets) could still be new, but 2505.16950 already frames KV rewriting explicitly as IB-driven compression-vs-prediction tradeoff at the same component for the same purpose. |
| 270 | TAKEN | 4 | 3 | information-bottleneck KV compression | an explicit information-bottleneck objective applied to the kv-cache so that eviction is replaced by a trained compression that provably keeps only what predicts future tokens | 2505.16950; 2604.25975 | Little remains new: the core twist (frozen base model, trained per-layer side network replacing about-to-be-evicted KV blocks with a fixed-size summary optimized via an explicit rate+task IB loss) is essentially what Bottlenecked Transformers already implements as IB-justified, non-causal, in-place KV cache rewrites via an auxiliary Cache Processor. |
| 271 | TAKEN | 4 | 3 | Sinkhorn-balanced head-to-value matching | Optimal-transport-sinkhorn applied to attention-scores so that queries and keys are matched under a transport cost with marginal constraints instead of independent per-query softmax, redistributing mass to avoid multiple queries collapsing onto the same few keys. | 2002.11296; 2509.23436 | A version that provably preserves strict causality with per-row cumulative column marginals varying by position (rather than fixed target vectors) and demonstrates measurable prevention of attention-sink pathologies specifically versus Sparse Sinkhorn Attention's efficiency framing and LOTFormer's low-rank linear-time framing could still be a narrow contribution, but the core claimed mechanism-placement-purpose triple is already published. |
| 272 | TAKEN | 4 | 3 | Sinkhorn-transported causal attention | Optimal-transport Sinkhorn matching applied to attention-scores so that query-key alignment is computed as a balanced transport plan under an explicit cost rather than an unconstrained bilinear dot product. | 2605.12879; 2605.20433 | Only the causal, sliding-window streaming variant with a running column-mass budget under strict autoregressive masking is not explicitly tested in these papers, but this is a scale/setting variant of an already-established mechanism (Sinkhorn/OT replacing softmax to enforce balanced marginals and prevent attention collapse), not a new capability. |
| 273 | TAKEN | 3 | 5 | PID-controlled expert capacity | Proportional-integral-derivative feedback control applied to the moe-router so that per-expert capacity thresholds are continuously adjusted to drive measured load imbalance to zero, replacing the static aux-loss weight. | 2408.15664; 2512.03915 | Only if the PID's integral/derivative terms demonstrably solve a failure mode (oscillation/overshoot) that the pure proportional bias in Loss-Free Balancing provably cannot, with evidence the extra terms are not just a tuning of the same proportional-bias mechanism. |
| 274 | TAKEN | 3 | 5 | Conformal recalibration of post-trained logits | Conformal prediction applied to the output head so that after preference post-training, the model emits a calibrated prediction set with a distribution-free coverage guarantee instead of a miscalibrated point-softmax. | 2604.01170; 2502.12601 | Nothing distinctive survives: applying split conformal calibration to a frozen post-trained model's softmax scores to yield per-step coverage-guaranteed token sets for abstention/verification is exactly what ORCA, COPU, and VACP already do; only the specific 'paperkiln events.jsonl' logging source is new, which is not a methodological contribution. |
| 275 | TAKEN | 3 | 5 | Conformal abstention token | Conformal prediction applied to the output-head so that the model can refuse to commit to any single token when no calibrated prediction set of bounded size achieves target coverage. | 2512.22682; 2302.08703 | Nothing genuinely new survives: 2512.22682 already computes conformal nonconformity scores from softmax at each decoding step, calibrates a threshold for target coverage, and explicitly addresses the case where the set would be too large (their whole 'efficiency' contribution is about shrinking oversized sets), which functionally subsumes the proposal's abstain-when-too-large behavior; only the explicit token name 'ABSTAIN' differs from suppressing/refusing to sample. |
| 276 | TAKEN | 4 | 3 | diffusion patch-repair over committed spans | masked-diffusion-denoising applied to the sampler so that a fixed trailing window of already-committed tokens can be jointly re-masked and re-denoised using bidirectional context from tokens generated after them, before being permanently frozen | 2606.01026; 2602.06161 | Little room remains: the specific fixed cadence (k=16/w=8), the small side-network sharing embeddings while the base LM stays frozen, and the paperkiln KV-lane implementation are engineering variants, not a new capability beyond visible-token revision already demonstrated by D3IM and COVER. |
| 277 | TAKEN | 4 | 3 | renormalized coarse-to-fine attention topology | renormalisation-group applied to attention-topology so that the mask is built hierarchically: coarse-grained token blocks attend first, and only blocks that survive a relevance threshold get refined into fine-grained fixed attention, instead of applying one fixed-scale mask everywhere | 2603.28458; 2605.18753 | Nothing genuinely new survives: the RG framing is just relabeling the same coarse-filter-then-fine-attend block-sparse mechanism already implemented (with joint training and matched compute) in HISA and DashAttention. |
| 278 | TAKEN | 3 | 4 | Attention-weighted PQ cache | Product-quantisation applied to the kv-cache so that entries' storage precision (codebook granularity) is allocated in proportion to their running attention mass rather than uniformly | 2407.12820; 2605.08317 | Only a genuinely novel twist—e.g. online k-means codebook updates jointly trained with an EMA-attention-mass threshold policy that dynamically re-tiers entries between full precision and PQ during training itself (not just post-hoc/inference-time)—might be new, but this is a training-recipe nuance rather than a new capability beyond PQCache/RDKV's attention-weighted precision allocation. |
| 279 | TAKEN | 4 | 2 | Sleep-phase KV consolidation | Sleep-replay-consolidation applied to the kv-cache so that an offline, periodic 'sleep' pass reactivates recently evicted cache traces and compresses them into a stable long-term store, then discards the raw traces. | 2603.14517; 2505.16950 | Only if the sleep pass is framed as literally reconstructing attention outputs via replay-trained autoencoding (rather than gating/summarizing or routed accumulation) with a strict discard-after-consolidate buffer, but this is a training-detail variation on mechanisms already published. |
| 280 | TAKEN | 3 | 4 | product-quantised shared KV store | product quantisation applied to the kv-cache so that KV vectors are stored as compact codebook indices in a shared cross-request store, letting identical or near-identical prefixes be looked up by code rather than recomputed | 2601.10155; 2504.03661 | The specific claim of sub-vector code matching across paraphrased/partially-overlapping prefixes (rather than just per-request compression or exact prefix reuse) is not explicitly tested by these papers, but it's a fairly incremental combination of existing PQ-KV compression (LOOKAT/MILLION) with existing shared-cache reuse (PolyKV/prefix caching systems), so any novelty would rest narrowly on the cross-request code-level deduplication claim rather than the core mechanism. |
| 281 | TAKEN | 3 | 4 | Hebbian erase-write buffer | hebbian fast-weights applied to the residual stream so that recently-written associations can be actively overwritten by new outer-product updates within a short window, rather than only accumulated | 2412.06464; 2606.26560 | Only a genuinely distinct twist—e.g. applying this exact erase-write fast-weight state as an additive term directly into the transformer residual stream (rather than as the recurrent state of a standalone linear-attention layer) with a short fixed forget horizon—could be new, but this is a placement/framing detail, not a new mechanism or capability. |
| 282 | TAKEN | 3 | 4 | contrastive multi-horizon head | A contrastive objective applied to the output-head so that hidden states are trained to be predictive of representations several tokens ahead, not just the immediate next token. | 2607.12277; 2411.16695 | Only a genuinely different twist (e.g., proving the InfoNCE negatives specifically prevent representation collapse or degenerate shortcut solutions that BYOL/summary losses do not, or showing gains transfer specifically to re-ranking tasks not covered by prior work) would be new; the core mechanism-placement-purpose triple is already demonstrated by NONTP's TCL. |
| 283 | TAKEN | 3 | 4 | SMC particle decoding | sequential-monte-carlo applied to the sampler so that multiple candidate continuations are tracked and reweighted in parallel, letting low-weight hypotheses be dropped and replaced rather than committing to a single irrevocable token | 2404.17546; 2306.03081 | Nothing genuinely new remains; the exact mechanism (particle population at the sampler, importance weights from lookahead/reward, periodic resampling, optional trained twist via self-normalized importance sampling) is already established by Lew et al. and the twisted-SMC line, matching the generator's own nearest-known guess. |
| 284 | TAKEN | 3 | 4 | Thompson-sampled decoding temperature | Bandit posterior sampling applied to the sampler so that the temperature/top-k value at each position is drawn from a posterior over which value would maximize a reward proxy, rather than fixed a priori. | 2603.09065; 2004.10874 | A genuinely new contribution would need to show Thompson-sampling-specific posterior exploration (vs. RL/policy-gradient adapters) gives measurably better exploration-exploitation tradeoffs than the contextual-bandit/POMDP decoding policy already demonstrated in 2603.09065, but the core capability (per-position/per-prompt online selection of decoding hyperparameters via bandit-style reward feedback) is already claimed there. |
| 285 | TAKEN | 3 | 4 | cross-prompt kNN prefill cache | kNN-retrieval applied to prefill so that instead of recomputing K/V from scratch for every prompt, the model looks up cached K/V (or hidden-state) representations from structurally similar previously-seen prompts and blends them in, amortising the quadratic cost across a corpus of prompts. | 2311.04934; 2606.13361 | A genuinely new angle would require approximate (non-exact-prefix) retrieval via embedding-based kNN over non-identical but structurally similar blocks with a trained adapter correcting for context mismatch, rather than exact-match prefix/document caching as in the cited systems—but this is already anticipated by adapter-based cross-cache correction work (e.g., C2KV, cross-model KV mapping) applied to the same reuse-at-prefill goal, so the novelty margin is thin. |
| 286 | TAKEN | 3 | 4 | Conformal coverage speculative drafting | Conformal prediction applied to speculative drafting so that the draft model proposes calibrated prediction sets with a guaranteed coverage level instead of single-token guesses, and the target verifies set membership rather than exact match. | 2510.09942; 2606.30265 | A conformal-specific twist with a formal (1-alpha) marginal coverage guarantee on the draft set (rather than heuristic top-k/multi-draft or KL-margin certificates) could still be distinct, but 2510.09942 already applies online conformal prediction to shape the draft/retained token set in speculative decoding, closely matching the proposed mechanism and placement. |
| 287 | TAKEN | 3 | 4 | SMC-weighted multi-branch drafting | Sequential Monte Carlo applied to speculative-drafting so that the draft stage maintains a weighted population of candidate continuations instead of a single greedy path, resampled by target-model likelihood before verification | 2604.15672 | No clear twist survives; the proposal's specific schedule details (K=4-8, resample threshold, extension length k tuned on validation) are hyperparameter choices within the same mechanism already published as SMC-SD. |
| 288 | TAKEN | 3 | 4 | test-time-trained stopping threshold | test-time training applied to the draft model's stopping head so that a small parameter set is updated via a self-supervised loss on the incoming stream itself, letting draft length adapt within a single long generation without external labels | 2411.18462; 2405.19715 | A twist restricted to updating only a small stopping-threshold adapter (not the whole draft distribution) via self-supervised accept/reject pseudo-labels rather than teacher-logit distillation might be marginally different, but SpecDec++ and SVIP already cover threshold/length adaptation from in-stream signals, and TTS already covers verification-driven online adaptation of the draft model itself, so little room remains. |
| 289 | TAKEN | 3 | 4 | Thompson-sampled draft length | Bandits-thompson applied to speculative-drafting so that draft length is chosen by sampling from a posterior over expected accepted-tokens-per-call for each candidate k, rather than fixed. | 2406.03853; 2505.15141 | Only a genuinely different posterior parameterization or action-set structure (e.g., continuous k via Gaussian process bandits) not covered by these discretized bandit/Thompson approaches would be new, but the core claim is already realized. |
| 290 | TAKEN | 3 | 4 | PID-controlled learning rate | PID feedback-control applied to the lr-schedule so that the learning rate is driven by proportional, integral, and derivative terms on a measured error signal (deviation of observed loss-decrease-rate from a target decay trajectory) rather than following a fixed predetermined curve. | 1911.07710; 2605.21968 | Nothing substantive: PID-controlled LR schedules driven by loss-decrease error against a target trajectory are already established (PID optimizers, PD-control LR schedules); a genuinely new twist would need a different error signal or control target not yet explored (e.g. controlling against a theoretically derived optimal trajectory with formal stability guarantees tied to transformer-specific loss curvature). |
| 291 | TAKEN | 3 | 4 | Thompson-sampled learning-rate bandit | Thompson sampling over a discrete bank of candidate learning rates applied to the lr-schedule so that step-size choice is made by posterior sampling over observed loss-improvement reward rather than a fixed curve | 2410.12598; 2408.04046 | Only a genuinely new twist—such as a formal Beta/Normal-Gamma Thompson-sampling posterior update proven to outperform the epsilon-greedy/model-selection LRRL approach, or a specific theoretical regret guarantee tailored to LR-bandit non-stationarity—would be new, but the core capability (bandit-selected LR reward-driven adaptive reallocation replacing fixed schedule) is already covered by LRRL. |
| 292 | TAKEN | 3 | 4 | Mixture-density next-token head | Mixture-density heads applied to the training objective so that next-token prediction outputs a learned mixture over K component distributions instead of a single softmax, exposing multimodal predictive uncertainty as a first-class target | 1807.09995; 2212.07328 | Only a genuinely different twist—e.g. proving mixture-density heads scale favorably at LLM vocabulary size/hidden-width tradeoffs, or a new theoretical link between component collapse and calibration guarantees—would be new; applying MDN heads to next-token prediction itself is a straightforward reapplication of Bishop's MDN mechanism already used for the same aleatoric-multimodality purpose in autoregressive/sequential settings. |
| 293 | TAKEN | 4 | 2 | predictive-coding gradient error routing | predictive-coding applied to the backward pass so that each layer propagates a locally-verified prediction error (top-down prediction minus bottom-up activation) rather than a single unverified end-to-end gradient signal | 2508.08762; 2605.31022 | Only the specific gating rule (skipping/scaling a layer's update when its local error is already small) is a minor addition not explicitly tested in these papers, but it is a straightforward inference-time thresholding on the already-standard PC local error signal, not a new mechanism or placement. |
| 294 | TAKEN | 3 | 4 | PQ-compressed KV cache lanes | product-quantisation applied to the context-window's stored key/value vectors so that positions contributing little to attention outputs occupy far less memory than high-information positions | 2407.12820; 2504.03661 | Only a genuinely different twist—such as training the PQ codebooks jointly with recency-aware eviction policy learned end-to-end, or provable guarantees on which positions are safe to compress—would be new; the core mechanism (per-layer/head PQ on KV cache with exact cache for recent tokens) is already established. |
| 295 | TAKEN | 3 | 4 | Spectral context compression | spectral-transform applied to the context-window's cached representations so that long-range structure becomes localized in a frequency basis, allowing far coarser truncation of high-frequency (locally-redundant) components without losing long-range (low-frequency) dependencies | 2505.00570; 2507.20030 | Nothing distinguishes the proposal's DCT/learned-basis block-wise scheme applied specifically at sliding-window eviction boundary from FreqKV's iterative frequency-domain compression of the growing KV cache, which already keeps low-frequency coefficients and discards high-frequency detail for the same long-range-preservation purpose. |
| 296 | TAKEN | 3 | 4 | ARC-policy KV eviction | an adaptive-replacement-cache policy (dynamically balancing recency and frequency lists) applied to the KV cache so that eviction decisions adapt online to the actual access/attention pattern instead of a fixed recency-only window | 2606.21238; 2608.18098 | Only a literal implementation of ARC's ghost-list adaptive parameter-tuning mechanism (as opposed to a simpler two-score blend) could be distinct, but that is a minor algorithmic variant, not a new capability. |
| 297 | TAKEN | 4 | 2 | RG coarse-graining of stale context | A renormalisation-group coarse-graining step applied to the context window so that older KV blocks are iteratively merged into fewer, coarser summary tokens whose surviving degrees of freedom are exactly those relevant at longer range, instead of every position costing the same regardless of age. | 2510.16392; 2507.14204 | Only the explicit dyadic multi-level scale hierarchy (many nested coarsening resolutions each with its own trainable map, rather than a single window/compression stage) could be a genuine twist, but RGMem already frames this as literal multi-scale RG coarse-graining and LaCache/AHN already implement the iterative block-merging and distillation-trained compression mechanism at the KV cache. |
| 298 | TAKEN | 3 | 4 | Importance-sampled context reweighting | Importance sampling applied to the context window so that positions are drawn/attended to with probability proportional to their estimated contribution to the next-token loss under a cheap proxy distribution, then reweighted to remain an unbiased estimate of full dense attention, instead of paying uniform cost to attend to every position. | 2605.01910; 2009.14794 | A genuinely new contribution would need a trained proxy scorer jointly optimized via a variance-reduction objective to match exact dense attention (rather than post-softmax or random-feature sampling) and a frozen-base-model graft ablation, but SANTA already implements the core mechanism (importance sampling of cache entries with unbiased correction) at the same component for the same purpose, so this is largely taken. |
| 299 | TAKEN | 3 | 4 | Hebbian fast-weight context compression | Hebbian outer-product fast-weight memory applied to the context window so that low-contribution positions decay out of an associative store within a few steps instead of occupying a fixed KV slot for their full lifetime in the window. | 2605.22884; 2601.00671 | Tensor Cache already implements the identical mechanism (decayed outer-product fast-weight matrix A, read via matrix-vector product, added to sliding-window attention, jointly trained end-to-end) for the identical purpose of freeing capacity beyond the window while retaining low-contribution-token decay; the proposal would need a genuinely different twist (e.g. a provably different decay/read rule with distinct guarantees) to survive, which is not stated. |
| 300 | TAKEN | 3 | 4 | LSH-bucketed KV cache sharing | Locality-sensitive hashing applied to serving-batch request states so that similar prompt/context prefixes across concurrent requests can share cached KV computation instead of recomputing independently. | 2509.24832; 2503.05530 | A twist could survive if the LSH hashing were applied to rolling mid-sequence windows (not just prefixes) with per-token verification fallback baked into the batch scheduler itself rather than a separate cache layer, but SemShareKV already covers token-level LSH matching for semantically similar (non-exact-prefix) cross-request KV reuse, closing most of this gap. |
| 301 | TAKEN | 3 | 4 | PID-controlled router balancing | feedback-control applied to the moe-router so that expert load balances via a closed-loop controller instead of a competing auxiliary loss | 2408.15664; 2512.03915 | Adding explicit integral and derivative terms (full PID vs. pure proportional bias) with online-tuned gains is only a tuning/parameterization refinement of the same mechanism already established as loss-free bias-based balancing, so it would need a demonstrated qualitative advantage (e.g., provably better transient stability or oscillation control) to not be a follow-up. |
| 302 | TAKEN | 3 | 3 | IB-consolidated KV summaries | Information-bottleneck applied to the kv-cache so that groups of old entries are compressed into a small sufficient-statistic summary trained explicitly to preserve only what predicts future tokens | 2505.16950; 2604.25975 | Only a genuinely different twist—such as a formal variational IB bound with an epsilon-constrained frozen predictor loss guarantee, or joint training with the base model rather than periodic in-place rewrites—would be new, but this is a training-recipe/framing difference, not a new capability. |
| 303 | TAKEN | 3 | 3 | Sparse dictionary KV atoms | Sparse-coding applied to the kv-cache so that each entry is represented as a sparse combination of a small shared dictionary of atoms rather than a dense per-token vector | 2412.08890; 2412.11741 | The interpretability side-channel (inspecting which atoms compose an entry) and joint from-scratch training at matched FLOPs are not emphasized in Lexico/CSR (which are largely post-hoc/training-light on pretrained models), so a genuinely new angle would be exploiting atom co-activation patterns as an interpretability tool rather than just compression, but the core mechanism-at-component-for-compression claim is already taken. |
| 304 | TAKEN | 3 | 3 | Shared cross-request KV retrieval | kNN-retrieval applied to the kv-cache so that identical or near-identical prefixes across requests are served from a shared cache instead of recomputed. | 2402.15220; 2504.11765 | Only a genuinely new twist—e.g. proving tight guarantees on approximate (non-exact) KV splicing with logit-verified tolerance across arbitrary non-prefix overlaps rather than exact/prefix-tree matching—would be new, but the core capability (shared, persistent, cross-request KV cache retrieval to skip prefill) is already established by ChunkAttention, RadixAttention-style systems, and disk/object KV sharing works. |
| 305 | TAKEN | 3 | 3 | RG-coarsened attention gating | A renormalisation-group coarse-graining step applied to attention-scores so that a query first attends to a coarse-grained summary of key-blocks and only 'refines' (spends mass) on blocks whose coarse signal survives a learned relevance threshold, letting irrelevant blocks be dropped entirely rather than mixed at full resolution. | 2605.18753; 2603.28458 | Only if the RG framing contributes a genuinely different mathematical mechanism (e.g. a multi-scale flow with provable fixed-point/relevance criteria across many coarsening levels, not just one coarse-then-fine pass) rather than being a relabeling of existing coarse-block-gate-then-refine designs. |
| 306 | TAKEN | 3 | 3 | Thompson-sampled expert selection | Thompson sampling applied to the moe-router so that expert selection during training is drawn from a posterior over each expert's per-token value rather than taken as a deterministic top-k of point-estimate scores | 2609.13058; 2509.23830 | A count-based Beta/Gaussian per-expert-token-cluster posterior updated from per-token training loss as reward, with explicit sample-at-train/mean-at-inference switching framed strictly as a bandit exploration-exploitation solution replacing load-balancing losses, is a narrow variant but not clearly distinct from the Bayesian-routing and expert-space-exploration frameworks already proposed for the identical purpose at the identical component. |
| 307 | TAKEN | 3 | 3 | Sinkhorn balanced-assignment routing | Optimal-transport Sinkhorn matching applied to the moe-router so that expert assignment is a balanced transport plan computed jointly for the batch, removing the need for an auxiliary load-balancing loss. | 2511.08972; 2604.07298 | Nothing distinguishes the proposal from SSR (2511.08972), which already implements Sinkhorn-derived gating scores as a drop-in replacement for aux-loss balancing in language modeling MoEs; the proposed gpt2-nano/llama-nano ablation would be a direct scale-down follow-up rather than a new mechanism or purpose. |
| 308 | TAKEN | 3 | 3 | Fast-weight residual capacity buffer | Hebbian outer-product fast weights applied to the residual stream so that a token can temporarily borrow extra effective width from a decaying associative buffer instead of being constrained to the block's fixed channel count. | 2506.22696; 2509.25223 | Nothing substantive: RMT already implements a causal outer-product fast-weight memory added into the residual pathway at matched compute budget specifically to let the residual stream carry more information than its fixed width, which is exactly the claimed new capability. |
| 309 | TAKEN | 3 | 3 | self-distilled position extrapolation | online self-distillation applied to positional-encoding so that a slow teacher copy processing shorter, in-training-distribution windows teaches a fast student copy operating at longer test-time positions, transferring calibrated behavior from seen to unseen position ranges without new labels. | 2604.17535; 2604.06070 | Only a genuinely new twist—continuous online EMA-teacher updating during streaming inference itself (not a separate fine-tuning phase) with adapter-only backprop on position-dependent projections—could differentiate it, but this is a minor implementation variant of the same mechanism already covered by OPSDL and the RoPE-distillation papers. |
| 310 | TAKEN | 2 | 5 | Proper-scoring-rule post-hoc recalibration head | A proper scoring rule (CRPS-style / Brier-style loss on a held-out calibration split) applied to the output-head so that the post-preference-trained logits recover calibrated probabilities without touching the base weights. | 2409.19817; 2601.23096 | Using a strictly proper scoring rule (Brier/CRPS-style) as the specific fitting objective for the small post-hoc map, rather than NLL/temperature-only, is a minor variant already implicitly covered since temperature/Platt scaling under log-loss is itself a proper scoring rule; no genuinely new mechanism or placement remains. |
| 311 | TAKEN | 3 | 3 | particle-filter multi-horizon decoding | Sequential Monte Carlo applied to the output-head so that a population of weighted multi-token continuations is maintained and resampled during generation instead of committing to a single greedy/sampled token at each step. | 2604.16453; 2007.11936 | Little room remains; a genuinely new twist would need a mechanism distinct from reward-weighted SMC resampling over LM continuations, e.g., a fundamentally different weighting signal not reducible to a lookahead reward/value model, or joint training of the base LM with the particle dynamics rather than leaving it untouched. |
| 312 | TAKEN | 2 | 5 | PID-controlled entropy sampler | Feedback-control applied to the sampler so that temperature is adjusted per-token to drive measured output entropy to a target trajectory rather than being fixed globally. | 2603.03310; 2602.13035 | Only the specific PID (proportional-integral-derivative) control law with offline-tuned gains might be a minor implementation detail difference, but the core capability—closed-loop per-token entropy regulation via adaptive temperature at the sampler—is already demonstrated in 2603.03310 and related works. |
| 313 | TAKEN | 3 | 3 | PQ-compressed key prefill cache | Product-quantisation applied to the K/V vectors produced during prefill so that the cache built from a long prompt is stored and attended to in compressed codebook space, cutting the memory/bandwidth cost that drives quadratic prefill cost at large context. | 2407.12820 | Only a genuinely different twist (e.g. training codebooks jointly end-to-end for exact ADC attention scoring rather than approximate top-k retrieval, or provable full-attention equivalence rather than sparsified retrieval) could separate this from PQCache, since PQ-on-prefill-KV for memory/bandwidth reduction is already claimed there. |
| 314 | TAKEN | 2 | 5 | History-conditioned draft-length prediction | Branch-prediction applied to speculative-drafting so that the number of tokens speculated per round is predicted from a small per-position history table rather than fixed, extending further when past predictions at similar contexts were reliably accepted | 2405.19715; 2411.18462 | A concrete twist could be an explicit hashed saturating-counter table keyed on quantized entropy-history (literal branch-predictor structure with online table updates rather than a trained head or entropy-threshold rule), but this is a narrow implementation variant on an already crowded, well-covered capability. |
| 315 | TAKEN | 3 | 3 | self-distilled adaptive drafter | online self-distillation applied to the draft model so that a slow, larger 'teacher' copy continuously updates the fast draft's stopping/confidence behavior from the live token stream, adapting draft length to distribution shift within a session | 2605.09329; 2603.12617 | Restricting the online update to only a small stopping/confidence head (rather than the draft backbone) and running distillation asynchronously off a replay buffer in a background thread is a minor implementation variant, not a new capability, since TTS/OnlineSPEC/OSD already achieve on-the-fly adaptive drafting via online distillation from target feedback. |
| 316 | TAKEN | 3 | 3 | self-distilled early exit gate | online-self-distillation applied to the layer-schedule so that a shallow-copy student predicts when the full-depth teacher's output has already stabilized, letting easy tokens exit early while the same weights keep training on the live stream | 2209.08625; 2206.12420 | Only if the 'live stream / trained from scratch matched budget' online-continual aspect (rather than post-hoc fine-tuning of a pretrained model) were shown to matter mechanistically, but as stated this is the same joint self-distillation early-exit recipe already published. |
| 317 | TAKEN | 3 | 3 | gated reflex-deliberate depth | dual-process-gating applied to the layer-schedule so that easy tokens exit after a shallow reflex path while a learned gate routes hard tokens through additional deliberate blocks | 2605.05222; 2603.01914 | Nothing distinguishes this proposal from TSA/AdaPonderLM/MoR beyond superficial framing ('reflex vs deliberate') and a specific FLOPs-budget penalty, which are not novel twists. |
| 318 | TAKEN | 2 | 5 | Conformal coverage-calibrated token sets | Conformal prediction applied to the token-level output distribution so that the model can emit a prediction set with a distribution-free, finite-sample coverage guarantee instead of an uncalibrated softmax probability. | 2512.22682; 2309.03797 | No clear twist remains; the proposal's exact mechanism (nonconformity score = 1 - softmax probability, calibration quantile threshold, per-position token sets with coverage guarantee) is already implemented and evaluated at the token level in 2512.22682. |
| 319 | TAKEN | 3 | 3 | Delta-rule fast-weight inference memory | Delta-rule associative memory applied to weights-at-inference so that the model overwrites stale key-value associations from the current stream instead of only accumulating them | 2102.11174; 2605.22791 | Nothing distinguishes this proposal from DeltaNet/Gated DeltaNet-family delta-rule fast-weight memories already trained end-to-end to overwrite stale KV associations at inference; a genuinely new angle would need a different erase/write decoupling or object (e.g. erasing along the query direction as in QED) beyond vanilla delta rule. |
| 320 | TAKEN | 2 | 5 | PID-controlled activation steering | Proportional-integral-derivative feedback control applied to weights-at-inference so that a measured behavioral error (e.g. drift in output calibration or repetition rate) is driven to zero by continuously adjusting a small steering vector rather than leaving generation uncorrected | 2510.04309; 2506.18831 | Nothing distinguishes the proposal's generic error signal (entropy/repetition rate) and PID-on-steering-vector formalism from 2510.04309's general PID steering framework or 2506.18831's closed-loop PID steering; a new twist would need a fundamentally different control target or correction mechanism beyond swapping the probed statistic. |
| 321 | TAKEN | 3 | 3 | distilled fast-mask predictor | online self-distillation applied to attention-topology so that a fast per-token mask-predictor learns to open/close attention edges from a slow teacher's realized attention patterns | 2410.13276; 2603.02227 | The specific slow-EMA-teacher-vs-fast-student online self-distillation loop (rather than joint training or frozen-model distillation) is a minor recipe variant, not a new mechanism/placement, so little survives as genuinely new. |
| 322 | TAKEN | 3 | 3 | Predictive-coding cache correction | Predictive coding applied to attention-topology so that every k tokens each cache entry is corrected not by overwriting its value but by adding a learned residual proportional to the prediction error between what the entry implied and what later tokens actually needed, propagated only forward into future reads. | 2508.09001; 2604.15356 | Little room remains unless the predictive-coding framing yields a demonstrably different training/inference behavior (e.g. explicit top-down generative prediction rather than a learned residual/delta), but as an in-place additive correction to cached values driven by prediction error it is functionally covered by RetroAttention's retrospective revision and delta-coding/residual-correction approaches already in the pool. |
| 323 | TAKEN | 3 | 3 | Kalman-fused gradient estimate | kalman-filtering applied to the gradient step so that the noisy minibatch gradient is fused with a running model-based prediction of the gradient in proportion to their estimated uncertainties, instead of used raw | 2410.06397; 2604.19485 | DiSK already implements a simplified per-parameter Kalman filter fusing a running gradient prediction with fresh noisy gradient measurements (via clipped/noised gradients) to reduce effective noise with a trackable covariance state, which is essentially the same mechanism, component, and purpose as the proposal; the only distinguishing detail is DiSK's differential-privacy motivation versus this proposal's plain optimizer noise-reduction motivation, which is not a substantive twist. |
| 324 | TAKEN | 3 | 3 | Sparse dictionary context compression | sparse-coding applied to the context-window's hidden-state history so that each position's contribution to future attention is represented as a sparse combination of a small learned atom dictionary instead of a full dense vector | 2412.08890; 2508.11661 | Only a genuinely different twist (e.g., per-layer online K-SVD dictionary adaptation jointly trained with the base model, or theoretical guarantees on reconstruction fidelity vs. context length scaling) not already covered by Lexico's universal sparse-coding dictionary for KV cache would remain novel. |
| 325 | TAKEN | 3 | 3 | entropy-adaptive position budgeting | arithmetic-coding-style entropy accounting applied to the context window so that each position's storage cost in the KV cache is scaled inversely to its measured predictive-surprise contribution, giving low-entropy (predictable) spans a compressed representation and high-entropy spans full fidelity | 2603.04411; 2604.04722 | Only the specific arithmetic-coding framing and joint training of a percentile threshold plus rank-r compressor via reconstruction/distillation loss might be a narrow implementation delta, but the core idea (measure per-token entropy/predictive-uncertainty for free during the forward pass, then allocate differential KV storage fidelity accordingly to extend effective context under fixed budget) is already realized by DynaKV, InfoKV, and the entropy-based quantization papers. |
| 326 | TAKEN | 3 | 3 | IB-gated KV compression | An information-bottleneck objective applied to the KV cache so that each cached position is compressed to the minimum statistic sufficient to predict future tokens, giving variable bit-cost per position instead of uniform cost. | 2604.25975; 2505.16950 | Only a genuinely new twist—such as a formally derived rate-distortion bound tied to a provably optimal variable-bit-per-position allocation with guarantees beyond what CapKV's capacity-maximization or Bottlenecked Transformer's IB-justified rewrites already offer—could still be novel, but the core idea of IB-gated variable-cost KV compression is already staked out. |
| 327 | TAKEN | 3 | 3 | wavelet-banded context allocation | Multiresolution wavelet decomposition applied to the context window so that distant positions are stored at coarser resolution and near positions at full resolution, giving variable effective cost per position instead of uniform cost. | 2606.00724; 2501.04987 | A genuinely new angle would require showing the fixed multiresolution wavelet basis (versus learned/attention-guided pooling) yields provably better reconstruction fidelity or a formally derived error bound per scale that these heuristic hierarchical/summary methods lack, but as stated the mechanism-component-purpose triple is already covered. |
| 328 | TAKEN | 3 | 3 | PQ-shared KV cache pool | Product-quantisation applied to the serving-batch's per-request KV caches so that structurally similar prefixes across concurrent requests share compressed cache entries instead of storing full-precision K/V independently. | 2604.24971; 2604.03143 | A twist emphasizing online k-means codebook updates shared across the batch specifically to deduplicate near-identical subvectors across different requests' prefixes (not just one compressed copy injected into many contexts) with an exact recency buffer could differ, but PolyKV already establishes the shared-compressed-pool-for-concurrency capability, making this a follow-up rather than new. |
| 329 | TAKEN | 3 | 3 | offline cache consolidation across requests | sleep-replay-consolidation applied to serving-batch so that recurring prefix/KV patterns across independent requests are compressed into a shared reusable store | 2601.03067; 2609.13692 | The only room left is if the 'sleep-replay-consolidation' framing does continuous background clustering/autoencoder-based dictionary learning with fuzzy (non-exact) matching that generalizes beyond block-level exact fusion, but 2601.03067 already does cross-request fusion of similar (not just identical) KV blocks into shared representations at serving time, closing most of that gap. |
| 330 | TAKEN | 2 | 4 | Spectral KV cache compression | Spectral-transform applied to the kv-cache so that stored entries are represented in a frequency basis (e.g. DCT over the time axis per feature) where long-range slowly-varying structure collapses onto a few low-frequency coefficients | 2505.00570; 2506.11886 | Nothing substantive remains new: per-block DCT over rolling time windows with top-f coefficient retention and inverse-DCT reconstruction at read time is essentially what FreqKV and FourierAttention already implement and evaluate. |
| 331 | TAKEN | 2 | 4 | ARC-policy KV eviction | cache-replacement-arc applied to kv-cache so that entries are evicted by an adaptive recency/frequency balance instead of a fixed window or static top-k rule | 2606.21238; 2608.18098 | The specific ARC ghost-list bookkeeping (tracking recency-evicted vs frequency-evicted shadow entries to adapt the split point) is a distinct implementation detail, but the capability and placement it targets are already claimed by existing recency/frequency adaptive KV caching work. |
| 332 | TAKEN | 2 | 4 | LSH-bucketed KV prefix sharing | LSH-hashing applied to the kv-cache so that requests with locality-similar prefixes collide into shared buckets and reuse cached KV blocks instead of recomputing prefill. | 2509.24832; 2503.05530 | Nothing substantive survives; SemShareKV already implements LSH-bucketed fuzzy prefix matching on token embeddings to reuse KV cache blocks across semantically similar but lexically different prompts, which is the same mechanism at the same component for the same purpose. |
| 333 | TAKEN | 2 | 4 | posterior-weighted expert averaging | Bayesian model averaging applied to the moe-router so that expert outputs are combined by posterior mass over a small ensemble of gate hypotheses rather than a single argmax/top-k choice, so that the effective gate value reflects a calibrated mixture weight. | 2603.09453; 2509.23830 | Nothing distinguishes the proposal's specific 'last N router checkpoints or K dropout passes + per-expert temperature/prior fit on a calibration split' recipe from VMoER's amortised variational/temperature routing or Bayesian-MoE's thesis-level exploration of weight/logit/selection-space uncertainty, so no genuine twist survives. |
| 334 | TAKEN | 2 | 4 | mixture-density MoE router | Mixture-density-heads applied to the moe-router so that gate outputs become calibrated probability estimates of expert correctness rather than uninterpreted scores. | 2603.09453; 2509.23830 | Little room remains unless the mixture-density-of-Gaussians parameterization trained against realized downstream loss (rather than variational/Laplace/Bayesian posteriors over routing logits) yields materially different calibration behavior or enables new abstention/compute-triggering mechanisms not demonstrated in VMoER or Bayesian-MoE. |
| 335 | TAKEN | 2 | 4 | spectral-domain long-range position code | spectral-transform applied to positional-encoding so that position is represented in a frequency basis chosen so long-range dependencies become local, rather than fixed local index | 2607.10134; 2607.07678 | Nothing distinguishes the proposal from LeRoPE's learned-per-frequency RoPE at matched budget; a genuinely new angle would need a different parameterization (e.g. explicit small discrete spectral basis with sparsity/selection over coefficients rather than a continuous learned scalar per existing frequency slot) or a distinct evaluation targeting periodic long-range retrieval specifically. |
| 336 | TAKEN | 2 | 4 | wavelet multiresolution position code | wavelet multiresolution decomposition applied to positional-encoding so that position is encoded as a fixed orthogonal basis of localized, multi-scale wavelets instead of a single global sinusoidal frequency bank | 2302.08647; 2602.12189 | Using a fixed, non-learned orthogonal discrete wavelet basis (e.g., Haar/Daubechies dyadic scales) evaluated at integer positions and substituted directly for RoPE rotation angles, rather than a learned/continuous Morlet or adaptive DWT variant, is a narrow parameterization choice already covered in spirit by WavePE's fixed orthogonal wavelet PE. |
| 337 | TAKEN | 2 | 4 | transactional multi-token commit blocks | transactions-and-rollback applied to the sampler so that groups of speculatively generated tokens commit atomically or abort as a whole, exposing an explicit commit boundary the model can condition future speculation width on | 2412.18910; 2411.18462 | Framing the accept/abort bitmap as an explicit 'transaction log' with atomic commit/abort semantics is just terminology over the same accept-then-predict-next-K loop already implemented by AdaEAGLE/SVIP/AdaEDL, so no new capability survives unless the transactional rollback itself (e.g., multi-step abort/rollback across cache state beyond simple prefix truncation) does something these lack. |
| 338 | TAKEN | 3 | 2 | sparse dictionary prefill compression | Sparse-coding applied to the stream of prefill hidden states so that the prompt's K/V cache is represented as a small number of active dictionary atoms per token instead of a dense vector, giving compute and memory that scale with the sparsity level rather than the raw prompt length. | 2412.08890 | Only a narrow twist survives: explicit claims/evidence that shared atoms specifically exploit repeated/boilerplate prompt structure so effective compute scales with intrinsic content complexity rather than token count, which Lexico does not frame or measure, but the core mechanism-placement is already taken. |
| 339 | TAKEN | 2 | 4 | LSH-bucketed prefill sparsification | Locality-sensitive hashing applied to prefill so that attention during prompt encoding is restricted to same-bucket tokens, replacing full quadratic prefill with sparse bucketed attention. | 2607.19368; 2510.18413 | Only a genuinely new twist—such as jointly learned hash functions co-trained with causal windowing that provably preserve exactness guarantees beyond SimHash-based macro-token aggregation—would be distinct from Spectral-LSH's already-published LSH-bucketed sub-quadratic prefill compression. |
| 340 | TAKEN | 2 | 4 | Kalman-gated adaptive draft length | Kalman-filtering applied to speculative-drafting so that draft length is chosen per-step from a fused estimate of the draft model's confidence trajectory rather than fixed a priori. | 2411.18462; 2410.18351 | Only the specific Kalman recursion (explicit process/measurement noise scalars fit offline) as opposed to entropy-threshold or rolling-average fusion could be new, but this is a filtering-method substitution on the same object for the same purpose, not a new capability. |
| 341 | TAKEN | 2 | 4 | confidence-gated draft length | dual-process-gating applied to speculative-drafting so that the draft model emits a variable number of tokens per round, chosen by a fast reflex head, before target verification | 2405.19715; 2410.18351 | Nothing distinguishes the proposal from SpecDec++'s trained acceptance-prediction head with threshold-based stopping; a genuinely new twist would need a different signal source, joint drafting/verification co-adaptation, or a different object being rewritten, none of which is present here. |
| 342 | TAKEN | 2 | 4 | Branch-predicted layer skipping | Branch-prediction applied to the layer-schedule so that the router's continue/exit decision at each layer is predicted from a short history of recent tokens' continue/exit outcomes and speculatively executed, with cheap correction if the prediction is wrong. | 2206.04685; 2206.07269 | A hardware-branch-predictor-style, non-learned, hashed-history counter table specifically overlapping the router latency with computation (rather than replacing the exit decision itself or using a full draft/verify pass) is a narrow implementation twist, but the capability (predict exit/continue ahead of time and speculatively proceed, correcting on misprediction) is already covered by Predictive Exit and Exit Predictor combined with LayerSkip's speculative-verify-correct pattern. |
| 343 | TAKEN | 2 | 4 | BMA ensemble logit averaging head | Bayesian model averaging applied to the training objective so that the model's output distribution is a posterior-weighted mixture over an ensemble of lightweight parameter samples rather than a single point estimate | 2405.14438; 2310.00035 | Only a genuinely different twist—such as using the ensemble disagreement specifically to localize under-trained data regions for active data collection rather than just calibration—would be new, but this is not clearly claimed as distinct from existing calibration-focused LoRA-ensemble/BMA work. |
| 344 | TAKEN | 2 | 4 | Thompson-sampled curriculum bandit | Bandits-thompson applied to data-order so that the choice of which data shard/difficulty-bucket to sample from next is drawn from a posterior over each bucket's expected training value, rather than fixed random ordering. | 2102.03662; 2606.25178 | Only a genuinely different posterior parameterization or reward signal (e.g., provably optimal regret bounds tailored to LM pretraining loss dynamics, or explicit Beta/Gaussian per-token loss-reduction posteriors validated at pretraining scale) would be new, since bandit-driven bucket/domain curriculum sampling for LLM training is already demonstrated in multiple works. |
| 345 | TAKEN | 2 | 4 | importance-weighted replay buffer | Importance-sampling applied to data-order so that examples are effectively revisited by upweighting the loss contribution of high-current-loss examples drawn from a fixed-order stream, correcting for the mismatch between the arrival distribution and the model's current error distribution. | 2502.06733; 2006.13169 | Only a genuinely different correction mechanism, unbiasedness guarantee, or a different placement (not loss-based instance reweighting during single-pass pretraining) would remain novel; the proposal's core (loss-triggered importance-weighted reservoir replay for unbiased gradient correction in pretraining) is already covered by 2502.06733. |
| 346 | TAKEN | 2 | 4 | homeostatic mixture normalization | homeostatic-plasticity applied to data-order so that each domain's sampling rate is continuously rescaled to hold a fixed target statistic (e.g. per-domain gradient-norm contribution or per-domain average token loss) constant across training, rather than letting naturally easy domains dominate gradient signal | 2411.14318; 2502.06733 | Little; the homeostatic multiplicative gain toward an explicit shared target statistic is essentially the same continuous loss-based domain-reweighting loop Velocitune and DomainPilot already implement, so only a genuinely different target statistic (e.g. gradient-norm share rather than loss) with demonstrated distinct dynamics could still be new. |
| 347 | TAKEN | 2 | 4 | Loss-tilted importance data order | Importance-sampling reweighting applied to data-order selection so that the effective training distribution is corrected toward the model's current error distribution while remaining an unbiased estimator of the original data distribution's gradient. | 1602.02283; 2607.05806 | Little room remains beyond combining smoothed per-example loss scores with explicit IS-ratio gradient scaling in a large-scale/production LLM training loop, which is an engineering variant rather than a new capability given 1602.02283 already established unbiased loss/gradient-informed importance sampling for minibatches. |
| 348 | TAKEN | 3 | 2 | Kalman-fused causal cache update | Kalman filtering applied to attention-topology so that each cached key/value is treated as a noisy state estimate that gets fused with a new 'measurement' derived from later context, weighted by learned uncertainty, instead of being either frozen or fully overwritten. | 2609.07816 | Only a narrow implementation difference (explicit per-slot KV cache in a standard transformer attention topology vs. linear-attention recurrent state) could remain novel, but the core mechanism—Kalman-gain-weighted fusion of new evidence into a cached state representing uncertainty—is already claimed. |
| 349 | TAKEN | 2 | 4 | streaming teacher from own past | online self-distillation applied to checkpoints so that the continuous trajectory between saved snapshots is compressed into a running teacher signal instead of being discarded | 2605.08577; 2603.04422 | Nothing distinguishes the proposal from SD-GAN's mechanism except domain (language/general models vs GANs); a genuinely new twist would need a different placement or a capability EMA-self-distillation doesn't already give, such as explicitly compressing multiple discarded intermediate checkpoints' trajectory information beyond what a single EMA can encode. |
| 350 | TAKEN | 2 | 3 | EMA-consolidated KV trajectory | ema-averaging applied to the KV cache so that each cached value is replaced by an exponential moving average over its own revision trajectory (computed from repeated local re-encodes) rather than the raw single-pass endpoint. | 2602.04268; 2605.08840 | The only defensible twist is doing the EMA over multiple perturbed re-encodes of the same position during prefill (variance reduction via repeated forward passes) rather than EMA over sequential time-steps of a single trajectory, but this is a minor procedural variant of an already-published in-place EMA smoothing of cached K/V vectors. |
| 351 | TAKEN | 2 | 3 | MDL-regularized expert set selection | Minimum-description-length selection applied to moe-router so that top-k picks the expert subset that minimizes total code length (token reconstruction cost plus routing description cost) rather than maximizing independent per-expert scores. | 2606.26287; 2607.20427 | A concrete greedy KL-based conditional-redundancy penalty computed from expert output projections (rather than gating entropy or Huffman-code framing) could be a distinguishing implementation detail, but the core claim—MDL-based selection at the router to penalize redundant expert sets—is already proposed in GeMoE and explicitly called for in the Huffman-coding paper. |
| 352 | TAKEN | 2 | 3 | LSH-bucketed FFN keys | Hashing-LSH applied to the FFN memory so that keys are grouped into locality-sensitive hash buckets and only the token's own bucket is scored instead of all keys. | 2403.07221 | Only a genuinely different twist (e.g. learned multi-probe hashing jointly optimized end-to-end with a straight-through estimator shown to preserve quality at scale, or theoretical guarantees on bucket collision for arbitrary key growth) would be new, since LookupFFN already establishes LSH-bucketed FFN key lookup as the mechanism for scaling FFN keys sub-linearly. |
| 353 | TAKEN | 2 | 3 | Self-distilling FFN fast copy | Online self-distillation applied to the FFN so that a fast, cheaply-updated shadow copy of the FFN weights tracks the stream in real time while the slow copy stays fixed as a stable teacher. | 2608.01672 | Little room remains beyond minor recipe differences (LoRA-rank-8 parameterization, KL-on-logits vs hidden-state discrepancy, explicit reset-to-slow-copy framing), none of which change the core mechanism-at-component-for-purpose match. |
| 354 | TAKEN | 2 | 3 | EMA-smoothed key-value trace | EMA averaging applied to attention-topology's stored keys/values so that each cache entry represents a smoothed trajectory estimate rather than a single noisy endpoint, letting later tokens implicitly 'denoise' earlier representations without violating causality. | 2602.04268; 2609.03515 | A learned per-head alpha trained end-to-end at matched FLOPs as a first-class architectural component (rather than a training-free inference-time smoothing heuristic) is the only remaining distinction, and it is a minor scale/training-recipe difference already anticipated by the proposal's own exclusion criteria. |
| 355 | TAKEN | 2 | 3 | checkpoint-trajectory kNN retrieval | kNN-retrieval applied to the sequence of training checkpoints so that inference can mix in parameter-space neighbours from the optimization trajectory instead of using only the final weights. | 2607.10810; 2508.07556 | The specific retrieval-based kNN selection of nearby checkpoints via learned sketch-distance (rather than fixed windows or all-checkpoint aggregation) is a minor implementation variant, not a new capability beyond what DSI and the trajectory-ensembling uncertainty work already deliver. |
| 356 | TAKEN | 1 | 4 | Entropy-coded optimiser state checkpoints | Arithmetic-coding applied to optimiser-state so that the per-parameter moment tensors are stored/transmitted at their empirical entropy rather than fixed-width floats, exploiting the fact that most moments are structurally similar within a module. | 2506.12000; 2009.13003 | Nothing distinguishes this proposal from 2506.12000/2009.13003/ExCP, which already quantize+entropy-code optimizer moment/gradient tensors per empirical distribution purely as an I/O-path checkpoint codec; a genuinely new twist would need a different guarantee (e.g. provable bit-exact bounded-loss reconstruction certificates or exploiting cross-step redundancy in a way none of these do). |
| 357 | TAKEN | 2 | 2 | wavelet-banded moment memory | Wavelet multiresolution decomposition applied to optimiser-state so that per-parameter second-moment trajectories are stored as a few low-frequency wavelet coefficients instead of one running scalar, giving the optimiser explicit access to curvature at multiple timescales simultaneously. | 2501.07237; 2510.05361 | The proposal's twist of using explicit per-parameter multiresolution (approximation+detail) coefficients with globally tuned per-level damping weights is essentially what GWT already implements (multilevel Haar transform maintaining moment states on the compact coefficients), so no clearly distinct capability remains. |

## Top OPEN, in full

### atomic commit fast-weight edits  (splash 4, feasibility 4, id 8d53aebded)

**Claim.** transactions-and-rollback applied to weights-at-inference so that a batch of proposed within-sequence weight edits is validated against a held-out probe before being committed, with automatic rollback to the frozen base weights if the probe check fails

**New capability.** the system can attempt speculative per-sequence weight adaptation (e.g. from a few in-context examples) with a guarantee that adaptation never degrades output quality below the frozen baseline, since bad edits are provably discarded

**Placement.** Every k tokens, accumulate a candidate ΔW (Hebbian outer-product or gradient-based) in a staging buffer; before committing, run the last m tokens of context through both frozen weights and staged (W+ΔW) and compare loss; commit ΔW into the live forward pass only if staged loss <= frozen loss - margin, else discard and reset staging buffer to zero. The comparator threshold and staging update rule are trained/tuned; base weights are never modified, only ever read exactly or ΔW-perturbed.

**Invariant.** exactness is recoverable by verification

**Falsifier.** H: transactional commit of fast-weight edits achieves strictly non-negative delta-perplexity vs always-frozen baseline on a mixed easy/adversarial-context stream, while naive always-commit fast-weights sometimes regresses Metric: per-token perplexity delta vs frozen baseline, fraction of committed transactions, worst-case regression Baseline: frozen weights (no adaptation) and always-commit Hebbian fast-weights (no rollback) at matched extra FLOPs Protocol: 12 seeds, nano-llama preset, mixed in-context-learning + distribution-shift synthetic stream, report worst-seed regression GPU-hours: 25

**Nearest (generator's guess).** Fast weight programmers with gating; model souping / speculative decoding-style accept-reject (but applied to weight edits rather than tokens, which is the novel part)

**Scoper.** closest: [2605.28053] RW-TTT: Batched Serving for Request-Owned Test-Time Training State: Introduces commit/versioning semantics for fast-weight edits during inference but for serving correctness under batching, not for validating edit quality against a held-out probe with rollback to frozen baseline.; [2512.23675] End-to-End Test-Time Training for Long Context: Performs per-sequence weight adaptation at inference (TTT) but with no probe-based validate/commit/rollback guarantee against a frozen baseline.; [2605.22791] DeltaBox: Scaling Stateful AI Agents with Millisecond-Level Sandbox Checkpoint/Rollback: Implements transactional checkpoint/rollback but at the level of agent sandbox filesystem/process state, not weight deltas validated against a loss probe. | survives: Yes: none of these papers implement a staged ΔW buffer that is accept/reject-gated by comparing frozen-vs-staged loss on a held-out probe window before committing into the live forward pass, which is the proposal's specific transactional guarantee against a frozen baseline; nearest work either handles multi-tenant serving correctness (RW-TTT) or unconditional TTT without any validation/rollback gate (TTT-E2E), or transactional rollback for non-weight agent state (DeltaBox). | confidence 0.72

### Barrier-constrained expert routing  (splash 4, feasibility 3, id c427a1d0b8)

**Claim.** Control-barrier-functions applied to the moe-router so that gate logits are projected at inference onto the nearest point of a safety set (e.g. minimum per-expert utilization over a sliding window, or maximum logit gap bound) whenever the raw top-k choice would leave that set, guaranteeing the constraint holds every token without retraining.

**New capability.** The system can guarantee, by construction and verifiable at runtime, that no expert is starved or overloaded within any window of W tokens, a property the open-loop router cannot certify because it has no mechanism to correct a choice before it is committed.

**Placement.** Define h(state) = per-expert utilization over the last W tokens minus a floor epsilon; at each step compute the unconstrained top-k logits, then if projected utilization would violate h>=0, solve a small QP (closed-form for top-k, since it's just a logit bias) that minimally perturbs logits to keep h>=0 for the next step. No new trainable parameters; W and epsilon are hyperparameters; runs every token, O(num_experts) extra work.

**Invariant.** exactness recoverable by verification

**Falsifier.** H: CBF-constrained routing eliminates expert starvation events (zero-utilization windows) entirely while degrading perplexity by <1% vs. matched-FLOPs unconstrained router. Metric: count of starvation windows, held-out perplexity Baseline: standard top-k router with aux load-balance loss only, matched FLOPs Protocol: nano gpt2 MoE, 8 experts, 12 seeds, inject adversarial input distribution shift mid-eval to stress starvation, paperkiln events.jsonl logging GPU-hours: 26

**Nearest (generator's guess).** safe-RL control barrier functions (Ames et al.); no published application to MoE routing

**Scoper.** closest: [2504.03038] Learning to Adapt Control Barrier Functions Under Epistemic and Aleatoric Uncertainty: Uses CBF-QP safety filtering at runtime but for robot motion planning, not MoE expert-utilization routing.; [2509.23830] Bayesian Mixture-of-Experts: Towards Making LLMs Know What They Don't Know: Modifies the router mechanism itself for calibration/uncertainty, not a CBF-based hard constraint guaranteeing per-expert utilization bounds.; [2406.13233] AdaMoE: Token-Adaptive Routing with Null Experts for Mixture-of-Experts Language Models: Addresses expert load balancing via training-time loss, not a per-token runtime-verifiable safety projection with formal guarantees. | survives: Nothing in the set applies control-barrier-function-style constrained projection of router logits at inference to guarantee per-expert utilization/load bounds within a sliding window without retraining; existing MoE load-balancing work uses training-time losses or capacity heuristics rather than a certified per-token QP projection onto a safety set. | confidence 0.78

### TTT gate on residual  (splash 4, feasibility 3, id 4fa44608b8)

**Claim.** test-time-training applied to the residual stream so that a block can attenuate or overwrite directions written by earlier blocks rather than only add to them

**New capability.** the network can suppress or invert an earlier layer's contribution based on later context, instead of being restricted to superposition of all past writes

**Placement.** Insert a small per-block gating module (a low-rank linear map, rank 16-64, plus a scalar per-channel gate) that is updated by a few steps of gradient descent on a self-supervised reconstruction loss (predict the block's own input from a masked/noised version of the current residual) computed only from tokens up to and including position t; the fast weights are reset every k tokens (k=32-128) or decay with a fixed half-life. Trained jointly with the base model at nano-scale (~50-150M params) on gpt2 preset in paperkiln; the outer loop trains the initialization and learning rate of the inner TTT update, the inner loop runs at both train and inference time.

**Invariant.** outputs stay causal

**Falsifier.** H: adding a TTT-gated erase path to the residual stream reduces loss on tasks requiring overwriting stale bound information (e.g. variable-reassignment / state-tracking synthetic tasks) more than a matched-FLOPs deeper/wider baseline Metric: perplexity and task accuracy on a synthetic state-tracking benchmark (sequences with repeated variable rebinding) plus held-out validation loss Baseline: matched-FLOPs/params gpt2-nano with extra depth or width absorbing the TTT module's compute, no gating Protocol: nano-scale (~50-125M params), 12 seeds, 40 GPU-hour budget, pre-registered accuracy threshold on state-tracking task, ablate gate reset period k in {32,64,128} GPU-hours: 36

**Nearest (generator's guess).** Test-Time-Training layers (Sun et al. 2024) / TTT-Linear, applied here as a gating mechanism on residual writes rather than as a full sequence-mixing layer replacement

**Scoper.** closest: [2510.13876] What Layers When: Learning to Skip Compute in LLMs with Residual Gates: Nearest mechanism gates residual writes but only scales/skips branch outputs via a fixed learned gate, not via test-time gradient descent (TTT) on a self-supervised reconstruction loss to suppress/invert earlier contributions based on later context.; [2409.19606] Hyper-Connections: Also modifies how residual writes combine across depth, but via a static learned mixing matrix, not an inference-time gradient-based fast-weight update.; [2310.04625] Copy Suppression: Comprehensively Understanding an Attention Head: Shows an existing mechanism (attention head) that suppresses an earlier-layer contribution based on later context, but this is a fixed learned circuit, not a test-time-trained gate. | survives: Yes: no candidate uses inner-loop gradient descent on a self-supervised reconstruction loss to produce a fast-weight gate that attenuates/inverts specific earlier blocks' residual contributions conditioned on context up to position t; existing gating/hyper-connection work uses static learned parameters rather than test-time training. | confidence 0.72

### Kalman-fused router logits  (splash 3, feasibility 4, id 342139d049)

**Claim.** Kalman filtering applied to the moe-router so that expert selection fuses a prior gate score with a noisy downstream-error measurement from the previous token's routing outcome, in proportion to their estimated uncertainties.

**New capability.** The router can down-weight its own prediction for a token when recent routing has been measurably unreliable, instead of always trusting the raw gate score.

**Placement.** At each token position t, maintain a per-expert scalar state (mean, variance) updated online: prediction = router logit at t; measurement = a cheap proxy for expert t-1's usefulness (e.g. drop in next-token loss or gradient-norm signal from the auxiliary loss, computed causally using only tokens up to t-1); Kalman gain blends the two into the logit actually used for top-k selection at t. The gate and a learned per-expert measurement-noise estimate are trained jointly with the LM; the filter itself has no extra trainable weights beyond noise scalars, so the compute cost is one scalar update per expert per token.

**Invariant.** outputs stay causal

**Falsifier.** H: Kalman-fused routing reduces expert-choice regret (measured as loss of the greedily-best-in-hindsight expert vs. chosen expert) by >=10% relative to a matched-FLOPs top-k MoE baseline, without hurting perplexity. Metric: held-out perplexity and per-token expert regret Baseline: standard top-k linear router, matched params/FLOPs, same expert count Protocol: nano gpt2 MoE (4-8 experts, top-2) on paperkiln, 12 seeds, 40M-token budget, matched FLOPs via reduced hidden size on the KF variant to offset the state buffers GPU-hours: 30

**Nearest (generator's guess).** expert-choice routing / adaptive gating with EMA load-balancing (Zhou et al. 2022); no published Kalman-specific router

**Scoper.** closest: [2509.23830] Bayesian Mixture-of-Experts: Towards Making LLMs Know What They Don't Know: Closest match in placing uncertainty modeling directly on router logits, but uses variational/Bayesian distributions rather than a Kalman filter fusing a prior gate score with a causal downstream-error measurement.; [2605.11800] ROMER: Expert Replacement and Router Calibration for Robust MoE LLMs on Analog Compute-in-Memory Systems: Also adjusts router logits based on a noise/error signal, but the mechanism is static percentile normalization for hardware noise, not a per-token Kalman gain blending prior and measurement uncertainty.; [2609.08189] Do Dynamic Routers Need Memory? HeRo: History-Aware Routing for Efficient LLM Inference: Shares the idea of maintaining router state updated over sequence/depth, but uses linear-attention memory aggregation rather than a variance-weighted Kalman fusion of prediction and measurement. | survives: The specific mechanism—maintaining an explicit per-expert (mean, variance) Kalman state that computes a gain from estimated prediction vs. measurement noise to blend the raw gate logit with a causal downstream-usefulness proxy from the previous token—is not implemented by any candidate; the nearest works either impose static/Bayesian distributions over routing or recalibrate logits without an online uncertainty-weighted fusion rule. | confidence 0.72

### Transactional block commit  (splash 3, feasibility 4, id 35f9b215c9)

**Claim.** transactions-and-rollback applied to the residual stream so that a block's write is either fully committed or fully discarded based on a learned validity check, giving the stream an erase-like veto it lacks by default

**New capability.** the model can detect that a block's proposed update conflicts with or is redundant/harmful given the current stream state and abstain from writing it, something pure addition cannot express

**Placement.** Each block computes its usual delta d_l, then a small learned scalar 'commit gate' c_l in [0,1] (a 2-layer MLP on [residual, d_l]) trained end-to-end via a straight-through estimator; the effective write is c_l * d_l, and c_l can hard-round to 0 at inference (rollback = full discard) checked against a validation KL-divergence budget so aggregate behavior stays close to the always-commit baseline unless discarding strictly helps. Trained from scratch at matched FLOPs/params on paperkiln gpt2-nano preset, gate parameters counted against the parameter budget.

**Invariant.** matched FLOPs/params

**Falsifier.** H: learned commit-gating reduces catastrophic interference on tasks with distractor context (irrelevant earlier tokens that a block would otherwise blend in) versus a matched-budget baseline with no gating Metric: validation perplexity and accuracy on a distractor-insertion synthetic task where correct answer requires ignoring a spurious earlier write Baseline: matched-FLOPs gpt2-nano, same depth/width, gate replaced by an always-1 (always commit) constant Protocol: nano-scale, 12 seeds, 40 GPU-hour budget, measure fraction of gates that saturate near 0 vs 1, report ablation with gate frozen at 1 GPU-hours: 30

**Nearest (generator's guess).** Highway networks / GRU-style gated residual (Srivastava et al. 2015), reframed as commit/rollback

**Scoper.** closest: [2409.19606] Hyper-Connections: closest neighbouring mechanism (gated/learned residual mixing at the residual stream) but it reweights/mixes multiple streams continuously rather than learning a discrete commit/veto gate that can fully discard a block's write based on a validity check against the current state; [1909.12117] Balanced Binary Neural Networks with Gated Residual: uses a gated residual mechanism but for compensating binarization information loss, not for a learned all-or-nothing commit/rollback veto trained via straight-through estimation with a KL budget; [1710.04773] Residual Connections Encourage Iterative Inference: analyzes residual blocks as iterative refinement but does not propose or study a learned gate that can veto/discard a block's update | survives: Yes: none of the candidates implement a per-block scalar commit gate trained end-to-end (straight-through) that can hard round to zero at inference to fully discard a conflicting/redundant update while bounding aggregate KL drift from an always-commit baseline; the Hyper-Connections family reweights/mixes continuous streams rather than learning a discrete abstain-from-writing veto conditioned on write-write conflict detection. | confidence 0.62

### graph-propagated position state  (splash 4, feasibility 2, id ed07b1373e)

**Claim.** graph-message-passing applied to positional-encoding so that each token's position representation is updated by passing messages along an explicit sequence graph (edges to recent tokens, sink tokens, and cache-retained tokens) instead of being a fixed function of its integer index

**New capability.** the model can give two tokens the 'same effective position' when the graph collapses paths between them (e.g. after a deletion/edit in a streaming edit-buffer), something impossible when position is index-only and edits shift every later index

**Placement.** define an explicit DAG over the sequence: each token connects to its immediate predecessor, to sink tokens, and to any tokens marked 'retained' after a KV-cache eviction; run 2 rounds of GNN message passing per layer over this DAG at every generation step to produce a positional feature that is added to RoPE angle before attention; the GNN (2-layer, shared across positions) is trained jointly from scratch at matched budget.

**Invariant.** outputs stay causal; the DAG only contains edges from later to earlier tokens (message passing respects generation order), so no node aggregates information from a future token

**Falsifier.** H: graph-based position recovers coherent continuations after simulated mid-stream edits (insertions/deletions) better than fixed-index RoPE, which must renumber and thus disrupts learned position-content associations Metric: perplexity/accuracy on a constructed edit-continuation task where a chunk is deleted mid-context and generation must continue coherently Baseline: gpt2-nano fixed RoPE re-indexed after edit Protocol: nano from-scratch, 12 seeds, sliding-window lane with synthetic edit insertion, matched FLOPs (GNN cost budgeted against extra attention layers removed) GPU-hours: 38

**Nearest (generator's guess).** graph-based/relative position schemes for edited or tree-structured sequences (e.g. Graphformer, structural position encodings for code); no direct nano precedent for streaming edit-buffers

**Scoper.** closest: [2401.17791] Graph Transformers without Positional Encodings: Uses graph-structure-aware attention as an alternative to explicit PE, but not graph-message-passing over a streaming edit/eviction DAG to enable position collapse after deletions.; [2305.17589] Graph Inductive Biases in Transformers without Message Passing: Addresses graph positional encoding without message passing, the inverse of the proposal's use of message passing for sequence position, and targets static graphs not streaming KV-cache edit buffers.; [2602.15239] Size Transferability of Graph Transformers with Convolutional Positional Encodings: Closest in using GNN-based PE for transformers, but for static graph-size transferability, not for collapsing positions across edits/deletions in a streaming KV cache. | survives: None of the papers apply message-passing PE to an explicit dynamically-edited sequence DAG (predecessor + sink + cache-retained edges) specifically to let post-deletion tokens share collapsed effective positions in a streaming edit-buffer/KV-cache setting; this specific mechanism-purpose pairing for streaming edits appears absent. | confidence 0.72

### test-time position adapter  (splash 3, feasibility 4, id 4f5c2656aa)

**Claim.** test-time-training applied to positional-encoding so that a small set of position-dependent parameters (e.g. RoPE frequency scaling factors or ALiBi slopes) are updated via a self-supervised next-token loss on the incoming long stream itself, adapting the effective position function to the actual lengths encountered at inference.

**New capability.** the model can recalibrate its notion of 'distance' online as it streams past its training length, rather than being frozen into whatever position function was fixed at train time.

**Placement.** expose a handful of scalar parameters (per-head RoPE base frequency, or per-head ALiBi slope) as the only trainable tensors at inference; every k tokens (e.g. every 256), compute a self-supervised loss (next-token cross-entropy on the last window) and take a few gradient steps on just these scalars, holding all other weights frozen; reset or persist state depending on session. Training signal is the stream's own continuation, no external labels.

**Invariant.** base model untouched (only a handful of scalar position parameters are updated, all other weights frozen); outputs remain causal since updates at step t only ever use tokens ≤ t and are applied to future predictions.

**Falsifier.** H: TTT-adapted position scalars reduce perplexity at long context vs frozen positional encoding, matched FLOPs (the TTT gradient steps counted against budget). Metric: perplexity at 2x/4x training length with vs without TTT updates; wall-clock overhead per token Baseline: frozen RoPE/ALiBi at same lengths, and a matched-FLOPs baseline that spends the extra TTT compute on additional forward-pass ensembling instead Protocol: nano-llama preset with RoPE, enable TTT only on frequency/slope scalars, update every 256 tokens, 12 seeds, cap at 40 GPU-hours GPU-hours: 25

**Nearest (generator's guess).** TTT (test-time training) layers of Sun et al.; NTK-aware RoPE scaling adapted online

**Scoper.** closest: [2606.21803] Test-Time Training with Next-Token Prediction: Applies TTT with a next-token self-supervised signal at test time on long contexts, but the adapted object is a fast-weight/value-write mechanism in the cache, not the positional-encoding parameters (RoPE frequencies/ALiBi slopes).; [2512.12167] Extending the Context of Pretrained LLMs by Dropping Their Positional Embeddings: Also recalibrates the model's handling of position at inference/post-training time for length extension, but by dropping PE entirely rather than test-time-training a handful of RoPE/ALiBi scalar parameters via gradient steps on the incoming stream.; [2310.16450] CLEX: Continuous Length Extrapolation for Large Language Models: Adapts RoPE-style scaling factors for length extrapolation, but the scaling function is learned during training/fine-tuning, not test-time-trained online via self-supervised next-token loss on the incoming stream itself. | survives: Yes: no paper in the set actually performs online gradient updates on RoPE/ALiBi scalar parameters using a self-supervised next-token loss computed on the live inference stream itself (i.e., true test-time-training restricted to positional parameters, updated every k tokens during a session), as opposed to fixed/learned-at-train-time scaling functions or fast-weight cache rewrites elsewhere in the architecture. | confidence 0.72

### Kalman-fused positional estimate  (splash 4, feasibility 2, id 763d1f4c36)

**Claim.** Kalman filtering applied to positional-encoding so that the position signal fed to attention is a fused estimate combining the nominal index (a noisy 'prediction' that becomes unreliable past train length) with a measurement derived from content-based cues (e.g. induction-head-style repeat detection or attention-entropy signals), weighted inversely to each source's estimated uncertainty at that distance.

**New capability.** the model can maintain a usable notion of relative position past the training length by trusting content-derived position cues more heavily exactly where the parametric position encoding's uncertainty (learned from training data) is known to be high.

**Placement.** at each layer, alongside RoPE angle theta_nominal(t), compute a content-derived position estimate theta_hat(t) from a lightweight learned function of recent attention patterns (e.g. a small linear probe on attention-entropy/repeat statistics); maintain a scalar uncertainty schedule sigma_nominal(d) fit at train time (from validation loss vs distance) and sigma_measurement fit similarly; fuse via standard Kalman update to get theta_fused, used in place of theta_nominal for rotary application. Trained once at pretraining time (probe + uncertainty schedules), frozen at inference, applied every token.

**Invariant.** matched FLOPs/params (probe is small, added to both baseline and treatment as dead weight for fairness); outputs remain causal since theta_hat(t) only depends on attention statistics up to t.

**Falsifier.** H: Kalman-fused positions yield lower perplexity beyond training length than nominal RoPE alone, matched params, and the fusion weight shifts toward the content estimate as distance grows past train length (verifiable from logged sigma schedules). Metric: perplexity at 1x/2x/4x train length; fraction of Kalman gain attributed to measurement term vs distance Baseline: matched-param nano-llama with plain RoPE, and an ablation with the probe present but gain fixed at 0 (pure nominal) Protocol: nano-llama preset, 12 seeds, log gain/sigma schedules to events.jsonl for falsification, 40 GPU-hour cap GPU-hours: 35

**Nearest (generator's guess).** no direct match; adjacent to ALiBi/xPos decay-based extrapolation and induction-head literature, but explicit Kalman fusion of two position estimates is novel here

**Scoper.** closest: [2505.22842] Bayesian Attention Mechanism: A Probabilistic Framework for Positional Encoding and Context Length Extrapolation: Closest conceptually: treats positional encoding probabilistically with uncertainty to aid extrapolation, but uses a fixed prior distribution, not a Kalman fusion of two dynamically-weighted position estimates (nominal vs content-derived).; [2506.00920] Position as Probability: Self-Supervised Transformers that Think Past Their Training for Length Extrapolation: Also maintains a probabilistic/filter-like position estimate for extrapolation, but it is a self-contained histogram filter learned end-to-end, not a fusion of a nominal RoPE angle with a separate content-derived (induction-head-style) measurement via explicit Kalman gain.; [2405.14722] DAPE: Data-Adaptive Positional Encoding for Length Extrapolation: Shares the idea of adapting positional encoding using content/context signals, but does not fuse two explicit uncertainty-weighted estimates via Kalman filtering, nor derive the content estimate from induction-head/attention-entropy cues. | survives: The specific mechanism of explicit two-source Kalman fusion (nominal RoPE angle as noisy prediction + content-derived induction/attention-entropy measurement, combined via inverse-uncertainty weighting fit from validation loss vs distance) is not directly instantiated in any candidate; the closest works either use a fixed probabilistic prior (BAM) or a self-supervised histogram filter (PRISM) rather than fusing two distinct estimator sources with a Kalman-style gain, so a proposal specifically demonstrating this dual-source fusion with learned uncertainty schedules would still be new. | confidence 0.62

### fast-weight sampling memory  (splash 3, feasibility 4, id 19dba99fb8)

**Claim.** Hebbian outer-product fast-weights applied to the sampler so that recently sampled tokens transiently bias per-position top-k/temperature decisions for the next few steps, giving the decoder a short adaptive memory instead of one static global knob.

**New capability.** The sampler can locally suppress or boost token probabilities based on very recent sampling history (e.g. avoid immediate repetition or lock onto a recently established register) without any change to the frozen model weights or a hand-tuned repetition penalty, and this bias decays automatically within a few steps by construction of the fast-weight update rule.

**Placement.** Maintain a small fast-weight matrix F (vocab x vocab or embedding x embedding) initialized to zero at sequence start. After sampling token x_t, update F += eta * outer(embed(x_t), embed(x_t)) then decay F *= gamma each step (gamma<1, few-step memory). At each step, add F applied to the current hidden state (or a lookup bias on logits) before top-k/temperature truncation. Only eta, gamma, and the projection matrix (if any) are trained/tuned on a small held-out set; the base model is untouched.

**Invariant.** base model untouched; the fast-weight bias is an additive term computed and applied entirely at decode time on top of frozen logits, with F reset per sequence so it cannot leak information across examples or violate causality (F at step t depends only on tokens 1..t).

**Falsifier.** H: Fast-weight-biased sampling reduces degenerate repetition (measured by repetition rate / self-BLEU) relative to a matched-entropy static top-k/temperature baseline without degrading perplexity beyond a preregistered margin. Metric: repetition rate (n-gram recurrence), self-BLEU, and perplexity delta vs baseline Baseline: fixed top-k=40/temperature=0.8, and separately a standard repetition-penalty baseline Protocol: nano-gpt2, 12 seeds, generations of length 512, matched decode FLOPs (fast-weight update cost budgeted), sweep eta/gamma on 3 seeds held out from evaluation GPU-hours: 15

**Nearest (generator's guess).** repetition penalty / frequency penalty decoding, and fast-weight programmer architectures (Schmidhuber, Ba et al.) applied at train time rather than decode time

**Scoper.** closest: [2307.03214] PREADD: Prefix-Adaptive Decoding for Controlled Text Generation: Both modify logits pre-sampling to control generation, but PREADD uses static prompt contrast, not a decaying Hebbian trace of recently sampled tokens.; [2604.00018] Think Twice Before You Write -- an Entropy-based Decoding Strategy to Enhance LLM Reasoning: Adapts sampling decisions per step from recent context signals, but uses entropy-based branching rather than a persistent outer-product fast-weight memory biasing logits.; [2309.02772] Hot or Cold? Adaptive Temperature Sampling for Code Generation with Large Language Models: Adjusts sampling temperature adaptively at decode time, but based on token category/position, not a Hebbian trace of just-sampled tokens with built-in decay. | survives: The specific mechanism of an outer-product Hebbian fast-weight matrix over sampled-token embeddings, updated and exponentially decayed step-by-step to bias top-k/temperature at the sampler with automatic few-step forgetting, is not implemented by any candidate; nearest works use static prompt contrast, entropy-triggered branching, or heuristic temperature schedules rather than a self-decaying associative memory keyed on recent sampling history. | confidence 0.75

### multi-lane consensus prefill  (splash 3, feasibility 4, id 3733172871)

**Claim.** consensus-protocols applied to prefill so that several cheap, differently-approximated prefill lanes (e.g. sliding-window, sink, low-rank) vote on the token-1 distribution and the vote itself is used as the emission signal, with the exact full-attention prefill running as an arbiter only when lanes disagree

**New capability.** the system can emit the first token as soon as a quorum of cheap approximate lanes agree, skipping full exact prefill entirely on the (measurable) fraction of prompts where lanes concur, while retaining the exact pass as a fallback whenever they don't — giving average-case TTFT below the exact-prefill lower bound without ever silently returning a wrong first token.

**Placement.** Run 3-5 cheap prefill lanes in parallel (paperkiln's existing sliding-window and sink attention lanes plus a truncated-rank lane), each producing a token-1 logit distribution; compare pairwise KL/agreement across lanes; if all lanes agree within threshold, emit the majority-vote token and continue generation, deferring exact full-attention prefill to run in the background and only used to verify/replace if a later check (e.g. exact prefill result compared post-hoc) reveals disagreement, triggering a corrective re-emission. No component is retrained beyond calibrating the agreement threshold on a held-out prompt set.

**Invariant.** exactness is recoverable by verification; the exact full-attention prefill always eventually runs and its result is compared against the emitted token, so any consensus error is caught and corrected (base model untouched, lanes are pre-existing attention modes in paperkiln).

**Falsifier.** H: quorum agreement across cheap lanes predicts exact-prefill token-1 output with high precision, giving a TTFT reduction on the agreement fraction of prompts without increasing wrong-first-token rate versus exact baseline Metric: TTFT (agreement-fraction-weighted), quorum precision (P(exact token = vote | agreement)), corrective re-emission rate Baseline: standard exact prefill only, matched FLOPs summed across all lanes vs single exact pass Protocol: paperkiln nano gpt2/llama presets, exact/sliding-window/sink lanes as-is plus one added low-rank lane, 12 seeds, prompts 128-1024 tokens, sweep agreement threshold GPU-hours: 22

**Nearest (generator's guess).** ensemble/self-consistency early-exit and mixture-of-attention-lanes, closest to speculative decoding's draft-verify structure but voting-based rather than single-draft

**Scoper.** closest: [2503.18076] A Multi-Model Adaptation of Speculative Decoding for Classification: This is the closest known mechanism (multiple cheap workers vote, expensive judge only on disagreement) but applied to classification labels, not to accelerating the first-token emission of prefill by skipping exact full attention.; [2503.05330] Speculative Decoding for Multi-Sample Inference: Uses consensus across parallel generation paths as the draft signal, but for multi-sample decode-time drafting, not for replacing exact prefill with cheap approximate-attention lanes voting on token-1.; [2502.02789] Speculative Prefill: Turbocharging TTFT with Lightweight and Training-Free Token Importance Estimation: Targets TTFT/prefill speedup via a cheap approximation, but reduces token count fed to exact prefill rather than using a voting quorum of multiple cheap attention-approximation lanes to skip exact prefill outright. | survives: No paper places multiple cheap differently-approximated prefill lanes (sliding-window, sink, low-rank) in parallel, uses cross-lane agreement/KL as a quorum gate to emit token-1 without ever running exact full-attention prefill on the agreeing fraction of prompts, and falls back to exact prefill only as a post-hoc arbiter/corrective mechanism — this specific consensus-gated skip-or-fallback structure at the prefill component for TTFT reduction is not covered by the nearest classification-consensus, multi-sample-consensus, or token-pruning-prefill papers. | confidence 0.68

### hebbian fast-weight depth gate  (splash 3, feasibility 4, id ae20f33a42)

**Claim.** Hebbian fast-weight outer-product updates applied to the layer-schedule controller so that recently-seen token/context patterns transiently lower or raise the depth a token is routed through within a sequence, without any slow-weight (backprop) update.

**New capability.** Within a single streaming sequence, the model can adapt its per-token compute allocation based on very recent within-sequence repetition (e.g. a repeated entity or phrase gets progressively shallower routing as its fast-weight association strengthens) without retraining or altering base parameters — a static layer schedule cannot exhibit this within-sequence adaptivity.

**Placement.** Maintain a small fast-weight matrix A (outer-product accumulator, decayed each step: A <- lambda*A + eta*h_t h_t^T over a low-dim projection of hidden state) updated at every token during the forward pass only, read by a gating scalar g_t = sigmoid(h_t^T A h_t) that determines how many of the top blocks to skip for token t; A is reset at sequence start (streaming-compatible) and never touched by the optimizer. Base model is untouched (graft-trained): only the low-rank projection matrices feeding into A are trained, at matched budget, on top of a frozen pretrained nano-gpt2.

**Invariant.** base model untouched; the fast-weight gate and its small projection matrices are the only trained/adapted component, frozen base weights are never modified, and streaming holds because A is causal and sequence-local (reset per sequence, updated only from past tokens).

**Falsifier.** H: The Hebbian fast-weight depth gate reduces average blocks-executed by >=15% on sequences with repeated spans (e.g. synthetic repeated-entity benchmark) at <=0.5% perplexity increase versus frozen full-depth baseline. Metric: average depth used vs perplexity on repetition-heavy vs non-repetitive held-out sets Baseline: frozen nano-gpt2 full fixed depth, no gate Protocol: nano-gpt2 preset frozen base, 12 seeds, pre-registered decay lambda and learning rate eta grid, 40 GPU-hour budget GPU-hours: 26

**Nearest (generator's guess).** fast weight programmers (Schmidhuber); Hebbian fast weights for attention (Ba et al. 2016); early-exit with recurrent state

**Scoper.** closest: [2503.23798] Adaptive Layer-skipping in Pre-trained LLMs: Same capability (input-dependent depth reduction, especially for repeated tokens) via a trained plug-in router/adapter on a frozen backbone, but the router is a slow-weight trained gate, not a Hebbian fast-weight outer-product accumulator updated online within the sequence.; [2602.01219] Mixture-of-Top-k Attention: Efficient Attention via Scalable Fast Weights: Uses the fast-weight framing for routing/attention but applies it to token-expert/attention routing, not to a layer-depth-skipping gate driven by a Hebbian outer-product accumulator over hidden states.; [2606.16112] Scaling Adaptive Depth with Norm-Agnostic Residual Networks: Also produces an adaptive per-token depth-skip gate, but the gate is learned via backprop on residual-norm structure, not a training-free Hebbian fast-weight accumulator reset per sequence. | survives: No candidate combines a genuinely non-backprop, per-sequence-reset Hebbian outer-product accumulator (A <- lambda*A + eta*h h^T) as the actual gating signal for layer-skip depth; all depth-adaptive/layer-skip works here use slow-weight trained routers/gates (even when noting repetition lowers required depth empirically), so the specific mechanism-purpose pairing (fast-weight Hebbian trace driving a depth gate, reset per sequence, no optimizer touch) is not demonstrated. | confidence 0.62

### Kalman-gated block re-entry  (splash 4, feasibility 2, id ca627b8ae2)

**Claim.** kalman-filtering applied to the layer-schedule so that a block's re-application is triggered and weighted by fusing the block's own output (measurement) with a running prediction of the token's hidden state (prior), re-applying the block only when the fused uncertainty stays high

**New capability.** the model can decide, per token and per pass, how much to trust a fresh block application versus its prior running estimate, and loop the block only while uncertainty (residual innovation) remains large, giving a principled data-driven halting signal rather than a fixed schedule

**Placement.** at one designated block, maintain a running estimate mu_t and uncertainty sigma_t^2 of the token's hidden state; each pass, treat the block's raw output as a noisy measurement with a learned observation-noise head R(h), compute the Kalman gain K = sigma_t^2/(sigma_t^2+R), update mu_{t+1} = mu_t + K*(measurement-mu_t) and shrink sigma_t^2, and stop re-applying the block when sigma_t^2 falls below a threshold or a max-pass cap is reached; the observation-noise head and threshold are trained jointly with the LM loss, from scratch at matched budget with max-pass capped to match baseline FLOPs.

**Invariant.** outputs stay causal; the filter only fuses the current token's own successive measurements over passes, never reading future tokens, so causality is preserved.

**Falsifier.** H: Kalman-gated re-entry reduces val loss vs matched-FLOPs fixed-depth baseline, with fewer passes spent on low-loss (easy) tokens Metric: val loss, mean passes per token vs per-token baseline loss correlation Baseline: fixed-depth gpt2-nano at matched average FLOPs (max-pass cap tuned to match) Protocol: nano preset, 12 seeds, max-pass cap in {1,2,3}, 40 GPU-hour budget GPU-hours: 30

**Nearest (generator's guess).** Adaptive Computation Time; no direct Kalman-filter-as-halting-signal precedent found in transformer depth literature

**Scoper.** closest: [2603.01914] AdaPonderLM: Gated Pondering Language Models with Token-Wise Adaptive Depth: same capability (per-token, per-pass halting for looped block re-application) but via learned MLP gates on hidden state, not a Kalman filter fusing measurement/prior with an explicit gain and uncertainty variable.; [2605.23872] Training-Free Looped Transformers: shares the idea of repeatedly re-applying a block as an iterative refinement/update but uses an ODE/damping view, not a Kalman gain/innovation-based stopping rule.; [2607.20519] Adaptive Depth in Looped Transformers: Diagnosing Learned Halting Gates and Trajectory Readouts: addresses halting-gate design for looped blocks (same component/purpose) but analyzes generic learned confidence gates, not a Kalman-filter fusion of measurement and prior with gain-weighted update. | survives: The specific mechanism—explicit Bayesian fusion (Kalman gain) of the block's raw output as a noisy measurement against a running mean/variance prior, with a learned observation-noise head and innovation-based halting threshold—is not instantiated in any candidate; all found methods use scalar confidence/halting gates or ODE-style damping rather than a two-moment (mu, sigma^2) recursive estimator, so this remains a live, non-duplicated idea. | confidence 0.72

### Graph-message-passing FFN memory  (splash 4, feasibility 2, id 76a6c40498)

**Claim.** Graph message-passing applied to the FFN so that its key-value memory slots are nodes that exchange updates with each other (not just with the token stream) at each step, letting related memory slots co-adapt.

**New capability.** The model can propagate a single new piece of information into multiple related memory slots simultaneously (e.g. updating one fact updates its semantic neighbors), which a per-token, per-slot-independent FFN cannot do by construction.

**Placement.** Treat the FFN's N hidden units (key-value memory slots) as nodes of a fixed sparse graph (top-k cosine-similarity among key vectors, computed once at init or periodically every E tokens); after each token's FFN write (e.g. via the delta-rule update from 43e1b3d069, or a simple additive Hebbian write), run one message-passing round where each slot's value is updated as a weighted average with its graph neighbors' values, gated by a learned mixing coefficient. Trained from scratch at nano-scale; the graph structure and mixing gate are the only new learned components.

**Invariant.** outputs remain causal; message passing only propagates information already written by tokens <= t, and executes before token t+1 is read, so no future leakage.

**Falsifier.** H: Graph-propagated FFN memory generalizes a single injected fact to semantically related probes (zero-shot) better than an equivalent per-slot-independent memory-write baseline at matched FLOPs/params. Metric: accuracy on held-out semantically-related probe set after single fact injection, vs. unrelated control probes (should NOT improve, checking for spurious leakage) Baseline: matched-FLOPs delta-rule FFN without the message-passing round (i.e. same base mechanism, graph step ablated) Protocol: 12 seeds, synthetic knowledge-graph-structured associative recall task (facts + paraphrase/neighbor probes), matched param/FLOP budget via slightly smaller hidden dim to pay for message-passing cost GPU-hours: 32

**Nearest (generator's guess).** Hopfield-network / graph-structured fast weights; Memory networks with relational propagation (e.g. Graph Memory Networks); not standard in transformer FFNs

**Scoper.** closest: [2605.08143] HoReN: Normalized Hopfield Retrieval for Large-Scale Sequential Model Editing: Uses associative dynamics among key-value memory entries in an MLP-wrapped memory for editing, closest in mechanism flavor but applied to sequential edit storage/retrieval, not per-token FFN message-passing among hidden units during normal forward computation.; [2412.08193] Mixture of Experts Meets Decoupled Message Passing: Towards General and Adaptive Node Classification: Combines message-passing with FFN/expert-like structures, but for graph node classification, not for propagating updates among a transformer's FFN key-value memory slots.; [2510.08284] Neuron-Level Analysis of Cultural Understanding in Large Language Models: Analyzes FFN neurons as discrete knowledge units but does not propose any mechanism for them to exchange updates with each other. | survives: Nothing in the set makes FFN hidden units into graph nodes that exchange value updates with each other after each token write; the nearest work (HoReN) only does content-addressed retrieval/denoising over a codebook for offline sequential edits, not per-token online message-passing between memory slots during normal forward passes, so the proposed simultaneous propagation to semantic neighbors during training-time inference remains untested. | confidence 0.72

### spectral cross-sequence normalisation  (splash 3, feasibility 4, id 8fc4b86001)

**Claim.** computing normalisation statistics in a frequency basis over a sliding window of token activations applied to normalisation so that low-frequency (long-range/slowly-varying) components are whitened jointly instead of per-token

**New capability.** the model can suppress sequence-level drift (e.g. slow topic or scale shifts across a document) that per-token LayerNorm cannot see because it normalises each token independently

**Placement.** at each block, before the existing LayerNorm, take the last W token activations (W=128-512, causal window) per channel, apply a real DCT along the sequence axis, divide the low-frequency coefficients by their running RMS (causal, computed only from positions <= current), inverse-transform, then apply standard per-token LayerNorm on top. Only the causal-window DCT statistics are new parameters-free machinery; the surrounding network trains from scratch at matched FLOPs/params by allocating the DCT's extra multiply-adds against a slightly smaller FFN.

**Invariant.** outputs remain causal; the DCT window only ever reads positions up to and including the current token, and the transform is recomputed identically at every step so nothing depends on future tokens

**Falsifier.** H: spectral cross-token normalisation reduces validation loss and improves long-range copy/retrieval tasks vs matched-FLOPs LayerNorm baseline Metric: val perplexity and synthetic long-range associative-recall accuracy Baseline: gpt2-nano with standard LayerNorm, same param count (FFN width reduced to match FLOPs) Protocol: nano gpt2 preset, 12 seeds, 40M token budget, sliding-window W in {128,256}, report mean+-std, pre-registered stop at fixed step count GPU-hours: 30

**Nearest (generator's guess).** FNet / spectral mixing layers; RMSNorm variants with windowed statistics

**Scoper.** closest: [2504.11513] Multi-output Classification Framework and Frequency Layer Normalization for Compound Fault Diagnosis in Motor: This applies a frequency-domain normalization but as a domain-adaptation feature-preservation tool for fault diagnosis signals, not as a causal sliding-window DCT whitening layer inserted before LayerNorm in a transformer to suppress sequence-level drift.; [2406.19770] Self-Supervised Spatial-Temporal Normality Learning for Time Series Anomaly Detection: Concerns temporal normality modeling for anomaly detection, not a causal spectral normalization mechanism inserted at the transformer block level.; [2312.04501] Graph Metanetworks for Processing Diverse Neural Architectures: Only tangentially about normalization layers as objects of study, unrelated to spectral cross-sequence statistics. | survives: Nothing in this set implements a causal windowed-DCT whitening of low-frequency coefficients inserted before LayerNorm at each transformer block to suppress sequence-level drift that per-token normalization cannot see; the nearest analog (FLN) is a domain-adaptation frequency-preserving normalization for fault diagnosis, not a causal in-place spectral rewrite of a sliding activation window for language/sequence models. | confidence 0.72

### Mixture-density attention abstention  (splash 3, feasibility 3, id 1d02d5b812)

**Claim.** A mixture-density-head applied to attention-scores so that each query predicts a small mixture of candidate attention distributions (including a degenerate 'attend-to-nothing' component) instead of one fixed softmax, letting the model represent abstention as one mixture mode with nonzero learned weight.

**New capability.** The model can express multi-modal uncertainty about where to attend, including a distinct abstain mode whose weight is learned per-query, rather than being forced into a single normalized distribution over content keys.

**Placement.** Replace per-head softmax with K small mixture components (K=2-4): each component is an independent softmax over keys-plus-null, and a router MLP on the query predicts mixture weights pi_1..K over components (one component pinned to be all-mass-on-null, i.e. pure abstain). Output is the weighted sum of component outputs. Router and component logits trained jointly with LM loss; computed every layer, every token, nano-GPT2/Llama presets.

**Invariant.** outputs stay causal; router and all mixture components condition only on the query and its visible keys.

**Falsifier.** H: Mixture-density attention increases effective abstain-mode usage on low-information contexts (measured by pi_abstain) without degrading perplexity vs matched-FLOPs single-softmax baseline. Metric: validation perplexity; mean pi_abstain on synthetic no-signal-prefix probes vs natural text; calibration of pi_abstain against ground-truth 'irrelevant context' labels in a constructed probe set Baseline: matched-FLOPs single-softmax attention with equivalent extra params spent on wider heads Protocol: nano-scale, 12 seeds, K in {2,3,4}, matched FLOPs, fixed probe suite pre-registered GPU-hours: 30

**Nearest (generator's guess).** Mixture-of-softmaxes (Yang et al., 'Breaking the Softmax Bottleneck') applied to output layer, here moved to attention weights themselves — the move is non-trivial but adjacent

**Scoper.** closest: [2502.00281] Sigmoid Self-Attention has Lower Sample Complexity than Softmax Self-Attention: A Mixture-of-Experts Perspective: uses a mixture-of-experts framing to analyze attention scores but as a theoretical lens for sigmoid vs softmax comparison, not to add a learned abstain component with multiple mixture modes at inference.; [2606.18283] Gaussian Mixture Attention: Linear-Time Sequence Mixing via Probabilistic Latent Routing: replaces pairwise attention with a mixture-of-Gaussians routing mechanism over keys, but the goal is linear-time efficiency, not multi-modal uncertainty or an explicit abstain mode.; [2505.06708] Gated Attention for Large Language Models: Non-linearity, Sparsity, and Attention-Sink-Free: introduces a gating mechanism that can suppress attention output (functionally an abstain-like effect) but via a single scalar sigmoid gate, not a mixture-of-softmaxes with multiple learned modes including a dedicated null component. | survives: None of the papers implement a per-query mixture of several full softmax-over-keys distributions (K>=2) with one component pinned to pure null/abstain and a router predicting mixture weights trained jointly with the LM loss; the closest works either use mixture-of-experts as a theoretical attention-analysis tool, replace attention with Gaussian-mixture routing for efficiency, or add a single scalar gate for sink suppression, so the proposed multi-modal discrete mixture with an explicit abstain mode as one of several attention distributions appears genuinely novel. | confidence 0.68

### Multiresolution residual channels  (splash 3, feasibility 3, id b49bab8f1b)

**Claim.** Wavelet-multiresolution decomposition applied to the residual stream so that each token's representation is carried at several explicit bandwidths simultaneously, letting cheap tokens use only coarse channels while hard tokens engage fine channels.

**New capability.** The model can allocate effective per-token capacity along the residual width by writing/reading only the resolution bands a token needs, rather than every token paying for the full width at every layer.

**Placement.** Partition the residual channel dimension into a fixed number of bands (e.g. 4 dyadic bands via a learned orthogonal/Haar-like transform applied once at embedding and inverted at output). Each block writes its update through a per-band gate produced by a tiny linear-softmax head conditioned on the current token's coarsest band only (causal, no lookahead needed since gate depends on that token's own state). Trained from scratch at matched parameter count by folding the transform+inverse into the embedding and unembedding matrices; gates add O(bands) extra scalar params per block.

**Invariant.** matched FLOPs/params; the transform is a fixed invertible linear map plus small gating heads sized to equalize total parameter and multiply-add count against a vanilla residual-stream baseline of the same nominal width.

**Falsifier.** H: Gated multiresolution residual channels reduce validation loss at matched FLOPs/params versus a vanilla same-width residual stream, with the gain concentrated on high-loss (harder) tokens. Metric: token-level cross-entropy, broken out by baseline-model per-token loss quartile Baseline: gpt2-nano preset, same depth/width, matched param count via the transform folded into embed/unembed Protocol: 12 seeds, nano preset on paperkiln, 3 concurrent L4 sessions, fixed token budget matched to baseline's FLOPs, held-out validation slice for stratified loss reporting GPU-hours: 30

**Nearest (generator's guess).** Multi-scale/hierarchical representations (e.g. Funnel-Transformer, wavelet neural nets); not standard in residual-stream design

**Scoper.** closest: [2606.18246] Variable-Width Transformers: Allocates residual width non-uniformly but across depth (per-layer), not per-token bandwidth bands within a wavelet-decomposed residual channel.; [2510.13876] What Layers When: Learning to Skip Compute in LLMs with Residual Gates: Achieves per-token adaptive compute via gating on the residual stream, but skips whole layers rather than partitioning channel width into wavelet bands.; [2606.31859] Review Residuals: Update-Conditioned Residual Gating for Transformers: Gates residual updates per-token but as a scalar multiplier on the whole update, not a multiresolution channel-band decomposition. | survives: The specific mechanism of a fixed wavelet/dyadic multiresolution decomposition of the residual channel dimension, with per-band causal gating so tokens selectively read/write coarse vs fine bands, is not implemented by any candidate; nearest works vary depth-wise width or gate whole-layer/whole-update magnitude rather than partitioning and selectively activating explicit frequency bands within the residual width. | confidence 0.72

### renormalized multiscale position code  (splash 3, feasibility 3, id 4d55a30238)

**Claim.** a renormalization-group coarse-graining procedure applied to positional-encoding so that position is represented as a hierarchy of scales where only the frequency components that survive block-averaging at each scale are kept, letting extrapolation reuse low-frequency (coarse-scale) structure unseen-length-independent

**New capability.** the model can represent position at a sequence of dyadic scales (1,2,4,...,2^k tokens) with an explicit RG flow linking adjacent scales, so at inference on longer sequences it can fall back to the coarse, length-invariant scales instead of the whole encoding degrading uniformly

**Placement.** precompute a pyramid of position codes: at scale s, position index is block-averaged into buckets of size 2^s and given a sinusoidal code; train a small linear map (the RG step) that predicts scale-s+1 code from scale-s code, applied recursively at initialization; at train time attention uses a weighted sum of scale codes with scale-weights that are themselves a function of current sequence length, trained from scratch at matched params (weights taken from a spare embedding row budget).

**Invariant.** matched FLOPs/params

**Falsifier.** H: multiscale RG position code degrades less in perplexity than RoPE when evaluated at 2x-4x train length Metric: perplexity vs sequence length curve, area under degradation curve beyond train length Baseline: matched-FLOPs RoPE nano-llama trained at same length Protocol: nano-llama preset trained at 512 tokens, evaluated at 512/1024/2048, 12 seeds GPU-hours: 35

**Nearest (generator's guess).** hierarchical/multiscale positional encodings; ALiBi's length-extrapolation motivation; wavelet positional embeddings

**Scoper.** closest: [2607.07678] How Data Shapes RoPE Frequency Usage: From Positional Scale Matching to Length Generalization: closest in spirit (multiscale frequency analysis of position encoding for length generalization) but analyzes existing RoPE frequency usage rather than building an explicit RG-coarse-graining hierarchy with trained inter-scale maps and length-dependent scale-weighting.; [2302.08647] Multiresolution Graph Transformers and Wavelet Positional Encoding for Learning Hierarchical Structures: uses multiresolution/wavelet positional encoding for hierarchical structure representation, but on graphs/molecules, not for sequence-length extrapolation via RG flow.; [2405.14722] DAPE: Data-Adaptive Positional Encoding for Length Extrapolation: targets the same capability (length extrapolation) via adaptive PE, but not through dyadic block-averaged RG coarse-graining with explicit scale-to-scale flow. | survives: The explicit dyadic RG coarse-graining procedure with a trained scale-to-scale renormalization map and length-dependent scale-weighting for graceful fallback to coarse scales is not implemented in any candidate; nearest work either analyzes frequency usage post hoc or builds multiresolution PE for a different domain/purpose. | confidence 0.62

### Hebbian fast-weight position refresh  (splash 3, feasibility 3, id 89e6fb06df)

**Claim.** Hebbian fast-weight outer-product updates applied to positional-encoding so that a short-lived, rapidly-decaying associative memory binds recent absolute positions to content, letting the model 'relearn' local position-content correlations online at any length rather than relying solely on the frozen, out-of-range static encoding.

**New capability.** The model can adapt its effective notion of position within a single long stream, without gradient updates, by accumulating a fast outer-product trace between recent token content and relative-position tags that decays over a short window, restoring locality signal that the static encoding loses beyond training length.

**Placement.** At each layer, maintain a fast-weight matrix F (small, rank-limited) updated every token as F <- decay*F + eta * v_t outer p_t, where v_t is the token's value vector and p_t a learned relative-position tag (bounded range, e.g. -64..64); F is added as a low-rank bias to the attention value projection, read out at every step. F is reset at sequence boundaries and never persists across documents. Only eta, decay, and the tag embedding are trained, at nano scale, on top of a frozen base model (graft) or jointly at matched budget (from-scratch), both configurations run.

**Invariant.** streaming; F is updated causally token-by-token using only past tokens' v_t and decays automatically, so the mechanism works without ever seeing future context or the whole sequence.

**Falsifier.** H: Hebbian fast-weight position refresh reduces long-context perplexity degradation relative to matched-FLOPs static-RoPE baseline, particularly on local-dependency (short relative-distance) tasks beyond training length. Metric: perplexity at 2x/4x/8x length, decomposed by relative-distance bucket Baseline: static RoPE nano model, matched FLOPs (fast-weight FLOPs subtracted from baseline width) Protocol: llama-nano preset, both graft-onto-frozen-base and from-scratch-matched-budget conditions, 12 seeds each, train 512, eval to 4096 GPU-hours: 30

**Nearest (generator's guess).** Fast weight programmers (Schmidhuber); Hebbian linear-attention fast weights (Ba et al. 2016; Schlag & Schmidhuber 2021)

**Scoper.** closest: [2608.27763] Fast Weight Attention for Continual Learning: Uses fast-weight outer-product-style online updates for sequence modeling and length extrapolation, but the update targets value/key content association for prediction, not a positional-encoding tag bound to relative position via a decaying Hebbian trace.; [2503.03046-DAPE] DAPE: Data-Adaptive Positional Encoding for Length Extrapolation: Also aims to make positional encoding adaptive to input content for length extrapolation, but via a learned adjustment network on attention scores, not a decaying Hebbian outer-product fast-weight trace binding content to relative position tags.; [2602.16704] Reinforced Fast Weights with Next-Sequence Prediction: Fast-weight state update mechanism for long-context modeling, but purpose is improving representation learning via RL-based training objective, not restoring position-content locality signal beyond training length via a positional tag outer product. | survives: None of the retrieved papers place a decaying Hebbian outer-product trace specifically between content vectors and bounded relative-position tags, added as a low-rank bias to the value projection and reset per sequence, purely to refresh position-content locality beyond trained length; the nearest works either adapt positional bias via learned networks (DAPE) or use fast-weights for general content/state memory (Fast Weight Attention, REFINE), so a mechanism whose sole state is a position-tag outer product for locality restoration remains open. | confidence 0.68

### PID-controlled dynamic retokenisation  (splash 3, feasibility 3, id 748183974f)

**Claim.** Feedback control applied to the tokeniser's merge table so that vocabulary composition tracks live model prediction error instead of being frozen from pre-training frequency stats.

**New capability.** The system can adapt segmentations mid-training toward units the current model actually struggles to predict, rather than units that were merely frequent in a static corpus scan.

**Placement.** Every N training steps (e.g. every 200), compute per-merge-candidate error signal as the average next-token cross-entropy on sequences containing that byte-pair; maintain a PID loop per candidate merge whose setpoint is a target entropy contribution, with proportional/integral/derivative terms on the error driving a merge-priority score; promote/demote merges into an active vocabulary window of fixed size K when the controller output crosses a threshold. Only the merge-selection controller is trained/updated online; the transformer trains as usual on whatever vocabulary is currently active, with embedding rows for demoted tokens frozen or recycled via mean-init for new ones. Runs on gpt2-nano preset with a small custom tokeniser harness outside paperkiln's C++ trainer, feeding merge updates as periodic vocabulary-swap checkpoints.

**Invariant.** matched FLOPs/params; the transformer architecture and total embedding budget are held fixed across the run, only which subwords occupy vocabulary slots changes.

**Falsifier.** H: PID-driven dynamic retokenisation reaches a given validation loss in fewer training tokens than a static BPE vocabulary of the same size. Metric: validation cross-entropy at matched token budget Baseline: static BPE vocabulary of identical size, same model, same schedule Protocol: gpt2-nano, 12 seeds, 40M token budget, vocabulary swap every 200 steps for first half of training then frozen GPU-hours: 30

**Nearest (generator's guess).** BPE-dropout / dynamic vocabulary learning (e.g. VOLT, entropy-based vocabulary learning); not standard PID control but conceptually adjacent

**Scoper.** closest: [2603.03583] ByteFlow: Language Modeling through Adaptive Byte Compression without a Tokenizer: adapts segmentation online during training but via a learned end-to-end module, not a PID feedback loop on merge candidates driven by prediction error, and removes the tokenizer entirely rather than controlling a merge table.; [2506.18831] Adaptive Activation Steering for Efficient LLM Reasoning via Closed-Loop PID Control: uses an equivalent PID feedback mechanism but applies it to activation steering strength at decode time, not to promoting/demoting tokenizer merges based on prediction error.; [1811.04352] Open Vocabulary Learning for Neural Chinese Pinyin IME: online vocabulary adaptation during use, but driven by frequency/sampling not a PID error controller, and for a P2C IME task not LM pretraining. | survives: Nothing in the set closes a PID (proportional/integral/derivative) feedback loop specifically over BPE merge-candidate promotion/demotion keyed to live per-merge cross-entropy error during LM pretraining; that specific mechanism-object pairing (control-theoretic error signal steering the tokenizer's active merge table mid-training) remains undemonstrated. | confidence 0.72

### Mixture-density boundary prediction  (splash 3, feasibility 3, id d01ccd95c5)

**Claim.** Mixture-density heads applied to the tokeniser's segmentation decision so that token boundaries are drawn from a learned multimodal distribution over segmentation points instead of committed once via greedy frequency merges.

**New capability.** The system can maintain and sample multiple plausible segmentations of the same surface string, letting ambiguous spans (e.g. compound words, code identifiers) be scored under several boundary hypotheses rather than being locked to one, and use the mixture's entropy as an uncertainty signal for where tokenisation is unreliable.

**Placement.** A small boundary-predictor network, trained jointly with the LM at matched budget, outputs at each character position a mixture of K Gaussians/categoricals over next-boundary-distance, from which the actual segmentation used for that forward pass is sampled (during training) or taken as the mixture mean (at inference); the LM sees whichever discrete tokens the sampled boundaries imply. Trained from scratch alongside the main model on gpt2-nano preset; only the boundary head's parameters and the resulting discrete tokenisation change per step, main transformer architecture is untouched.

**Invariant.** outputs stay causal; the boundary mixture at position t only conditions on characters up to t, so sampled segmentations never use future characters.

**Falsifier.** H: Sampling from a learned multimodal boundary distribution during training improves validation loss on morphologically ambiguous test spans versus static BPE at matched FLOPs. Metric: validation cross-entropy on a curated ambiguous-compound test split, plus mixture entropy correlation with human-judged ambiguity Baseline: static BPE, matched total parameters (boundary head parameters subtracted from main model width) Protocol: gpt2-nano, 12 seeds, 40M tokens, K=3 mixture components GPU-hours: 32

**Nearest (generator's guess).** subword regularization / BPE-dropout (stochastic segmentation), and neural segmentation models like Sentence-piece unigram LM; mixture-density heads are borrowed from Bishop's MDN for continuous regression, novel combination here

**Scoper.** closest: [2303.08888] Stochastic Segmentation with Conditional Categorical Diffusion Models: Nearest in spirit (multimodal segmentation distributions with uncertainty) but applies to semantic image segmentation, not tokenizer boundary prediction in language models.; [2603.03583] ByteFlow: Language Modeling through Adaptive Byte Compression without a Tokenizer: Learns adaptive tokenization boundaries jointly with the LM, but uses compression-driven Top-K selection rather than a mixture-density head producing a sampleable multimodal distribution over boundaries.; [1903.06260] Mixture Modeling of Global Shape Priors and Autoencoding Local Intensity Priors for Left Atrium Segmentation: Uses mixture-of-Gaussians modeling for segmentation ambiguity, but in medical image shape-prior segmentation, not text tokenization boundaries. | survives: No paper places a mixture-density head directly on the tokenizer's boundary decision to yield a sampleable, entropy-scored multimodal segmentation distribution jointly trained with the LM; the nearest works either learn adaptive/tokenizer-free segmentation without multimodal distributional heads (ByteFlow) or apply mixture/diffusion-based multimodal prediction to image segmentation, not text tokenization, so this specific combination remains open. | confidence 0.72

### predictive-coding residual head  (splash 3, feasibility 3, id 536f74e9e4)

**Claim.** Predictive coding applied to the output-head so that upper layers pass down only the prediction error against the current top-down next-token guess, rather than the layers being trained purely bottom-up to a single softmax target.

**New capability.** The model can iteratively refine its next-token (and few-steps-ahead) prediction within a single forward pass via top-down/bottom-up error correction, giving a built-in multi-pass self-correction mechanism absent from a single feedforward unembedding.

**Placement.** Replace the final block's single feedforward pass with T inner iterations (T=2-3, small, fixed compute budget matched to baseline depth): at each iteration compute a top-down logit prediction, compute the error against the bottom-up hidden state via a small error-unit MLP, and feed the error back to update the hidden state before the next iteration's logits are computed; final iteration's logits are used for the loss. Trained from scratch at matched FLOPs (fewer real layers to compensate for the inner loop cost).

**Invariant.** outputs stay causal; the inner iterations all operate on hidden states derived from tokens up to and including the current position only, with no access to future tokens across iterations.

**Falsifier.** H: The predictive-coding head reduces perplexity on rare/long-tail tokens (bottom 10% frequency) by >=8% relative to a matched-FLOPs standard feedforward head, with the largest gains on next-token entropy-high positions. Metric: perplexity stratified by token frequency and entropy; overall matched-FLOPs perplexity Baseline: same nano preset with equivalent extra depth/parameters allocated to standard feedforward layers instead of PC iterations Protocol: from-scratch nano GPT2 preset, 12 seeds, T in {1(baseline),2,3}, matched FLOPs via layer-count compensation GPU-hours: 24

**Nearest (generator's guess).** predictive coding networks (Rao & Ballard; Whittington & Bogacz), universal transformer / adaptive computation time

**Scoper.** closest: [2410.18160] Future Token Prediction -- Causal Language Modelling with Per-Token Semantic State Vector for Multi-Token Prediction: Closest in spirit (multi-token lookahead via an auxiliary decoder head) but uses a single feedforward cross-attention pass, not an iterative top-down/bottom-up error-correction loop within the final block.; [2601.15286] Iterative Refinement Improves Compositional Image Generation: Shares the concept of iterative self-correction refinement, but operates at inference time on full images via an external VLM critic, not inside a single forward pass at the output head via predictive-coding error units.; [2404.19737] Better & Faster Large Language Models via Multi-token Prediction: Addresses multi-token prediction at the output head but via independent parallel heads, not an iterative predictive-coding error loop that refines the same next-token logits. | survives: Nothing in the set implements a predictive-coding-style iterative top-down/bottom-up error correction loop inside the final block that refines the same-position next-token logits via explicit error units before the loss; the nearest works either predict multiple future tokens with separate heads/decoders in one pass, or perform iterative refinement using external critics/VLMs at a different granularity, so a genuine within-forward-pass error-unit correction mechanism at the output head remains unclaimed. | confidence 0.72

### chunked atomic prefill commit  (splash 3, feasibility 3, id 9308cd36b6)

**Claim.** transactions-and-rollback applied to prefill so that the prompt is processed in committed chunks, each of which speculatively unlocks token emission for a prefix boundary before the whole prompt is consumed, with rollback if a later chunk changes the meaning of already-committed KV entries

**New capability.** the system can begin emitting tokens after processing only the first chunk of a long prompt, then correct (roll back and regenerate) the emitted continuation if later chunks of the prompt retroactively invalidate the shallow encoding — trading TTFT for a bounded probability of a visible correction event, which the vanilla exact-prefill system cannot do at all (it has zero-or-full latency, no partial-commit option).

**Placement.** Split the prompt into fixed-size chunks (e.g. 64 tokens); process chunk 1 through the full model depth and commit its KV entries as a 'transaction'; begin generation immediately. As each subsequent chunk is processed, compare its effect on early-chunk attention outputs (via a cheap gate: recompute attention scores from later chunk onto chunk 1 positions) against a divergence threshold; if under threshold, commit silently, if over, roll back generation to the chunk boundary and resume after full re-prefill. Trained component: only the small gate network that predicts divergence from partial second-chunk statistics, on held-out prompts.

**Invariant.** exactness is recoverable by verification; any drift introduced by early emission is bounded because the gate checks true (not predicted) downstream attention effects on committed KV before allowing continued generation, and rollback restores the exact-prefill trajectory when the check fails.

**Falsifier.** H: chunked-commit prefill achieves lower expected TTFT than exact prefill at a fixed rollback-rate budget (e.g. <5% of generations require rollback), without loss of final-sequence exactness Metric: expected TTFT, rollback rate, total wall-clock including rollback cost vs exact baseline Baseline: standard exact prefill, matched FLOPs, same nano gpt2/llama preset Protocol: sliding-window and exact attention lanes on paperkiln, prompt lengths 256/1024, 12 seeds, sweep chunk size and gate threshold GPU-hours: 20

**Nearest (generator's guess).** chunked prefill / prefix caching in serving systems (e.g. vLLM chunked prefill), but rollback-on-divergence gating is a distinct verify mechanism not present there

**Scoper.** closest: [2603.13358] Not All Prefills Are Equal: PPD Disaggregation for Multi-turn LLM Serving: involves reusing partially committed KV state and routing decode-before-full-prefill, but has no divergence gate or rollback of already-emitted tokens.; [2510.08055] From Tokens to Layers: Redefining Stall-Free Scheduling for MoE Serving with Layered Prefill: chunked prefill interleaved with decode is structurally similar to chunk-based commit, but tokens generated always reflect the full processed prefix so far with no speculative unlock before full consumption and no rollback mechanism.; [2502.02789] Speculative Prefill: Turbocharging TTFT with Lightweight and Training-Free Token Importance Estimation: speculatively estimates prompt content to save compute but does not begin token emission before the whole prompt is processed, nor rolls back emitted continuations. | survives: The core untaken mechanism is speculative unlocking of decode after only a prefix chunk of the prompt, paired with an explicit rollback-and-regenerate protocol triggered by a trained divergence gate when later chunks retroactively invalidate the shallow encoding; none of the retrieved systems commit to emitting tokens before the full prompt is seen and then visibly roll back generation. | confidence 0.72

### retrieval-mixed normalisation statistics  (splash 3, feasibility 3, id 7acf4d3247)

**Claim.** computing per-token normalisation statistics as a mixture of the token's own stats and the stats of its k nearest neighbours retrieved from a cached activation store applied to normalisation so that normalisation is informed by cross-sequence and cross-batch structure instead of being purely local to one token

**New capability.** the model can stabilize normalisation for rare/out-of-distribution tokens by borrowing statistics from similar activations seen earlier in the same stream or in a small memory bank, which per-token LayerNorm structurally cannot do

**Placement.** maintain a small causal FIFO cache of (key=projected activation, value=mean/var) pairs per layer, updated every token; at each token, retrieve k=8 nearest neighbours by cosine similarity among cached keys from positions strictly before the current one, and set the normalisation mean/var to a learned convex combination of the token's own stats and the retrieved neighbours' stats; the combination weight is a trained scalar/small MLP, base LayerNorm affine parameters unchanged, trained from scratch at matched params (retrieval adds negligible parameters, FLOPs matched by trimming FFN width slightly).

**Invariant.** outputs remain causal; the cache only ever contains activations from strictly earlier positions in the same sequence (or from a frozen snapshot of prior training batches for the cross-batch variant), so no future information leaks

**Falsifier.** H: retrieval-mixed normalisation improves perplexity on long-tailed/rare-token subsets more than matched-FLOPs standard LayerNorm, without hurting common-token perplexity Metric: perplexity stratified by token frequency decile; overall val perplexity Baseline: matched-FLOPs gpt2-nano with standard LayerNorm, FFN width adjusted to equalize FLOPs Protocol: nano llama preset, 12 seeds, k in {4,8,16}, 40M tokens, report stratified perplexity with pre-registered decile bins GPU-hours: 35

**Nearest (generator's guess).** kNN-LM (Khandelwal et al.); retrieval-augmented normalization is not standard

**Scoper.** closest: [2606.06363] GMBFormer: An NDVI-Guided Global Memory Bank Transformer for Urban Green-Space Extraction: uses a retrieved memory bank of similar features to augment representations, but this is applied as a feature-fusion mechanism in a segmentation decoder, not to compute normalization statistics (mean/var) at a LayerNorm.; [2512.20420] Simplifying Multi-Task Architectures Through Task-Specific Normalization: modifies normalization statistics per task/context, but conditions on task identity rather than retrieved nearest-neighbour activations from a cache.; [2002.06038] Never Give Up: Learning Directed Exploration Strategies: uses kNN retrieval over a cached experience store, structurally similar retrieval mechanism, but applied to reward shaping in RL, not to normalization statistics in a transformer. | survives: Nothing found computes LayerNorm mean/variance as a learned mixture of the token's own statistics and those of cached kNN-retrieved past activations; this specific placement (retrieval-conditioned normalization statistics rather than retrieval-conditioned features/outputs/rewards) is untouched by the candidates. | confidence 0.78

### MDL-selected normalisation window  (splash 3, feasibility 3, id 5f68786475)

**Claim.** choosing, per layer and online, the normalisation aggregation window (single token vs. causal running window of length L) that minimises a two-part code length (bits to describe the chosen L plus bits of the residual after normalisation) applied to normalisation so that the model adaptively normalises over the sequence extent that actually compresses activations best, instead of always at the single-token granularity

**New capability.** the model can dynamically switch between per-token and cross-token (running-window) normalisation depending on local sequence statistics, choosing whichever granularity yields a shorter code, something fixed per-token LayerNorm cannot do by construction

**Placement.** at each layer, maintain a small set of candidate causal window lengths L in {1,4,16,64}; for each, compute the running mean/var over the causal window and the resulting residual's empirical entropy (coded via a fixed quantization + entropy estimate), plus log2|L-set| bits to specify the choice; pick the L minimizing total code length via a straight-through argmin (or soft-select with Gumbel during training, hard select at inference); trained from scratch, matched FLOPs by evaluating all candidates cheaply (running means are O(1) update) and only the selection logic adds parameters (negligible).

**Invariant.** outputs remain causal; every candidate window and the selection statistic are computed only from positions up to and including the current one, and exactness is recoverable in the sense that L=1 (plain per-token LayerNorm) is always an available candidate, so the method can verify against and fall back to the exact baseline

**Falsifier.** H: MDL-selected normalisation window achieves lower validation perplexity than fixed per-token LayerNorm at matched FLOPs, with the model preferring larger L in low-entropy (repetitive/structured) regions Metric: val perplexity; distribution of selected L conditioned on local n-gram entropy Baseline: matched-FLOPs gpt2-nano with fixed per-token LayerNorm (L=1 only) Protocol: nano gpt2 preset, 12 seeds, 40M tokens, pre-registered correlation test between selected L and local text entropy GPU-hours: 32

**Nearest (generator's guess).** MDL-based architecture/feature selection (Rissanen); adaptive/dynamic normalization variants (e.g. Switchable Normalization) but MDL-driven selection specifically is novel

**Scoper.** closest: [2603.25025] System-Anchored Knee Estimation for Low-Cost Context Window Selection in PDE Forecasting: closest analogue of selecting a context/window length from a candidate set, but applied to PDE forecasting context windows, not to per-layer normalization aggregation, and not via an MDL two-part code length.; [1902.10658] Regularity Normalization: Neuroscience-Inspired Unsupervised Attention across Neural Network Layers: applies MDL-based normalization per layer, but uses it to compute a normalization factor (universal code length) rather than to select between discrete causal window granularities (per-token vs running window) for normalization statistics.; [2006.01893] Unsupervised Discretization by Two-dimensional MDL-based Histogram: shares the two-part MDL code-length selection mechanism but applies it to histogram discretization, not to choosing a normalization window in a neural network layer. | survives: A genuinely new contribution would be the specific placement: using an MDL two-part code (bits for L plus residual entropy) to select among discrete causal normalization window lengths per layer online, since no candidate paper combines MDL model selection with the normalization-window/aggregation-granularity choice at a neural network layer. | confidence 0.72

### Energy-refined example replay  (splash 3, feasibility 3, id adbe30237e)

**Claim.** Energy-based refinement applied to data-order so that examples are not merely resampled by a fixed schedule but are iteratively refined (via gradient or MCMC steps on an auxiliary energy score) into harder or more informative variants before being re-shown to the model

**New capability.** The system can revisit a training example on the model's own terms by generating a locally-perturbed, higher-loss-under-current-model variant of that example (an energy-descended near-miss) rather than replaying the identical bytes, giving a targeted curriculum signal that fixed-order training cannot produce.

**Placement.** Maintain a small buffer of the last N seen examples with their current per-token loss; periodically (every k steps) pick the top-loss examples, run a few steps of energy-based refinement (e.g. Langevin-style perturbation in embedding space guided by the model's own loss as energy) to produce adversarially-nudged replay examples, and inject these into the next training batch. Only the base model trains, using its own current loss as the energy function; the refinement loop is inference-only (no extra parameters), run every k=500 steps for a fixed compute budget.

**Invariant.** outputs stay causal; refinement only perturbs already-past training examples used as future training inputs, and generation-time causality is untouched since this is a training-time data augmentation loop, not an inference-time mechanism.

**Falsifier.** H: Energy-refined replay reduces validation perplexity and improves sample efficiency (perplexity vs tokens-seen curve) compared to a matched-FLOPs random-shuffle-only baseline. Metric: Validation perplexity vs training tokens seen; loss on a held-out hard-example probe set Baseline: Standard single-epoch or random-shuffled multi-epoch training at matched total FLOPs (including refinement compute charged as extra forward/backward passes) Protocol: nano-gpt2 on paperkiln, 12 seeds, k=500 step refresh, refinement budget capped at 10% of total FLOPs, matched total budget vs baseline extra epochs GPU-hours: 30

**Nearest (generator's guess).** Hard example mining / adversarial data augmentation; contrastive divergence

**Scoper.** closest: [1511.05952] Prioritized Experience Replay: Closest known mechanism reorders/reweights which existing examples to replay by loss, but never generates a perturbed, energy-refined variant of the example.; [1905.11475] GAT: Generative Adversarial Training for Adversarial Example Detection and Robust Classification: Involves adversarial perturbation but for detection/robustness classification, not for iteratively rewriting replayed training examples via energy-based refinement during LM-style training.; [2403.06798] Dynamic Perturbation-Adaptive Adversarial Training on Medical Image Classification: Uses loss-driven adaptive perturbation generation during training, closest in spirit but applied as adversarial training for robustness in medical image classification, not as a replay-buffer curriculum mechanism that re-injects energy-refined near-miss variants into subsequent batches. | survives: Yes: no candidate combines a loss-indexed replay buffer with inference-only, model-own-loss-as-energy Langevin/gradient refinement in embedding space to produce harder near-miss variants re-injected into training batches as a curriculum signal; this specific placement (data-order component, energy-refinement mechanism, replay-buffer object) is not covered by prioritization, adversarial-training, or curriculum-learning papers found here. | confidence 0.68
