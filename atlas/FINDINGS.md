# Atlas findings registry

19 claims; every row carries its receipts. Retractions are rows, not deletions.

| id | status | claim | evidence | scope |
|---|---|---|---|---|
| S2-muon | ✅✅ replicated | Muon (hybrid per-head) improves final val loss and whole-run loss AUC over AdamW at tiny scale | t=-6.21 (n=36) | llama family, 0.95-2.7M params, TinyStories, 400 steps |
| S2-lr-null | ↻ superseded | Learning rate (1e-3 vs 3e-3) has no effect on final val loss | t=+1.20 (n=36) | llama family, tiny scale, 400 steps |
| S2-ctx | ❌ retracted | Longer context (T=256 vs 128) improves final val loss | t=-2.77 (n=36) | llama family, tiny scale, FIXED STEP COUNT |
| S2-heads-null | ✅ supported | Head count (4 vs 8) has no effect on any measured metric at tiny scale | t=-0.31 (n=36) | llama family, tiny scale, 400 steps; null on all 5 metrics |
| S2-spike-metric | ❌ retracted | Larger batch increases gradient spikes | t=+3.13 (n=36) | llama family, tiny scale |
| S3-lrxopt | ✅ supported | lr x optimizer is a real interaction: 3e-3 is the best lr under Muon and the worst under AdamW (orthogonalized updates widen the stable lr range) | t=-3.12 (n=48) | llama family, tiny scale, 3x S2 token budget; also spikes t=-4.01, AUC t=-2.69 |
| S3-muon | ✅ supported | Muon's val-loss advantage replicates at 3x token budget | t=-7.38 (n=48) | llama family, tiny scale |
| S3-ctx-null | ✅ supported | With tokens held equal, context length (T=128/1200steps vs T=256/600steps) has no val-loss effect | t=-1.48 (n=48) | llama family, tiny scale, 614400 tokens both levels |
| S3-d-unpaid | ✅ supported | Width d=192 does not improve val loss over d=128 even at 3x token budget (crossover, if any, lies above this scale) | t=+1.10 (n=48) | llama family, tiny scale; throughput cost -209 tok/s (t=-6.6) is significant |
| SRD-recall | ❌ retracted | Surprise-routed density improves needle recall over its falsifier lane |  | 2-block parity model, synthetic needle task |
| SRD-gate-conc | ↻ superseded | The SRD gate concentrates on needle positions (interpretation OPEN: retrieval router vs novelty detector) |  | 2-block parity model; needle tokens are distributionally distinct BY CONSTRUCTION in this design |
| EX-scorer | ✏️ amended | Contribution-vs-mention scoring separates used from mentioned architecture flavors better than first-match. AMENDED 2026-08-31: the "zero wrong assertions" half held for 29 papers and broke THREE times over the next eleven, in two distinct ways (evidential x2, lexical x1) — the separation claim stands and improved as the set grew (0.905 vs 0.841 naive), the absolute no-wrong claim does not | 0.905 [CI 0.79–1.0] | arXiv LaTeX sources; 40-paper truth set (31 Aug 2026, the CI-band target met); 66/92 verdicts correct, 5 contested, **3 wrong**, abstention-first. The growth batch that broke it: OLMo (names RMSNorm and ReLU as considered-and-rejected — scorer survived) and Megatron-LM (states its own GeLU only by ATTRIBUTING it to GPT-2/BERT while the contrasted ReLU sits in a bare declarative clause — scorer applied relu) and Cerebras-GPT (true positional is inherited from "a GPT-3-like architecture" while RoPE appears ONLY as future work and as other models' choice — scorer applied rope). ONE ROOT CAUSE, TWO SURFACES: evidence for what a paper USES is often indirect (inherited, attributed, or negated), and indirect evidence currently loses to any direct-looking mention. Two concrete fixes named: a future-work mention should VETO like an explicit rejection, and "X-like architecture" should register as an inheritance cue. Both registered as KNOWN_WRONG in flavor_bench.py with diagnoses; the gate still fails on any NEW wrong assertion. Growing the truth set 26 -> 33 found both — which is what a benchmark is for |
| R2-novelty | ✅ supported | The SRD gate is a novelty detector: it tracks distributional out-of-place-ness, not retrieval-criticality (concentration +0.59 with distinct needles vs -0.003 in-distribution; decoy-chasing DCI 0.97-1.89; target/non-target selectivity ~0) |  | 2-block parity model, synthetic needle task, models that did NOT master retrieval (control lane 0.000 acc at this config) |
| R2-efficiency | ⏳ pending | SRD-hardened gating beats random and positional gating at matched density (the efficiency reading of SRD) |  | UNANSWERABLE at 2-block/d=128: the task family does not resolve at this scale for any lane (see NEEDLE-scale-negative); requires model-scale escalation under a new pre-registration |
| NEEDLE-scale-negative | ✅ supported | The synthetic needle associative-recall family does not resolve at 2-block/d=128: NO lane (exact attention included) exceeds ~19% answer accuracy; typical outcome is the CE floor, across lr {1e-3, 3e-3}, difficulty (npairs,nkeys) from (2,8) to (8,64), batch {1,4}, and budgets up to 3000 steps |  | 2-block parity model, d=128, learned positions, word-level synthetic vocab. Also tested and DISCONFIRMED here: the S3-lrxopt hypothesis that AdamW@3e-3 explained the plateau (lr=1e-3 floors identically) — a scope boundary for that finding's transfer. |
| S1-swa-beats-exact | ↻ superseded | Sliding-window attention (w=64 + 1 sink, computing 44.3% of full causal's attention entries at T=256) beats full attention on final val loss at tiny scale on natural text | t=-10.35 (n=20) | gpt2 2-block d=128, T=256, TinyStories, 400 steps, AdamW@1e-3 (the S3-lrxopt safe quadrant), batch 4. Mechanism NOT established (regularization story is interpretation); budget-conditionality unknown — exact may catch up at longer budgets; window size unexplored beyond w=64. Throughput not significantly different (reference implementation materializes [T,T]; the fast kernel belongs to coalfire). MECHANISM AMENDED 2026-08-06 by S1b-window-monotone: the 'locality is a favorable inductive prior' reading is WITHDRAWN. Loss falls monotonically as the window shrinks (w=16 < 32 < 64 < 128 < exact, every adjacent contrast signal), so there is no interior optimum and no evidence for a locality-matched prior at this scale. The empirical effect stands; the explanation does not. Budget-conditionality is under test in experiments/sparse_s1_budget. SUPERSEDED 2026-08-07: the 400-step measurement below is correct and stands, but the comparison REVERSES by 1200 steps (exact ahead, Delta +0.0123) so the headline reading 'sliding window beats full attention' is budget-conditional and must not be cited as a general ordering. See S1c-budget-reversal. |
| S1b-window-monotone | ✅ supported | Loss falls MONOTONICALLY as the attention window shrinks, down to 12.3% density: w=16 (3.785) < w=32 (3.822) < w=64 (3.852) < w=128 (3.894) < w=256=exact (3.899). No interior optimum exists above w=16, which falsifies the locality-as-inductive-prior explanation of S1-swa-beats-exact | t=-5.89 (n=20) | gpt2 2-block d=128, T=256, TinyStories, 400 steps, AdamW@1e-3, batch 4, sinks=1. LIMITATION STATED IN THE PREREG: a monotone curve down to w=16 is also consistent with an interior optimum BELOW 16 (w=8, w=4, w=1) — this cell cannot separate 'capacity artifact' from 'optimum lower than we looked'. The discriminating test is budget, not another window rung. |
| S1c-budget-reversal | ✅ supported | The sliding-window advantage over full attention is a SHORT-BUDGET artifact that reverses with training: paired Delta (swa@64 - exact) moves from -0.0472 at 400 steps to +0.0123 at 1200 steps, a change of +0.0595 in the same direction on every seed. At this scale the SIGN of a sparse-vs-dense comparison depends on where you stop | t=+5.09 (n=20) | gpt2 2-block d=128, T=256, TinyStories, AdamW@1e-3, batch 4, swa w=64 sinks=1. Both lanes were still improving at 1200 (no early stop, 12 evals, loss ~3.86 -> ~3.59), so this is the still-training regime, not an overfitting artifact. STATISTICAL PRECISION: the SHRINKAGE is established two-tailed (t=5.09, crit 2.776); the REVERSAL itself (Delta(1200)>0, t=2.74) clears the one-tailed crit 2.132 but NOT the two-tailed 2.776 — the one-tailed reading is licensed only because P2 committed the direction to git before the runs. Does NOT establish that any published result is wrong; the general claim is about reporting discipline (state and vary the budget), not other people's conclusions. |
| S1d-sink-inert | ✅ supported | The attention sink is INERT at this scale: removing it moves val loss by +0.0003 at 400 steps (t=0.16) and -0.0001 at 1200 (t=-0.03), against a capacity-model prediction of ~0.0008 made and committed BEFORE the runs. Both prior S1 findings (window monotone, budget reversal) reproduce without the sink — they are properties of the window alone. The reversal without sinks clears the two-tailed bar outright (+0.0122, t=2.89), which the sinked version did not | t=+0.16 (n=10) | gpt2 2-block d=128, T=256, TinyStories, AdamW@1e-3, batch 4, w=64, one sink vs none. Does NOT license 'sinks don't matter' in general — sinks earn their keep in streaming/long-context regimes this cell does not touch. Magnitude caveat: 0.0003 and the predicted 0.0008 are indistinguishable at this resolution; what is confirmed is a null where a null was predicted. EXECUTION NOTE: first run lost to WSL /tmp cleanup after completion; rerun to persistent storage reproduced the surviving numbers exactly (deterministic seeds) — recorded in the manifest. |
| S1e-bstar-distribution | ✅ supported | B\*(256) — the budget where exact attention overtakes swa(w=64, sinks=1) at d=256 — is a DISTRIBUTION over seeds, not a point: at 3600 steps the pooled n=15 sign is undetermined (Delta +0.0195, SD 0.0377, t=2.00 < two-tailed 2.145, split 9+/6−), per-seed persistent-crossing budgets span 1600–3600 with 6/15 never crossing, and both prongs of the pre-committed dispersion criterion (\|mean\|<SD, minority ≥4/15) are met. The budget-conditionality direction (shrink Delta(3600)−Delta(1200)>0) replicated on 10 virgin seeds for the FOURTH pre-committed time: +0.0541, t=4.13, clearing even the unlicensed two-tailed bar | t=+4.13 (n=10 shrink) / t=2.00 (n=15 sign) | gpt2-nano d=256 L=2, T=256, TinyStories + chat7b vocab, AdamW@1e-3, batch 4, CPU numerics, still-training regime throughout (best_val within last 3 evals in 14/15 — overfit onset never reached, so dispersion is not an overfitting artifact). Mean Delta marches monotonically upward across all nine slices while SD roughly doubles: early unanimity (15/15 at b=400) is the cheap wrong signal; dispersion is what budget buys. Bounds dispersion at ONE width — whether it grows with d is transfer_s1's F3 question. Any single-number crossover-budget claim is now a MEASURED under-specification. |

Reproduce any experimental row: `python tools/reproduce.py <id>`.
